---
phase: 02-intro-dataset-e-problema-fase-1-edm
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apresentacao/index.html
autonomous: true
requirements:
  - INTRO-01
tags:
  - presentation
  - reveal-js
  - intro-slide
  - dataset
  - csedm
  - progsnap2
  - phase2-edm
must_haves:
  truths:
    - "Existe um slide novo em apresentacao/index.html com cabeçalho `> o dataset csedm` e voz em 1ª pessoa do plural"
    - "O slide apresenta CSEDM (Java CS1, coleta 2019, CSEDM 2021) armazenado em ProgSnap2 (Price, 2020)"
    - "O slide mostra os 3 números do dataset: 413 estudantes, 50 problemas, 201 mil eventos (ou 201.570)"
    - "O slide tem rodapé `Fonte: Price (2020); CSEDM 2021.`"
    - "Navegando para http://127.0.0.1:8000/#/7 o slide INTRO-01 aparece sem layout quebrado"
  artifacts:
    - path: "apresentacao/index.html"
      provides: "Section INTRO-01 inserido entre slide Yağcí e slide MARKER-01"
      contains: "o dataset csedm"
  key_links:
    - from: "apresentacao/index.html (section INTRO-01)"
      to: "apresentacao/assets/theme-unifacens.css (.slide-related, .rel-lead, .rel-cite, .deck-topic)"
      via: "classes existentes da fase 1; sem CSS novo"
      pattern: "class=\"deck-slide slide-related\""
---

<objective>
Inserir o slide INTRO-01 ("o dataset csedm") em `apresentacao/index.html`, ANTES do slide MARKER-01 (que já foi inserido pelo plan 02-01). Resultado: deck navega Yağcí → INTRO-01 → MARKER-01 → slide-code.

Implementa INTRO-01 (D-31..D-35, D-38, D-42..D-46): apresenta o dataset CSEDM em ProgSnap2 com voz em 1ª pessoa do plural e os 3 números brutos do MainTable (413/50/201570). Cita Price (2020) parenteticamente; rodapé `Fonte: Price (2020); CSEDM 2021.`. Sem em-dash; sem citação direta literal.

Purpose: implementar requirement INTRO-01 do ROADMAP fase 2. Sem CSS novo (reuso direto de `.slide-related`/`.rel-lead`/`.rel-cite`/`.deck-topic` já consolidados na fase 1).

Output:
- Section novo em `apresentacao/index.html` inserido após o slide Yağcí (linha 147) e antes do MARKER-01 (inserido pelo plan 02-01).
- 1 commit atômico `apresentacao: slide INTRO-01 - dataset CSEDM (Price, 2020)`.

NOTA POSICIONAL: este plan depende do plan 02-01 (MARKER-01 já no DOM). Após este plan, INTRO-01 ocupa `#/7` e MARKER-01 desloca para `#/8`. Subsequentes plans 02-03 e 02-04 vão inserir INTRO-03a e INTRO-03b entre INTRO-01 e MARKER-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-CONTEXT.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-RESEARCH.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-PATTERNS.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-01-PLAN.md
@apresentacao/STYLE.md
@apresentacao/index.html
@docs/ProgSnap2.pdf

<interfaces>
<!-- Padrões já existentes que este plan reutiliza. ZERO CSS novo. -->

Template estrutural canônico (slide 4 Zorić fundido, apresentacao/index.html linhas 100-111):

```html
<!-- ============ SLIDE · Mineracao de Dados Educacionais (Zoric, 2020) - fusao p1+p2 ============ -->
<section data-background-color="#F1F6FB">
  <div class="deck-slide slide-related">
    <svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>

    <p class="deck-topic"><span class="ps1">&gt;</span>mineração de dados educacionais<span class="caret blink"></span></p>

    <p class="rel-lead">Nosso trabalho aplica o processo de mineração de dados educacionais (<i>Educational Data Mining</i>, EDM), área interdisciplinar...</p>

    <p class="rel-cite">Fonte: Zorić (2020).</p>
  </div>
</section>
```

Classes reutilizadas (sem CSS novo):
- `.deck-topic` (cabeçalho `> [seção]` minúsculas, caret blink)
- `.rel-lead` (parágrafo Arial 25px justificado)
- `.rel-cite` (rodapé Fonte: Arial 18px `#5b6472` esquerda, `margin-top: auto`)

Phrasing-alvo travado (RESEARCH §1.2.3):
> "Nosso dataset é o CSEDM (curso introdutório CS1 em Java, coleta de 2019, divulgado na competição CSEDM 2021), armazenado em ProgSnap2 (Price, 2020), formato que registra cada evento de programação do estudante (edição, compilação, execução), preservando o histórico completo das tentativas a cada problema."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validar números do dataset (D-38 + RESEARCH §2)</name>
  <files></files>
  <read_first>
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-RESEARCH.md §2 (números 413/50/201570 já validados; comando pandas registrado)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-CONTEXT.md D-38 (D-38 estimou ~360k que está INCORRETO; o valor correto é 201.570 per RESEARCH §2)
    - CLAUDE.md (Pipeline de Notebooks + Dataset CSEDM - Fatos Críticos)
  </read_first>
  <action>
    Rodar o comando de verificação dos 3 números do dataset (RESEARCH §2):

    ```bash
    cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && python3 -c "import pandas as pd; df = pd.read_csv('data/CSEDM/MainTable.csv'); print('SubjectID:', df['SubjectID'].nunique()); print('ProblemID:', df['ProblemID'].nunique()); print('Events:', len(df))"
    ```

    Esperado (RESEARCH §2 capturou):
    ```
    SubjectID: 413
    ProblemID: 50
    Events: 201570
    ```

    Decisão de formato no slide (RESEARCH §2 recomendação C):
    - Estudantes: `413`
    - Problemas: `50`
    - Eventos: `201 mil` (recomendação principal pt-BR) OU `201.570` (formato preciso com ponto separador de milhar) — escolher UM e usar consistente. Não usar `360k`, `~360k`, `360.000` (estimativa incorreta de D-38).

    Se algum dos 3 números divergir do esperado, PAUSAR e reportar antes de prosseguir. Em caso de match, continuar com Task 2 usando os números confirmados.
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && python3 -c "import pandas as pd; df = pd.read_csv('data/CSEDM/MainTable.csv'); assert df['SubjectID'].nunique() == 413 and df['ProblemID'].nunique() == 50 and len(df) == 201570, 'Numbers diverged'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Comando Python sai com `OK` (assertion passa)
    - 3 valores confirmados: 413, 50, 201570
    - Decisão de formato registrada para Task 2 (default recomendado: "201 mil eventos")
  </acceptance_criteria>
  <done>
    Os 3 números do dataset CSEDM estão confirmados (413/50/201570). Formato escolhido para o slide (default "201 mil") definido. Pronto para gravar markup.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inserir section INTRO-01 em apresentacao/index.html antes do MARKER-01</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html (linhas 134-150 para confirmar posição atual: Yağcí fundido termina em ~linha 147, MARKER-01 começa logo após — verificar offset real após o plan 02-01)
    - apresentacao/index.html linhas 100-111 (template Zorić fundido para CLONE)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-RESEARCH.md §1.2 (phrasing Price 2020 confirmado), §1.3 (resumo de phrasing), §2 (números formato C "201 mil"), §4.3 (comentário sugerido)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-PATTERNS.md §"INTRO-01" (markup completo e adaptações)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-CONTEXT.md (D-34a cabeçalho, D-35 voz 1ª pessoa, D-38 números, D-42 padrão, D-43 sem direta, D-44 sem em-dash, D-45 rodapé)
    - docs/ProgSnap2.pdf §Abstract, §1 Introduction, §2 (ler para confirmar phrasing factual: ano 2020, "registra eventos", múltiplas tentativas)
  </read_first>
  <action>
    Inserir o markup do INTRO-01 entre o fim do slide Yağcí (linha 147 atual) e o início do slide MARKER-01 (inserido pelo plan 02-01). Localizar o ponto exato re-lendo o arquivo: o `</section>` do Yağcí seguido pelo comentário `<!-- ============ SLIDE · MARKER · ...`.

    Markup a inserir (LITERAL, com indentação 6 espaços para `<section>` raiz):

    ```html
          <!-- ============ SLIDE · INTRO-01 · O dataset CSEDM em ProgSnap2 (Price, 2020) ============ -->
          <section data-background-color="#F1F6FB">
            <div class="deck-slide slide-related">
              <svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>

              <p class="deck-topic"><span class="ps1">&gt;</span>o dataset csedm<span class="caret blink"></span></p>

              <p class="rel-lead">Nosso dataset é o <b>CSEDM</b> (curso introdutório CS1 em Java, coleta de 2019, divulgado na competição CSEDM 2021), armazenado em <b>ProgSnap2</b> (Price, 2020), formato que registra cada evento de programação do estudante (edição, compilação, execução), preservando o histórico completo das tentativas a cada problema.</p>

              <p class="rel-lead intro-stats-line"><b>413</b> estudantes, <b>50</b> problemas, <b>201 mil</b> eventos.</p>

              <p class="rel-cite">Fonte: Price (2020); CSEDM 2021.</p>
            </div>
          </section>
    ```

    Travas obrigatórias (per CONTEXT D-34a..D-46):
    - **Cabeçalho LITERAL D-34a:** `o dataset csedm` (minúsculas, sem caracteres especiais)
    - **Voz 1ª pessoa do plural D-35:** começa com "Nosso dataset é..."
    - **Citação parentética D-35:** `(Price, 2020)` no corpo (NÃO autor prominente)
    - **Números D-38 + RESEARCH §2:** `413`, `50`, `201 mil` (formato C recomendado). Se preferir precisão, `201.570` (ponto = milhar pt-BR). NÃO usar `~360k`, `360k`, `360.000`.
    - **Rodapé LITERAL D-35:** `Fonte: Price (2020); CSEDM 2021.`
    - **Marca d'água:** `<svg class="wm">` PRESENTE
    - **Sem em-dash D-44:** preview do RESEARCH menciona "CSEDM — curso introdutório" — NÃO copiar. Usar parênteses "CSEDM (curso introdutório CS1 em Java, coleta de 2019, ...)" conforme markup acima.
    - **Sem `<blockquote>` D-43:** texto corre apenas em `<p class="rel-lead">`
    - **D-46 estrangeirismos:** `*knowledge tracing*` em itálico NÃO aparece aqui (este slide não cita KT como conceito). ProgSnap2 e CSEDM como nomes próprios (não itálico). "Java" sem itálico.
    - **Indentação:** 6 espaços para `<section>` raiz; mesma indentação dos sections existentes; EOL Unix.

    Nota sobre `intro-stats-line`: classe extra cosmética para diferenciar o parágrafo dos números se desejado; sem CSS novo, herda `.rel-lead`. Pode ser omitida se preferir só `.rel-lead`. Default: manter como `<p class="rel-lead">` simples, mover a classe extra para parênteses futuros se quiser dar destaque visual sem alterar CSS.

    Alternativa (se preferir lista visual em vez de inline): substituir o segundo `.rel-lead` por:
    ```html
    <ul class="intro-stats">
      <li><span class="stat-num">413</span> <span class="stat-lbl">estudantes</span></li>
      <li><span class="stat-num">50</span> <span class="stat-lbl">problemas</span></li>
      <li><span class="stat-num">201 mil</span> <span class="stat-lbl">eventos</span></li>
    </ul>
    ```
    Esta alternativa requer CSS novo (`.intro-stats`, `.stat-num`, `.stat-lbl`) — só usar se houver tempo para validar no browser e/ou definir CSS de display flex/horizontal. Default conservador: parágrafo inline.
  </action>
  <verify>
    <automated>grep -c '> o dataset csedm' apresentacao/index.html | grep -v '^0$' && grep -c 'Nosso dataset' apresentacao/index.html | grep -v '^0$' && grep -c 'CSEDM' apresentacao/index.html | grep -v '^0$' && grep -c 'ProgSnap2' apresentacao/index.html | grep -v '^0$' && grep -c '(Price, 2020)' apresentacao/index.html | grep -v '^0$' && grep -c 'Fonte: Price (2020); CSEDM 2021.' apresentacao/index.html | grep -v '^0$' && grep -c '413' apresentacao/index.html | grep -v '^0$' && grep -E '50.*problemas|problemas.*50' apresentacao/index.html | head -1 && (grep -c '201 mil' apresentacao/index.html | grep -v '^0$' || grep -c '201.570' apresentacao/index.html | grep -v '^0$')</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '>o dataset csedm<' apresentacao/index.html` retorna 1 (cabeçalho LITERAL D-34a)
    - Source: `grep -c 'Nosso dataset é o' apresentacao/index.html` retorna 1 (voz 1ª pessoa D-35)
    - Source: `grep -c '(Price, 2020)' apresentacao/index.html` retorna >= 1 (citação parentética)
    - Source: `grep -c 'Fonte: Price (2020); CSEDM 2021.' apresentacao/index.html` retorna 1 (rodapé literal D-35/D-45)
    - Source: o parágrafo da INTRO-01 contém os 3 números literais. `awk '/SLIDE · INTRO-01/,/<\/section>/' apresentacao/index.html | grep -E '413|50|201' | wc -l` retorna >= 3
    - Source NEGATIVO: NÃO contém `360k`, `~360k` ou `360.000`. `awk '/SLIDE · INTRO-01/,/<\/section>/' apresentacao/index.html | grep -cE '360k|~360k|360.000'` retorna 0
    - Source NEGATIVO: SEM em-dash dentro do block INTRO-01. `awk '/SLIDE · INTRO-01/,/<\/section>/' apresentacao/index.html | grep -c '—'` retorna 0
    - Source NEGATIVO: SEM `<blockquote>` no block INTRO-01. `awk '/SLIDE · INTRO-01/,/<\/section>/' apresentacao/index.html | grep -c 'blockquote'` retorna 0
    - Source NEGATIVO: SEM mencionar `min_attempts`, `410`, `328`, `82`, ou filtro Shi (D-38c). `awk '/SLIDE · INTRO-01/,/<\/section>/' apresentacao/index.html | grep -cE 'min_attempts|filtro Shi|410|328|82'` retorna 0
    - Source: `grep -c '<section data-background-color' apresentacao/index.html` retorna 14 (era 13 após plan 02-01; +1 com INTRO-01 = 14)
    - Behavior: re-leitura do arquivo confirma INTRO-01 vem ANTES de MARKER-01 (`<!-- SLIDE · INTRO-01` aparece antes de `<!-- SLIDE · MARKER` no arquivo)
  </acceptance_criteria>
  <done>
    Section INTRO-01 inserido em `apresentacao/index.html` ANTES do MARKER-01; cabeçalho `> o dataset csedm` literal; voz 1ª pessoa "Nosso dataset é o CSEDM..."; 3 números do dataset (413/50/201 mil) visíveis; `(Price, 2020)` parentético; rodapé `Fonte: Price (2020); CSEDM 2021.` literal; sem em-dash; sem `<blockquote>`; sem menção a filtro Shi/410; marca d'água presente; total de sections no deck = 14.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validação visual do INTRO-01 no browser</name>
  <what-built>
    Section INTRO-01 inserido em `apresentacao/index.html` entre slide Yağcí (#/6) e MARKER-01 (que desloca para #/8). Slide apresenta o dataset CSEDM em ProgSnap2 com os 3 números brutos do MainTable.
  </what-built>
  <how-to-verify>
    1. Em um terminal, rodar (ou manter o servidor do plan 02-01):
       ```bash
       cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt/apresentacao && python3 -m http.server 8000
       ```

    2. Abrir no navegador: `http://127.0.0.1:8000/#/7` (INTRO-01)

    3. Inspecionar visualmente:
       - [ ] Cabeçalho `> o dataset csedm` em cima, `>` em azul UniFacens, caret piscando ao fim
       - [ ] Parágrafo principal começa com "Nosso dataset é o **CSEDM** (curso introdutório CS1 em Java, coleta de 2019, divulgado na competição CSEDM 2021), armazenado em **ProgSnap2** (Price, 2020),..."
       - [ ] CSEDM e ProgSnap2 em negrito; texto entre parênteses; SEM em-dash
       - [ ] Parágrafo de números: "**413** estudantes, **50** problemas, **201 mil** eventos." (ou formato escolhido)
       - [ ] Rodapé: `Fonte: Price (2020); CSEDM 2021.` em Arial 18px cor cinza `#5b6472`, alinhado à esquerda
       - [ ] Marca d'água Facens canto superior direito
       - [ ] Sem texto saindo do slide; layout cabe em 1280×720

    4. Navegar próximo (`N` ou →): deve chegar em #/8 que é o MARKER-01.

    5. Console (F12): sem erros.

    6. Verificar com `Esc`/`O` (overview): o slide INTRO-01 entra ANTES do MARKER-01 na sequência visual.
  </how-to-verify>
  <resume-signal>
    Aprovar com "approved" ou "ok intro-01" se tudo bate visualmente. Se algo está fora, descrever.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Slide reveal.js estático (HTML/CSS sem backend, sem network, sem user input). Sem superfície de ataque relevante. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-N/A | — | slide HTML estático | N/A | Slide servido localmente via `python3 -m http.server`. Sem persistência, sem auth, sem rede externa. Threat model não se aplica. |
</threat_model>

<verification>
- Section INTRO-01 entre Yağcí e MARKER-01 (ordem confirmada via re-leitura do arquivo)
- Cabeçalho `> o dataset csedm` LITERAL (D-34a)
- Voz 1ª pessoa "Nosso dataset é o CSEDM..." (D-35)
- 3 números do dataset visíveis: 413, 50, 201 mil (D-38 + RESEARCH §2)
- `(Price, 2020)` parentético no corpo
- Rodapé `Fonte: Price (2020); CSEDM 2021.` LITERAL (D-45)
- SEM em-dash, SEM `<blockquote>`, SEM menção a filtro Shi/410 (D-38c)
- Total sections no deck = 14
- Marca d'água presente
- Validação visual no browser passou
</verification>

<success_criteria>
1. **Requirement INTRO-01 entregue:** o slide do dataset existe no deck e apresenta CSEDM em ProgSnap2 com 3 números brutos.
2. **Fidelidade científica:** `(Price, 2020)` parentético, `Fonte: Price (2020); CSEDM 2021.` rodapé; phrasing factualmente verdadeiro (CSEDM em ProgSnap2 — confirmado em RESEARCH §1.2).
3. **Números corretos:** 413/50/201 mil (não a estimativa errada ~360k de D-38).
4. **Voz autoral 1ª pessoa:** "Nosso dataset é..." (D-35).
5. **Restrições atendidas:** sem em-dash (D-44), sem citação direta literal (D-43), sem menção a filtro Shi/410 (D-38c).
6. **Commit atômico:** `apresentacao: slide INTRO-01 - dataset CSEDM (Price, 2020)`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-02-SUMMARY.md` documentando:
- Linhas do HTML inseridas (intervalo)
- Formato escolhido para os 3 números (201 mil vs 201.570)
- Ajustes visuais aplicados durante checkpoint (se houver)
- Confirmação visual do reviewer humano
- Commit hash
</output>
