---
phase: 02-intro-dataset-e-problema-fase-1-edm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apresentacao/assets/theme-unifacens.css
  - apresentacao/index.html
autonomous: true
requirements:
  - MARKER-01
tags:
  - presentation
  - reveal-js
  - css-component
  - slide-marker
  - phase2-edm
must_haves:
  truths:
    - "Existe um slide novo em apresentacao/index.html com classe .slide-marker.slide-marker--phase1 e progress bar das 4 fases EDM"
    - "A primeira caixa do progress bar é 'Definição do Problema' preenchida em azul UniFacens com checkmark"
    - "As outras 3 caixas exibem 'Preparação dos Dados', 'Modelagem e Avaliação', 'Implantação' em outline cinza com número"
    - "O componente .slide-marker está definido em theme-unifacens.css de forma reutilizável (modificadores --done/--pending)"
    - "Navegando para http://127.0.0.1:8000/#/7 o slide MARKER-01 aparece sem layout quebrado"
  artifacts:
    - path: "apresentacao/assets/theme-unifacens.css"
      provides: "Bloco CSS .slide-marker / .marker-track / .marker-step / .marker-step--done / .marker-step--pending / .marker-arr / .slide-marker .marker-fonte"
      contains: ".slide-marker {"
    - path: "apresentacao/index.html"
      provides: "Section MARKER-01 inserido após Yağcí fundido"
      contains: "slide-marker--phase1"
  key_links:
    - from: "apresentacao/index.html (section MARKER-01)"
      to: "apresentacao/assets/theme-unifacens.css (.slide-marker block)"
      via: "class names .slide-marker .marker-track .marker-step--done .marker-step--pending .marker-arr .marker-fonte"
      pattern: "class=\"deck-slide slide-marker slide-marker--phase1\""
---

<objective>
Implementar o slide MARKER-01 (progress bar horizontal das 4 fases da EDM, com "Definição do Problema" preenchida e ✓) PRIMEIRO, junto com o componente CSS reutilizável `.slide-marker` que será reaproveitado por MARKER-02 (fase 3), MARKER-03 (fase 4) e MARKER-04 (fase 5).

Purpose: derisca o único risco visual desta fase (CSS novo). Com `.slide-marker` validado no browser, os 3 slides INTRO que seguem viram paste-and-modify de `.slide-related` sem risco.

Output:
- Bloco CSS novo em `apresentacao/assets/theme-unifacens.css` (componente `.slide-marker` com modificadores `--done` e `--pending`).
- Section novo em `apresentacao/index.html` inserido após o slide Yağcí fundido (linha 147), classe `slide-marker slide-marker--phase1`.
- 1 commit atômico `apresentacao: slide MARKER-01 - definicao do problema (fase 2)`.

NOTA POSICIONAL: este plan modifica `apresentacao/index.html` na linha 148 (em branco entre 147 e 149). Plans subsequentes (02, 03, 04) farão inserções DEPOIS deste section, então a ordem dos waves importa: este plan é wave 1, seguintes wave 2-4. Após execução, o MARKER-01 fica em `#/7` no deck temporariamente; quando os 3 INTRO forem inseridos antes dele, ele desloca para `#/10`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-CONTEXT.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-RESEARCH.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-PATTERNS.md
@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css

<interfaces>
<!-- Padrões já existentes no codebase que este plan reutiliza. -->

Estrutura externa OBRIGATÓRIA de section reveal.js (do slide Yağcí, linhas 134-147):

```html
<!-- ============ SLIDE · descrição ============ -->
<section data-background-color="#F1F6FB">
  <div class="deck-slide slide-XYZ">
    <svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>
    <!-- conteúdo aqui -->
  </div>
</section>
```

Variáveis CSS existentes em theme-unifacens.css (`:root`, linhas 11-20):
- `--uni-blue` = #2667FF
- `--uni-light` = #F1F6FB (fundo de slide)
- `--uni-gray` (cinza icônico)
- `--uni-ink` = #111317 (texto principal)
- `--mono` (Cascadia)

Analog primário do componente: `.slide-bridge .bridge-seq` em theme-unifacens.css linhas 197-210 (caixas horizontais brancas borda preta 1.5px, setas pretas).

Analog secundário (rodapé centralizado): `.slide-kcfig .kcfig-fonte` (linhas 297-300) e `.slide-fig .fig-fonte` (linha 319). Ambos Arial 18px `#5b6472` centralizado.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar bloco CSS .slide-marker reutilizável em theme-unifacens.css</name>
  <files>apresentacao/assets/theme-unifacens.css</files>
  <read_first>
    - apresentacao/assets/theme-unifacens.css (linhas 197-210 do .slide-bridge .bridge-seq como referência; linhas 297-300 .kcfig-fonte; final do arquivo para append)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-RESEARCH.md §3 (CSS rascunho COMPLETO já pronto em §3.2)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-PATTERNS.md §"apresentacao/assets/theme-unifacens.css § bloco .slide-marker" (bloco literal para colar)
    - apresentacao/STYLE.md (§"Identidade visual", paleta, tipografia)
  </read_first>
  <action>
    Append ao final de `apresentacao/assets/theme-unifacens.css` o bloco CSS abaixo (vindo literal do RESEARCH §3.2 e PATTERNS §"CSS-alvo a adicionar"). NÃO modificar regras existentes; só adicionar.

    Bloco a inserir (literal, sem em-dash em comentários per D-44):

    ```css
    /* ===========================================================================
       SLIDE · Marker · progress bar das 4 fases EDM
       Estende a estetica do .bridge-seq (caixas pretas, fundo branco, setas pretas)
       acrescentando estados --done (azul UniFacens + checkmark) e --pending
       (outline cinza). Reusado por MARKER-01..04 (fases 2-5).
       =========================================================================== */
    .slide-marker {
      display: flex; flex-direction: column; background: var(--uni-light);
      padding: 80px 64px 40px; --caret-color: var(--uni-blue);
      font-family: Arial, "Helvetica Neue", sans-serif;
      align-items: center; justify-content: center;
    }
    .slide-marker .wm { position: absolute; top: 26px; right: 34px; width: 58px; color: var(--uni-gray); opacity: .9; pointer-events: none; }

    .marker-track {
      display: flex; align-items: stretch; justify-content: center; gap: 0;
      width: 100%; max-width: 1120px;
    }
    .marker-step {
      flex: 1 1 0; text-align: center; font-size: 19px; font-weight: 700;
      background: #fff; border: 1.5px solid #1f1f1f; border-radius: 0;
      padding: 28px 12px; display: flex; flex-direction: column; align-items: center;
      justify-content: center; gap: 8px; min-height: 110px; line-height: 1.25;
    }
    .marker-step__mark {
      display: inline-flex; align-items: center; justify-content: center;
      width: 28px; height: 28px; border-radius: 50%;
      font-family: var(--mono); font-size: 18px; line-height: 1; font-weight: 700;
    }
    .marker-step--done {
      background: var(--uni-blue); color: #fff; border-color: var(--uni-blue);
    }
    .marker-step--done .marker-step__mark {
      background: #fff; color: var(--uni-blue);
    }
    .marker-step--pending {
      background: #fff; color: #5b6472; border-color: #5b6472;
    }
    .marker-step--pending .marker-step__mark {
      border: 1.5px solid #5b6472; color: #5b6472;
    }
    .marker-arr {
      flex: none; align-self: center; color: #1f1f1f; font-size: 26px; font-weight: 700;
      padding: 0 14px;
    }

    .slide-marker .marker-fonte {
      margin-top: 36px; text-align: center;
      font-family: Arial, "Helvetica Neue", sans-serif; font-size: 18px; color: #5b6472;
    }
    ```

    Regras OBRIGATÓRIAS (per D-44, D-46, PATTERNS):
    - Usar APENAS variáveis CSS existentes (`--uni-blue`, `--uni-light`, `--uni-gray`, `--mono`); NÃO criar novas.
    - SEM `border-radius` nas caixas (estética ABNT Word, herdada de `.bridge-seq`).
    - SEM em-dash (`—`) em comentário CSS. Usar vírgula, dois-pontos ou parênteses.
    - Cabeçalho de bloco com `===========================================================================` (padrão dos outros blocos do arquivo).
  </action>
  <verify>
    <automated>grep -c '.slide-marker {' apresentacao/assets/theme-unifacens.css | grep -v '^0$' && grep -c '.marker-step--done' apresentacao/assets/theme-unifacens.css | grep -v '^0$' && grep -c '.marker-step--pending' apresentacao/assets/theme-unifacens.css | grep -v '^0$' && grep -c '.marker-arr' apresentacao/assets/theme-unifacens.css | grep -v '^0$' && ! grep -E '^[^/*].*—' apresentacao/assets/theme-unifacens.css | grep -v '^//\|^/\*'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '.slide-marker {' apresentacao/assets/theme-unifacens.css` retorna >= 1
    - Source: `grep -c '.marker-step--done' apresentacao/assets/theme-unifacens.css` retorna >= 2 (declaração + filho `.marker-step__mark`)
    - Source: `grep -c '.marker-step--pending' apresentacao/assets/theme-unifacens.css` retorna >= 2
    - Source: `grep -c '.marker-arr' apresentacao/assets/theme-unifacens.css` retorna >= 1
    - Source: `grep -c '.marker-fonte' apresentacao/assets/theme-unifacens.css` retorna >= 1
    - Source: `grep -c 'var(--uni-blue)' apresentacao/assets/theme-unifacens.css` retorna >= valor anterior + 2 (usado em `.marker-step--done` background e em `.marker-step--done .marker-step__mark` color)
    - Source NEGATIVO: bloco novo SEM em-dash em comentário. `awk '/SLIDE · Marker/,/marker-fonte/' apresentacao/assets/theme-unifacens.css | grep -c '—'` retorna 0
    - Source: bloco delimitado por linha `==========...=====` no padrão dos demais componentes
    - Behavior: arquivo `apresentacao/assets/theme-unifacens.css` continua válido como CSS (sem erro de parsing; testar abrindo deck no browser na Task 3)
  </acceptance_criteria>
  <done>
    Bloco CSS `.slide-marker` adicionado ao fim do `theme-unifacens.css`, todas as 9 classes definidas (`.slide-marker`, `.slide-marker .wm`, `.marker-track`, `.marker-step`, `.marker-step__mark`, `.marker-step--done`, `.marker-step--done .marker-step__mark`, `.marker-step--pending`, `.marker-step--pending .marker-step__mark`, `.marker-arr`, `.slide-marker .marker-fonte`), todas as cores via variáveis CSS existentes, sem em-dash em comentário, padrão de cabeçalho de bloco respeitado.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inserir section MARKER-01 em apresentacao/index.html entre linhas 147 e 149</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html (linhas 134-149 para confirmar boundaries do slide Yağcí e ponto de inserção; conferir que linha 147 ainda é `</section>` do Yağcí e linha 149 ainda é o comentário do slide-code)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-RESEARCH.md §3.3 (markup-alvo MARKER-01 literal) e §4 (boundaries DOM)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-PATTERNS.md §"apresentacao/index.html § MARKER-01" (markup literal completo)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-CONTEXT.md (D-31 posição, D-34d sem deck-topic, D-39 estética, D-40 textos literais, D-41 reuso)
  </read_first>
  <action>
    Inserir o markup abaixo entre as linhas 147 e 149 atuais do `apresentacao/index.html` (na linha em branco 148). O markup vem literal do RESEARCH §3.3 e PATTERNS §"Markup-alvo (RESEARCH §3.3, copiar como-é)".

    Indentação: 6 espaços para `<section>` raiz (alinhado com sections existentes; conferir indentação dos outros sections do arquivo).

    Markup a inserir (LITERAL, sem em-dash; textos das 4 caixas literais conforme D-40):

    ```html
          <!-- ============ SLIDE · MARKER · Definicao do Problema concluida (Zoric, 2020) ============ -->
          <section data-background-color="#F1F6FB">
            <div class="deck-slide slide-marker slide-marker--phase1">
              <svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>

              <div class="marker-track">
                <span class="marker-step marker-step--done">
                  <span class="marker-step__mark">&check;</span>
                  Definição do Problema
                </span>
                <span class="marker-arr">&rarr;</span>
                <span class="marker-step marker-step--pending">
                  <span class="marker-step__mark">2</span>
                  Preparação dos Dados
                </span>
                <span class="marker-arr">&rarr;</span>
                <span class="marker-step marker-step--pending">
                  <span class="marker-step__mark">3</span>
                  Modelagem e Avaliação
                </span>
                <span class="marker-arr">&rarr;</span>
                <span class="marker-step marker-step--pending">
                  <span class="marker-step__mark">4</span>
                  Implantação
                </span>
              </div>

              <p class="marker-fonte">Fonte: adaptado de Zorić (2020).</p>
            </div>
          </section>
    ```

    Travas literais (per D-40):
    - Caixa 1: "Definição do Problema" (com acentos), modificador `--done`, mark `&check;`
    - Caixa 2: "Preparação dos Dados", `--pending`, mark "2"
    - Caixa 3: "Modelagem e Avaliação", `--pending`, mark "3"
    - Caixa 4: "Implantação", `--pending`, mark "4"
    - 3 setas `<span class="marker-arr">&rarr;</span>` entre as caixas
    - Rodapé `Fonte: adaptado de Zorić (2020).` (Ç com cedilha, ć com agudo)

    Travas obrigatórias:
    - SEM `<p class="deck-topic">` neste slide (D-34d)
    - Marca d'água `<svg class="wm">` PRESENTE (A6 do Assumptions Log)
    - Comentário acima do `<section>` no padrão `<!-- ============ SLIDE · ... ============ -->` (12 sinais `=` de cada lado)
    - SEM em-dash (`—`) em nada do markup novo nem no comentário HTML (no comentário use "Definicao do Problema concluida" sem acentos se a saída do editor for ASCII, ou com acentos no UTF-8)
    - EOL Unix; indentação 6 espaços no `<section>` raiz
  </action>
  <verify>
    <automated>grep -c 'slide-marker--phase1' apresentacao/index.html | grep -v '^0$' && grep -c 'Definição do Problema' apresentacao/index.html | grep -v '^0$' && grep -c 'Preparação dos Dados' apresentacao/index.html | grep -v '^0$' && grep -c 'Modelagem e Avaliação' apresentacao/index.html | grep -v '^0$' && grep -c 'Implantação' apresentacao/index.html | grep -v '^0$' && grep -c 'marker-step--done' apresentacao/index.html | grep -v '^0$' && grep -c 'Fonte: adaptado de Zorić (2020).' apresentacao/index.html | grep -v '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c 'slide-marker--phase1' apresentacao/index.html` retorna 1
    - Source: `grep -c 'marker-step--done' apresentacao/index.html` retorna 1 (só uma caixa)
    - Source: `grep -c 'marker-step--pending' apresentacao/index.html` retorna 3 (três caixas)
    - Source: `grep -c 'marker-arr' apresentacao/index.html` retorna 3 (três setas)
    - Source: `grep -c 'Definição do Problema' apresentacao/index.html` retorna 1
    - Source: `grep -c 'Preparação dos Dados' apresentacao/index.html` retorna 1
    - Source: `grep -c 'Modelagem e Avaliação' apresentacao/index.html` retorna 1
    - Source: `grep -c '>Implantação' apresentacao/index.html` retorna 1 (a tag span fechando junto com `<` antes)
    - Source: `grep -c 'Fonte: adaptado de Zorić (2020).' apresentacao/index.html` retorna 1
    - Source: `grep -c '&check;' apresentacao/index.html` retorna 1 (uma vez, na caixa --done)
    - Source NEGATIVO: o block do MARKER-01 NÃO tem `.deck-topic`. `awk '/SLIDE · MARKER · Definicao/,/<\/section>/' apresentacao/index.html | grep -c 'deck-topic'` retorna 0
    - Source NEGATIVO: SEM em-dash dentro do block MARKER-01. `awk '/SLIDE · MARKER/,/<\/section>/' apresentacao/index.html | grep -c '—'` retorna 0
    - Source: count total de `<section>` no `<div class="slides">` passou de 12 para 13. `grep -c '<section data-background-color' apresentacao/index.html` retorna 13
  </acceptance_criteria>
  <done>
    Section MARKER-01 inserido em `apresentacao/index.html` entre o fim do slide Yağcí e o início do slide-code; markup literal de RESEARCH §3.3 colado; 4 caixas na ordem D-40 com textos literais; 1ª caixa em `--done`+`&check;`, 2-4 em `--pending`+número; 3 setas entre as caixas; sem `.deck-topic`; marca d'água presente; rodapé `Fonte: adaptado de Zorić (2020).` centralizado; sem em-dash no markup; total de sections no deck = 13.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validação visual do MARKER-01 no browser</name>
  <what-built>
    Bloco CSS `.slide-marker` em `apresentacao/assets/theme-unifacens.css` e section MARKER-01 em `apresentacao/index.html` (entre slide Yağcí e slide-code). Componente reutilizável; será aproveitado por MARKER-02/03/04 nas fases 3-5.
  </what-built>
  <how-to-verify>
    1. Em um terminal, rodar:
       ```bash
       cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt/apresentacao && python3 -m http.server 8000
       ```
       Se o CSS foi alterado e o browser tinha cache aberto, usar porta alternativa (`python3 -m http.server 8001`) para forçar reload.

    2. Abrir no navegador: `http://127.0.0.1:8000/#/7`
       (Após este plan, o slide MARKER-01 fica em `#/7` porque os INTRO ainda não foram inseridos. Quando os plans 02-02/03/04 rodarem, ele desloca para `#/10`.)

    3. Inspecionar visualmente:
       - [ ] 4 caixas em linha, larguras aproximadamente iguais
       - [ ] Caixa 1 com fundo AZUL UniFacens (`#2667FF`), texto branco "Definição do Problema", checkmark `✓` em circuito branco
       - [ ] Caixas 2, 3, 4 com fundo branco, outline cinza `#5b6472`, números "2", "3", "4" em círculo cinza
       - [ ] Textos: "Definição do Problema", "Preparação dos Dados", "Modelagem e Avaliação", "Implantação" (com acentos pt-BR corretos)
       - [ ] 3 setas `→` pretas entre as 4 caixas
       - [ ] Marca d'água Facens no canto superior direito
       - [ ] Rodapé `Fonte: adaptado de Zorić (2020).` centralizado, cinza, Arial 18px
       - [ ] Sem texto saindo do slide; layout cabe em 1280×720

    4. Pressionar `Esc` ou `O` para ver visão geral (16 slides em grade — note que ainda são 13 totais nesta fase parcial).

    5. Abrir Console (F12) e confirmar: nenhum erro JS de console ao navegar para #/7.

    6. Possíveis ajustes ao vivo (se necessário):
       - Se "Modelagem e Avaliação" estourar uma linha: reduzir `font-size` da `.marker-step` para `18px` OU inserir `<br>` entre "Modelagem e" e "Avaliação"
       - Se altura visual estiver desbalanceada: ajustar `min-height` da `.marker-step` para 130-140px
  </how-to-verify>
  <resume-signal>
    Aprovar com "approved" ou "ok marker-01" se tudo bate visualmente. Se algo precisa ajustar, descrever (ex.: "caixa 3 estourou em 2 linhas, reduzir font").
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Slide reveal.js estático (HTML/CSS sem backend, sem network, sem user input, sem JS dinâmico). Não há superfície de ataque relevante. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-N/A | — | slide HTML/CSS estático | N/A | Slide estático servido localmente via `python3 -m http.server` em ambiente do dev. Sem persistência, sem auth, sem rede externa. Threat model não se aplica. |
</threat_model>

<verification>
- Bloco CSS `.slide-marker` em theme-unifacens.css com todas as classes (verificável por grep)
- Section MARKER-01 em index.html com 4 caixas + 3 setas + textos literais D-40 (verificável por grep)
- Validação visual no browser (#/7): 4 caixas alinhadas, caixa 1 azul com ✓, caixas 2-4 outline cinza com número, setas pretas, rodapé centralizado
- Total de `<section>` no deck = 13
- SEM em-dash no markup novo (grep)
- SEM `.deck-topic` no section MARKER-01 (D-34d)
</verification>

<success_criteria>
1. **Componente reutilizável definido:** `.slide-marker` com modificadores `--done` e `--pending` em theme-unifacens.css; usável para MARKER-02/03/04 sem alteração CSS adicional (só troca de qual caixa fica `--done`).
2. **Slide MARKER-01 navegável:** em `#/7`, o deck mostra progress bar com "Definição do Problema ✓" em azul UniFacens, demais em outline cinza, sem layout quebrado.
3. **Fidelidade aos textos travados (D-40):** as 4 caixas exibem LITERALMENTE "Definição do Problema", "Preparação dos Dados", "Modelagem e Avaliação", "Implantação" com acentos pt-BR.
4. **Rodapé presente:** `Fonte: adaptado de Zorić (2020).` centralizado, Arial 18px cor `#5b6472`.
5. **Sem em-dash, sem `.deck-topic`:** consistente com D-44 e D-34d.
6. **Commit atômico:** mensagem `apresentacao: slide MARKER-01 - definicao do problema (fase 2)` (sem acentos no subject por convenção; corpo livre).
</success_criteria>

<output>
After completion, create `.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-01-SUMMARY.md` documentando:
- Linhas do CSS adicionadas (intervalo)
- Linhas do HTML inseridas (intervalo e ponto de inserção verificado)
- Ajustes visuais aplicados durante checkpoint (se houver)
- Confirmação visual do reviewer humano
- Commit hash
</output>
