---
phase: 02-intro-dataset-e-problema-fase-1-edm
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - apresentacao/index.html
autonomous: true
requirements:
  - INTRO-03
tags:
  - presentation
  - reveal-js
  - intro-slide
  - knowledge-tracing
  - shi-2022
  - phase2-edm
must_haves:
  truths:
    - "Existe um slide novo em apresentacao/index.html com cabeçalho `> o problema do kt binário`"
    - "O slide apresenta, em paráfrase indireta com autor prominente, que BKT e DKT usam apenas acerto/erro e ignoram a estrutura do código"
    - "Code-DKT NÃO é mencionado em nenhum momento (é fase 4)"
    - "O slide tem rodapé `Fonte: Shi et al. (2022).`"
    - "Navegando para http://127.0.0.1:8000/#/8 o slide INTRO-03a aparece sem layout quebrado"
  artifacts:
    - path: "apresentacao/index.html"
      provides: "Section INTRO-03a inserido entre INTRO-01 e MARKER-01"
      contains: "o problema do kt binário"
  key_links:
    - from: "apresentacao/index.html (section INTRO-03a)"
      to: "apresentacao/assets/theme-unifacens.css (.slide-related, .rel-lead, .rel-cite, .deck-topic)"
      via: "classes existentes da fase 1; sem CSS novo"
      pattern: "class=\"deck-slide slide-related\""
---

<objective>
Inserir o slide INTRO-03a ("o problema do kt binário") em `apresentacao/index.html`, ANTES do slide MARKER-01 e DEPOIS do INTRO-01. Resultado: deck navega Yağcí → INTRO-01 → INTRO-03a → MARKER-01 → slide-code.

Implementa metade do requirement INTRO-03 do ROADMAP fase 2 (diagnóstico): paráfrase indireta de Shi et al. (2022) com autor prominente, centrada em "BKT e DKT usam apenas acerto/erro e ignoram a estrutura do código". Code-DKT NÃO é mencionado (é fase 4, MODEL-01). Sem citação direta literal entre aspas. Rodapé `Fonte: Shi et al. (2022).`.

Purpose: implementar a primeira metade do INTRO-03 (diagnóstico do problema; INTRO-03b vem no plan 02-04 cobrindo a consequência pedagógica). Junto, os dois slides cumprem INTRO-03 do ROADMAP fase 2.

Output:
- Section novo em `apresentacao/index.html` inserido entre INTRO-01 e MARKER-01.
- 1 commit atômico `apresentacao: slide INTRO-03a - problema do kt binario (Shi et al. 2022)`.

NOTA POSICIONAL: este plan depende do plan 02-02 (INTRO-01 já inserido). Após este plan, ordem fica: #/7 INTRO-01, #/8 INTRO-03a, #/9 MARKER-01. Plan 02-04 insere INTRO-03b entre INTRO-03a e MARKER-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-CONTEXT.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-RESEARCH.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-PATTERNS.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-02-PLAN.md
@apresentacao/STYLE.md
@apresentacao/index.html
@docs/Code-DKT.pdf

<interfaces>
<!-- Padrões já existentes que este plan reutiliza. ZERO CSS novo. -->

Template estrutural canônico (slide 4 Zorić fundido, linhas 100-111 do index.html):

```html
<!-- ============ SLIDE · descricao ============ -->
<section data-background-color="#F1F6FB">
  <div class="deck-slide slide-related">
    <svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>
    <p class="deck-topic"><span class="ps1">&gt;</span>cabecalho<span class="caret blink"></span></p>
    <p class="rel-lead">corpo</p>
    <p class="rel-cite">Fonte: ...</p>
  </div>
</section>
```

Classes reutilizadas: `.deck-topic`, `.rel-lead`, `.rel-cite` (fase 1; sem CSS novo).

Phrasing-alvo travado (RESEARCH §1.1, §1.3):
> "Shi et al. (2022) apontam que modelos clássicos de *knowledge tracing*, como BKT e DKT, usam apenas a informação de **acerto ou erro** e ignoram a estrutura do código produzido pelo estudante."

Reforço opcional (segunda sentença, mantém voz autoral): "Toda a riqueza do código submetido fica fora do modelo."

Trechos literais de fidelidade ao paper (RESEARCH §1.1, NÃO copiar entre aspas no slide; usar como referência de paráfrase):
- Abstract Shi 2022 p.50: "models including the state-of-the-art Deep Knowledge Tracing (DKT) mainly leverage each student's response either as correct or incorrect, ignoring its content"
- §2.1 Related Work p.51: "no information about the students' answer beyond binary correctness information"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inserir section INTRO-03a em apresentacao/index.html entre INTRO-01 e MARKER-01</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html (re-ler para localizar fim do INTRO-01 e início do MARKER-01; offset mudou após plans 02-01 e 02-02)
    - apresentacao/index.html linhas 100-111 (template Zorić fundido para CLONE)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-RESEARCH.md §1.1 (Shi 2022 trechos literais para REFERÊNCIA DE FIDELIDADE, não copiar entre aspas), §1.3 (resumo de phrasing), §4.3 (comentário sugerido)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-PATTERNS.md §"INTRO-03a" (markup completo e adaptações)
    - .planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-CONTEXT.md (D-34b cabeçalho, D-36 paráfrase autor prominente, D-42 padrão, D-43 sem direta, D-44 sem em-dash, D-45 rodapé, D-46 estrangeirismos)
    - docs/Code-DKT.pdf §Abstract + §1 + §2.1 (LER para confirmar phrasing factual; convenção CLAUDE.md "ler referência completa antes de escrever sobre ela")
  </read_first>
  <action>
    Localizar o ponto de inserção: imediatamente após o `</section>` do INTRO-01 (cuja primeira linha é `<!-- ============ SLIDE · INTRO-01 · O dataset CSEDM em ProgSnap2 ...`) e imediatamente antes do `<!-- ============ SLIDE · MARKER · ...`.

    Inserir o markup abaixo (LITERAL, indentação 6 espaços para `<section>` raiz):

    ```html
          <!-- ============ SLIDE · INTRO-03a · O problema do KT binario (Shi et al., 2022) ============ -->
          <section data-background-color="#F1F6FB">
            <div class="deck-slide slide-related">
              <svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>

              <p class="deck-topic"><span class="ps1">&gt;</span>o problema do kt binário<span class="caret blink"></span></p>

              <p class="rel-lead">Shi et al. (2022) apontam que modelos clássicos de <i>knowledge tracing</i>, como <b>BKT</b> e <b>DKT</b>, usam apenas a informação de <b>acerto ou erro</b> e ignoram a estrutura do código produzido pelo estudante.</p>

              <p class="rel-lead">Toda a riqueza do código submetido fica fora do modelo.</p>

              <p class="rel-cite">Fonte: Shi et al. (2022).</p>
            </div>
          </section>
    ```

    Travas obrigatórias (per CONTEXT D-34b, D-36, D-42..D-46):
    - **Cabeçalho LITERAL D-34b:** `o problema do kt binário` (minúsculas, com acento agudo em "binário")
    - **Voz D-36 (autor prominente):** "Shi et al. (2022) apontam que..." (Shi como sujeito da oração, sem `(Shi et al., 2022)` parentético adicional no corpo — autor já está prominente)
    - **BKT e DKT mencionados literalmente** (gate forte D-36)
    - **Code-DKT NÃO mencionado** (Pitfall 3 do RESEARCH; gate forte; é fase 4 MODEL-01/04)
    - **Sem `<blockquote>` D-43:** texto corre em `<p class="rel-lead">`; SEM aspas literais do paper (os trechos do RESEARCH §1.1 são REFERÊNCIA DE FIDELIDADE, não markup)
    - **D-46 estrangeirismos:** `<i>knowledge tracing</i>` em itálico minúsculas; BKT/DKT sem itálico, em negrito como ênfase
    - **Sem em-dash D-44:** usar vírgula, dois-pontos ou parênteses
    - **Rodapé LITERAL D-36:** `Fonte: Shi et al. (2022).`
    - **Marca d'água:** `<svg class="wm">` PRESENTE
    - **Indentação:** 6 espaços para `<section>` raiz; EOL Unix

    Variantes aceitas para a segunda sentença (reforço opcional):
    - "Toda a riqueza do código submetido fica fora do modelo." (default)
    - "A estrutura do código fica fora da modelagem."
    - "O modelo não vê o código; vê apenas o resultado."

    Pitfall a evitar (RESEARCH Pitfall 5):
    - NÃO escrever `<blockquote>` nem aspas literais (Shi 2022 está em inglês; usar paráfrase pt-BR autoral)
    - NÃO escrever "Shi propõe o Code-DKT para resolver" — gate Pitfall 3 (fase 4)
  </action>
  <verify>
    <automated>grep -c '>o problema do kt binário<' apresentacao/index.html | grep -v '^0$' && grep -c 'Shi et al. (2022) apontam' apresentacao/index.html | grep -v '^0$' && grep -c 'BKT' apresentacao/index.html | grep -v '^0$' && grep -c 'DKT' apresentacao/index.html | grep -v '^0$' && grep -c 'Fonte: Shi et al. (2022).' apresentacao/index.html | grep -v '^0$' && (awk '/SLIDE · INTRO-03a/,/<\/section>/' apresentacao/index.html | grep -ci 'code-dkt' || echo 0) | grep -E '^(0|)$'</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -c '>o problema do kt binário<' apresentacao/index.html` retorna 1 (cabeçalho LITERAL D-34b)
    - Source: `grep -c 'Shi et al. (2022) apontam' apresentacao/index.html` retorna 1 (voz autor prominente D-36)
    - Source: block INTRO-03a contém "BKT" literal. `awk '/SLIDE · INTRO-03a/,/<\/section>/' apresentacao/index.html | grep -c 'BKT'` retorna >= 1
    - Source: block INTRO-03a contém "DKT" literal. `awk '/SLIDE · INTRO-03a/,/<\/section>/' apresentacao/index.html | grep -c 'DKT'` retorna >= 1
    - Source: `grep -c 'Fonte: Shi et al. (2022).' apresentacao/index.html` retorna 1 (rodapé literal D-36)
    - Source: `awk '/SLIDE · INTRO-03a/,/<\/section>/' apresentacao/index.html | grep -c '<i>knowledge tracing</i>'` retorna >= 1 (itálico D-46)
    - Source NEGATIVO (GATE FORTE): block INTRO-03a NÃO contém "Code-DKT" (case-insensitive). `awk '/SLIDE · INTRO-03a/,/<\/section>/' apresentacao/index.html | grep -ci 'code-dkt'` retorna 0
    - Source NEGATIVO: SEM em-dash dentro do block. `awk '/SLIDE · INTRO-03a/,/<\/section>/' apresentacao/index.html | grep -c '—'` retorna 0
    - Source NEGATIVO: SEM `<blockquote>` no block. `awk '/SLIDE · INTRO-03a/,/<\/section>/' apresentacao/index.html | grep -c 'blockquote'` retorna 0
    - Source NEGATIVO: SEM aspas duplas literais do paper. `awk '/SLIDE · INTRO-03a/,/<\/section>/' apresentacao/index.html | grep -cE 'ignoring its content|binary correctness'` retorna 0
    - Source: `grep -c '<section data-background-color' apresentacao/index.html` retorna 15 (era 14 após plan 02-02; +1 com INTRO-03a = 15)
    - Behavior: re-leitura do arquivo confirma INTRO-03a vem APÓS INTRO-01 e ANTES de MARKER-01 (ordem dos comentários no arquivo)
  </acceptance_criteria>
  <done>
    Section INTRO-03a inserido em `apresentacao/index.html` entre INTRO-01 e MARKER-01; cabeçalho `> o problema do kt binário` literal; voz "Shi et al. (2022) apontam que..."; BKT e DKT mencionados; Code-DKT NÃO mencionado (gate); `<i>knowledge tracing</i>` em itálico; rodapé `Fonte: Shi et al. (2022).` literal; sem em-dash; sem `<blockquote>` nem aspas literais; total de sections no deck = 15.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Validação visual do INTRO-03a no browser</name>
  <what-built>
    Section INTRO-03a inserido entre INTRO-01 (#/7) e MARKER-01 (que desloca para #/9). Slide apresenta o diagnóstico do problema do KT binário em paráfrase indireta de Shi et al. (2022).
  </what-built>
  <how-to-verify>
    1. Em um terminal, rodar (ou manter o servidor):
       ```bash
       cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt/apresentacao && python3 -m http.server 8000
       ```
       (Se servidor já estava aberto e o conteúdo não recarregou, abrir em porta nova `8001`.)

    2. Abrir no navegador: `http://127.0.0.1:8000/#/8` (INTRO-03a)

    3. Inspecionar visualmente:
       - [ ] Cabeçalho `> o problema do kt binário` em cima, `>` em azul UniFacens, caret piscando ao fim
       - [ ] Parágrafo principal começa com "Shi et al. (2022) apontam que modelos clássicos de *knowledge tracing*,..."
       - [ ] BKT e DKT mencionados em negrito
       - [ ] *knowledge tracing* em itálico minúsculas
       - [ ] Segunda frase: "Toda a riqueza do código submetido fica fora do modelo." (ou variante)
       - [ ] **Code-DKT NÃO aparece** (gate visual)
       - [ ] SEM aspas literais entre `"` (paráfrase indireta)
       - [ ] Rodapé: `Fonte: Shi et al. (2022).` em Arial 18px cor cinza `#5b6472`, alinhado à esquerda
       - [ ] Marca d'água Facens canto superior direito
       - [ ] Sem texto saindo do slide; layout cabe em 1280×720

    4. Navegar próximo (`N` ou →): deve chegar em #/9 que é o MARKER-01.

    5. Console (F12): sem erros.
  </how-to-verify>
  <resume-signal>
    Aprovar com "approved" ou "ok intro-03a" se tudo bate visualmente. Se algo está fora, descrever.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Slide reveal.js estático (HTML/CSS sem backend, sem network, sem user input). Sem superfície de ataque relevante. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-N/A | — | slide HTML estático | N/A | Slide servido localmente via `python3 -m http.server`. Sem persistência, sem auth, sem rede externa. Threat model não se aplica. |
</threat_model>

<verification>
- Section INTRO-03a entre INTRO-01 e MARKER-01 (ordem confirmada via re-leitura)
- Cabeçalho `> o problema do kt binário` LITERAL (D-34b)
- Voz autor prominente "Shi et al. (2022) apontam que..." (D-36)
- BKT e DKT mencionados; Code-DKT NÃO mencionado (gate forte)
- `<i>knowledge tracing</i>` em itálico (D-46)
- Rodapé `Fonte: Shi et al. (2022).` LITERAL (D-45)
- SEM em-dash, SEM `<blockquote>`, SEM aspas literais
- Total sections no deck = 15
- Marca d'água presente
- Validação visual no browser passou
</verification>

<success_criteria>
1. **Metade de INTRO-03 entregue (diagnóstico):** o slide do problema do KT binário existe no deck.
2. **Fidelidade científica:** paráfrase indireta de Shi et al. (2022); rodapé canônico; sem overclaim (Code-DKT excluído, é a solução, fica para fase 4).
3. **Restrições atendidas:** sem citação direta literal (D-43), sem em-dash (D-44), sem Code-DKT (Pitfall 3).
4. **Voz autoral:** autor prominente (Shi como sujeito) mantém o tom da paráfrase indireta.
5. **Commit atômico:** `apresentacao: slide INTRO-03a - problema do kt binario (Shi et al. 2022)`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-intro-dataset-e-problema-fase-1-edm/02-03-SUMMARY.md` documentando:
- Linhas do HTML inseridas (intervalo)
- Microcópia exata escolhida para a segunda sentença
- Confirmação visual do reviewer humano
- Commit hash
</output>
