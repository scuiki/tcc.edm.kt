---
phase: 04-modelagem-e-avalia-o-fase-3-edm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apresentacao/index.html
  - apresentacao/assets/ast_codedkt_ptbr.svg
autonomous: false
requirements: [MODEL-01, MODEL-03]
must_haves:
  truths:
    - "Ao navegar para `#/16` no browser, o usuário lê o cabeçalho `> como o code-dkt funciona` e vê uma cronologia BKT (1995) → DKT (2015) → Code-DKT (2022) acima de duas colunas (texto à esquerda, AST à direita)."
    - "A AST renderiza visualmente como inset à direita do texto, escalada para caber em ~420px sem distorção, sem produzir 404 no console."
    - "O usuário não vê em-dash em parte alguma do slide novo; toda citação de Shi usa `<i>et al.</i>`; rodapé `Fonte:` aparece em cinza Arial 18px."
    - "Os slides existentes a partir do `#/16` (slide-code) deslocam para `#/17`; o deck navega do início ao novo `#/24` sem erro de console."
  artifacts:
    - path: "apresentacao/index.html"
      provides: "Section MODEL-01 inserida após MARKER-02 e antes do slide-code"
      contains: "como o code-dkt funciona"
    - path: "apresentacao/assets/ast_codedkt_ptbr.svg"
      provides: "Cópia local do SVG da AST para evitar path quebrado (Pitfall 6)"
      contains: "<svg"
  key_links:
    - from: "apresentacao/index.html (MODEL-01 section)"
      to: "apresentacao/assets/ast_codedkt_ptbr.svg"
      via: "<img src=\"assets/ast_codedkt_ptbr.svg\">"
      pattern: "src=\"assets/ast_codedkt_ptbr\\.svg\""
    - from: "MODEL-01 section"
      to: "componente .bridge-seq do tema"
      via: "<div class=\"deck-slide slide-related slide-bridge\"> herda regra .slide-bridge .bridge-seq"
      pattern: "deck-slide slide-related slide-bridge"
---

<objective>
Entregar o slide MODEL-01 (`> como o code-dkt funciona`), o primeiro slide novo do bloco de Modelagem (Fase 3 EDM) inserido entre MARKER-02 (`#/15`) e o slide-code (`#/16` → `#/17`). O slide combina (a) cronologia de 3 chips horizontais BKT (1995) → DKT (2015) → Code-DKT (2022) em `.bridge-seq` reaproveitada do slide Yağcí, (b) corpo em duas colunas com 1 frase de contexto + 4 bullets à esquerda e um inset SVG da AST à direita, (c) rodapé `Fonte: adaptado de Shi <i>et al.</i> (2022).`

Purpose: Apresentar a linhagem do Code-DKT (BKT → DKT → Code-DKT) e o mecanismo de alto nível (javalang → AST → code2vec → atenção → LSTM) em um único slide visual, conforme decisão de fusão MODEL-01+MODEL-02 (REQUIREMENTS.md, 2ª rodada). O slide é o ponto de abertura da Fase 3 da EDM no deck e deve casar visualmente com EDA-01/02 (mesmo template `slide-related`) e com Yağcí (mesmo `.bridge-seq`).

Output: 1 nova `<section>` em `apresentacao/index.html` + 1 novo asset SVG em `apresentacao/assets/ast_codedkt_ptbr.svg` (cópia mecânica). Sections do deck: 21 → 22 (intermediário, antes dos outros plans). 1 commit funcional. No-op declarado para MODEL-03 (reaproveita slide-code adjacente sem alteração).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-CONTEXT.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md
@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css

<interfaces>
<!-- Componentes CSS pré-existentes que MODEL-01 consome. NÃO redefinir. -->

Do theme-unifacens.css L42-50:
  .deck-topic                         /* cabeçalho `> [seção]`, Cascadia 24px cor #5b6472 */
  .deck-topic .ps1                    /* o `>` em azul UniFacens (--uni-blue #2667FF) */
  .caret, .caret.blink                /* caret piscando, 1.05s steps */

Do theme-unifacens.css L197-210 (regra aninhada em .slide-bridge):
  .slide-bridge .bridge-seq           /* container flex; padding ~28px; cantos retos */
  .slide-bridge .bridge-seq .step     /* caixa Word/ABNT: bg #fff, border 1.5px #1f1f1f, font 19px */
  .slide-bridge .bridge-seq .arr      /* seta preta, font 26px */
  NOTA: .bridge-seq SÓ ganha layout se o ancestral tiver classe .slide-bridge.

Do theme-unifacens.css (estilos do .slide-related):
  .rel-lead                           /* Arial 22-23px justificado (corpo de prosa) */
  .rel-cite                           /* Arial 18px cinza centralizado (rodapé Fonte:) */

Do index.html L16-21:
  <symbol id="sym">                   /* marca d'água Facens, consumida via <use href="#sym"/> */

Do index.html L135-147 (Yağcí, snippet de cronologia 3 chips):
  Template do uso já validado de .bridge-seq com 3 .step + 2 .arr.

Do docs/figures/ast_codedkt_ptbr.svg L2:
  viewBox="0 0 560 620"               /* AST nativa 560×620; escala automática com width:100% max-width:420px */
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copiar AST SVG para apresentacao/assets/</name>
  <files>apresentacao/assets/ast_codedkt_ptbr.svg</files>
  <read_first>
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md (Pitfall 6 — path relativo `../docs/figures/` quebra com python3 -m http.server rodado em `apresentacao/`)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md (Plan 3, secção "Risco-chave")
    - docs/figures/ast_codedkt_ptbr.svg (verificar que o arquivo existe e está íntegro; 4302 bytes, mai 17 15:08)
  </read_first>
  <action>
    Copiar `docs/figures/ast_codedkt_ptbr.svg` para `apresentacao/assets/ast_codedkt_ptbr.svg` SEM modificar conteúdo. Comando: `cp docs/figures/ast_codedkt_ptbr.svg apresentacao/assets/ast_codedkt_ptbr.svg`. Validar MD5 idêntico após cópia (`md5sum docs/figures/ast_codedkt_ptbr.svg apresentacao/assets/ast_codedkt_ptbr.svg` retorna o mesmo hash nas duas linhas). Esta cópia é obrigatória por Pitfall 6: `python3 -m http.server 8000` rodado dentro de `apresentacao/` não serve `../docs/`, então um `<img src="../docs/figures/...">` quebraria com 404; usar `<img src="assets/ast_codedkt_ptbr.svg">` na Task 2.
  </action>
  <acceptance_criteria>
    - `test -f apresentacao/assets/ast_codedkt_ptbr.svg` retorna 0 (arquivo existe).
    - `md5sum apresentacao/assets/ast_codedkt_ptbr.svg docs/figures/ast_codedkt_ptbr.svg | awk '{print $1}' | sort -u | wc -l` retorna `1` (MD5 idêntico entre origem e destino).
    - `grep -c 'viewBox="0 0 560 620"' apresentacao/assets/ast_codedkt_ptbr.svg` retorna `1` (SVG íntegro, viewBox preservado).
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>SVG da AST disponível em `apresentacao/assets/ast_codedkt_ptbr.svg` com MD5 igual ao original em `docs/figures/`; pronto para ser consumido por `<img src="assets/ast_codedkt_ptbr.svg">` no slide MODEL-01.</done>
</task>

<task type="auto">
  <name>Task 2: Inserir section MODEL-01 entre MARKER-02 e slide-code</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html (linhas 332-375 = MARKER-02 análogo de envelope; linhas 134-147 = Yağcí .bridge-seq com 3 chips, análogo de cronologia; linhas 149-164 = INTRO-03a, análogo de prosa em slide-related; linhas 377-378 = início do slide-code para confirmar ponto de inserção)
    - apresentacao/assets/theme-unifacens.css (L42-50 .deck-topic e .caret; L197-210 .slide-bridge .bridge-seq; .rel-lead e .rel-cite)
    - apresentacao/STYLE.md (§Cabeçalho `> [seção]`, §Regras de redação — sem em-dash, termos estrangeiros em itálico, estudantes nunca alunos, voz própria como padrão)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md (Plan 3 — snippet estrutural completo da Pattern 3; "Decisão de class explícita para MODEL-01: <div class=\"deck-slide slide-related slide-bridge\">")
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-CONTEXT.md (D-77a..D-77f — phrasing alvo travado)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md (Pattern 3; Pitfall 6 confirma `src="assets/ast_codedkt_ptbr.svg"`)
    - docs/Code-DKT.pdf §3.2 (Code Representation, Model Input, Model Structure) — confirmar phrasing dos 4 bullets sem citar literalmente (paráfrase indireta, D-86)
  </read_first>
  <action>
    Inserir uma nova `<section>` em `apresentacao/index.html` IMEDIATAMENTE APÓS o `</section>` do MARKER-02 (linha 375 atual) e ANTES do comentário `<!-- ============ SLIDE · O que o Code-DKT olha ... -->` (linha 377 atual). Cabeçalho de comentário acima da section: `<!-- ============ SLIDE · MODEL-01 · Como o Code-DKT funciona (cronologia BKT->DKT->Code-DKT; AST inset; Shi et al., 2022) ============ -->`.

    Estrutura da section (seguir literalmente o snippet da Pattern 3 em 04-PATTERNS.md L283-316):
    - `<section data-background-color="#F1F6FB">` com `<div class="deck-slide slide-related slide-bridge">` (a classe `slide-bridge` é OBRIGATÓRIA para herdar a regra CSS `.slide-bridge .bridge-seq` — sem ela, o flex layout dos chips quebra)
    - Marca d'água: `<svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>`
    - Cabeçalho: `<p class="deck-topic"><span class="ps1">&gt;</span>como o code-dkt funciona<span class="caret blink"></span></p>` (D-77a)
    - Cronologia em `<p class="bridge-seq" style="margin-top: 24px;">` com EXATAMENTE 3 `.step` + 2 `.arr`:
      - Chip 1: `<b>BKT</b> (1995)` + `<br>` + `<span style="font-size: 16px;">Bayes &middot; habilidades por KC</span>`
      - `<span class="arr">&rarr;</span>`
      - Chip 2: `<b>DKT</b> (2015)` + `<br>` + `<span style="font-size: 16px;">RNN &middot; histórico sequencial</span>`
      - `<span class="arr">&rarr;</span>`
      - Chip 3: `<b>Code-DKT</b> (2022)` + `<br>` + `<span style="font-size: 16px;">RNN + paths AST &middot; <i>code2vec</i></span>`
    - Body em duas colunas: `<div style="display: grid; grid-template-columns: 1fr 0.85fr; gap: 32px; margin-top: 28px;">`
      - Coluna esquerda: `<div>` contendo `<p class="rel-lead" style="margin: 0;">O Code-DKT incorpora o conteúdo do código que o DKT ignorava, mantendo o <i>tracing</i> sequencial.</p>` + `<ul style="font-family: Arial; font-size: 21px; margin-top: 18px; padding-left: 22px; line-height: 1.55;">` com 4 `<li>` na ordem D-77c: (1) `<code>javalang</code> &rarr; AST`; (2) `<i>code2vec</i> &rarr; caminhos folha-a-folha`; (3) `atenção pondera os caminhos`; (4) `LSTM combina com (<code>ProblemID</code>, acerto/erro)`
      - Coluna direita: `<div style="display: flex; align-items: center; justify-content: center;">` contendo `<img src="assets/ast_codedkt_ptbr.svg" alt="AST com um caminho folha-a-folha entre input e &quot;valor&quot;" style="width: 100%; max-width: 420px; height: auto;">`
    - Rodapé: `<p class="rel-cite">Fonte: adaptado de Shi <i>et al.</i> (2022).</p>` (D-77e, D-88 ABNT)

    Regras vinculantes em todo o markup:
    - Nenhum em-dash (`—` U+2014); usar vírgula, dois-pontos ou parênteses (D-87)
    - `<i>et al.</i>` ABNT em qualquer citação Shi (D-88)
    - Termos estrangeiros em itálico: `<i>code2vec</i>`, `<i>tracing</i>` (D-88). NÃO itálico: BKT, DKT, Code-DKT, RNN, AST, LSTM, ProblemID
    - "Estudantes" (não "alunos") se o termo aparecer (não aparece neste slide, mas vale como princípio — D-89)
    - Voz autoral, paráfrase indireta; NENHUMA citação direta literal (D-86)
    - Comentário HTML acima da `<section>` no formato `<!-- ============ SLIDE · ... ============ -->` (convenção do deck)
  </action>
  <acceptance_criteria>
    - `grep -c 'class="deck-slide slide-related slide-bridge"' apresentacao/index.html` aumenta em 1 vs HEAD (passa de 1 para 2; MODEL-01 herda a classe que estava só no Yağcí L136).
    - `grep -c 'como o code-dkt funciona' apresentacao/index.html` retorna pelo menos `1`.
    - `grep -c 'MODEL-01' apresentacao/index.html` retorna pelo menos `1` (comentário HTML).
    - `grep -c 'src="assets/ast_codedkt_ptbr.svg"' apresentacao/index.html` retorna `1`.
    - `grep -c 'Fonte: adaptado de Shi <i>et al.</i> (2022).' apresentacao/index.html` aumenta em 1 (MODEL-01 adiciona a linha; outras instâncias podem existir em rodapé alheio).
    - `awk '/SLIDE · MODEL-01/,/SLIDE · O que o Code-DKT olha/' apresentacao/index.html | grep -c '—'` retorna `0` (sem em-dash na region MODEL-01).
    - `awk '/SLIDE · MODEL-01/,/SLIDE · O que o Code-DKT olha/' apresentacao/index.html | grep -c 'Shi (2022)[^a-z]'` retorna `0` E `awk ... | grep -c 'Shi <i>et al.</i> (2022)'` retorna pelo menos `1` (Shi sempre com et al. em itálico ABNT).
    - `grep -n 'class="step"' apresentacao/index.html | awk -F: 'NR==FNR{next} {print}'` mostra 3 ocorrências NOVAS na MODEL-01 (3 chips). Verificação alternativa: contagem total de `class="step"` aumenta em 3.
    - `grep -c '<ul style="font-family: Arial; font-size: 21px' apresentacao/index.html` retorna `1` (lista de 4 bullets).
    - `grep -c '<li><code>javalang</code> &rarr; AST</li>' apresentacao/index.html` retorna `1`.
    - O número total de `<section>` em `apresentacao/index.html` aumenta em 1 vs HEAD.
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>Section MODEL-01 inserida com cabeçalho `> como o code-dkt funciona`, cronologia 3 chips em `.bridge-seq`, 2 colunas (texto + AST inset), rodapé `Fonte: adaptado de Shi <i>et al.</i> (2022).`. Zero em-dash. ABNT `et al.` aplicado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual humano — MODEL-01 no browser</name>
  <what-built>Section MODEL-01 (`#/16` após inserção) com cronologia BKT (1995) → DKT (2015) → Code-DKT (2022), texto à esquerda + inset SVG da AST à direita, rodapé `Fonte: adaptado de Shi <i>et al.</i> (2022).`</what-built>
  <how-to-verify>
    1. Em terminal: `cd apresentacao && python3 -m http.server 8000`. Se a porta estiver ocupada, usar `8001`.
    2. No browser: abrir `http://127.0.0.1:8000/#/16` (slide MODEL-01).
    3. Conferir visualmente:
       - Cabeçalho `> como o code-dkt funciona` em Cascadia, com caret piscando ao final, ponta `>` em azul UniFacens.
       - Cronologia em 3 caixas (chips) ABNT (fundo branco, borda preta 1.5px, cantos retos), com setas pretas `→` entre elas. Conteúdo das caixas (esquerda → direita): `BKT (1995) / Bayes · habilidades por KC`, `DKT (2015) / RNN · histórico sequencial`, `Code-DKT (2022) / RNN + paths AST · code2vec`.
       - Abaixo da cronologia: duas colunas. À esquerda, a frase de contexto + lista de 4 bullets com `javalang`, `code2vec`, atenção, LSTM. À direita, a AST renderiza (figura com nodos conectados, caminho folha-a-folha em vermelho destacado).
       - SVG da AST cabe na coluna direita (largura aprox. 420px); sem barra de rolagem; sem 404 no console DevTools.
       - Rodapé `Fonte: adaptado de Shi <i>et al.</i> (2022).` em Arial 18px cinza centralizado; `et al.` em itálico.
       - Marca d'água Facens visível no canto superior direito.
    4. Navegar `#/15` → `#/16` → `#/17` com ←/→: MARKER-02 → MODEL-01 → slide-code, sem erro de console.
    5. Abrir DevTools (F12) > Console: nenhum erro vermelho (esp. nenhum 404 para `ast_codedkt_ptbr.svg`).
    6. Se algo soar visualmente quebrado (caixas com alturas diferentes, texto cortado, AST distorcida), descrever na resposta para iteração textual antes do commit.
  </how-to-verify>
  <resume-signal>Type "approved" para liberar commit. Se houver iterações textuais (microcópia, alturas), descrever os ajustes pedidos.</resume-signal>
  <action>Human reviewer follows the steps in &lt;how-to-verify&gt; in a local browser (`cd apresentacao && python3 -m http.server 8000`), then responds per &lt;resume-signal&gt;. Implementer waits for explicit approval before proceeding to the next task.</action>
  <verify>
    <automated>MISSING - reviewer signal recorded in resume response; no automated check substitutes the human visual approval. Executor MUST NOT proceed without "approved" or equivalent decision.</automated>
  </verify>
  <done>Human reviewer approved the slide visually (or recorded a specific pick / fallback decision), unblocking subsequent tasks.</done>
</task>

<task type="auto">
  <name>Task 4: Commit funcional do MODEL-01</name>
  <files>apresentacao/index.html, apresentacao/assets/ast_codedkt_ptbr.svg</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (lowercase português, prefixo `apresentacao:` nas commit messages)
    - git log --oneline -10 (confirmar padrão de mensagens recentes, e.g. `c92b9ff apresentacao: slide INTRO-03b - sinal pedagogico perdido`)
  </read_first>
  <action>
    Stagear os arquivos modificados (`git add apresentacao/index.html apresentacao/assets/ast_codedkt_ptbr.svg`) e criar commit funcional com mensagem no padrão do projeto:

    `apresentacao: slide MODEL-01 - como o code-dkt funciona (cronologia + AST inset, Shi et al. 2022)`

    Sem assinatura Co-Authored-By (alinhar com commits anteriores como `c92b9ff` que não usam). Sem `--amend`. Em caso de pre-commit hook falhar, diagnosticar e fazer um commit NOVO (não amendar).
  </action>
  <acceptance_criteria>
    - `git log --oneline -1` mostra a mensagem começando com `apresentacao: slide MODEL-01`.
    - `git diff HEAD~1 --name-only` lista exatamente `apresentacao/index.html` e `apresentacao/assets/ast_codedkt_ptbr.svg`.
    - `git status` retorna working tree limpo.
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>1 commit funcional contendo MODEL-01 inserido e SVG copiado.</done>
</task>

</tasks>

<no_op_coverage>
**MODEL-03 (REQ-ID coberto neste plan a título de traceability):** O slide-code (atual `#/16`, vira `#/17` após esta inserção) é reaproveitado como MODEL-03 sem qualquer alteração de conteúdo. Já foi reformatado na fase 1 com cabeçalho `> o que o code-dkt olha` (D-10) e movido para o bloco final (D-16). Esta fase apenas confirma a posição. Não tocar em `<div class="deck-slide slide-code">` (linhas 379-426 atuais). Conformidade com D-80 (CONTEXT 04).
</no_op_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev local → browser | navegação local via `python3 -m http.server 8000`; deck servido como conteúdo estático; **sem auth, sem network externa, sem input do usuário, sem endpoint, sem estado persistido** |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | I (Information Disclosure) | SVG inline em `<img src="assets/ast_codedkt_ptbr.svg">` | accept | SVG é asset estático local auditado (cópia byte-a-byte de `docs/figures/`, MD5 validado em Task 1); nenhum conteúdo dinâmico. Padrão herdado das fases 1-3 (`fig-codedkt-martins-curves.png` etc. seguem o mesmo modelo). |
| T-04-02 | T (Tampering) | `apresentacao/index.html` | accept | apenas dev local; deck não é deploado nesta fase; o git é o controle de integridade. Cada `<section>` é diff revisado antes do commit. |
| T-04-03 | S/D/E/R | n/a | n/a | sem auth, sem network, sem persistência, sem endpoint — categorias inaplicáveis a um slide deck estático servido em loopback. |

No attack surface significativo. Slide deck estático servido por `http.server` local apenas em dev; nenhum input do usuário; nenhum endpoint; nenhum estado persistido. XSS via SVG inline já mitigada pelo padrão herdado das fases 1-3 (SVGs vêm de assets locais auditados, não input remoto).
</threat_model>

<verification>
Após o checkpoint humano e antes/durante o commit:
- `grep -c 'como o code-dkt funciona' apresentacao/index.html` >= 1
- `grep -c 'src="assets/ast_codedkt_ptbr.svg"' apresentacao/index.html` == 1
- `grep -c 'class="deck-slide slide-related slide-bridge"' apresentacao/index.html` >= 2 (era 1 antes desta fase)
- `awk '/SLIDE · MODEL-01/,/SLIDE · O que o Code-DKT olha/' apresentacao/index.html | grep -c '—'` == 0 (sem em-dash)
- `awk '/SLIDE · MODEL-01/,/SLIDE · O que o Code-DKT olha/' apresentacao/index.html | grep -c 'Shi <i>et al.</i> (2022)'` >= 1
- `md5sum apresentacao/assets/ast_codedkt_ptbr.svg docs/figures/ast_codedkt_ptbr.svg | awk '{print $1}' | sort -u | wc -l` == 1
- `python3 -m http.server 8000 -d apresentacao` em background, então `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8000/index.html` retorna `200`; `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8000/assets/ast_codedkt_ptbr.svg` retorna `200`.
</verification>

<success_criteria>
- Section MODEL-01 inserida entre MARKER-02 (`</section>` na linha 375 atual) e slide-code (`<!-- ============ SLIDE · O que o Code-DKT olha ... -->` na linha 377 atual).
- SVG `ast_codedkt_ptbr.svg` copiado para `apresentacao/assets/` com MD5 idêntico ao original em `docs/figures/`.
- Cabeçalho `> como o code-dkt funciona` em Cascadia com caret piscando.
- Cronologia 3 chips BKT → DKT → Code-DKT em `.bridge-seq` herdada via `slide-bridge`.
- 2 colunas: texto (1 frase contexto + 4 bullets) à esquerda; AST inset à direita.
- Rodapé `Fonte: adaptado de Shi <i>et al.</i> (2022).` (`<i>et al.</i>` ABNT D-88).
- Zero em-dash. Termos estrangeiros (`<i>code2vec</i>`, `<i>tracing</i>`) em itálico.
- Browser navega `#/15` → `#/16` (MODEL-01) → `#/17` (slide-code) sem erro de console.
- Checkpoint humano aprovado.
- 1 commit funcional no padrão `apresentacao: slide MODEL-01 - ...`.
- D-80 honrado: slide-code (MODEL-03) não tocado.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-01-SUMMARY.md` documenting: (a) commit hash; (b) eventuais iterações textuais pós-checkpoint (microcópia ajustada, fallback para `.chrono-step` se aplicado); (c) confirmação de no-op para MODEL-03; (d) decisões ad-hoc se houver (e.g. ajuste de `font-size` na cronologia, mudança de bullet phrasing).
</output>
</content>
</invoke>