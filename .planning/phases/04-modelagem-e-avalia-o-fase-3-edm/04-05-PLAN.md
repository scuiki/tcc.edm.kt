---
phase: 04-modelagem-e-avalia-o-fase-3-edm
plan: 05
type: execute
wave: 5
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - apresentacao/index.html
  - apresentacao/STYLE.md
autonomous: false
requirements: [MARKER-03]
must_haves:
  truths:
    - "Ao navegar para `#/24` no browser (último slide do deck pós-fase 4), o usuário vê um slide marcador com 4 pills: as 3 primeiras (Definição do Problema, Preparação dos Dados, Modelagem e Avaliação) em `--done` com check; a 4ª (Implantação) em `--running` com ícone de reload girando."
    - "Pill 4 mostra badge `[running]` em Cascadia azul, visível (sem classe `marker-badge--empty`); pills 1, 2, 3 mostram badge `[done]`."
    - "Animação `marker-spin` aplica APENAS na pill 4 (Implantação); pills 1-3 estão estáticas."
    - "Título `> AS QUATRO FASES DA EDM` em caixa alta com caret piscando; rodapé `Fonte: adaptado de Zorić (2020).`."
    - "Deck navega `#/0` → `#/24` (MARKER-03) sem erro de console; total final de sections = 25 (21 pré-fase 4 + 4 novos)."
    - "STYLE.md §Inventário de slides atualizado com as 4 entradas novas (MODEL-01, MODEL-04, MODEL-05, MARKER-03) e reposicionamentos; §Gaps reservados realocado para apontar à fase 5."
  artifacts:
    - path: "apresentacao/index.html"
      provides: "Section MARKER-03 ao final do `<div class=\"slides\">`"
      contains: "slide-marker--phase3"
    - path: "apresentacao/STYLE.md"
      provides: "Inventário atualizado pós-fase 4 (25 slides) e §Gaps reservados realocado"
      contains: "MARKER-03"
  key_links:
    - from: "MARKER-03 section"
      to: "componente .slide-marker (CSS L358-452)"
      via: "<div class=\"deck-slide slide-marker slide-marker--phase3\">"
      pattern: "slide-marker slide-marker--phase3"
    - from: "Pill 4 Implantação"
      to: "@keyframes marker-spin (CSS L447-450)"
      via: "classe .marker-pill--running"
      pattern: "marker-pill marker-pill--running"
---

<objective>
Entregar o slide MARKER-03 (sem cabeçalho temático, apenas o título estilizado `> AS QUATRO FASES DA EDM`) ao final do deck. Implementação 100% mecânica: copy-paste do markup de MARKER-02 + 4 deltas (classe modificadora `--phase3`; pill 3 vira `--done` com check; pill 4 vira `--running` com reload; badges atualizadas). Zero CSS novo. Após inserir o slide, atualizar `apresentacao/STYLE.md` §Inventário de slides para refletir o estado final de 25 sections e realocar §Gaps reservados para a fase 5.

Purpose: Cumprir Success Criteria 7 do ROADMAP (MARKER-03 "Modelagem e Avaliação ✓") e Success Criteria 8 (deck navega até MARKER-03 sem quebra). Fecha narrativamente a Fase 3 da EDM no deck. Pill 4 em `--running` antecipa a fase 5 (TOOL-01/03 da implantação) sem fechar tudo (MARKER-04 da fase 5 finaliza).

Output: 1 nova `<section>` em `apresentacao/index.html` (ao fim do `<div class="slides">`) + STYLE.md atualizado (§Inventário + §Gaps reservados). Sections do deck: 24 → 25. 1 commit funcional (markup + STYLE.md juntos, alinhado com padrão da fase 1 plan 01-07 que mesclava STYLE.md no commit funcional).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-CONTEXT.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md
@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css

<interfaces>
<!-- Componentes CSS pré-existentes que MARKER-03 consome. ZERO CSS NOVO (D-84a). -->

Do theme-unifacens.css (commit 5d44606, CI/CD ABNT redesign):
  L368-373 .slide-marker                       /* container flex column, padding 52px 64px 40px */
  L374    .slide-marker .wm                    /* marca d'água absoluta */
  L376-382 .marker-title + .marker-title .ps1  /* Arial 24px caixa alta, letterspacing */
  L384-387 .marker-track                       /* flex row, gap 14px, max-width 1180px */
  L388-391 .marker-stage                       /* flex column, gap 8px */
  L392-409 .marker-pill                        /* retângulo branco, borda 1.5px, fonte Arial 17px */
  L411-416 .marker-pill--done .marker-pill-icon   /* check branco em círculo azul */
  L418-423 .marker-pill--running .marker-pill-icon /* reload azul girando, animation marker-spin */
  L426-432 .marker-pill--pending                  /* cinza */
  L434-438 .marker-arrow                          /* seta preta */
  L440-444 .marker-badge                          /* Cascadia mono 14px, cor --uni-blue */
  L445    .marker-badge--empty                   /* visibility: hidden (oculta a badge) */
  L447-450 @keyframes marker-spin                 /* rotação 360° contínua, 2.4s linear infinite */
  L452    .slide-marker .rel-cite                 /* Arial 18px cinza */

Estado das pills por fase (todos as fases respondem ao mesmo componente):
  MARKER-01: pill 1 done · pills 2-4 pending
  MARKER-02: pills 1-2 done · pill 3 running · pill 4 pending (empty badge)
  MARKER-03 (este plan): pills 1-3 done · pill 4 running (badge visível)
  MARKER-04 (fase 5, fora deste plan): todas done (provavelmente)

Markup análogo (MARKER-02 atual, ~L332-375 mas deslocado após 04-01/02/03/04):
  Localizar dinamicamente com `grep -n 'slide-marker--phase2' apresentacao/index.html`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inserir section MARKER-03 ao final do `<div class="slides">`</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html (linhas do MARKER-02 atual, localizadas com `grep -n 'slide-marker--phase2' apresentacao/index.html` — esse é o template a copiar; linhas finais do `<div class="slides">` localizadas com `grep -n '</div>' apresentacao/index.html | tail -10` — a `</section>` final do slide-fig precede o fechamento `</div></div>` em L553-554 atuais; ponto de inserção = IMEDIATAMENTE APÓS o `</section>` do slide-fig CLOSE-03 e ANTES do fechamento do `<div class="slides">`)
    - apresentacao/assets/theme-unifacens.css (L358-452 — toda a família `.slide-marker` para confirmar regras invocadas)
    - apresentacao/STYLE.md (§Identidade visual; §Cabeçalho — MARKER usa `.marker-title` que é variante específica, NÃO `.deck-topic`)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md (Plan 1 — snippet HTML completo do MARKER-02 análogo L37-83 + tabela dos 4 deltas L87-97; D-84b travado)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-CONTEXT.md (D-84a..D-84d)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md (Pattern 1; reuso 100% mecânico)
  </read_first>
  <action>
    Localizar dinamicamente o `</section>` final do slide-fig (CLOSE-03 após 04-04) e a linha do fechamento `</div></div>` que encerra `<div class="slides">` e `<div class="reveal">`. A `<section>` MARKER-03 vai IMEDIATAMENTE ANTES de `</div></div>` e DEPOIS do `</section>` do slide-fig. Antes da section, inserir o comentário:
    `<!-- ============ SLIDE · MARKER · As quatro fases da EDM, fase 3 concluida (Zoric, 2020) ============ -->`

    Estrutura da section (copy-paste do snippet PATTERNS Plan 1 L37-83 + 4 deltas D-84b):

    - `<section data-background-color="#F1F6FB">`
    - `<div class="deck-slide slide-marker slide-marker--phase3">` **(DELTA 1: phase2 → phase3)**
    - Marca d'água `<svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>` (idêntico ao MARKER-02)
    - Título: `<p class="marker-title"><span class="ps1">&gt;</span>AS QUATRO FASES DA EDM<span class="caret blink"></span></p>` (literal em caixa alta no markup; CSS `.marker-title` aplica letterspacing; idêntico ao MARKER-02)
    - `<div class="marker-track">` contendo na ordem:
      - `marker-stage` Pill 1 (Definição do Problema): `.marker-pill marker-pill--done` + `&check;` + nome + badge `[done]` (IDÊNTICO ao MARKER-02)
      - `<span class="marker-arrow">&rarr;</span>`
      - `marker-stage` Pill 2 (Preparação dos Dados): `.marker-pill marker-pill--done` + `&check;` + nome + badge `[done]` (IDÊNTICO ao MARKER-02)
      - `<span class="marker-arrow">&rarr;</span>`
      - `marker-stage` Pill 3 (Modelagem e Avaliação): `.marker-pill marker-pill--done` + `&check;` + nome + badge `[done]` **(DELTA 2: era `--running` com `&#x21BB;` e badge `[running]` no MARKER-02; agora done)**
      - `<span class="marker-arrow">&rarr;</span>`
      - `marker-stage` Pill 4 (Implantação): `.marker-pill marker-pill--running` + `&#x21BB;` + nome + badge SEM classe `--empty` com texto `[running]` **(DELTA 3: era `--pending` com `&#x25CB;` e badge `marker-badge--empty []` no MARKER-02; agora running + badge visível)**
    - `<p class="rel-cite">Fonte: adaptado de Zorić (2020).</p>` (idêntico ao MARKER-02)

    Regras vinculantes:
    - Zero CSS novo (D-84a). Tudo herda de `theme-unifacens.css`.
    - Pill 4 badge: a classe `marker-badge--empty` faz `visibility: hidden`; REMOVER essa classe (não apenas trocar o conteúdo) é o que torna `[running]` visível.
    - Ícones HTML entities exatos: `&check;` (check), `&#x21BB;` (clockwise gapped circle arrow, reload), `&#x25CB;` (circle white) — só o `&check;` e `&#x21BB;` aparecem em MARKER-03; nenhum `&#x25CB;` (era da pill 4 pending).
    - Cabeçalho `> AS QUATRO FASES DA EDM` em CAIXA ALTA no markup (alinhado com MARKER-02; o CSS reforça com `text-transform`, mas literal já é caixa alta).
    - Rodapé `Fonte: adaptado de Zorić (2020).` (D-84c idêntico ao MARKER-02; Zorić tem 1 autor, NÃO `et al.`).
    - Sem em-dash (D-87, embora MARKER-03 não tenha prosa livre).
  </action>
  <acceptance_criteria>
    - `grep -c 'slide-marker--phase3' apresentacao/index.html` retorna `1`.
    - `grep -c 'SLIDE · MARKER · As quatro fases da EDM, fase 3 concluida' apresentacao/index.html` retorna `1`.
    - `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c 'marker-pill marker-pill--done'` retorna `3` (pills 1, 2, 3).
    - `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c 'marker-pill marker-pill--running'` retorna `1` (pill 4).
    - `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c 'marker-pill marker-pill--pending'` retorna `0` (NENHUMA pending).
    - `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c '&check;'` retorna `3` (3 checks nas pills done).
    - `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c '&#x21BB;'` retorna `1` (ícone reload, somente na pill 4 running).
    - `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c '&#x25CB;'` retorna `0` (nenhum ícone pending — diferencia de MARKER-02).
    - `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c 'marker-badge--empty'` retorna `0` (DELTA 3 aplicado).
    - `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c '\[done\]'` retorna `3`.
    - `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c '\[running\]'` retorna `1`.
    - `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c 'Fonte: adaptado de Zorić (2020).'` retorna `1`.
    - `grep -c '<section data-background-color="#F1F6FB">' apresentacao/index.html` aumenta em 1 vs estado pós-04-04.
    - `grep -c 'AS QUATRO FASES DA EDM' apresentacao/index.html` aumenta em 1 vs estado pós-fase 3 (era 2: MARKER-01 + MARKER-02; agora 3: + MARKER-03).
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>Section MARKER-03 inserida ao final do `<div class="slides">`, com pills 1-3 em `--done` (check + `[done]`) e pill 4 em `--running` (reload + `[running]` visível). Markup com 4 deltas D-84b aplicados; zero CSS novo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual humano — MARKER-03 + navegação fim-a-fim do deck</name>
  <what-built>Section MARKER-03 ao final do deck (`#/24`); marcador da Fase 3 EDM com pills 1-3 done e pill 4 running animada. Toda a Fase 4 do GSD encerrada: deck final de 25 sections (`#/0` a `#/24`).</what-built>
  <how-to-verify>
    1. Em terminal: `cd apresentacao && python3 -m http.server 8000`.
    2. No browser: abrir `http://127.0.0.1:8000/#/24` (MARKER-03 final).
    3. Conferir visualmente:
       - Título `> AS QUATRO FASES DA EDM` em caixa alta (Arial 24px), com caret piscando ao final.
       - 4 pills horizontais separadas por 3 setas `→`:
         - Pill 1 `Definição do Problema`: check branco em círculo azul preenchido, badge `[done]` em Cascadia azul abaixo.
         - Pill 2 `Preparação dos Dados`: idem pill 1.
         - Pill 3 `Modelagem e Avaliação`: idem pill 1 (esta é a fase que acabou de fechar).
         - Pill 4 `Implantação`: ícone de reload (`↻`) AZUL e GIRANDO continuamente (animação `marker-spin`, 2.4s), badge `[running]` em Cascadia azul abaixo.
       - Rodapé `Fonte: adaptado de Zorić (2020).` em Arial 18px cinza centralizado.
       - Marca d'água Facens no canto superior direito.
    4. **Navegação fim-a-fim D-91:** abrir `#/0` e navegar de `→` em `→` até `#/24` (25 slides totais; 25 cliques de seta).
       - Sem erro de console em nenhum slide (DevTools F12 > Console; nenhum vermelho).
       - Verificar especialmente os 4 slides novos da fase 4: `#/16` (MODEL-01) com SVG AST visível, `#/18` (MODEL-04) com tabela `.eda-grid` numérica, `#/19` (MODEL-05) com pipeline 5 caixas, `#/24` (MARKER-03) com animação pill 4.
       - CLOSE-01 (`#/21`), CLOSE-02 (`#/22`), CLOSE-03 (`#/23`) navegam corretamente; conteúdo intacto.
    5. Se a animação spin não estiver girando na pill 4, ou se alguma pill done não tiver o check em círculo azul, descrever para iteração.
  </how-to-verify>
  <resume-signal>Type "approved" para liberar Task 3 (STYLE.md update + commit). Se houver iterações visuais ou de console, descrever os ajustes necessários antes do commit.</resume-signal>
  <action>Human reviewer follows the steps in &lt;how-to-verify&gt; in a local browser (`cd apresentacao && python3 -m http.server 8000`), then responds per &lt;resume-signal&gt;. Implementer waits for explicit approval before proceeding to the next task.</action>
  <verify>
    <automated>MISSING - reviewer signal recorded in resume response; no automated check substitutes the human visual approval. Executor MUST NOT proceed without "approved" or equivalent decision.</automated>
  </verify>
  <done>Human reviewer approved the slide visually (or recorded a specific pick / fallback decision), unblocking subsequent tasks.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar STYLE.md (Inventário + §Gaps reservados)</name>
  <files>apresentacao/STYLE.md</files>
  <read_first>
    - apresentacao/STYLE.md (linhas 108-141 — inventário atual pós-fase 3 com 21 slides; linhas 136-141 — §Gaps reservados que precisam realocação para fase 5)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-CONTEXT.md (Claude's Discretion "Atualização do STYLE.md §Inventário de slides ao fim da fase 4")
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md (idem)
  </read_first>
  <action>
    Etapa A — atualizar §Inventário de slides (linhas 108-132 atuais) acrescentando as 4 entradas novas e renumerando todas que deslocaram. Estado final esperado (25 slides, índice 0-based):

    Linhas 0-15 INTOCADAS (mesmas da pós-fase 3). Acrescentar/alterar a partir do índice 16:

    | # | classe | cabeçalho | conteúdo |
    |---|---|---|---|
    | 16 | slide-related slide-bridge | `> como o code-dkt funciona` | MODEL-01: cronologia BKT (1995) → DKT (2015) → Code-DKT (2022); 2 colunas com texto + AST inset (Shi <i>et al.</i>, 2022) |
    | 17 | slide-code | `> o que o code-dkt olha` | MODEL-03 (slide-code reaproveitado, deslocado de 16): atenção do Code-DKT no `&&` da submissão real (Code-DKT, Shi <i>et al.</i>, 2022) |
    | 18 | slide-related | `> code-dkt no csedm` | MODEL-04: Tabela 2 ABNT first-attempt AUC × 5 assignments + linha Shi (D-78g; Shi <i>et al.</i>, 2022 Tab. 2) |
    | 19 | slide-related slide-bridge | `> extração automática de kcs` | MODEL-05: pipeline 5 etapas (sampling → LLM → clustering → rotulagem → Q-matrix) (Duan <i>et al.</i>, 2025) |
    | 20 | slide-kcfig | `> kcs semânticos extraídos` | Mapeamento dos KCs (saída visual do pipeline MODEL-05) para dificuldades de Martins (Duan <i>et al.</i>, 2025; Martins, Marin e Alves, 2024) |
    | 21 | slide-problem | `> retomando o problema` | CLOSE-01 (Martins p2, 13 autores; citação direta mantida) |
    | 22 | slide-problem | `> retomando o problema` | CLOSE-02 (Martins p3, 10 autores) |
    | 23 | slide-fig | `> evolução por dificuldade` | CLOSE-03: curva de aprendizado do Code-DKT por sub-dificuldade (PENDING-04 resolvido em 04-04) |
    | 24 | slide-marker--phase3 | (sem temático) | MARKER-03 — Fase 3 EDM concluída; pill 4 (Implantação) em `--running` apontando para a fase 5 |

    Logo abaixo da tabela, atualizar a frase de Estado: "**Estado do deck:** 25 slides após a fase 4 (era 21 pós-fase 3; 4 novos da fase 4 inseridos: MODEL-01 entre MARKER-02 e slide-code; MODEL-04 entre slide-code e slide-kcfig; MODEL-05 entre MODEL-04 e slide-kcfig; MARKER-03 ao fim do deck)."

    Etapa B — reescrever §Gaps reservados (linhas 136-141 atuais) para apontar à fase 5:

    "**Gaps reservados para fase 5:**\n\n- Após MARKER-03 (slide 24): TOOL-01 (proposta da ferramenta TCC 2 + pipeline mini-horizontal), TOOL-03 (dashboard), MARKER-04 (Fase 4 EDM concluída — todas as 4 pills em `--done`), END-01 (agradecimento).\n- AGENDA-01 (slide 2) será revisado na fase 5 para refletir a estrutura final do deck (PENDING-01)."

    Etapa C — atualizar §Linhagem de KT (linha 141 atual) substituindo "preenchida em fases futuras" por "consolidada na fase 4" para refletir que Corbett & Anderson e Piech entraram no MODEL-01 cronologia, e Duan no MODEL-05.

    Regras gerais: sem em-dash (D-87); `<i>et al.</i>` ABNT (D-88); markdown linkando ao README se for o caso. NÃO alterar §Identidade visual, §Tipografia, §Cabeçalho (estáveis desde fase 1).
  </action>
  <acceptance_criteria>
    - `grep -c '> como o code-dkt funciona' apresentacao/STYLE.md` retorna pelo menos `1`.
    - `grep -c '> code-dkt no csedm' apresentacao/STYLE.md` retorna pelo menos `1`.
    - `grep -c '> extração automática de kcs' apresentacao/STYLE.md` retorna pelo menos `1`.
    - `grep -c 'slide-marker--phase3' apresentacao/STYLE.md` retorna pelo menos `1`.
    - `grep -c 'MODEL-01' apresentacao/STYLE.md` retorna pelo menos `1`.
    - `grep -c 'MARKER-03' apresentacao/STYLE.md` retorna pelo menos `1`.
    - `grep -c '25 slides após a fase 4' apresentacao/STYLE.md` retorna `1`.
    - `grep -c 'TOOL-01' apresentacao/STYLE.md` retorna pelo menos `1` (§Gaps fase 5).
    - `grep -c '—' apresentacao/STYLE.md` permanece 0 (sem em-dash inserido).
    - `grep -c 'Antes do trio Martins+fig (entre MARKER-02/slide-code e Martins p2)' apresentacao/STYLE.md` retorna `0` (gap antigo da fase 4 substituído).
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>STYLE.md §Inventário lista 25 slides com 4 entradas novas e reposicionamentos; §Gaps reservados realocados para fase 5; §Linhagem de KT marcada como consolidada.</done>
</task>

<task type="auto">
  <name>Task 4: Commit funcional do MARKER-03 + STYLE.md (fecha a fase 4)</name>
  <files>apresentacao/index.html, apresentacao/STYLE.md</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md
    - git log --oneline -10 (padrão `c92b9ff apresentacao: slide INTRO-03b - sinal pedagogico perdido`; precedente similar de inclusão de STYLE.md no commit funcional: `907a4b5 apresentacao: atualizar STYLE.md para padrão > [seção] (D-21, D-25)`)
  </read_first>
  <action>
    Stagear ambos arquivos: `git add apresentacao/index.html apresentacao/STYLE.md`.

    Criar commit funcional fechando a fase 4 com mensagem:
    `apresentacao: slide MARKER-03 + STYLE inventário pós-fase 4 (Modelagem e Avaliação ✓, pill 4 running)`

    Sem `Co-Authored-By`. Sem `--amend`.

    Após commit, validar:
    - `git log --oneline -1` mostra a mensagem do MARKER-03.
    - `git diff HEAD~1 --name-only` lista exatamente `apresentacao/index.html` e `apresentacao/STYLE.md`.
    - `git status` retorna working tree limpo.
  </action>
  <acceptance_criteria>
    - `git log --oneline -1` mostra mensagem começando com `apresentacao: slide MARKER-03`.
    - `git diff HEAD~1 --name-only | sort` retorna exatamente `apresentacao/STYLE.md` e `apresentacao/index.html`.
    - `git status` retorna working tree limpo.
    - O deck pós-commit tem 25 sections totais: `grep -c '<section data-background-color="#F1F6FB">' apresentacao/index.html` >= 23 (24 slides com background F1F6FB; capa+title-tcc usam cores diferentes) — checar consistência narrativa.
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>Fase 4 fechada com 1 commit funcional contendo MARKER-03 + STYLE.md atualizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev local → browser | deck servido localmente via `python3 -m http.server`; sem auth, sem network externa, sem input do usuário, sem persistência |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | T (Tampering) | Markup MARKER-02 copiado para MARKER-03 | mitigate | acceptance criteria com `grep` específico por cada um dos 4 deltas (D-84b) — pill 3 done, pill 4 running, badge empty removida, classe phase3 |
| T-04-16 | T (Tampering) | STYLE.md §Inventário pode divergir do deck real | mitigate | acceptance criteria valida que cabeçalhos travados (`> como o code-dkt funciona`, etc.) aparecem em STYLE.md; reviewer humano confirma no checkpoint |
| T-04-17 | I (Information Disclosure) | n/a | n/a | nenhum dado sensível em STYLE.md ou no marcador |
| T-04-18 | S/R/D/E | n/a | n/a | sem auth, sem network, sem persistência, sem endpoint |

No attack surface significativo. Slide deck estático servido por `http.server` local apenas em dev; nenhum input do usuário; nenhum endpoint; nenhum estado persistido.
</threat_model>

<verification>
- `grep -c 'slide-marker--phase3' apresentacao/index.html` == 1
- `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c 'marker-pill marker-pill--done'` == 3
- `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c 'marker-pill marker-pill--running'` == 1
- `awk '/SLIDE · MARKER · As quatro fases da EDM, fase 3/,/<\/section>/' apresentacao/index.html | grep -c 'marker-badge--empty'` == 0
- `grep -c '25 slides após a fase 4' apresentacao/STYLE.md` == 1
- checkpoint humano APPROVED (navegação fim-a-fim do `#/0` ao `#/24` sem erro)
</verification>

<success_criteria>
- Section MARKER-03 inserida ao final do `<div class="slides">` (após CLOSE-03/slide-fig).
- Pills 1-3 em `--done` (Definição do Problema, Preparação dos Dados, Modelagem e Avaliação) + Pill 4 em `--running` (Implantação) com `[running]` visível e ícone girando.
- Zero CSS novo (D-84a).
- Cabeçalho `> AS QUATRO FASES DA EDM` em caixa alta com caret piscando.
- Rodapé `Fonte: adaptado de Zorić (2020).` (sem `et al.`, Zorić tem 1 autor).
- STYLE.md §Inventário atualizado para 25 slides com 4 entradas novas e reposicionamentos.
- STYLE.md §Gaps reservados realocados para fase 5 (TOOL-01, TOOL-03, MARKER-04, END-01, AGENDA-01).
- Navegação `#/0` → `#/24` sem erro de console.
- 1 commit funcional no padrão `apresentacao: slide MARKER-03 + STYLE inventário pós-fase 4 ...`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-05-SUMMARY.md` documenting: (a) commit hash; (b) confirmação dos 4 deltas D-84b aplicados; (c) eventual iteração textual pós-checkpoint (microcópia das pills ou rodapé); (d) confirmação do estado final do deck = 25 sections; (e) ponto de gancho para a fase 5 (TOOL-01 entra após MARKER-03 a `#/25`).

Depois, criar `.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/PHASE-SUMMARY.md` agregando os 5 plans 04-01..04-05 — modelo: `.planning/phases/03-eda-e-pr-processamento-fase-2-edm/PHASE-SUMMARY.md` (mesma estrutura).
</output>
</content>
</invoke>