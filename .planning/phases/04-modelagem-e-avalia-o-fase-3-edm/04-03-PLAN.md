---
phase: 04-modelagem-e-avalia-o-fase-3-edm
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - apresentacao/index.html
  - apresentacao/assets/theme-unifacens.css
autonomous: false
requirements: [MODEL-05]
must_haves:
  truths:
    - "Ao navegar para `#/19` (após inserções 04-01+04-02+04-03), o usuário lê o cabeçalho `> extração automática de kcs`, a frase de abertura `Construímos um pipeline de cinco etapas para extrair Knowledge Components do CSEDM.`, um pipeline horizontal de 5 caixas em `.bridge-seq`, a frase de fechamento sobre código bruto vs AST, e o rodapé Duan."
    - "As 5 caixas do pipeline têm a ordem exata D-79d: Sampling → LLM → Clustering → Rotulagem → Q-matrix; cada caixa tem 2 linhas (verbo bold + descrição curta); 4 setas separam as 5 caixas."
    - "A caixa 5 NÃO inclui o número '28 KCs' (D-79d-rev); rótulo é `Q-matrix / por <i>assignment</i>` sem número."
    - "Slide-kcfig (próximo, `#/20`) navega sem quebra logo após MODEL-05 (não duplica KCs nem mapeamento)."
    - "Toda citação Duan usa `<i>et al.</i>`; zero em-dash; primeira pessoa do plural na voz."
  artifacts:
    - path: "apresentacao/index.html"
      provides: "Section MODEL-05 inserida entre MODEL-04 e slide-kcfig"
      contains: "extração automática de kcs"
    - path: "apresentacao/assets/theme-unifacens.css"
      provides: "Classe `.bridge-seq--narrow` adicionada APENAS se 5 caixas não couberem em 1280px no checkpoint visual (último recurso)"
      contains: ".bridge-seq--narrow"
  key_links:
    - from: "apresentacao/index.html (MODEL-05 section)"
      to: "componente .bridge-seq do tema (via .slide-bridge)"
      via: "<div class=\"deck-slide slide-related slide-bridge\">"
      pattern: "deck-slide slide-related slide-bridge"
    - from: "MODEL-05 (pipeline)"
      to: "slide-kcfig (saída visual do pipeline, REQ-ID slide-kcfig)"
      via: "ordem narrativa do deck: MODEL-05 → slide-kcfig sem duplicação"
      pattern: "extração automática de kcs.*kcs semânticos extraídos"
---

<objective>
Entregar o slide MODEL-05 (`> extração automática de kcs`), inserido entre MODEL-04 e o slide-kcfig (saída visual do pipeline). Estrutura: 1 frase de abertura → pipeline horizontal de 5 caixas em `.bridge-seq` estendida → 1 frase de fechamento sobre código bruto vs AST → rodapé Duan. As 5 caixas implementam o pipeline KCGen-KT na forma do notebook 03b (sampling → LLM → clustering → rotulagem → Q-matrix), override registrado em D-79f sobre PROJECT.md original (que dizia 3 etapas).

Purpose: Cumprir Success Criteria 4 do ROADMAP (Duan + pipeline 5 etapas; código bruto não AST; saída em slide-kcfig que segue no deck sem duplicação). MODEL-05 é o slide-irmão de MODEL-01 (mesma família `.bridge-seq`, mas pipeline em vez de cronologia), e introduz a contribuição de KCs semânticos do TCC. Risco principal: 5 caixas em 1280px com CSS calibrado para 3 — mitigação tipográfica documentada e fallback CSS reservado para checkpoint.

Output: 1 nova `<section>` em `apresentacao/index.html`. CSS opcional `.bridge-seq--narrow` (último recurso, somente se checkpoint visual confirmar layout quebrado). Sections do deck: 23 → 24 (após 04-01+04-02). 1 commit funcional (ou 2 se fallback CSS for ativado pós-checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-CONTEXT.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md
@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css

<interfaces>
<!-- Componentes CSS reusados e regra estendida condicional -->

Do theme-unifacens.css L197-210 (regra aninhada em .slide-bridge):
  .slide-bridge .bridge-seq           /* flex container; testado em 3 caixas (Yağcí) */
  .slide-bridge .bridge-seq .step     /* font 19px; .step ocupa flex: 1 1 0 → fração igual */
  .slide-bridge .bridge-seq .arr      /* seta preta, font 26px */

Cálculo para 5 caixas em 1280px (Pitfall 7):
  padding lateral ~64px × 2 = 128px
  4 setas × ~16px = 64px
  espaço útil = 1280 - 128 - 64 = 1088px
  por caixa: 1088 / 5 ≈ 218px
  → APERTADO; calibrar font-size 17px + 2 linhas <br> + ≤18 chars/linha

Eventual fallback CSS condicional (somente se browser confirmar quebra):
  .slide-bridge .bridge-seq--narrow .step  { font-size: 16px; padding: 14px 12px; }
  .slide-bridge .bridge-seq--narrow .arr   { font-size: 22px; margin: 0 6px; }

Phrasing-alvo travado (D-79c, D-79d, D-79e):
  Abertura: "Construímos um pipeline de cinco etapas para extrair Knowledge Components do CSEDM."
  Caixas (ordem fixa):
    1. Sampling | n=5/problema
    2. LLM | KCs brutos
    3. Clustering | Sentence-BERT + HAC
    4. Rotulagem | LLM nos clusters
    5. Q-matrix | por assignment (SEM número, D-79d-rev)
  Fechamento: "A decisão-chave foi alimentar o LLM com código bruto, não AST (Duan et al., 2025, Tab. 4)."
  Rodapé: "Fonte: adaptado de Duan et al. (2025)."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inserir section MODEL-05 entre MODEL-04 e slide-kcfig</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html (linhas 134-147 = Yağcí `.bridge-seq` 3 chips, análogo direto; trecho da section MODEL-04 já inserida por 04-02 = `</section>` âncora superior; trecho da section slide-kcfig começando com `<!-- ============ SLIDE · KCs semânticos extraídos ... -->` = âncora inferior)
    - apresentacao/assets/theme-unifacens.css (L197-210 `.slide-bridge .bridge-seq` + `.step` + `.arr`)
    - apresentacao/STYLE.md (§Regras de redação — voz primeira pessoa do plural, sem em-dash, termos estrangeiros em itálico; §Cabeçalho)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md (Plan 4 — snippet HTML completo, calibração tipográfica obrigatória)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-CONTEXT.md (D-79a..D-79i — phrasing travado, D-79d-rev resolveu caixa 5 sem número)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md (Pattern 4; Pitfall 3 — Duan está em `docs/AutomatedKC.pdf`, NÃO em `docs/2025.EDM.short-papers.83.pdf` que é srcML-DKT; Pitfall 4 — paper Duan tem 3 etapas, nosso pipeline tem 5; paráfrase autoral "construímos" deixa claro; Pitfall 5 — sem número de KCs na caixa 5; Pitfall 7 — 5 caixas em 1280px requer calibração)
    - docs/AutomatedKC.pdf §3.1 e Fig.1 caption "three-step automated KC generation"; Tab. 4 (código bruto vs AST); Tab. 5 (n=5 sampling) — confirmar phrasing autoral sem citar literalmente
    - notebooks/03b_kc_generation.ipynb cell 0 (5 etapas listadas literalmente; fonte da expansão 3 → 5)
  </read_first>
  <action>
    Localizar dinamicamente as âncoras com `grep -n 'SLIDE · KCs semânticos extraídos' apresentacao/index.html` (essa linha é a âncora inferior; a `<section>` MODEL-05 vai IMEDIATAMENTE ANTES desse comentário). A âncora superior é o `</section>` da MODEL-04 inserida pelo plan 04-02. Inserir cabeçalho de comentário acima da nova section: `<!-- ============ SLIDE · MODEL-05 · Extração automática de KCs (pipeline 5 etapas; Duan et al., 2025) ============ -->`.

    Estrutura da section (seguir literalmente o snippet da Pattern 4 em 04-PATTERNS.md L362-388):
    - `<section data-background-color="#F1F6FB">` com `<div class="deck-slide slide-related slide-bridge">` (`slide-bridge` OBRIGATÓRIO para herdar regra CSS `.slide-bridge .bridge-seq`)
    - Marca d'água: `<svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>`
    - Cabeçalho: `<p class="deck-topic"><span class="ps1">&gt;</span>extração automática de kcs<span class="caret blink"></span></p>` (D-79a)
    - Abertura: `<p class="rel-lead">Construímos um <i>pipeline</i> de cinco etapas para extrair <i>Knowledge Components</i> do CSEDM.</p>` (D-79c; "Construímos" = primeira pessoa do plural, D-79g; `<i>pipeline</i>` em itálico estrangeiro D-88; `<i>Knowledge Components</i>` em itálico estrangeiro)
    - Pipeline `<p class="bridge-seq" style="margin-top: 28px; font-size: 17px;">` com EXATAMENTE 5 `.step` + 4 `.arr` na ordem D-79d (preservando phrasing curto para caber em 218px/caixa por Pitfall 7):
      - `<span class="step"><b>Sampling</b><br>n=5/problema</span>`
      - `<span class="arr">&rarr;</span>`
      - `<span class="step"><b>LLM</b><br>KCs brutos</span>`
      - `<span class="arr">&rarr;</span>`
      - `<span class="step"><b>Clustering</b><br>Sentence-BERT + HAC</span>`
      - `<span class="arr">&rarr;</span>`
      - `<span class="step"><b>Rotulagem</b><br>LLM nos clusters</span>`
      - `<span class="arr">&rarr;</span>`
      - `<span class="step"><b>Q-matrix</b><br>por <i>assignment</i></span>` (D-79d-rev SEM número de KCs)
    - Fechamento: `<p class="rel-lead" style="margin-top: 28px;">A decisão-chave foi alimentar o LLM com código bruto, não AST (Duan <i>et al.</i>, 2025, Tab. 4).</p>` (D-79e)
    - Rodapé: `<p class="rel-cite">Fonte: adaptado de Duan <i>et al.</i> (2025).</p>` (D-79h)

    Regras vinculantes:
    - Voz em primeira pessoa do plural: "Construímos", "alimentar" (D-79g)
    - `<i>et al.</i>` em Duan (Duan tem 7 autores Fernandez/Hassany/Sampaio/Brusilovsky/Akram/Lan; D-88, anti-pattern `Duan (2025)` sem et al.)
    - Termos estrangeiros em itálico: `<i>pipeline</i>`, `<i>Knowledge Components</i>`, `<i>assignment</i>`
    - NÃO itálico: Sentence-BERT, HAC, LLM, AST, KCs (nomes próprios/abreviações)
    - Sem em-dash `—` (D-87); usar vírgula ou parênteses
    - "Estudantes" se aparecer (não aparece neste slide; vale como princípio D-89)
    - NENHUMA citação direta literal Duan (D-86); apenas paráfrase autoral
    - Caixa 5 SEM número de KCs (D-79d-rev, Pitfall 5): rótulo final é `Q-matrix / por <i>assignment</i>`. NÃO escrever "28 KCs" nem "17 KCs" nem nenhum número — slide-kcfig (próximo no deck) mostra a contagem real.
    - NÃO confundir paper Duan (`docs/AutomatedKC.pdf`) com srcML-DKT (`docs/2025.EDM.short-papers.83.pdf`, Pankiewicz et al.) — Pitfall 3
  </action>
  <acceptance_criteria>
    - `grep -c 'extração automática de kcs' apresentacao/index.html` retorna pelo menos `1`.
    - `grep -c 'MODEL-05' apresentacao/index.html` retorna pelo menos `1`.
    - `awk '/SLIDE · MODEL-05/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -c 'class="step"'` retorna `5` (exatamente 5 caixas).
    - `awk '/SLIDE · MODEL-05/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -c 'class="arr"'` retorna `4` (exatamente 4 setas).
    - `grep -F 'Construímos um <i>pipeline</i> de cinco etapas' apresentacao/index.html` retorna pelo menos `1`.
    - `grep -F 'A decisão-chave foi alimentar o LLM com código bruto, não AST' apresentacao/index.html` retorna pelo menos `1`.
    - `grep -c 'Duan <i>et al.</i> (2025)' apresentacao/index.html` aumenta em pelo menos 2 (fechamento + rodapé); existe pelo menos uma ocorrência prévia no slide-kcfig (Fonte:).
    - `awk '/SLIDE · MODEL-05/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -cE '28 KCs|17 KCs|72 KCs|70 KCs'` retorna `0` (sem número de KCs na caixa 5 — D-79d-rev / Pitfall 5).
    - `awk '/SLIDE · MODEL-05/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -c '—'` retorna `0` (sem em-dash).
    - `awk '/SLIDE · MODEL-05/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -cE 'Pankiewicz|srcML-DKT'` retorna `0` (sem confusão de papers — Pitfall 3).
    - `awk '/SLIDE · MODEL-05/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -c 'Duan (2025)[^a-z]'` retorna `0` E `grep -c 'Duan <i>et al.</i>'` retorna pelo menos `1` (sempre com et al.).
    - O número total de `<section>` em `apresentacao/index.html` aumenta em 1 vs estado pós 04-02.
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>Section MODEL-05 inserida com cabeçalho `> extração automática de kcs`, abertura "Construímos um pipeline...", 5 caixas + 4 setas em `.bridge-seq`, fechamento sobre código bruto vs AST, rodapé Duan. Caixa 5 sem número de KCs. Sem em-dash. ABNT et al. aplicado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual humano — MODEL-05 no browser (verifica layout 5 caixas)</name>
  <what-built>Section MODEL-05 (`#/19` após inserção) com pipeline horizontal de 5 caixas (Sampling → LLM → Clustering → Rotulagem → Q-matrix) em `.bridge-seq` estendida; abertura "Construímos um pipeline de cinco etapas..."; fechamento "A decisão-chave foi alimentar o LLM com código bruto, não AST..."; rodapé Duan.</what-built>
  <how-to-verify>
    1. Em terminal: `cd apresentacao && python3 -m http.server 8000` (ou 8001 se ocupada).
    2. No browser: abrir `http://127.0.0.1:8000/#/19` (slide MODEL-05).
    3. Conferir visualmente:
       - Cabeçalho `> extração automática de kcs` em Cascadia com caret piscando.
       - Abertura em 1 parágrafo (`pipeline` e `Knowledge Components` em itálico).
       - 5 caixas brancas com borda preta 1.5px (cantos retos, padrão ABNT/Word), separadas por 4 setas pretas `→`.
       - Em cada caixa: verbo em negrito (linha 1) + descrição em fonte regular (linha 2).
       - Caixa 1 = `Sampling / n=5/problema`; Caixa 2 = `LLM / KCs brutos`; Caixa 3 = `Clustering / Sentence-BERT + HAC`; Caixa 4 = `Rotulagem / LLM nos clusters`; Caixa 5 = `Q-matrix / por assignment` (sem número de KCs).
       - **VALIDAÇÃO CRÍTICA (Pitfall 7):** as 5 caixas cabem em 1280px sem que texto seja cortado, sem barra de rolagem horizontal, sem caixas com alturas drasticamente diferentes; setas alinhadas verticalmente com o meio das caixas.
       - Fechamento em 1 parágrafo abaixo do pipeline; citação `(Duan <i>et al.</i>, 2025, Tab. 4)` no fim da frase.
       - Rodapé `Fonte: adaptado de Duan <i>et al.</i> (2025).` em cinza Arial 18px centralizado.
       - Marca d'água Facens no canto superior direito.
    4. Navegar `#/18` → `#/19` → `#/20`: MODEL-04 → MODEL-05 → slide-kcfig, sem erro de console; transição narrativa MODEL-05 → slide-kcfig deve fluir (slide-kcfig mostra os KCs gerados; MODEL-05 não duplica).
    5. Abrir DevTools (F12) > Console: nenhum erro vermelho.
    6. **Cenários de quebra de layout (acionar fallback se ocorrer):**
       - texto de uma caixa estoura da borda (overflow horizontal),
       - 2 caixas com alturas visivelmente diferentes (> 8px),
       - barra de rolagem horizontal aparece no slide,
       - setas desalinhadas verticalmente das caixas.
       Se qualquer um dos 4 cenários acontecer, descrever na resposta para acionar a Task 3 (fallback CSS).
  </how-to-verify>
  <resume-signal>Type "approved" para liberar commit. Se layout quebrou em 1280px, escrever "fallback" para acionar Task 3. Se houver iterações textuais (microcópia, ajuste de verbo), descrever os ajustes pedidos.</resume-signal>
  <action>Human reviewer follows the steps in &lt;how-to-verify&gt; in a local browser (`cd apresentacao && python3 -m http.server 8000`), then responds per &lt;resume-signal&gt;. Implementer waits for explicit approval before proceeding to the next task.</action>
  <verify>
    <automated>MISSING - reviewer signal recorded in resume response; no automated check substitutes the human visual approval. Executor MUST NOT proceed without "approved" or equivalent decision.</automated>
  </verify>
  <done>Human reviewer approved the slide visually (or recorded a specific pick / fallback decision), unblocking subsequent tasks.</done>
</task>

<task type="auto">
  <name>Task 3: Fallback CSS .bridge-seq--narrow (CONDICIONAL — somente se Task 2 sinalizar quebra de layout)</name>
  <files>apresentacao/assets/theme-unifacens.css, apresentacao/index.html</files>
  <read_first>
    - apresentacao/assets/theme-unifacens.css (L197-210 — regra original `.slide-bridge .bridge-seq` para entender padding e font-size de base)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md (Pitfall 7 mitigação 4 = "último recurso, não default")
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md ("CSS new (conditional, last resort only)")
  </read_first>
  <action>
    **EXECUTAR APENAS SE TASK 2 sinalizou quebra de layout** ("fallback" no resume-signal). Se Task 2 aprovou sem ressalvas, PULAR esta task.

    Etapa A — anexar regra CSS em `apresentacao/assets/theme-unifacens.css` ao final do arquivo (depois da última regra `.slide-bridge .bridge-seq ...`):

    ```
    /* MODEL-05 — variante estreita do .bridge-seq para 5 caixas em 1280px (Pitfall 7) */
    .slide-bridge .bridge-seq--narrow { gap: 6px; }
    .slide-bridge .bridge-seq--narrow .step { font-size: 16px; padding: 14px 12px; }
    .slide-bridge .bridge-seq--narrow .arr  { font-size: 22px; margin: 0 4px; }
    ```

    Etapa B — em `apresentacao/index.html`, na section MODEL-05, trocar `<p class="bridge-seq" style="margin-top: 28px; font-size: 17px;">` para `<p class="bridge-seq bridge-seq--narrow" style="margin-top: 28px;">` (remover o inline-style font-size porque a regra cobre; manter `margin-top`).

    Etapa C — refazer a verificação visual da Task 2 (mesmos 4 cenários de quebra; se ainda quebrar, escalar ao usuário para microcópia ainda mais curta nas caixas).
  </action>
  <acceptance_criteria>
    - SE Task 2 aprovou direto: este arquivo CSS NÃO é modificado; `git status` na CSS retorna sem mudanças; este step é skipped.
    - SE Task 2 sinalizou fallback: `grep -c '.bridge-seq--narrow' apresentacao/assets/theme-unifacens.css` retorna pelo menos `1`; `grep -c 'class="bridge-seq bridge-seq--narrow"' apresentacao/index.html` retorna `1`; re-verificação visual passa.
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>Layout MODEL-05 estabilizado em 1280px, com ou sem fallback CSS conforme decisão do checkpoint.</done>
</task>

<task type="auto">
  <name>Task 4: Commit funcional do MODEL-05</name>
  <files>apresentacao/index.html, apresentacao/assets/theme-unifacens.css</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (lowercase português, prefixo `apresentacao:`)
    - git log --oneline -10 (padrão `c92b9ff apresentacao: slide INTRO-03b - ...`)
  </read_first>
  <action>
    Stagear os arquivos modificados:
    - SE Task 3 NÃO executou: `git add apresentacao/index.html`
    - SE Task 3 executou: `git add apresentacao/index.html apresentacao/assets/theme-unifacens.css`

    Criar commit funcional com mensagem:
    - Caso default: `apresentacao: slide MODEL-05 - extração automática de kcs (pipeline 5 etapas, Duan et al. 2025)`
    - Caso fallback CSS ativado: pode ser commit único com mensagem `apresentacao: slide MODEL-05 + .bridge-seq--narrow fallback (pipeline 5 etapas, Duan et al. 2025)`, ou 2 commits separados (CSS primeiro, depois HTML).

    Sem `Co-Authored-By`. Sem `--amend`.
  </action>
  <acceptance_criteria>
    - `git log --oneline -1` mostra mensagem começando com `apresentacao: slide MODEL-05`.
    - `git diff HEAD~1 --name-only` lista `apresentacao/index.html` (e talvez `apresentacao/assets/theme-unifacens.css` se Task 3 executou).
    - `git status` retorna working tree limpo.
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>1 (ou 2) commit funcional contendo MODEL-05 inserido (e opcional fallback CSS).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev local → browser | deck servido localmente via `python3 -m http.server`; sem auth, sem network externa, sem input do usuário, sem persistência |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | T (Tampering) | Phrasing Duan vs srcML-DKT (Pitfall 3) | mitigate | acceptance criteria explícito grep `Pankiewicz\|srcML-DKT` == 0 dentro da region MODEL-05; reviewer humano confirma no checkpoint visual |
| T-04-08 | I (Information Disclosure) | Pipeline 5 etapas vs paper 3 etapas (Pitfall 4) | mitigate | phrasing-alvo "Construímos um pipeline de cinco etapas" é paráfrase autoral (1ª pessoa do plural), evita afirmar "Duan propõe 5 etapas" (falso); D-79g confirma; reviewer humano valida transparência narrativa |
| T-04-09 | T (Tampering) | Caixa 5 sem número de KCs (D-79d-rev) | mitigate | acceptance criteria explícito grep `(28\|17\|72\|70) KCs` == 0; reviewer humano confirma no checkpoint visual |
| T-04-10 | S/R/D/E | n/a | n/a | sem auth, sem network, sem persistência, sem endpoint |

No attack surface significativo. Slide deck estático servido por `http.server` local apenas em dev; nenhum input do usuário; nenhum endpoint; nenhum estado persistido.
</threat_model>

<verification>
- `grep -c 'extração automática de kcs' apresentacao/index.html` >= 1
- `awk '/SLIDE · MODEL-05/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -c 'class="step"'` == 5
- `awk '/SLIDE · MODEL-05/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -c 'class="arr"'` == 4
- `awk '/SLIDE · MODEL-05/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -cE '28 KCs|17 KCs|72 KCs|70 KCs'` == 0
- `awk '/SLIDE · MODEL-05/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -c '—'` == 0
- `awk '/SLIDE · MODEL-05/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -cE 'Pankiewicz|srcML-DKT'` == 0
- Checkpoint humano APPROVED (com ou sem fallback CSS)
</verification>

<success_criteria>
- Section MODEL-05 inserida entre MODEL-04 (`</section>`) e slide-kcfig (`<!-- ============ SLIDE · KCs semânticos extraídos ... -->`).
- Cabeçalho `> extração automática de kcs`.
- Abertura `Construímos um pipeline de cinco etapas para extrair Knowledge Components do CSEDM.` (voz 1ª pessoa do plural, D-79g).
- Pipeline em `.bridge-seq` com EXATAMENTE 5 `.step` + 4 `.arr` na ordem D-79d.
- Caixa 5 (Q-matrix) SEM número de KCs (D-79d-rev, Pitfall 5).
- Fechamento `A decisão-chave foi alimentar o LLM com código bruto, não AST (Duan <i>et al.</i>, 2025, Tab. 4).`.
- Rodapé `Fonte: adaptado de Duan <i>et al.</i> (2025).`.
- Zero em-dash; toda citação Duan com `<i>et al.</i>`; nenhuma menção a srcML-DKT ou Pankiewicz.
- Layout valida em 1280px no checkpoint (com ou sem fallback `.bridge-seq--narrow`).
- 1 (ou 2) commit funcional no padrão `apresentacao: slide MODEL-05 - ...`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-03-SUMMARY.md` documenting: (a) commit hash(es); (b) se Task 3 (fallback CSS) executou ou não, e por quê; (c) iterações textuais pós-checkpoint (microcópia das caixas se ajustada); (d) confirmação que caixa 5 ficou sem número de KCs.
</output>
</content>
</invoke>