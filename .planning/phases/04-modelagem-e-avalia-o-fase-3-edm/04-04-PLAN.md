---
phase: 04-modelagem-e-avalia-o-fase-3-edm
plan: 04
type: execute
wave: 4
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - apresentacao/index.html
  - apresentacao/assets/fig-codedkt-difficulty.png
  - apresentacao/assets/fig-codedkt-kc-curves.png
  - apresentacao/assets/fig-codedkt-level-vs-slope.png
  - apresentacao/assets/fig-codedkt-martins-curves.png
autonomous: false
requirements: [CLOSE-01, CLOSE-02, CLOSE-03, PENDING-04]
must_haves:
  truths:
    - "Após o plan, o slide-fig (`#/23`) exibe UM PNG escolhido pelo usuário no checkpoint visual; os 3 slides temporários `slide-fig-pick-N` foram REMOVIDOS; sections do deck voltam a 24 (após 04-01/02/03 inserirem 3 e este plan adicionar 0 no líquido)."
    - "Comentário HTML linha ~541 (após 04-01/02/03 deslocaram tudo) agora aponta ao filename real do PNG no slide-fig (consistência Pitfall 9)."
    - "`<p class=\"fig-read\">` reflete o insight legítimo do PNG escolhido; se o candidato 1 (default `curves_by_martins`) for mantido, o texto atual sobre 'Estruturas de controle' permanece; caso contrário, foi refraseado."
    - "CLOSE-01 (Martins p2) e CLOSE-02 (Martins p3) NÃO foram tocados em conteúdo; permanecem no deck reformatados pela fase 1 com `> retomando o problema` e citação direta literal preservada (D-28)."
    - "Asset PNG escolhido reside em `apresentacao/assets/fig-codedkt-martins-curves.png` (filename canônico mantido por D-83a); 3 assets temporários removidos do disco se o usuário escolheu candidato 1, ou substituídos se escolheu outro."
  artifacts:
    - path: "apresentacao/index.html"
      provides: "slide-fig (CLOSE-03) consistente: comentário linha ~541 corrigido + img + fig-read coerentes"
      contains: "src=\"assets/fig-codedkt-martins-curves.png\""
    - path: "apresentacao/assets/fig-codedkt-martins-curves.png"
      provides: "PNG canônico de CLOSE-03 (substitui ou mantém conforme escolha)"
      contains: "PNG binary"
  key_links:
    - from: "apresentacao/index.html (slide-fig CLOSE-03)"
      to: "apresentacao/assets/fig-codedkt-martins-curves.png"
      via: "<img src=\"assets/fig-codedkt-martins-curves.png\">"
      pattern: "src=\"assets/fig-codedkt-martins-curves.png\""
    - from: "Comentário HTML CLOSE-03"
      to: "PNG real escolhido"
      via: "<!-- figura: results/<nome>.png -->"
      pattern: "<!-- figura: results/fig_codedkt_"
---

<objective>
Resolver PENDING-04 (pick visual do gráfico Code-DKT) sem retreinar nada — todos os 4 PNGs candidatos já existem em `results/` desde 2026-05-25 (D-83a). O plan executa o ciclo "inserir 3 candidatos temporários no deck → checkpoint visual humano → cleanup" para que o usuário escolha visualmente 1 dos 4 PNGs candidatos. Em seguida, corrige o comentário HTML que aponta a filename errado (Pitfall 9) e refrasea `<p class="fig-read">` se a escolha exigir.

Purpose: Cumprir Success Criteria 6 do ROADMAP (gráfico CLOSE-03 validado antes da inclusão; PENDING-04 resolvido) e o Critério 1 do CLAUDE.md (figura de evidência alinhada aos números canônicos). CLOSE-03 é parte do eixo prioritário da defesa (CLOSE-01 + CLOSE-02 + CLOSE-03, 30-40s cada) — qualidade do gráfico é vinculante. O plan TAMBÉM documenta as no-ops de CLOSE-01 e CLOSE-02 (Martins p2/p3) para fechar traceability dos REQ-IDs.

Output: 0 sections líquidas adicionadas (3 temporários inseridos + 3 removidos); `apresentacao/index.html` com slide-fig consistente; 1-3 PNGs em `apresentacao/assets/` (1 sempre presente; 3 temporários removidos no cleanup). 1 commit funcional (o checkpoint + cleanup gera um único commit semântico).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-CONTEXT.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md
@apresentacao/STYLE.md
@apresentacao/index.html

<interfaces>
<!-- Componentes CSS já existentes; nenhuma classe nova. -->

Do theme-unifacens.css (slide-fig family, pré-existente):
  .slide-fig                          /* container */
  .fig-wrap                           /* wrapper do <img> */
  .fig-read                           /* frase de leitura abaixo da figura */
  .fig-fonte                          /* rodapé Fonte: */

Do index.html L540-551 (slide-fig CLOSE-03 atual, pré-existente):
  - <!-- ... SLIDE · Evolução por dificuldade ... -->
  - <!-- figura: results/fig_codedkt_difficulty_martins.png ... -->   ← LINHA INCONSISTENTE (Pitfall 9)
  - <img src="assets/fig-codedkt-martins-curves.png" ...>            ← img real (curves_by_martins por MD5)
  - <p class="fig-read"><b>Estruturas de controle</b> ...</p>         ← insight específico de curves_by_martins
  - <p class="fig-fonte">Fonte: ... Shi <i>et al.</i>, 2022; ... Martins, Marin e Alves, 2024.</p>

PNGs candidatos disponíveis em results/ (D-83b):
  1. fig_codedkt_curves_by_martins.png  (145.673 B, 2026-05-25 02:02; MD5 idêntico ao asset atual)
  2. fig_codedkt_difficulty_martins.png ( 95.266 B, 2026-05-25 01:48)
  3. fig_codedkt_kc_curves.png          (186.582 B, 2026-05-25 01:42; 28 curvas por KC)
  4. fig_codedkt_level_vs_slope.png     (132.890 B, 2026-05-25 01:42; scatter)

Insight atual (`<p class="fig-read">`) é específico do candidato 1; ver D-83d.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copiar 3 PNGs candidatos para apresentacao/assets/ e inserir 3 sections temporárias</name>
  <files>apresentacao/index.html, apresentacao/assets/fig-codedkt-difficulty.png, apresentacao/assets/fig-codedkt-kc-curves.png, apresentacao/assets/fig-codedkt-level-vs-slope.png</files>
  <read_first>
    - apresentacao/index.html (linhas ~540-551 do slide-fig atual; localizar dinamicamente com `grep -n 'SLIDE · Evolução por dificuldade' apresentacao/index.html` — esse comentário pode ter deslocado de L540 por inserções dos plans 04-01/02/03; encontrar a `</section>` que segue o `<p class="fig-fonte">` para ser a âncora superior dos temporários)
    - apresentacao/STYLE.md (§Cabeçalho — manter formato `> [seção]` mesmo nos slides temporários para evitar surpresa visual)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md (Plan 5 — Etapa A; snippet HTML completo dos 3 temporários L477-509)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-CONTEXT.md (D-83b — lista dos 4 candidatos; D-83c — procedimento; D-83a — confirma que MD5 do candidato 1 = asset atual)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md (Pitfall 9 — comentário L541; "Procedimento de pick visual" no PATTERNS Plan 5)
    - ls -la results/fig_codedkt_*.png (confirmar que os 4 arquivos existem com tamanhos esperados)
  </read_first>
  <action>
    Etapa A — copiar 3 PNGs ausentes para `apresentacao/assets/` (o candidato 1 já está como `assets/fig-codedkt-martins-curves.png`):

    ```
    cp results/fig_codedkt_difficulty_martins.png apresentacao/assets/fig-codedkt-difficulty.png
    cp results/fig_codedkt_kc_curves.png         apresentacao/assets/fig-codedkt-kc-curves.png
    cp results/fig_codedkt_level_vs_slope.png    apresentacao/assets/fig-codedkt-level-vs-slope.png
    ```

    Etapa B — inserir 3 `<section>` temporários em `apresentacao/index.html` IMEDIATAMENTE APÓS o `</section>` do slide-fig atual (CLOSE-03 candidato 1, sem modificar a section CLOSE-03 original) e ANTES da próxima `<section>` (será o MARKER-03 quando 04-05 rodar; agora é o fechamento `</div></div>` do `<div class="slides">`).

    Cada `<section>` temporário usa a estrutura do snippet em PATTERNS Plan 5 (L477-509):
    - `<section data-background-color="#F1F6FB">` + `<div class="deck-slide slide-fig" id="slide-fig-pick-N">` (N=2,3,4)
    - Marca d'água `<svg class="wm">` padrão
    - Cabeçalho `<p class="deck-topic"><span class="ps1">&gt;</span>pick: <descritor><span class="caret blink"></span></p>` onde `<descritor>` é o filename curto (ex: `difficulty_martins`, `kc_curves (28 curvas)`, `level_vs_slope (scatter)`)
    - `<div class="fig-wrap"><img src="assets/<filename>.png" alt="Candidato N: <descritor>"></div>`
    - `<p class="fig-read">[temporário; defere para checkpoint]</p>`
    - `<p class="fig-fonte">[temporário] candidato N</p>`

    Filenames literais por candidato:
    - N=2: `assets/fig-codedkt-difficulty.png`, descritor `difficulty_martins`
    - N=3: `assets/fig-codedkt-kc-curves.png`, descritor `kc_curves (28 curvas)`
    - N=4: `assets/fig-codedkt-level-vs-slope.png`, descritor `level_vs_slope (scatter)`

    Cada `<section>` temporário precedida por comentário HTML estilo:
    `<!-- ============ SLIDE TEMP · CLOSE-03 pick · candidato 2 (fig-codedkt-difficulty) ============ -->`
  </action>
  <acceptance_criteria>
    - `test -f apresentacao/assets/fig-codedkt-difficulty.png && test -f apresentacao/assets/fig-codedkt-kc-curves.png && test -f apresentacao/assets/fig-codedkt-level-vs-slope.png` retornam 0.
    - `md5sum apresentacao/assets/fig-codedkt-difficulty.png results/fig_codedkt_difficulty_martins.png | awk '{print $1}' | sort -u | wc -l` retorna `1` (cópia byte-a-byte).
    - `md5sum apresentacao/assets/fig-codedkt-kc-curves.png results/fig_codedkt_kc_curves.png | awk '{print $1}' | sort -u | wc -l` retorna `1`.
    - `md5sum apresentacao/assets/fig-codedkt-level-vs-slope.png results/fig_codedkt_level_vs_slope.png | awk '{print $1}' | sort -u | wc -l` retorna `1`.
    - `grep -c 'id="slide-fig-pick-2"' apresentacao/index.html` retorna `1`.
    - `grep -c 'id="slide-fig-pick-3"' apresentacao/index.html` retorna `1`.
    - `grep -c 'id="slide-fig-pick-4"' apresentacao/index.html` retorna `1`.
    - `grep -c 'SLIDE TEMP · CLOSE-03 pick' apresentacao/index.html` retorna `3`.
    - O número total de `<section>` em `apresentacao/index.html` aumenta em 3 vs estado pós 04-03.
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>3 PNGs copiados para `apresentacao/assets/`; 3 sections temporárias inseridas no deck logo após o slide-fig atual, com IDs `slide-fig-pick-2/3/4` e cabeçalhos descritivos.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual humano — pick do PNG CLOSE-03</name>
  <what-built>4 versões consecutivas do slide-fig: a original (`#/23`, atual `curves_by_martins`) seguida por 3 candidatos temporários (`#/24`, `#/25`, `#/26`). Cada um exibe um dos 4 PNGs do `results/`.</what-built>
  <how-to-verify>
    1. Em terminal: `cd apresentacao && python3 -m http.server 8000`.
    2. No browser: abrir `http://127.0.0.1:8000/#/23` (slide-fig original = candidato 1 `curves_by_martins`).
    3. Avançar com `→` até `#/26` (3 candidatos temporários).
    4. Para cada um dos 4 slides, observar:
       - Qual gráfico comunica melhor o argumento central do CLOSE-03 (evidência empírica: Code-DKT aprende melhor estruturas de controle, fica plano em vetores/funções; ou outra leitura legítima do gráfico).
       - Se o eixo, legenda e labels estão claros em 1280×720.
       - Se haveria espaço para um `<p class="fig-read">` de uma linha resumindo o insight.
    5. **Escolher 1 candidato** e responder em um dos formatos:
       - `pick: 1` (manter o atual `curves_by_martins`) — neste caso, `fig-read` atual ("Estruturas de controle aprende rápido; Vetores e Funções ficam planos") fica.
       - `pick: 2 / fig-read: <novo texto>` (escolher `difficulty_martins`; fornecer 1-2 frases de leitura).
       - `pick: 3 / fig-read: <novo texto>` (escolher `kc_curves` 28 curvas).
       - `pick: 4 / fig-read: <novo texto>` (escolher `level_vs_slope` scatter).
    6. Se o `fig-read` for refraseado, manter padrões D-87 (sem em-dash), D-89 (estudantes), D-88 (`<i>et al.</i>` se citar Shi); manter referência implícita aos números do MODEL-04 (e.g. evolução por dificuldade compatível com Code-DKT A439=73,27%).
  </how-to-verify>
  <resume-signal>Responder em uma das 4 formas: `pick: 1` | `pick: 2 / fig-read: <texto>` | `pick: 3 / fig-read: <texto>` | `pick: 4 / fig-read: <texto>`. Se a banca quiser alteração de cabeçalho `> evolução por dificuldade` (D-83e estabelece esse cabeçalho), descrever.</resume-signal>
  <action>Human reviewer follows the steps in &lt;how-to-verify&gt; in a local browser (`cd apresentacao && python3 -m http.server 8000`), then responds per &lt;resume-signal&gt;. Implementer waits for explicit approval before proceeding to the next task.</action>
  <verify>
    <automated>MISSING - reviewer signal recorded in resume response; no automated check substitutes the human visual approval. Executor MUST NOT proceed without "approved" or equivalent decision.</automated>
  </verify>
  <done>Human reviewer approved the slide visually (or recorded a specific pick / fallback decision), unblocking subsequent tasks.</done>
</task>

<task type="auto">
  <name>Task 3: Cleanup — remover 3 temporários, atualizar slide-fig se necessário, corrigir comentário linha 541</name>
  <files>apresentacao/index.html, apresentacao/assets/fig-codedkt-difficulty.png, apresentacao/assets/fig-codedkt-kc-curves.png, apresentacao/assets/fig-codedkt-level-vs-slope.png, apresentacao/assets/fig-codedkt-martins-curves.png</files>
  <read_first>
    - apresentacao/index.html (linha ~541 com comentário `<!-- figura: results/fig_codedkt_difficulty_martins.png ... -->` que precisa ser corrigido sempre, independente da escolha)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md (Pitfall 9 — fix obrigatório do comentário; "Etapa C — limpeza" no PATTERNS Plan 5)
  </read_first>
  <action>
    Etapa A — remover os 3 `<section>` temporários inseridos pela Task 1, junto com seus comentários `<!-- ============ SLIDE TEMP ... -->`. Use o ID `slide-fig-pick-N` para localizar e remover blocos inteiros do `<section data-background-color...>` ao `</section>` correspondente; remover também o comentário HTML imediatamente acima. Após remoção, o `index.html` volta ao número de sections de pós-04-03 + 1 (este plan adiciona 0 sections líquidas além das de 04-01/02/03).

    Etapa B — atualizar slide-fig original conforme escolha do checkpoint:
    - SE `pick: 1` (default): apenas corrigir o comentário HTML (Pitfall 9). Trocar a linha `<!-- figura: results/fig_codedkt_difficulty_martins.png (curvas de mastery prevista; oportunidade = problema distinto) -->` para `<!-- figura: results/fig_codedkt_curves_by_martins.png (= assets/fig-codedkt-martins-curves.png; PENDING-04 resolved em 04-04) -->`. NÃO mexer no `<img>` nem no `<p class="fig-read">`.
    - SE `pick: 2/3/4`: sobrescrever o asset canônico (D-83a registra `assets/fig-codedkt-martins-curves.png` como filename estável):
      - `pick: 2`: `cp results/fig_codedkt_difficulty_martins.png apresentacao/assets/fig-codedkt-martins-curves.png`
      - `pick: 3`: `cp results/fig_codedkt_kc_curves.png apresentacao/assets/fig-codedkt-martins-curves.png`
      - `pick: 4`: `cp results/fig_codedkt_level_vs_slope.png apresentacao/assets/fig-codedkt-martins-curves.png`
      - Refrasear `<p class="fig-read">` com o texto fornecido pelo checkpoint (preservando padrões D-87/D-88/D-89).
      - Atualizar comentário HTML para `<!-- figura: results/fig_codedkt_<escolhido>.png (= assets/fig-codedkt-martins-curves.png; PENDING-04 resolved em 04-04) -->`.
    - Em QUALQUER caso, atualizar o `alt=""` do `<img>` se o gráfico mudou: e.g. para `pick: 3`, `alt="28 curvas de aprendizado do Code-DKT, uma por KC, sobre os 5 assignments"`. Para `pick: 1` (default), alt atual `"Curvas de aprendizado do Code-DKT por sub-dificuldade de Martins"` fica.

    Etapa C — limpar PNGs temporários do disco (manter SOMENTE o filename canônico em uso pelo slide-fig):
    - `rm apresentacao/assets/fig-codedkt-difficulty.png apresentacao/assets/fig-codedkt-kc-curves.png apresentacao/assets/fig-codedkt-level-vs-slope.png` (os 3 temporários foram só para o checkpoint visual; removê-los evita confusão futura).
    - NÃO remover `apresentacao/assets/fig-codedkt-martins-curves.png` (esse é o canônico mantido por D-83a, possivelmente sobrescrito acima).
  </action>
  <acceptance_criteria>
    - `grep -c 'id="slide-fig-pick-' apresentacao/index.html` retorna `0` (3 temporários removidos).
    - `grep -c 'SLIDE TEMP · CLOSE-03' apresentacao/index.html` retorna `0`.
    - `test ! -f apresentacao/assets/fig-codedkt-difficulty.png && test ! -f apresentacao/assets/fig-codedkt-kc-curves.png && test ! -f apresentacao/assets/fig-codedkt-level-vs-slope.png` retornam 0 (temporários removidos do disco).
    - `test -f apresentacao/assets/fig-codedkt-martins-curves.png` retorna 0 (asset canônico continua existindo).
    - `grep -c '<!-- figura: results/fig_codedkt_difficulty_martins.png' apresentacao/index.html` retorna `0` (comentário antigo desapareceu — Pitfall 9 resolvido).
    - `grep -cE '<!-- figura: results/fig_codedkt_(curves_by_martins|difficulty_martins|kc_curves|level_vs_slope)\.png' apresentacao/index.html` retorna `1` (UM comentário corrigido, apontando ao filename real).
    - `grep -c 'PENDING-04 resolved em 04-04' apresentacao/index.html` retorna `1`.
    - O número total de `<section>` em `apresentacao/index.html` é igual ao estado pós-04-03 (sem adições líquidas neste plan).
    - Se `pick != 1`: MD5 de `apresentacao/assets/fig-codedkt-martins-curves.png` corresponde ao MD5 do PNG escolhido em `results/`. Verificação: `md5sum apresentacao/assets/fig-codedkt-martins-curves.png results/fig_codedkt_<escolhido>.png | awk '{print $1}' | sort -u | wc -l` retorna `1`.
    - `awk '/SLIDE · Evolução por dificuldade/,/SLIDE · MARKER ... fase 3/' apresentacao/index.html | grep -c '—'` retorna `0` (sem em-dash em prosa, inclusive `fig-read` refraseado).
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>Slide-fig em estado final: comentário HTML coerente com o `<img>` real, fig-read alinhado ao PNG escolhido, 3 temporários eliminados do markup E do disco.</done>
</task>

<task type="auto">
  <name>Task 4: Commit funcional do CLOSE-03 + cobertura no-op CLOSE-01/02</name>
  <files>apresentacao/index.html, apresentacao/assets/fig-codedkt-martins-curves.png</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md
    - git log --oneline -10
    - apresentacao/index.html (verificar que Martins p2 ~L498 e Martins p3 ~L521 estão INTACTOS — `git diff HEAD -- apresentacao/index.html | grep -E "^[+-].*Martins" | wc -l` deve ser 0)
  </read_first>
  <action>
    Etapa A — verificação obrigatória de no-op CLOSE-01/02 (D-82 vinculante):
    - `git diff HEAD~3 HEAD -- apresentacao/index.html | grep -E "^[+-].*(p\. 19|p\. 20|Martins; Marin; Alves|13 autores|10 autores)"` deve retornar VAZIO. Se alguma linha aparecer, **abortar e reverter** o trecho relacionado a Martins p2/p3 — não tocar nesses slides nesta fase é uma regra vinculante D-82.
    - `awk '/Retomando o problema · Martins p2/,/<\/section>/' apresentacao/index.html | grep -c '> retomando o problema'` retorna pelo menos `1` (cabeçalho intacto).
    - `awk '/Retomando o problema · Martins p2/,/<\/section>/' apresentacao/index.html | grep -c 'p. 19'` retorna pelo menos `1` (citação direta literal preservada).
    - `awk '/Retomando o problema · Martins p3/,/<\/section>/' apresentacao/index.html | grep -c 'p. 20'` retorna pelo menos `1`.

    Etapa B — stagear e commitar:
    - SE `pick: 1`: `git add apresentacao/index.html` (apenas comentário mudou); 1 commit.
    - SE `pick != 1`: `git add apresentacao/index.html apresentacao/assets/fig-codedkt-martins-curves.png`; 1 commit.

    Mensagem do commit:
    - `pick: 1`: `apresentacao: slide CLOSE-03 - PENDING-04 mantém curves_by_martins + fix comentário linha 541`
    - `pick: 2`: `apresentacao: slide CLOSE-03 - PENDING-04 troca para difficulty_martins + refraseia fig-read`
    - `pick: 3`: `apresentacao: slide CLOSE-03 - PENDING-04 troca para kc_curves (28 curvas) + refraseia fig-read`
    - `pick: 4`: `apresentacao: slide CLOSE-03 - PENDING-04 troca para level_vs_slope (scatter) + refraseia fig-read`

    Sem `Co-Authored-By`. Sem `--amend`.
  </action>
  <acceptance_criteria>
    - Verificação no-op: `awk '/Retomando o problema · Martins p2/,/<\/section>/' apresentacao/index.html | grep -c 'p. 19'` >= 1 E para p3 `>= 1` com `p. 20`.
    - `git log --oneline -1` mostra mensagem começando com `apresentacao: slide CLOSE-03`.
    - `git status` retorna working tree limpo.
    - `grep -c '<!-- figura: results/fig_codedkt_' apresentacao/index.html` retorna `1` (comentário corrigido, único).
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>1 commit funcional fechando PENDING-04. CLOSE-01/02 confirmados intactos. CLOSE-03 com comentário, img e fig-read coerentes.</done>
</task>

</tasks>

<no_op_coverage>
**CLOSE-01 (Martins p2, REQ-ID coberto aqui para traceability):** Section em ~L498 atual (deslocada pelos plans 04-01/02/03). NÃO TOCAR. Reformatada na fase 1 (commits `590ae34` + `2a86049`); cabeçalho `> retomando o problema`; citação direta literal `(Martins; Marin; Alves, 2024, p. 19)` preservada como exceção D-28 (argumento quantitativo "mencionada por 13 autores"). D-82 vinculante. Task 4 valida via grep que Martins p2 não sofreu diff.

**CLOSE-02 (Martins p3, REQ-ID coberto aqui para traceability):** Section em ~L521 atual. NÃO TOCAR. Idem CLOSE-01: cabeçalho `> retomando o problema`; citação `(Martins; Marin; Alves, 2024, p. 20)` preservada; argumento "citado por 10 autores"; D-82/D-28. Task 4 valida via grep.

**slide-kcfig (REQ-ID slide-kcfig, sem necessidade de REQ-ID separado):** Saída visual do MODEL-05 (pipeline). NÃO TOCAR. Já reformatado na fase 1 (commits 590ae34 + 2a86049) com cabeçalho `> kcs semânticos extraídos`; 17 KCs em 6 dificuldades Martins; rodapé Duan+Martins. D-79i mandata que MODEL-05 não duplique este conteúdo.

**MODEL-03 (REQ-ID coberto no plan 04-01 já):** slide-code reaproveitado, sem alteração. Confirmado em 04-01 no_op_coverage.
</no_op_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev local → browser | deck servido localmente via `python3 -m http.server`; sem auth, sem network externa, sem input do usuário, sem persistência |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | T (Tampering) | CLOSE-01/02 (Martins p2/p3) — risco de violar D-82 (não tocar) | mitigate | Task 4 inclui verificação obrigatória via `git diff` filtrando linhas Martins; se alguma linha mudou, abort & revert; D-82 explícito no no_op_coverage |
| T-04-12 | I (Information Disclosure) | PNG escolhido pode mostrar curva/dado obsoleto | accept | re-treino `codedkt_kc_retrained.pkl` (76MB, 2026-05-25 01:42) é a fonte alinhada; D-83a confirma; PENDING-04 explicitamente resolvido |
| T-04-13 | T (Tampering) | Comentário HTML linha 541 (Pitfall 9) inconsistente com img | mitigate | Task 3 acceptance criteria `grep -c '<!-- figura: results/fig_codedkt_difficulty_martins.png'` == 0 valida que o comentário antigo desapareceu |
| T-04-14 | S/R/D/E | n/a | n/a | sem auth, sem network, sem persistência, sem endpoint |

No attack surface significativo além do risco humano de tocar CLOSE-01/02 (mitigado por verificação automatizada na Task 4).
</threat_model>

<verification>
- `grep -c 'id="slide-fig-pick-' apresentacao/index.html` == 0 (no fim)
- `test -f apresentacao/assets/fig-codedkt-martins-curves.png` retorna 0
- `test ! -f apresentacao/assets/fig-codedkt-difficulty.png` retorna 0
- `grep -c '<!-- figura: results/fig_codedkt_' apresentacao/index.html` == 1 (comentário coerente)
- `awk '/Martins p2/,/<\/section>/' apresentacao/index.html | grep -c 'p. 19'` >= 1
- `awk '/Martins p3/,/<\/section>/' apresentacao/index.html | grep -c 'p. 20'` >= 1
- checkpoint humano APPROVED com escolha registrada
</verification>

<success_criteria>
- PENDING-04 resolvido: PNG do CLOSE-03 escolhido visualmente pelo usuário (1 dos 4 candidatos).
- Comentário HTML do slide-fig corrigido (Pitfall 9): linha aponta ao filename real do PNG em `<img>`.
- `<p class="fig-read">` coerente com o PNG escolhido (refraseado se `pick != 1`).
- 3 sections temporárias removidas; 3 PNGs temporários removidos do disco.
- Asset canônico `apresentacao/assets/fig-codedkt-martins-curves.png` existe e tem MD5 do PNG escolhido.
- CLOSE-01 (Martins p2) e CLOSE-02 (Martins p3) confirmados intactos via grep — D-82 honrado.
- slide-kcfig confirmado intacto (D-79i).
- 1 commit funcional no padrão `apresentacao: slide CLOSE-03 - PENDING-04 ...`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-04-SUMMARY.md` documenting: (a) commit hash; (b) qual candidato foi escolhido e por quê (1-2 linhas do raciocínio do checkpoint); (c) texto final de `<p class="fig-read">`; (d) confirmação que CLOSE-01/02 e slide-kcfig permaneceram intactos; (e) MD5 do PNG canônico final.
</output>
</content>
</invoke>