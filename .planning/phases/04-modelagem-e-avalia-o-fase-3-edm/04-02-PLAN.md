---
phase: 04-modelagem-e-avalia-o-fase-3-edm
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - apresentacao/index.html
autonomous: true
requirements: [MODEL-04]
must_haves:
  truths:
    - "Ao navegar para `#/18` (após inserções 04-01 e 04-02), o usuário lê o cabeçalho `> code-dkt no csedm` e vê uma tabela ABNT `.eda-grid` com 4 linhas (BKT, DKT, Code-DKT, Shi (2022)*) e 6 colunas (Modelo + A439..A502)."
    - "Os números travados em D-78g aparecem com vírgula decimal pt-BR: Code-DKT A439=73,27%; DKT A439=75,56%; BKT A439=63,21%; Shi (2022)* A439=75,74%; demais células da linha Shi em en-dash (`–`)."
    - "Caption discreto abaixo da tabela justifica first-attempt AUC como métrica primária, citando Shi <i>et al.</i> (2022), §5."
    - "Rodapé `Fonte: elaborado pelo autor (10 seeds); Shi <i>et al.</i> (2022) Table 2.` aparece em Arial 14px cinza centralizado via `.eda-source`."
    - "Zero em-dash em prosa; toda citação Shi com `<i>et al.</i>`; sem ponto decimal em números."
  artifacts:
    - path: "apresentacao/index.html"
      provides: "Section MODEL-04 inserida entre slide-code e slide-kcfig"
      contains: "code-dkt no csedm"
  key_links:
    - from: "apresentacao/index.html (MODEL-04 section)"
      to: "componente .eda-grid (CSS L466-484)"
      via: "<table class=\"eda-grid\">"
      pattern: "<table class=\"eda-grid\">"
    - from: "MODEL-04 numbers"
      to: "results/comparison_table_first_auc.md (canonical source)"
      via: "transcrição manual com conversão de ponto para vírgula"
      pattern: "73,27|75,74|75,56|63,21"
---

<objective>
Entregar o slide MODEL-04 (`> code-dkt no csedm`), inserido entre o slide-code (atualmente `#/16`, vira `#/17` após 04-01) e o slide-kcfig (atualmente `#/17`, vira `#/19` após 04-01+04-02), apresentando os resultados quantitativos do Code-DKT no CSEDM em uma tabela ABNT `.eda-grid`. A tabela tem 4 linhas × 6 colunas (BKT/DKT/Code-DKT/Shi (2022)* × Modelo/A439/A487/A492/A494/A502), com a linha Shi reportando apenas A439=75,74% (Pitfall 1) e as demais colunas em en-dash.

Purpose: Cumprir o Success Criteria 3 do ROADMAP (resultados Code-DKT lado a lado com Shi) e o Critério 1 do CLAUDE.md (A439 first_auc dentro de ±3pp do paper, delta atual -2,47pp). Render HTML, não PNG, mantém consistência ABNT/IBGE com EDA-01 e EDA-02. Métrica única (first-attempt AUC) ancora a defesa narrativa; all-attempts e Wilcoxon ficam para fala/QA.

Output: 1 nova `<section>` em `apresentacao/index.html`. Sections do deck: 22 → 23 (após 04-01 ter inserido MODEL-01). 1 commit funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-CONTEXT.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md
@.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md
@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css
@results/comparison_table_first_auc.md

<interfaces>
<!-- Componentes CSS reusados. NÃO redefinir. -->

Do theme-unifacens.css L42-50:
  .deck-topic, .deck-topic .ps1, .caret, .caret.blink

Do theme-unifacens.css L460-484 (ABNT/IBGE, fixado em commit aa69eb1):
  .eda-title                          /* Arial bold 18px centralizado, acima da tabela */
  .eda-grid                           /* 3 bordas horizontais (top, bottom-header, base); fundo transparente; sem verticais */
  .eda-grid th, .eda-grid td          /* padding 9px 18px, text-align center */
  .eda-grid th                        /* border-bottom 1.5px */
  .eda-grid tr td:last-child          /* última coluna em var(--uni-blue), font-weight 700 */

Do theme-unifacens.css L485-490:
  .eda-source                         /* Arial 14px cinza centralizado, abaixo da tabela */

Do theme-unifacens.css (.rel-lead):
  .rel-lead                           /* Arial 22-23px; com inline style font-size:18px usado para caption discreto */

Números canônicos (results/comparison_table_first_auc.md, multirun 10 seeds):
  BKT      A439=63.21 A487=68.40 A492=54.20 A494=57.81 A502=56.92
  DKT      A439=75.56 A487=76.70 A492=82.05 A494=80.17 A502=80.78
  Code-DKT A439=73.27 A487=79.56 A492=86.12 A494=81.85 A502=84.98
  Shi'22*  A439=75.74 (paper Table 2; demais assignments não publicados — usar &ndash;)
  CONVERSÃO OBRIGATÓRIA: ponto → vírgula em CADA célula (D-78g, Pitfall 8)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inserir section MODEL-04 entre slide-code e slide-kcfig</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html (linhas 245-271 = EDA-01 análogo exato — comentário, `.deck-slide slide-related`, `.rel-lead` introdução, `.eda-title`, `.eda-grid`, `.eda-source`; linhas 377-426 = slide-code, para confirmar âncora superior `</section>` em ~L426; linhas 428-431 = slide-kcfig, para confirmar âncora inferior; linhas 315-330 = EDA-02, exemplo secundário do mesmo template)
    - apresentacao/assets/theme-unifacens.css (L460-490 .eda-title, .eda-grid, .eda-source)
    - apresentacao/STYLE.md (§Convenções de citação, §Regras de redação, §Diagramas para padrão ABNT)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-PATTERNS.md (Plan 2 — snippet HTML completo do EDA-01 análogo + tabela de números travados + tratamento das células Shi em en-dash)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-CONTEXT.md (D-78a..D-78g)
    - .planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-RESEARCH.md (Pattern 2; Pitfall 1 — apenas A439 para Shi; Pitfall 2 — usar 73,27% multirun não 72,55% single-seed; Pitfall 8 — vírgula decimal pt-BR)
    - results/comparison_table_first_auc.md (fonte canônica dos números; conferir os 15 valores BKT/DKT/Code-DKT × 5 assignments)
    - docs/Code-DKT.pdf p.6 Table 2 (confirmar Shi A1 first=75.74±0.69; demais assignments não publicados em first-attempt)
  </read_first>
  <action>
    Localizar a linha do `</section>` do slide-code (atualmente L426, mas SHIFTED por 04-01 — usar grep para encontrar dinamicamente: `grep -n 'SLIDE · KCs semânticos extraídos' apresentacao/index.html` retorna a âncora inferior; a `<section>` MODEL-04 vai IMEDIATAMENTE ANTES dessa linha de comentário). Inserir cabeçalho de comentário acima da nova section: `<!-- ============ SLIDE · MODEL-04 · Code-DKT no CSEDM, resultados vs Shi (multirun 10 seeds; Shi et al., 2022 Tab. 2) ============ -->`.

    Estrutura da section (seguir literalmente o snippet da Pattern 2 em 04-PATTERNS.md L139-167 com as mudanças listadas na tabela L171-183):
    - `<section data-background-color="#F1F6FB">` com `<div class="deck-slide slide-related">` (sem `slide-bridge` aqui; MODEL-04 NÃO tem cronologia)
    - Marca d'água: `<svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>`
    - Cabeçalho: `<p class="deck-topic"><span class="ps1">&gt;</span>code-dkt no csedm<span class="caret blink"></span></p>` (D-78a)
    - Intro: `<p class="rel-lead">Comparamos os três modelos por <i>assignment</i> no <i>test set</i> do CSEDM Spring 2019; números são médias sobre 10 seeds.</p>`
    - Título da tabela: `<p class="eda-title">Tabela 2 &ndash; <i>First-attempt</i> AUC por modelo e <i>assignment</i> (%)</p>` (numeração "Tabela 2" porque EDA-01 já tomou "Tabela 1")
    - Tabela `.eda-grid` com:
      - `<thead><tr>` com `<th>Modelo</th><th>A439</th><th>A487</th><th>A492</th><th>A494</th><th>A502</th></tr>`
      - `<tbody>` com 4 `<tr>` na ordem EXATA D-78g:
        - `<tr><td>BKT</td><td>63,21</td><td>68,40</td><td>54,20</td><td>57,81</td><td>56,92</td></tr>`
        - `<tr><td>DKT</td><td>75,56</td><td>76,70</td><td>82,05</td><td>80,17</td><td>80,78</td></tr>`
        - `<tr><td>Code-DKT</td><td>73,27</td><td>79,56</td><td>86,12</td><td>81,85</td><td>84,98</td></tr>`
        - `<tr><td>Shi (2022)*</td><td>75,74</td><td>&ndash;</td><td>&ndash;</td><td>&ndash;</td><td>&ndash;</td></tr>` (Pitfall 1: paper Shi publica só A1 first; demais como en-dash HTML)
    - Caption discreto APÓS a tabela: `<p class="rel-lead" style="font-size: 18px; margin-top: 12px;"><i>* Paper Shi reporta apenas A1, equivalente a A439.</i> <i>First-attempt</i> AUC é a métrica primária: mede transferência entre problemas e evita autocorrelação intra-problema (Shi <i>et al.</i>, 2022, §5).</p>` (D-78d)
    - Rodapé: `<p class="eda-source">Fonte: elaborado pelo autor (10 seeds); Shi <i>et al.</i> (2022) Table 2.</p>` (D-78f, D-88 ABNT)

    Regras vinculantes:
    - VÍRGULA DECIMAL pt-BR em todos os 16 números numéricos (15 BKT+DKT+Code-DKT + 1 Shi A439). NUNCA ponto. Pitfall 8.
    - `<i>et al.</i>` em qualquer citação Shi (Shi tem 5 autores; D-88, Pitfall anti `Shi (2022)` sem et al.)
    - `&ndash;` HTML entity (en-dash de tabela, NÃO em-dash `—`) nas 4 células Shi vazias
    - Termos estrangeiros: `<i>assignment</i>`, `<i>test set</i>`, `<i>First-attempt</i>` (capitalizado em título; minúsculo em prosa)
    - Sem em-dash `—` em qualquer prosa (D-87); o `&ndash;` em `.eda-title` é en-dash, padrão ABNT já usado em EDA-01 L254 (`Tabela 1 &ndash; ...`)
    - "Autor" (singular) em "elaborado pelo autor" segue padrão EDA-01 (`Fonte: elaborado pelo autor sobre CSEDM (Spring 2019).` L269); CONTEXT D-78f trava esse phrasing
    - Voz autoral; nenhuma citação direta literal (D-86)
  </action>
  <acceptance_criteria>
    - `grep -c 'code-dkt no csedm' apresentacao/index.html` retorna pelo menos `1`.
    - `grep -c 'MODEL-04' apresentacao/index.html` retorna pelo menos `1`.
    - `grep -c '<table class="eda-grid">' apresentacao/index.html` aumenta em 1 vs HEAD (passa de 1 para 2; EDA-01 já tem uma).
    - `grep -F '73,27' apresentacao/index.html | wc -l` retorna pelo menos `1` (Code-DKT A439 multirun).
    - `grep -F '75,74' apresentacao/index.html | wc -l` retorna pelo menos `1` (Shi paper A439).
    - `grep -F '75,56' apresentacao/index.html | wc -l` retorna pelo menos `1` (DKT A439).
    - `grep -F '63,21' apresentacao/index.html | wc -l` retorna pelo menos `1` (BKT A439).
    - `grep -c 'Shi <i>et al.</i> (2022)' apresentacao/index.html` aumenta em pelo menos 2 (caption + rodapé); Shi sempre com et al. ABNT.
    - `grep -cE '7[3-6]\.[0-9]{2}|6[3-8]\.[0-9]{2}' apresentacao/index.html` permanece `0` (NENHUM número decimal com PONTO — todos com vírgula).
    - `awk '/SLIDE · MODEL-04/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -c '—'` retorna `0` (sem em-dash na region MODEL-04).
    - `awk '/SLIDE · MODEL-04/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -c '&ndash;'` retorna pelo menos `5` (1 no `.eda-title` + 4 nas células Shi).
    - O número total de `<section>` em `apresentacao/index.html` aumenta em 1 vs HEAD (depois de 04-01, esta task adiciona +1).
    - `grep -c 'Fonte: elaborado pelo autor (10 seeds); Shi <i>et al.</i> (2022) Table 2.' apresentacao/index.html` retorna `1`.
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>Section MODEL-04 inserida com tabela ABNT `.eda-grid` 4 linhas × 6 colunas; números canônicos do `comparison_table_first_auc.md` transcritos com vírgula decimal; linha Shi com 4 en-dash + A439=75,74%; caption justificando first-attempt AUC; rodapé padrão.</done>
</task>

<task type="auto">
  <name>Task 2: Verificação automatizada via http.server + commit funcional</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - .planning/codebase/CONVENTIONS.md (lowercase português, prefixo `apresentacao:`)
    - git log --oneline -10 (padrão de mensagens: e.g. `c92b9ff apresentacao: slide INTRO-03b - sinal pedagogico perdido`)
  </read_first>
  <action>
    Etapa A — Verificação automatizada (sem checkpoint humano; este plan é `autonomous: true` por ser tabela determinística com números pré-validados D-78g):
    1. Subir servidor em background: `cd apresentacao && python3 -m http.server 8000 &` (anotar PID; matar com `kill <PID>` ao final).
    2. `curl -s -o /tmp/index_check.html -w '%{http_code}' http://127.0.0.1:8000/index.html` deve retornar `200`.
    3. `grep -c 'code-dkt no csedm' /tmp/index_check.html` deve retornar pelo menos `1` (HTML servido tem o cabeçalho).
    4. `grep -c '73,27' /tmp/index_check.html` deve retornar pelo menos `1`.
    5. Matar o servidor: `kill <PID>`.

    Etapa B — Commit:
    Stagear `git add apresentacao/index.html` e criar commit funcional com mensagem:

    `apresentacao: slide MODEL-04 - code-dkt no csedm (tabela ABNT 4 modelos x 5 assignments vs Shi)`

    Sem `Co-Authored-By`. Sem `--amend`. Se pre-commit hook falhar, diagnosticar e fazer commit NOVO.
  </action>
  <acceptance_criteria>
    - `curl -s http://127.0.0.1:8000/index.html` retorna status 200 (durante a Etapa A; servidor é matado depois).
    - `git log --oneline -1` mostra mensagem começando com `apresentacao: slide MODEL-04`.
    - `git diff HEAD~1 --name-only` lista exatamente `apresentacao/index.html`.
    - `git status` retorna working tree limpo.
  </acceptance_criteria>
  <verify>
    <automated>Run the assertions listed in &lt;acceptance_criteria&gt; above; each grep/test/md5sum must return the expected count or exit code.</automated>
  </verify>
  <done>http.server confirma render do slide MODEL-04 sem 500/404; 1 commit funcional criado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev local → browser | deck servido localmente via `python3 -m http.server`; sem auth, sem network externa, sem input do usuário, sem persistência |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | T (Tampering) | Números MODEL-04 (transcritos manualmente de `results/comparison_table_first_auc.md`) | mitigate | Vinculação direta: cada `<td>` numérico tem o valor exato do D-78g; acceptance criteria com `grep -F` valida 4 dos 16 valores (73,27 / 75,74 / 75,56 / 63,21); reviewer humano pode auditar o restante no diff. |
| T-04-05 | I (Information Disclosure) | n/a | n/a | nenhum segredo; números são públicos do paper Shi e do nosso multirun |
| T-04-06 | S/R/D/E | n/a | n/a | sem auth, sem network, sem persistência, sem endpoint |

No attack surface significativo. Slide deck estático servido por `http.server` local apenas em dev; nenhum input do usuário; nenhum endpoint; nenhum estado persistido.
</threat_model>

<verification>
- `grep -c 'code-dkt no csedm' apresentacao/index.html` >= 1
- `grep -c '<table class="eda-grid">' apresentacao/index.html` >= 2
- `grep -F '73,27' apresentacao/index.html | wc -l` >= 1
- `grep -F '75,74' apresentacao/index.html | wc -l` >= 1
- `grep -cE '\b(73|75|82|76|79|86|81|84|80|68|54|57|56)\.[0-9]{2}' apresentacao/index.html` == 0 (zero ponto decimal nos números travados)
- `awk '/SLIDE · MODEL-04/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -c '—'` == 0
- `awk '/SLIDE · MODEL-04/,/SLIDE · KCs semânticos/' apresentacao/index.html | grep -c '&ndash;'` >= 5
- curl localhost:8000 retorna 200 para `index.html`
</verification>

<success_criteria>
- Section MODEL-04 inserida entre slide-code (`</section>` ~L426 antes de 04-01, deslocada por 04-01) e o comentário do slide-kcfig (~L428 antes de 04-01).
- Tabela ABNT `.eda-grid` com 4 linhas (BKT, DKT, Code-DKT, Shi (2022)*) × 6 colunas (Modelo + A439..A502).
- Números D-78g exatos com vírgula decimal pt-BR; 4 células Shi em `&ndash;` exceto A439=75,74%.
- Caption explicando first-attempt AUC com citação `Shi <i>et al.</i>, 2022, §5`.
- Rodapé `Fonte: elaborado pelo autor (10 seeds); Shi <i>et al.</i> (2022) Table 2.` via `.eda-source`.
- Zero em-dash; zero ponto decimal; toda citação Shi com `<i>et al.</i>`.
- `curl http://127.0.0.1:8000/index.html` retorna 200.
- 1 commit funcional no padrão `apresentacao: slide MODEL-04 - ...`.
</success_criteria>

<output>
After completion, create `.planning/phases/04-modelagem-e-avalia-o-fase-3-edm/04-02-SUMMARY.md` documenting: (a) commit hash; (b) confirmação que os 15 números canônicos foram transcritos exatamente do `comparison_table_first_auc.md` (sem regressão para single-seed); (c) ausência de checkpoint humano por se tratar de tabela determinística; (d) decisões ad-hoc se houver.
</output>
</content>
</invoke>