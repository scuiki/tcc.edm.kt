---
phase: 05-implanta-o-agenda-e-encerramento-fase-4-edm
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - apresentacao/index.html
  - apresentacao/assets/theme-unifacens.css
autonomous: false
requirements: [TOOL-01]
must_haves:
  truths:
    - "Slide TOOL-01 renderiza com cabeçalho `> proposta da aplicação` em padrão `.deck-topic`"
    - "Abertura em 1ª pessoa do plural per D-94b: `O processo apresentado pode ser instrumentalizado para professores. Propomos uma aplicação docente que organiza esse fluxo em seis etapas.`"
    - "Pipeline `.bridge-seq` com 6 etapas neutras (sem azul UniFacens em nenhuma) na ordem D-94c: Submissões dos estudantes → Extração de KCs → Professor valida → Preparação dos dados → Code-DKT → Dashboard"
    - "Pipeline cabe em 1280×720 sem overflow horizontal e sem texto quebrando em mais de 2 linhas (Pitfall 1)"
    - "Etapa 1 diz `Submissões dos estudantes`, NÃO `ProgSnap2` (gate D-94h herdado PROJECT.md Key Decision linha 183)"
    - "Vocabulário `aplicação` (D-92), `estudantes` (D-102), sem em-dash (D-100)"
    - "Rodapé `Fonte: elaborado pelo autor; baseado em <i>docs/tcc2_prototipo.html</i>.`"
    - "TOOL-01 fica em #/27 — entre MARKER-03 (#/26) e TOOL-03 (#/28 após inserção)"
  artifacts:
    - path: "apresentacao/index.html"
      provides: "Section TOOL-01 inserida entre MARKER-03 e TOOL-03"
      contains: "proposta da aplicação"
  key_links:
    - from: "apresentacao/index.html section TOOL-01"
      to: "componente `.bridge-seq` reusado com 6 .step (vs 5 do MODEL-05)"
      via: "inline-style replicado de MODEL-05 (flex-direction column, color #000, border-color #000, font-weight 400)"
      pattern: "bridge-seq.*step.*step.*step.*step.*step.*step"
    - from: "TOOL-01 fechamento opcional"
      to: "TOOL-03 #/28"
      via: "frase ponte `O dashboard fecha o ciclo e é o que detalhamos no próximo slide.`"
      pattern: "próximo slide"
---

<objective>
Inserir o slide TOOL-01 (proposta da aplicação docente para o TCC 2 + pipeline mini-horizontal de 6 etapas) entre MARKER-03 (#/26) e TOOL-03 (#/28 após plan 04). Pipeline ESPELHA o que já foi apresentado nas fases 2-4 (extração de KCs, preparação, Code-DKT, dashboard) sem detalhar cada etapa (D-94e).

Purpose: ordem 5 per RESEARCH §Plan-Level Execution Order — TOOL-01 é o slide mais denso visualmente (6 etapas em `.bridge-seq`); aproveita a calibragem dos plans 01-04 (especialmente o teste de `.bridge-seq` em layout não-sequencial feito em TOOL-03). Validação visual mais crítica do plan: pipeline 6 etapas em 1280×720 com microcópia curta (Pitfall 1). Maior chance de iterar no checkpoint.

Output:
- 1 nova `<section>` em `apresentacao/index.html` inserida entre MARKER-03 e TOOL-03. Posição final #/27.
- ZERO CSS novo (reusa `.bridge-seq`, `.slide-related`, `.slide-bridge`, `.deck-topic`, `.eda-title`, `.eda-source`, `.rel-lead`, `.rel-cite`, `.wm`)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-04-SUMMARY.md
@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css
@docs/tcc2_prototipo.html

<interfaces>
<!-- Componente .bridge-seq canônico (theme-unifacens.css linhas 197-206) com .step e .arr -->
<!-- MODEL-05 (index.html linhas 540-555) é a REFERÊNCIA principal: 5 .step + 4 .arr em pipeline horizontal monocromático -->
<!-- Para TOOL-01: 6 .step + 5 .arr, mesma estética monocromática -->

Padrão de section TOOL-01 alvo (RESEARCH §Code Example 5):
<section data-background-color="#F1F6FB">
  <div class="deck-slide slide-related slide-bridge">
    <svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>
    <p class="deck-topic"><span class="ps1">&gt;</span>proposta da aplicação<span class="caret blink"></span></p>
    <p class="rel-lead">O processo apresentado pode ser instrumentalizado para professores. Propomos uma aplicação docente que organiza esse fluxo em seis etapas.</p>
    <p class="eda-title" style="margin-top: 22px; color: #000;">Figura &ndash; Aplicação docente em seis etapas</p>
    <p class="bridge-seq" style="margin-top: 10px;">
      <span class="step" style="flex-direction: column; color: #000; border-color: #000; font-weight: 400;">Submissões<span style="margin-top: 4px;">dos estudantes</span></span>
      <span class="arr" style="color: #000;">&rarr;</span>
      <span class="step" style="flex-direction: column; color: #000; border-color: #000; font-weight: 400;">Extração<span style="margin-top: 4px;">de KCs</span></span>
      <span class="arr" style="color: #000;">&rarr;</span>
      <span class="step" style="flex-direction: column; color: #000; border-color: #000; font-weight: 400;">Professor<span style="margin-top: 4px;">valida</span></span>
      <span class="arr" style="color: #000;">&rarr;</span>
      <span class="step" style="flex-direction: column; color: #000; border-color: #000; font-weight: 400;">Preparação<span style="margin-top: 4px;">dos dados</span></span>
      <span class="arr" style="color: #000;">&rarr;</span>
      <span class="step" style="flex-direction: column; color: #000; border-color: #000; font-weight: 400;">Code-DKT<span style="margin-top: 4px;">treinado</span></span>
      <span class="arr" style="color: #000;">&rarr;</span>
      <span class="step" style="flex-direction: column; color: #000; border-color: #000; font-weight: 400;">Dashboard<span style="margin-top: 4px;">para o professor</span></span>
    </p>
    <p class="eda-source">Fonte: elaborado pelo autor; baseado em <i>docs/tcc2_prototipo.html</i>.</p>
    <p class="rel-lead" style="margin-top: 18px;">O dashboard fecha o ciclo e é o que detalhamos no próximo slide.</p>
  </div>
</section>

Microcópia das 6 etapas (D-94c + D-94e, curta para Pitfall 1):
1. Submissões / dos estudantes
2. Extração / de KCs
3. Professor / valida
4. Preparação / dos dados
5. Code-DKT / treinado
6. Dashboard / para o professor

Mitigações de overflow (RESEARCH §Pitfall 1) se 6 .step não couberem em 1280px:
1. Reduzir font-size inline do .step de 19px (padrão) para 17px
2. Reduzir padding inline para 12px 10px
3. Comprimir microcópia (remover linha 2 de algumas etapas)
4. (último recurso) anexar `.bridge-seq--6step` CSS dedicado

Frase de fechamento opcional (D-94f, RESEARCH Open Question #2): "O dashboard fecha o ciclo e é o que detalhamos no próximo slide." — defere ao checkpoint se manter ou cortar.

Gates herdados aplicáveis:
- D-94h: etapa 1 = "Submissões dos estudantes", NÃO "ProgSnap2" (gate PROJECT.md Key Decision linha 183)
- D-94d: todas as 6 caixas neutras (sem azul UniFacens em nenhuma; razão: pipeline é processo contínuo)
- D-100: sem em-dash
- D-101: `<i>docs/tcc2_prototipo.html</i>` em itálico
- D-102: "estudantes" não "alunos"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inserir a `<section>` TOOL-01 ENTRE MARKER-03 e TOOL-03</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html linhas 670-735 (final do MARKER-03 + área de inserção identificada nos plans anteriores)
    - apresentacao/index.html (markup do TOOL-03 inserido pelo plan 04, para identificar onde colocar TOOL-01 ANTES dele)
    - apresentacao/index.html linhas 530-555 (MODEL-05 inteiro: gabarito de `.bridge-seq` com 5 etapas e inline-style monocromático)
    - apresentacao/assets/theme-unifacens.css linhas 197-206 (`.bridge-seq` base)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Code Example 5 (markup completo do TOOL-01)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md §D-94a/b/c/d/e/f/g/h
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Pitfall 1 (estimativas de overflow e mitigações)
    - docs/tcc2_prototipo.html (referência conceitual: 6 páginas Upload, KC, EDA, KT, Recomendações, Chat; NÃO copiar layout per CONTEXT canonical_refs)
  </read_first>
  <action>
    Inserir a `<section>` TOOL-01 IMEDIATAMENTE após o `</section>` que fecha MARKER-03 (linha ~730 antes dos plans 04-05) e ANTES do `<!-- ============ SLIDE · TOOL-03 ... ============ -->` que abre o TOOL-03 (criado no plan 04).

    Estrutura do markup (RESEARCH §Code Example 5):
    - Comentário HTML: `<!-- ============ SLIDE · TOOL-01 · Proposta da aplicação + pipeline 6 etapas ============ -->`
    - `<section data-background-color="#F1F6FB">`
    - `<div class="deck-slide slide-related slide-bridge">` (mesmas classes do MODEL-05)
    - Marca d'água `<svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>`
    - Cabeçalho per D-94a: `<p class="deck-topic"><span class="ps1">&gt;</span>proposta da aplicação<span class="caret blink"></span></p>`
    - Abertura per D-94b (1ª pessoa do plural; phrasing-alvo travado): `<p class="rel-lead">O processo apresentado pode ser instrumentalizado para professores. Propomos uma aplicação docente que organiza esse fluxo em seis etapas.</p>`
    - Título Figura ABNT: `<p class="eda-title" style="margin-top: 22px; color: #000;">Figura &ndash; Aplicação docente em seis etapas</p>`
    - Pipeline `.bridge-seq` per D-94c com 6 .step + 5 .arr, todas neutras (preto puro `#000`, sem `--uni-blue`) per D-94d. Inline-style de cada .step: `flex-direction: column; color: #000; border-color: #000; font-weight: 400;`. Inline-style de cada .arr: `color: #000;`. Microcópia das 6 etapas na ordem D-94c (ver `<interfaces>` acima): cada .step tem `Verbo<span style="margin-top: 4px;">descrição curta</span>`.
    - Etapa 1: `Submissões<span style="margin-top: 4px;">dos estudantes</span>` — NÃO "ProgSnap2" per D-94h (gate herdado PROJECT.md Key Decision linha 183)
    - Legenda Fonte: `<p class="eda-source">Fonte: elaborado pelo autor; baseado em <i>docs/tcc2_prototipo.html</i>.</p>`
    - Frase de fechamento opcional per D-94f (manter no markup inicial, defere ao checkpoint se cortar): `<p class="rel-lead" style="margin-top: 18px;">O dashboard fecha o ciclo e é o que detalhamos no próximo slide.</p>`

    Gates aplicáveis ao escrever o markup:
    - "aplicação" no cabeçalho e na abertura per D-92
    - "estudantes" na etapa 1 per D-102
    - Sem em-dash per D-100 (usar vírgulas, dois-pontos, parênteses)
    - `<i>docs/tcc2_prototipo.html</i>` em itálico per D-101
    - Sem `<p class="rel-cite">` final (a Figura já tem `.eda-source` que faz papel de Fonte; mesmo padrão do MODEL-05)

    NÃO criar nenhum CSS novo neste plan. Todo o styling é via reuso de classes existentes + inline-style replicado de MODEL-05.
  </action>
  <verify>
    <automated>grep -c "proposta da aplicação" apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "Aplicação docente em seis etapas" apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "Submissões" apresentacao/index.html | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "Extração" apresentacao/index.html | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "Professor" apresentacao/index.html | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "Code-DKT" apresentacao/index.html | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "Dashboard" apresentacao/index.html | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "<section " apresentacao/index.html | awk '$1 == 31 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<section " apresentacao/index.html` retorna 31 (era 30 após plan 04; +1 TOOL-01) — TARGET FINAL DA FASE 5
    - `grep -n "<section " apresentacao/index.html` confirma ordem final: ..., MARKER-03 (#/26), TOOL-01 (#/27), TOOL-03 (#/28), MARKER-04 (#/29), END-01 (#/30)
    - `grep -c "proposta da aplicação" apresentacao/index.html` retorna 1
    - `grep -c "Submissões.*estudantes\|Submissões<span.*dos estudantes" apresentacao/index.html` retorna 1 (etapa 1 corretamente nomeada)
    - `grep -c "ProgSnap2" apresentacao/index.html` permanece igual ao número antes do plan (gate D-94h; ProgSnap2 só em INTRO-01 #/10)
    - `grep -c "ferramenta" apresentacao/index.html` permanece igual antes/depois (vocabulário "aplicação" per D-92)
    - 6 ocorrências de `class="step"` adicionadas + 5 de `class="arr"` adicionadas (contagem incremental vs antes do plan)
    - Sem em-dash novo: `grep -c "—" apresentacao/index.html` igual antes/depois
    - `grep -c "tcc2_prototipo\.html</i>" apresentacao/index.html` retorna >= 2 (em TOOL-01 + TOOL-03)
    - Browser http://127.0.0.1:8000/#/27: pipeline 6 etapas + 5 setas todos em preto, cabe em 1280px sem scroll horizontal, microcópia legível (Pitfall 1)
    - Demais slides #/0..#/26 e #/28..#/30 inalterados
  </acceptance_criteria>
  <done>
    Section TOOL-01 inserida entre MARKER-03 e TOOL-03 com pipeline `.bridge-seq` 6 etapas monocromático. Deck final = 31 sections. Zero CSS novo (todo reuso).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual TOOL-01 (Pitfall 1 overflow; Open Question #2 frase de fechamento)</name>
  <what-built>
    Section TOOL-01 com pipeline `.bridge-seq` 6 etapas (Submissões → Extração → Professor → Preparação → Code-DKT → Dashboard) + abertura 1ª pessoa do plural + frase de fechamento opcional.
  </what-built>
  <how-to-verify>
    1. Subir servidor: `cd apresentacao && python3 -m http.server 8000`
    2. Abrir http://127.0.0.1:8000/#/27
    3. Validar visualmente (Pitfall 1 — questão crítica deste checkpoint):
       a. Pipeline 6 etapas + 5 setas cabe inteiro em 1280px (sem scrollbar horizontal)
       b. Microcópia de cada .step legível (texto NÃO quebra em mais de 2 linhas)
       c. Espaçamento entre setas e steps consistente com MODEL-05 (5 etapas)
       d. Todas as 6 caixas neutras: borda 1.5px preta `#000`, fundo branco, texto preto, SEM azul UniFacens em nenhuma (D-94d)
       e. Setas `→` em preto (não azul)
       f. Cabeçalho `> proposta da aplicação` com caret piscando
       g. Abertura: `O processo apresentado pode ser instrumentalizado para professores. Propomos uma aplicação docente que organiza esse fluxo em seis etapas.` legível
       h. Título Figura: `Figura – Aplicação docente em seis etapas`
       i. Legenda Fonte: `Fonte: elaborado pelo autor; baseado em docs/tcc2_prototipo.html.`
       j. Frase de fechamento (opcional): `O dashboard fecha o ciclo e é o que detalhamos no próximo slide.`
       k. Etapa 1 diz "Submissões dos estudantes" (não "ProgSnap2") per D-94h
       l. Vocabulário: "aplicação" no cabeçalho, "estudantes" na etapa 1, sem em-dash em nenhum lugar
    4. Comparar com MODEL-05 (#/21): mesma estética, +1 etapa
    5. Navegar #/26 → #/27 → #/28 → #/29 → #/30:
       - #/26 MARKER-03 (intacto)
       - #/27 TOOL-01 (deste plan)
       - #/28 TOOL-03 (do plan 04)
       - #/29 MARKER-04 (do plan 01)
       - #/30 END-01 (do plan 03)
    6. F12 console clean em todos os slides
    7. Open Questions a decidir:
       - **#2 frase de fechamento**: manter "O dashboard fecha o ciclo e é o que detalhamos no próximo slide." OU cortar se slide ficar apertado em 1280×720 (per D-94f opcional)
       - **Pitfall 1 mitigação**: se overflow detectado, escolher uma das 4 mitigações (font 17px / padding 12-10 / comprimir microcópia / .bridge-seq--6step CSS dedicado)
       - **Cabeçalho `> proposta da aplicação`**: manter ou variar
       - **Microcópia das etapas**: ajustar descrições secundárias (ex.: "treinado" em Code-DKT pode soar técnico demais; alternativa "modelo treinado" ou simplesmente cortar)
  </how-to-verify>
  <resume-signal>
    Digite "approved" para manter como está; "cortar-fechamento" para remover a frase opcional; "mitigar:fonte" / "mitigar:padding" / "mitigar:microcopia" / "mitigar:css-dedicado" se houver overflow; "ajustar-microcopia:[etapa-N=novo-texto]" para mudar texto de etapas específicas; ou descreva ajustes e relance.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Apresentação HTML/CSS estática sem auth/network/data. Sem trust boundary aplicável. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| n/a | n/a | n/a | n/a | Sem superfície de ataque. |
</threat_model>

<verification>
- TOOL-01 renderiza com pipeline 6 etapas monocromático sem overflow em 1280×720
- Deck final tem 31 sections (`grep -c "<section " apresentacao/index.html` = 31)
- Ordem final no DOM: MARKER-03 → TOOL-01 → TOOL-03 → MARKER-04 → END-01
- Vocabulário "aplicação" + "estudantes" + sem em-dash; ProgSnap2 não aparece em TOOL-01
- Reviewer aprova layout no checkpoint (Pitfall 1 mitigado se houver overflow)
</verification>

<success_criteria>
- ROADMAP Phase 5 Success Criterion 1 atendido: "Slide TOOL-01 (fundido) apresenta a ferramenta proposta para o TCC 2 (baseada em docs/tcc2_prototipo.html) E o pipeline mini-horizontal num único slide; sem detalhar cada etapa; sem mencionar ProgSnap2 nominalmente"
- REQ TOOL-01 coberto
- Pipeline 6 etapas espelha o que já foi mostrado nas fases 2-4 (extração de KCs do MODEL-05, preparação dos dados do EDA-02, Code-DKT do MODEL-01/03/04, dashboard introduzido em TOOL-03) per D-94e
</success_criteria>

<output>
After completion, create `.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-05-SUMMARY.md` registrando:
- Markup final do TOOL-01 (com frase de fechamento ou sem; com mitigação Pitfall 1 ou sem)
- Decisões de checkpoint (frase de fechamento, microcópia, mitigação de overflow)
- Estado final do deck (31 sections; #/0..#/30; ordem confirmada)
- Commit hash final
</output>
