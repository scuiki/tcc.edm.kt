---
phase: 05-implanta-o-agenda-e-encerramento-fase-4-edm
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - apresentacao/index.html
  - apresentacao/assets/theme-unifacens.css
  - apresentacao/STYLE.md
autonomous: true
requirements: [AGENDA-01, PENDING-01]
must_haves:
  truths:
    - "Slide #/2 (slide-agenda) refatorado in-place exibe cabeçalho `> agenda` com caret piscando no padrão `.deck-topic`"
    - "4 fases EDM listadas verticalmente em ordem: Definição do Problema → Preparação dos Dados → Modelagem e Avaliação → Implantação"
    - "Caret blink aparece apenas no último item (Implantação) per D-93d"
    - "AGENDA fica em #/2 (posição inalterada per D-92.2); refatoração não desloca demais slides"
    - "Não há `<h2>Agenda</h2>` nem `agenda-side` (logo grande) nem `agenda-main` no slide refatorado"
    - "STYLE.md §Cabeçalho atualizado para incluir AGENDA no padrão `.deck-topic` (override D-93b)"
    - "Classes órfãs `.slide-agenda`, `.agenda-side`, `.agenda-main`, `.agenda-list` removidas do theme-unifacens.css se sem outros callers (D-93f)"
  artifacts:
    - path: "apresentacao/index.html"
      provides: "Section AGENDA-01 refatorada (linhas ~59-80 reescritas em-place)"
      contains: "deck-topic.*agenda"
    - path: "apresentacao/STYLE.md"
      provides: "§Cabeçalho de TODO slide (incluindo AGENDA) atualizado"
      contains: "incluindo a AGENDA"
    - path: "apresentacao/assets/theme-unifacens.css"
      provides: "Nova classe `.agenda-edm-list` (ou reuso de classe existente) + cleanup de 4 classes órfãs"
  key_links:
    - from: "apresentacao/index.html #/2 AGENDA-01"
      to: "apresentacao/assets/theme-unifacens.css `.deck-topic`"
      via: "classe `.deck-topic` aplicada ao parágrafo de cabeçalho"
      pattern: "deck-topic.*agenda"
    - from: "apresentacao/STYLE.md §Cabeçalho"
      to: "Regra `.deck-topic` aplicada a TODOS os slides (incluindo AGENDA)"
      via: "reescrita do título do § e do texto do bullet"
      pattern: "Cabeçalho de TODO slide.*incluindo AGENDA\\|incluindo a AGENDA"
---

<objective>
Refatorar in-place o slide AGENDA (#/2) para incorporar o novo padrão `> [seção]` `.deck-topic` (override explícito do STYLE.md linha 37-60 per D-93b) e listar as 4 fases EDM como sumário (resolve PENDING-01 via D-93a). Atualizar STYLE.md §Cabeçalho no mesmo plan, e fazer cleanup das classes CSS legadas órfãs.

Purpose: este plan é o cross-cutting da fase 5 — atualiza a fonte canônica de regras (STYLE.md §Cabeçalho) que os plans 03-05 (END-01, TOOL-03, TOOL-01) vão herdar. Ordem 2 (após MARKER-04) é a ótima per RESEARCH §Plan-Level Execution Order: TOOL-01/03 precisam ler a versão atualizada de STYLE.md. Slide-agenda atual (linhas 59-80 do index.html) usa `<h2>Agenda</h2>` + `agenda-side` (faixa azul + logo grande) + `agenda-main` com 8 bullets genéricos do template UniFacens; tudo isso é substituído pelo template `.slide-related` com cabeçalho `.deck-topic` + lista numerada de 4 fases.

Output:
- Section #/2 reescrita em-place (deck mantém em 28 sections após este plan; refatoração não muda contagem)
- STYLE.md §"Cabeçalho de todo slide após a AGENDA" → "Cabeçalho de TODO slide (incluindo AGENDA)"
- Nova classe `.agenda-edm-list` em theme-unifacens.css (per RESEARCH §Code Example 2)
- Cleanup: 4 declarações CSS órfãs (`.slide-agenda`, `.agenda-side`, `.agenda-main`, `.agenda-list`) removidas se sem caller
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-01-SUMMARY.md
@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css

<interfaces>
<!-- Template .slide-related canônico (theme-unifacens.css ~linhas 162-182): wrapper de slides de conteúdo -->
<!-- Classes a reusar: .deck-topic + .caret.blink (linhas 42-51), .wm (marca d'água), .slide-related -->
<!-- HTML symbol id="sym" definido em index.html linhas 16-21 -->

Padrão do cabeçalho .deck-topic (a aplicar à AGENDA per D-93b):
<p class="deck-topic"><span class="ps1">&gt;</span>agenda<span class="caret blink"></span></p>

Markup-alvo do AGENDA-01 refatorado (RESEARCH §Code Example 2, linhas 328-344):
<section data-background-color="#F1F6FB">
  <div class="deck-slide slide-related">
    <svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>
    <p class="deck-topic"><span class="ps1">&gt;</span>agenda<span class="caret blink"></span></p>
    <ol class="agenda-edm-list">
      <li>Definição do Problema</li>
      <li>Preparação dos Dados</li>
      <li>Modelagem e Avaliação</li>
      <li>Implantação<span class="caret blink"></span></li>
    </ol>
  </div>
</section>

CSS proposto para `.agenda-edm-list` (RESEARCH §Code Example 2 linhas 350-367):
- list-style: none; counter-reset: agenda
- contador numérico azul UniFacens com ::before content: counter(agenda) "."
- li: padding 10px 0 10px 50px; Arial 23px; line-height 1.4; cor --uni-ink
- max-width 900px para evitar que a lista se estique demais

Estado HEAD do slide-agenda (index.html linhas 59-80): usa `<h2>Agenda</h2>`, `data-background-gradient`, `agenda-side`, `agenda-main`, `agenda-list` (8 bullets genéricos), classe `slide-agenda`. TUDO isto é substituído.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refatorar a `<section>` AGENDA in-place (linhas ~59-80 do index.html) e adicionar `.agenda-edm-list` em theme-unifacens.css</name>
  <files>apresentacao/index.html, apresentacao/assets/theme-unifacens.css</files>
  <read_first>
    - apresentacao/index.html linhas 59-80 (section AGENDA atual, gabarito a ser SUBSTITUÍDO)
    - apresentacao/assets/theme-unifacens.css linhas 42-51 (`.deck-topic` + `.caret.blink`)
    - apresentacao/assets/theme-unifacens.css linhas 118-132 (`.slide-agenda`, `.agenda-side`, `.agenda-main`, `.agenda-list` — classes a remover na Task 2)
    - apresentacao/assets/theme-unifacens.css ~linha 162 (bloco `.slide-related` para localizar onde inserir `.agenda-edm-list` perto)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md §D-93a/b/c/d/e (estrutura, cabeçalho, layout, caret, sem Fonte)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Code Example 2 (markup completo + CSS completo)
  </read_first>
  <action>
    Substituir linhas 59-80 do `apresentacao/index.html` (a `<section>` AGENDA atual incluindo o comentário HTML acima) pelo markup definido no RESEARCH §Code Example 2 (também rascunhado em CONTEXT §Specifics):
    - Comentário HTML: `<!-- ============ SLIDE · Agenda · 4 fases da EDM ============ -->`
    - `<section data-background-color="#F1F6FB">` (REMOVER `data-background-gradient`; assumption A5 confirma uso único do gradient na agenda)
    - `<div class="deck-slide slide-related">` (REMOVER `slide-agenda`)
    - `<svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>` (marca d'água per D-93c)
    - `<p class="deck-topic"><span class="ps1">&gt;</span>agenda<span class="caret blink"></span></p>` (cabeçalho per D-93b)
    - `<ol class="agenda-edm-list">` com 4 `<li>` em ordem D-93a: Definição do Problema, Preparação dos Dados, Modelagem e Avaliação, Implantação
    - Caret blink APENAS no último item (Implantação) per D-93d: `<li>Implantação<span class="caret blink"></span></li>`
    - SEM `<p class="rel-cite">Fonte:</p>` per D-93e (Agenda é estrutural, não derivada)

    Em `apresentacao/assets/theme-unifacens.css`, anexar a classe `.agenda-edm-list` (conforme RESEARCH §Code Example 2 linhas 350-367) próximo ao bloco `.slide-related` (~linha 182) para coesão temática. Regras: `list-style: none; counter-reset: agenda; margin: 32px 0 0; padding: 0; max-width: 900px;` no `<ol>`, `counter-increment: agenda; position: relative; padding: 10px 0 10px 50px; Arial 23px; line-height 1.4; color: var(--uni-ink);` no `<li>`, e contador azul no `::before` (`content: counter(agenda) "."; position: absolute; left: 12px; top: 10px; color: var(--uni-blue); font-weight: 700;`).

    NÃO tocar nas declarações órfãs `.slide-agenda`, `.agenda-side`, `.agenda-main`, `.agenda-list` ainda — Task 2 faz cleanup após confirmar grep de zero callers. Não usar em-dash per D-100. Usar "estudantes" se aparecer (não aparece aqui) per D-102.
  </action>
  <verify>
    <automated>grep -c "agenda-edm-list" apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "agenda-edm-list" apresentacao/assets/theme-unifacens.css | awk '$1 >= 3 {exit 0} {exit 1}' && grep -c '>agenda<span class="caret blink"></span>' apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "<section " apresentacao/index.html | awk '$1 == 28 {exit 0} {exit 1}' && grep -v '^[[:space:]]*<!--\|^[[:space:]]*\*' apresentacao/index.html | grep -c "<h2>Agenda</h2>" | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<section " apresentacao/index.html` retorna 28 (refatoração não muda contagem; era 28 após plan 01)
    - `grep -c "agenda-edm-list" apresentacao/index.html` retorna 1
    - `grep -c "agenda-edm-list" apresentacao/assets/theme-unifacens.css` retorna >= 3 (`.agenda-edm-list`, `.agenda-edm-list li`, `.agenda-edm-list li::before`)
    - `grep -c "agenda-side\|agenda-main" apresentacao/index.html` retorna 0 (markup antigo removido)
    - `grep -c "<h2>Agenda</h2>" apresentacao/index.html` retorna 0
    - `grep -c '>agenda<span class="caret blink"></span>' apresentacao/index.html` retorna 1 (cabeçalho `.deck-topic` correto)
    - `grep -c "data-background-gradient" apresentacao/index.html` retorna 0 (assumption A5)
    - Browser http://127.0.0.1:8000/#/2: 4 itens numerados (`1.`, `2.`, `3.`, `4.`) em azul UniFacens, texto preto Arial 23px, caret piscando após "Implantação"; F12 console sem erros
    - Demais slides #/0 → #/26 → MARKER-04 inalterados visualmente
  </acceptance_criteria>
  <done>
    Section AGENDA-01 reescrita in-place com cabeçalho `> agenda` + lista numerada `.agenda-edm-list` (4 fases EDM); CSS da nova classe anexado próximo ao bloco `.slide-related`. Slide #/2 abre com layout `.slide-related` consistente com demais slides de conteúdo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cleanup CSS — remover declarações órfãs `.slide-agenda`, `.agenda-side`, `.agenda-main`, `.agenda-list` (D-93f, paralelo ao precedente `.rel-kicker` da fase 1)</name>
  <files>apresentacao/assets/theme-unifacens.css</files>
  <read_first>
    - apresentacao/assets/theme-unifacens.css linhas 118-132 (bloco completo com as 4 classes a deletar)
    - apresentacao/index.html (recém-modificado pela Task 1)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Pitfall 3 (confirmar com grep que não há outros callers antes de deletar)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md §D-93f (decisão de deletar do CSS se sem outros callers; paralelo a `.rel-kicker/.rel-title/.rel-sub` da fase 1 commit `30ba911`)
  </read_first>
  <action>
    Verificar via grep que `.slide-agenda`, `.agenda-side`, `.agenda-main`, `.agenda-list` são órfãs após Task 1:
    1. `grep -rn "agenda-side\|agenda-main\|agenda-list\|slide-agenda" apresentacao/ --include="*.html"` — esperado 0 hits
    2. Se confirmado 0 hits em todo o markup, REMOVER as declarações CSS do `apresentacao/assets/theme-unifacens.css` linhas 118-132 inclusive (`.slide-agenda { ... }`, `.slide-agenda .agenda-side { ... }`, `.slide-agenda .agenda-side h2 { ... }`, `.slide-agenda .agenda-side .logo { ... }`, `.slide-agenda .agenda-main { ... }`, `.agenda-list { ... }`, `.agenda-list li { ... }`, `.agenda-list li .ps1 { ... }`)
    3. Se houver QUALQUER caller restante (improvável dado o grep), PARAR e registrar em SUMMARY; NÃO deletar.
    Não introduzir em-dash em comentários CSS novos. Não tocar em `.agenda-edm-list` (nova classe criada na Task 1).
  </action>
  <verify>
    <automated>grep -c "\.slide-agenda\|\.agenda-side\|\.agenda-main\|\.agenda-list" apresentacao/assets/theme-unifacens.css | awk '$1 == 0 {exit 0} {exit 1}' && grep -c "agenda-side\|agenda-main\|agenda-list\|slide-agenda" apresentacao/index.html | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "\.slide-agenda\|\.agenda-side\|\.agenda-main\|\.agenda-list" apresentacao/assets/theme-unifacens.css` retorna 0 hits (todas as 4 classes legadas removidas)
    - `grep -n "agenda-side\|agenda-main\|agenda-list\|slide-agenda" apresentacao/index.html` retorna 0 hits (markup legado removido pela Task 1)
    - `grep -c "agenda-edm-list" apresentacao/assets/theme-unifacens.css` retorna >= 3 (nova classe intacta)
    - Slide #/2 ainda renderiza corretamente no browser após o cleanup (CSS removido não afeta o markup novo)
    - Demais slides #/0, #/1, #/3..#/27 inalterados visualmente
  </acceptance_criteria>
  <done>
    4 declarações órfãs (`.slide-agenda`, `.agenda-side`, `.agenda-main`, `.agenda-list`) removidas de theme-unifacens.css. Slide #/2 mantém renderização correta (depende apenas de `.slide-related` + `.deck-topic` + `.agenda-edm-list`).
  </done>
</task>

<task type="auto">
  <name>Task 3: Atualizar STYLE.md §Cabeçalho — override D-93b (linhas 37-60)</name>
  <files>apresentacao/STYLE.md</files>
  <read_first>
    - apresentacao/STYLE.md linhas 37-60 (§ atual "Cabeçalho de todo slide após a AGENDA")
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §"STYLE.md updates" Update 1 (mudanças linha a linha)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md §D-93b (override do STYLE.md linha 39-42)
  </read_first>
  <action>
    Editar `apresentacao/STYLE.md` aplicando as 3 mudanças mínimas do RESEARCH §STYLE.md updates Update 1:
    1. Linha 37 (título do §): trocar `## Cabeçalho de todo slide após a AGENDA` por `## Cabeçalho de TODO slide (incluindo AGENDA)`
    2. Linhas 46-47 (bullet sobre escopo da classe): trocar "Classe única: `.deck-topic`. Aplica a TODOS os slides de conteúdo (após a AGENDA), incluindo os que antes usavam o template `.slide-related` com par `.rel-kicker.kicker` + `.rel-title` + `.rel-sub`." por "Classe única: `.deck-topic`. Aplica a TODOS os slides de conteúdo, **incluindo a AGENDA** (refator em fase 5), com exceção da capa (`slide-cover-brand`), do título do TCC (`slide-title-tcc`), dos marcadores (`slide-marker`, que usam `.marker-title`) e do slide de encerramento (`slide-end`)."
    3. Linhas 50-54 (lista de exemplos travados): ADICIONAR `> agenda` à lista após `> introdução` (manter ordem dos demais).

    NÃO tocar em outras seções de STYLE.md (Inventário linhas 108-138 e §Gaps reservados linhas 142-146 serão tratados pelo plan 05-06 de fechamento). Não usar em-dash em prosa nova per D-100 (a lista de exceções acima usa vírgulas e parênteses; não introduzir `—`).
  </action>
  <verify>
    <automated>grep -c "## Cabeçalho de TODO slide" apresentacao/STYLE.md | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "incluindo a AGENDA" apresentacao/STYLE.md | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "> agenda" apresentacao/STYLE.md | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "## Cabeçalho de todo slide após a AGENDA" apresentacao/STYLE.md | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Cabeçalho de TODO slide" apresentacao/STYLE.md` retorna 1
    - `grep -c "## Cabeçalho de todo slide após a AGENDA" apresentacao/STYLE.md` retorna 0 (título antigo substituído)
    - `grep -c "incluindo a AGENDA" apresentacao/STYLE.md` retorna >= 1
    - `grep -c "> agenda" apresentacao/STYLE.md` retorna >= 1 (na lista de exemplos travados)
    - Demais seções de STYLE.md inalteradas: §Identidade visual, §Tipografia, §Convenções de citação, §Regras de redação, §Diagramas, §Inventário (será atualizado em 05-06)
    - Sem em-dash novo introduzido pela edição
  </acceptance_criteria>
  <done>
    STYLE.md §Cabeçalho reescrito para refletir override D-93b. Próximos plans (END-01, TOOL-03, TOOL-01) herdam regra atualizada; AGENDA é agora oficialmente parte do padrão `.deck-topic`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Apresentação HTML/CSS estática sem auth/network/data. Sem trust boundary aplicável. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| n/a | n/a | n/a | n/a | Sem superfície de ataque. |
</threat_model>

<verification>
- Slide #/2 abre no browser com cabeçalho `> agenda` + 4 itens numerados + caret blink no item 4
- Demais slides #/0..#/1 e #/3..#/27 (após plan 01) inalterados visualmente
- F12 console clean
- STYLE.md §Cabeçalho reflete o novo escopo "incluindo AGENDA"
- CSS sem classes órfãs `.slide-agenda`, `.agenda-side`, `.agenda-main`, `.agenda-list`
</verification>

<success_criteria>
- AGENDA-01 refatorado in-place sem deslocar outros slides (#/2 mantém posição per D-92.2)
- ROADMAP Phase 5 Success Criterion 5 atendido: "Slide AGENDA-01 revisado reflete a estrutura final (PENDING-01 resolvido); decisão entre `4 fases EDM como sumário` ou outra estrutura formalizada" — implementado as 4 fases EDM como sumário per D-93a
- REQ AGENDA-01 e REQ PENDING-01 cobertos
- STYLE.md atualizado serve de gabarito para plans 03 (END-01), 04 (TOOL-03), 05 (TOOL-01)
</success_criteria>

<output>
After completion, create `.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-02-SUMMARY.md` registrando:
- Markup final da nova section AGENDA-01
- Linhas exatas da classe `.agenda-edm-list` adicionada
- Confirmação do cleanup (4 classes órfãs removidas)
- Mudanças aplicadas em STYLE.md §Cabeçalho
- Decisões ad-hoc (caso emerjam durante execução)
- Commit hash final
</output>
