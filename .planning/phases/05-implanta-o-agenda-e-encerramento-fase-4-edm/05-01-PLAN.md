---
phase: 05-implanta-o-agenda-e-encerramento-fase-4-edm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apresentacao/index.html
  - apresentacao/assets/theme-unifacens.css
autonomous: false
requirements: [MARKER-04]
must_haves:
  truths:
    - "Slide #/29 MARKER-04 renderiza com 3 pills `--done` (Definição do Problema, Preparação dos Dados, Modelagem e Avaliação) + 1 pill `--planned` (Implantação)"
    - "Pill 4 (Implantação) NÃO gira (sem animação) e é visualmente distinguível de `--running` (MARKER-03 #/26) e de `--pending`"
    - "Badge da pill 4 é `[planned]`; badges das pills 1-3 são `[done]`"
    - "Título `> AS QUATRO FASES DA EDM` aparece com caret blink"
    - "Rodapé `Fonte: adaptado de Zorić (2020).` herdado de MARKER-01/02/03"
    - "Modificador CSS `--planned` é aditivo: MARKER-01 (#/9), MARKER-02 (#/15), MARKER-03 (#/26) continuam renderizando exatamente como antes do plan"
  artifacts:
    - path: "apresentacao/assets/theme-unifacens.css"
      provides: "Modificador `.marker-pill--planned` + override do ícone interno (border-style dashed)"
      contains: ".marker-pill--planned"
    - path: "apresentacao/index.html"
      provides: "Section MARKER-04 com classe `slide-marker slide-marker--phase4` no fim do deck"
      contains: "slide-marker--phase4"
  key_links:
    - from: "apresentacao/index.html section MARKER-04"
      to: "apresentacao/assets/theme-unifacens.css `.marker-pill--planned`"
      via: "classe `marker-pill--planned` aplicada à 4ª pill"
      pattern: "marker-pill marker-pill--planned"
    - from: "apresentacao/index.html section MARKER-04 pill 4 icon"
      to: "HTML entity `&#x25CB;` (círculo vazio)"
      via: "ícone estático sem `animation: marker-spin`"
      pattern: "marker-pill-icon.*&#x25CB;"
---

<objective>
Adicionar o slide MARKER-04 (Fase 4 EDM concluída) ao FIM do deck (após MARKER-03, posição #/29) com pill 4 (Implantação) no novo estado `--planned`, reconhecendo honestamente que a Implantação é proposta no TCC 1 mas não executada (TCC 2 implementará).

Purpose: fechar conceitualmente as 4 fases do processo EDM (Zorić, 2020) antes do END-01 (#/30), preservando a simetria com MARKER-01/02/03 e introduzindo o novo modificador `.marker-pill--planned` ao componente CSS canônico `.slide-marker`. Este plan vem primeiro por D-92.1 (ordem do RESEARCH) porque entrega o componente CSS aditivo cedo, calibra o ambiente visual da fase 5 e valida que `--planned` é distinguível de `--running` e `--pending`.

Output:
- 1 nova `<section>` em `apresentacao/index.html` (deck passa de 27 → 28 sections após este plan)
- ~8-12 linhas novas em `apresentacao/assets/theme-unifacens.css` definindo `.marker-pill--planned`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md
@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css

<interfaces>
<!-- Componente .slide-marker canônico (theme-unifacens.css linhas 355-448) -->
<!-- Estados existentes: --done (azul preenchido), --running (azul + animação marker-spin), --pending (cinza neutro) -->
<!-- A 4ª pill em MARKER-03 hoje usa --running com ícone &#x21BB; girando em animation: marker-spin 2.4s linear infinite -->
<!-- Classes a reusar inalteradas: .slide-marker, .marker-title, .marker-track, .marker-stage, .marker-pill, .marker-pill-icon, .marker-pill-name, .marker-arrow, .marker-badge, .marker-badge--empty -->
<!-- HTML entity para círculo vazio: &#x25CB; (já usado em --pending) -->

Padrão de section MARKER (referência MARKER-03, index.html linhas 687-730):
- Wrapper: <section data-background-color="#F1F6FB">
- Container: <div class="deck-slide slide-marker slide-marker--phaseN">
- Watermark: <svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>
- Título: <p class="marker-title"><span class="ps1">&gt;</span>AS QUATRO FASES DA EDM<span class="caret blink"></span></p>
- Track: <div class="marker-track"> com 4 .marker-stage (cada um com .marker-pill + .marker-badge) e 3 .marker-arrow entre eles
- Rodapé: <p class="rel-cite">Fonte: adaptado de Zorić (2020).</p>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar modificador `.marker-pill--planned` ao theme-unifacens.css (Alt A recomendada do RESEARCH §Code Example 1)</name>
  <files>apresentacao/assets/theme-unifacens.css</files>
  <read_first>
    - apresentacao/assets/theme-unifacens.css linhas 355-448 (bloco SLIDE · Marker completo, para localizar exatamente onde inserir o novo modificador)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Code Examples Exemplo 1 (3 alternativas com CSS exato; usar Alt A como default)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md §D-96c (restrições: zero animação, distinguível de --running e --pending, aditivo)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Pitfall 2 (cinza neutro #5b6472 idêntico ao --pending colide visualmente; usar borda tracejada Alt A para diferenciar)
  </read_first>
  <action>
    Inserir as regras CSS do modificador `.marker-pill--planned` em `apresentacao/assets/theme-unifacens.css` imediatamente APÓS o bloco `.marker-pill--pending .marker-pill-icon` (linhas 425-428) e ANTES de `.marker-arrow` (linha 430), seguindo Alt A do RESEARCH §Code Example 1: cor de texto `#5b6472`, `border-style: dashed` na pill, ícone com cor `#5b6472` + `border: 1.5px dashed #5b6472`. Adicionar comentário CSS curto explicando o estado: "Planned: ainda nao executada (TCC 2); circulo cinza vazio com borda tracejada, sem animacao; visualmente diferente de --pending (solido) e --running (anima)." (per D-96c, sem em-dash per D-100; usar parênteses e vírgulas). Não tocar em `.marker-pill--done`, `.marker-pill--running`, `.marker-pill--pending` (gate D-96d: aditivo). Não introduzir `@keyframes` novos (gate D-96f: zero animação).
  </action>
  <verify>
    <automated>grep -c "marker-pill--planned" apresentacao/assets/theme-unifacens.css | awk '$1 >= 2 {exit 0} {exit 1}' && grep -v '^/\*\|^\*\| \*' apresentacao/assets/theme-unifacens.css | grep -c "marker-pill--planned.*animation\|@keyframes.*planned" | awk '$1 == 0 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "marker-pill--planned" apresentacao/assets/theme-unifacens.css` retorna >= 2 (regra base + .marker-pill-icon override)
    - `grep -n "animation" apresentacao/assets/theme-unifacens.css | grep "planned"` retorna 0 linhas (gate D-96f, zero animação)
    - `grep -n "marker-pill--done\|marker-pill--running\|marker-pill--pending" apresentacao/assets/theme-unifacens.css` retorna o MESMO número de linhas que antes do plan (regras existentes intactas)
    - `grep -n "border-style: dashed\|border: 1.5px dashed" apresentacao/assets/theme-unifacens.css` retorna pelo menos 1 ocorrência associada a `.marker-pill--planned`
    - Servir local `cd apresentacao && python3 -m http.server 8000`: MARKER-01 (#/9), MARKER-02 (#/15), MARKER-03 (#/26) abrem sem regressão visual (3 pills done azul preenchidas; pill 4 do MARKER-03 mantém --running girando)
  </acceptance_criteria>
  <done>
    Modificador `.marker-pill--planned` definido em theme-unifacens.css com texto/borda cinza azulado `#5b6472`, borda tracejada `dashed`, ícone com a mesma cor e borda; comentário inline explicando o estado; zero animação; MARKER-01/02/03 inalterados visualmente.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inserir a `<section>` MARKER-04 ao fim do deck (após MARKER-03, posição #/29)</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html linhas 687-730 (section MARKER-03 inteiro, gabarito a copiar)
    - apresentacao/index.html linhas 730-735 (fim do `<div class="slides">` para inserir antes do `</div>` de fechamento)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md §D-96 (deltas em relação ao MARKER-03)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Code Example 4 (markup completo do MARKER-04 com 4 deltas vs MARKER-03)
  </read_first>
  <action>
    Copiar a estrutura completa da `<section>` MARKER-03 (linhas 687-730 do index.html) e inserir o duplicado IMEDIATAMENTE após o `</section>` da linha 730, antes do `</div>` que fecha `<div class="slides">` (linha 732). Aplicar 4 deltas na cópia (per D-96 + RESEARCH §Code Example 4):
    1. Atualizar o comentário HTML para `<!-- ============ SLIDE · MARKER · As quatro fases da EDM, fase 4 planejada (Zoric, 2020) ============ -->`
    2. Classe modificadora do `<div class="deck-slide slide-marker ...">`: trocar `slide-marker--phase3` por `slide-marker--phase4`
    3. Pill 3 (Modelagem e Avaliação) MANTÉM `marker-pill--done` com `&check;` e badge `[done]` (Modelagem foi entregue na fase 4 do projeto). NÃO alterar.
    4. Pill 4 (Implantação): trocar `marker-pill--running` por `marker-pill--planned`; trocar o ícone `&#x21BB;` por `&#x25CB;` (círculo vazio); trocar o badge `[running]` por `[planned]`. Não tocar no `marker-pill-name` (Implantação) nem na estrutura `.marker-stage`/`.marker-arrow`.
    Tudo o mais (título `> AS QUATRO FASES DA EDM`, rodapé `Fonte: adaptado de Zorić (2020).`, watermark) idêntico ao MARKER-03 per D-96e. Sem em-dash em prosa nova per D-100.
  </action>
  <verify>
    <automated>grep -c "slide-marker--phase4" apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "marker-pill--planned" apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "&#x25CB;" apresentacao/index.html | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c '\[planned\]' apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "<section " apresentacao/index.html | awk '$1 == 28 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<section " apresentacao/index.html` retorna 28 (era 27 antes deste plan; +1 MARKER-04)
    - `grep -c "slide-marker--phase4" apresentacao/index.html` retorna 1
    - `grep -c "marker-pill--planned" apresentacao/index.html` retorna 1
    - `grep -c '\[planned\]' apresentacao/index.html` retorna 1
    - `grep -c '\[running\]' apresentacao/index.html` retorna 1 (apenas MARKER-03; gate Validation Checklist do RESEARCH)
    - `grep -c "AS QUATRO FASES DA EDM" apresentacao/index.html` retorna 4 (MARKER-01/02/03/04)
    - MARKER-04 está APÓS MARKER-03 no DOM: `grep -n "slide-marker--phase" apresentacao/index.html` mostra phase1 < phase2 < phase3 < phase4 em ordem crescente de linha
    - Sem em-dash (`—`) na nova section: `grep -c "—" apresentacao/index.html` é o mesmo número antes/depois do plan
  </acceptance_criteria>
  <done>
    `<section>` MARKER-04 inserida no fim do deck (após linha 730 do MARKER-03) com classe `slide-marker--phase4`, pills 1-3 `--done` com check, pill 4 `--planned` com círculo vazio `&#x25CB;` e badge `[planned]`, título e rodapé herdados de MARKER-03. Deck passa de 27 → 28 sections.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual MARKER-04 (estética `--planned` Alt A vs Alt B vs Alt C, RESEARCH Open Question #4)</name>
  <what-built>
    1. Modificador CSS `.marker-pill--planned` em theme-unifacens.css (Alt A: borda tracejada cinza azulada `#5b6472`)
    2. Section MARKER-04 em index.html ao fim do deck (posição #/29) com 3 pills `--done` e pill 4 (Implantação) `--planned` sem animação
  </what-built>
  <how-to-verify>
    1. Subir servidor: `cd apresentacao && python3 -m http.server 8000` (ou 8001 se 8000 em uso)
    2. Abrir http://127.0.0.1:8000/#/29 no browser
    3. Verificar visualmente:
       a. As 4 pills lado a lado, conectadas por setas pretas `→`
       b. Pills 1, 2, 3 com check `✓` branco em círculo azul preenchido + badge `[done]` (idêntico ao MARKER-01/02/03)
       c. Pill 4 (Implantação) com círculo vazio `○` em borda tracejada cinza azulada + badge `[planned]`
       d. Pill 4 NÃO gira (zero animação; comparar com MARKER-03 em #/26 cuja pill 4 está girando)
       e. Título `> AS QUATRO FASES DA EDM` com caret piscando
       f. Rodapé `Fonte: adaptado de Zorić (2020).`
    4. Comparar a 3m de distância (Pitfall 2): pill 4 deve ser distinguível de pills 1-3 (done) e do estado `--pending` que aparece em MARKER-01 (#/9 mostra pills 2-4 em pending para referência)
    5. F12 console: zero erros JS/CSS
    6. Navegar #/26 → #/27 → #/28 → #/29: ainda não existem #/27 e #/28; #/29 deve abrir como #/27 nesta etapa intermediária (deck tem 28 sections)
    7. Decidir entre 3 alternativas de estética (per RESEARCH §Pitfall 2 + Open Question #4):
       - Alt A (atual, default): borda tracejada cinza azulada
       - Alt B: cor `--uni-blue` com opacity 0.7, ícone `&#x21BB;` estático sem animação
       - Alt C: ícone novo (calendário `&#x231A;` ou relógio), cor `#5b6472`, borda sólida
  </how-to-verify>
  <resume-signal>Digite "approved-alt-a" para manter Alt A; "switch-to-alt-b" ou "switch-to-alt-c" para trocar; ou descreva ajustes (cor, ícone, espessura da borda) que o executor deve aplicar e relançar este checkpoint.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Apresentação HTML/CSS estática sem auth/network/data. Sem trust boundary aplicável. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| n/a | n/a | n/a | n/a | Sem superfície de ataque: slides estáticos servidos por `python3 -m http.server` em loopback durante validação local. Nenhum input, nenhuma sessão, nenhum dado sensível. |
</threat_model>

<verification>
- Deck navega de #/0 a #/27 (ou #/28 dependendo de inserção parcial) sem erro de console no browser
- MARKER-01 (#/9), MARKER-02 (#/15), MARKER-03 (#/26) inalterados visualmente
- MARKER-04 renderiza com 3 pills done + 1 planned sem animação
- Pill 4 visualmente distinguível das outras a 3m
- Reviewer aprova a estética escolhida (Alt A default ou variante decidida no checkpoint)
</verification>

<success_criteria>
- 1 nova section MARKER-04 inserida em `apresentacao/index.html` ao fim do deck (sections 27 → 28)
- Modificador `.marker-pill--planned` definido em theme-unifacens.css, zero animação, distinguível de `--pending`/`--running`
- MARKER-01/02/03 callers existentes inalterados visualmente
- Validação visual no browser aprovada pelo reviewer (Success Criterion 3 do ROADMAP Phase 5: "Slide MARKER-04 mostra Implantação sinalizado como concluído" — aqui implementado como `--planned` honesto per D-96a override; reviewer aprova essa interpretação no checkpoint)
- Atende REQ MARKER-04 conforme D-96 (CONTEXT.md)
</success_criteria>

<output>
After completion, create `.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-01-SUMMARY.md` registrando:
- Alternativa escolhida no checkpoint (Alt A / B / C ou variante)
- Linhas exatas adicionadas ao theme-unifacens.css
- Linha de inserção da nova `<section>` em index.html
- Posição final do MARKER-04 (#/29 esperado ao fim da fase; #/27 nesta etapa intermediária dependendo do plan)
- Decisões ad-hoc registradas durante o checkpoint (cor, ícone, espessura de borda)
- Iterações pós-checkpoint (se houver)
- Commit hash final
</output>
