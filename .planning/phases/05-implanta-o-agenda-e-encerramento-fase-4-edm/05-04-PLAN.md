---
phase: 05-implanta-o-agenda-e-encerramento-fase-4-edm
plan: 04
type: execute
wave: 4
depends_on: [01, 02, 03]
files_modified:
  - apresentacao/index.html
  - apresentacao/assets/theme-unifacens.css
autonomous: false
requirements: [TOOL-03]
must_haves:
  truths:
    - "Slide TOOL-03 renderiza com cabeçalho `> dashboard da aplicação` (ou `> o dashboard` se reviewer trocar no checkpoint) em padrão `.deck-topic`"
    - "Wireframe 3 painéis lado a lado em estilo Word/ABNT monocromático (borda 1.5px preta, cantos retos `border-radius: 0`, fundo branco, sem azul UniFacens)"
    - "3 painéis nomeados na ordem D-95c: Respostas de código da turma → Predição de conhecimento por estudante → Dificuldade da turma por KC"
    - "3 painéis com altura uniforme (visual coeso); SEM `.marker-arrow` entre eles (painéis são paralelos, não sequenciais)"
    - "Vocabulário `aplicação` (D-92), nunca `ferramenta`; `estudantes` (D-102), nunca `alunos`"
    - "Frase de fechamento foca no professor: `O dashboard auxilia o professor a direcionar intervenções por estudante e por dificuldade.`"
    - "Rodapé `Fonte: elaborado pelo autor; baseado em <i>docs/tcc2_prototipo.html</i>.`"
    - "TOOL-03 fica em #/28 — entre MARKER-03 (#/26) e MARKER-04 (#/29 após inserção também de TOOL-01 no plan 05). Inserção neste plan coloca o TOOL-03 IMEDIATAMENTE após MARKER-03 e ANTES de MARKER-04"
  artifacts:
    - path: "apresentacao/index.html"
      provides: "Section TOOL-03 inserida entre MARKER-03 e MARKER-04"
      contains: "dashboard da aplicação\\|o dashboard"
    - path: "apresentacao/assets/theme-unifacens.css"
      provides: "Opcionalmente `.dash-card-row` + `.dash-card` (fallback se `.bridge-seq` não casar — D-95d)"
  key_links:
    - from: "apresentacao/index.html section TOOL-03"
      to: "componente `.bridge-seq` reusado OU `.dash-card-row` novo"
      via: "decisão no checkpoint Open Question #5 do RESEARCH"
      pattern: "bridge-seq\\|dash-card-row"
    - from: "TOOL-03 #/28"
      to: "TOOL-01 #/27 (inserido em plan 05)"
      via: "TOOL-01 fecha com `O dashboard fecha o ciclo e é o que detalhamos no próximo slide.` — ponte natural"
      pattern: "narrativa ponte"
---

<objective>
Inserir o slide TOOL-03 (dashboard wireframe 3 painéis) entre MARKER-03 (#/26) e MARKER-04 (criado no plan 01). TOOL-03 mostra os 3 painéis principais do dashboard da aplicação docente proposta para o TCC 2, em estilo Word/ABNT monocromático sem screenshot do `docs/tcc2_prototipo.html`.

Purpose: ordem 4 per RESEARCH §Plan-Level Execution Order — TOOL-03 vem ANTES de TOOL-01 (contraintuitivamente) porque testa se `.bridge-seq` adaptado serve para layout NÃO-sequencial (3 painéis paralelos, sem setas). Se o teste falhar, criamos `.dash-card-row`/`.dash-card` AQUI; TOOL-01 (plan 05) usa `.bridge-seq` puro inalterado. Inverter a ordem geraria risco de retrabalho em TOOL-01. TOOL-03 é o output final do pipeline mostrado em TOOL-01 e faz a ponte para MARKER-04.

Output:
- 1 nova `<section>` em `apresentacao/index.html` inserida ENTRE MARKER-03 (linha ~730) e MARKER-04 (criado no plan 01). Posição interna #/27 após este plan (MARKER-04 desloca para #/28, END-01 para #/29).
- 0 a ~15 linhas em `apresentacao/assets/theme-unifacens.css` se decidir pelo fallback `.dash-card-row` + `.dash-card` (RESEARCH §Code Example 6 linhas 545-564)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-03-SUMMARY.md
@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css
@docs/tcc2_prototipo.html

<interfaces>
<!-- Componente .bridge-seq canônico (theme-unifacens.css linhas 197-206 e seletor expandido em .slide-bridge) -->
<!-- .bridge-seq usa display:flex com .step e .arr; MODEL-05 (linha 540-550 do index.html) reusa para 5 etapas verticais -->
<!-- Para TOOL-03 (3 painéis paralelos NÃO-sequenciais): adaptar inline removendo .arr OU criar .dash-card-row CSS novo -->
<!-- Componente .eda-title e .eda-source (CSS perto de linhas 450+): título "Figura – ..." e rodapé "Fonte: elaborado..." em padrão ABNT -->

Padrão de section TOOL-03 alvo (RESEARCH §Code Example 6, opção principal `.bridge-seq` adaptado):
<section data-background-color="#F1F6FB">
  <div class="deck-slide slide-related slide-bridge">
    <svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>
    <p class="deck-topic"><span class="ps1">&gt;</span>dashboard da aplicação<span class="caret blink"></span></p>
    <p class="eda-title" style="margin-top: 26px; color: #000;">Figura &ndash; Painéis principais do dashboard</p>
    <p class="bridge-seq" style="margin-top: 14px; gap: 16px;">
      <span class="step" style="flex-direction: column; color: #000; border-color: #000; font-weight: 400; min-height: 280px; padding: 24px 18px; font-size: 18px;">
        <b>Respostas de código<br>da turma</b>
        <span style="margin-top: 22px; font-size: 14px; color: #5b6472;">[ilustração esquemática]</span>
      </span>
      <span class="step" style="flex-direction: column; color: #000; border-color: #000; font-weight: 400; min-height: 280px; padding: 24px 18px; font-size: 18px;">
        <b>Predição de conhecimento<br>por estudante</b>
        <span style="margin-top: 22px; font-size: 14px; color: #5b6472;">[ilustração esquemática]</span>
      </span>
      <span class="step" style="flex-direction: column; color: #000; border-color: #000; font-weight: 400; min-height: 280px; padding: 24px 18px; font-size: 18px;">
        <b>Dificuldade da turma<br>por KC</b>
        <span style="margin-top: 22px; font-size: 14px; color: #5b6472;">[ilustração esquemática]</span>
      </span>
    </p>
    <p class="eda-source">Fonte: elaborado pelo autor; baseado em <i>docs/tcc2_prototipo.html</i>.</p>
    <p class="rel-lead" style="margin-top: 18px;">O dashboard auxilia o professor a direcionar intervenções por estudante e por dificuldade.</p>
  </div>
</section>

Diferenças vs MODEL-05:
- 3 .step (não 5) em vez de pipeline sequencial; SEM .arr entre eles (painéis paralelos)
- .step tem min-height 280px (cria "painel" em vez de "etapa")
- gap: 16px inline na .bridge-seq (default é 0; cria espaçamento entre painéis)

Fallback `.dash-card-row` + `.dash-card` (RESEARCH §Code Example 6 linhas 545-577):
- grid-template-columns 1fr 1fr 1fr para painéis idênticos
- .dash-card: background #fff, border 1.5px solid #1f1f1f, border-radius 0, min-height 280px
- .dash-card-title (Arial 19px bold preto), .dash-card-illu (Arial 14px cinza)

Conceitualmente referenciar (NÃO copiar layout) `docs/tcc2_prototipo.html`:
- page-eda (linhas ~416+): painel de respostas e padrões de erro da turma
- page-kt (linhas ~494+): painel de predição por estudante
- page-rec (recomendação por dificuldade)

D-95c nomeia os 3 painéis:
1. Respostas de código da turma
2. Predição de conhecimento por estudante
3. Dificuldade da turma por KC
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inserir a `<section>` TOOL-03 ENTRE MARKER-03 e MARKER-04, usando `.bridge-seq` adaptado como tentativa primária (D-95d)</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html linhas 670-735 (final do MARKER-03 + área onde MARKER-04 e END-01 foram inseridos pelos plans 01 e 03; identificar exatamente o `</section>` do MARKER-03 e o `<!-- ============ SLIDE · MARKER · As quatro fases da EDM, fase 4 planejada (Zoric, 2020) ============ -->` do MARKER-04)
    - apresentacao/index.html linhas 540-555 (MODEL-05 pipeline `.bridge-seq` com 5 etapas como referência de markup e inline-style)
    - apresentacao/assets/theme-unifacens.css linhas 197-206 (`.bridge-seq` base + `.step` + `.arr`)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Code Example 6 (markup literal do TOOL-03 com inline-styles)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md §D-95a/b/c/d/e/f
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Open Question #5 (start com `.bridge-seq`, fallback `.dash-card` se necessário)
    - docs/tcc2_prototipo.html (ler conceitualmente page-eda linhas ~416 e page-kt linhas ~494 para validar conteúdo dos 3 painéis; NÃO copiar layout)
  </read_first>
  <action>
    Inserir a `<section>` TOOL-03 IMEDIATAMENTE após o `</section>` que fecha o MARKER-03 (atual linha ~730 do index.html antes dos plans) e ANTES do `<!-- ====== SLIDE · MARKER · ... fase 4 planejada ====== -->` que abre o MARKER-04 (criado no plan 01).

    Estrutura do markup (RESEARCH §Code Example 6 — Opção primária `.bridge-seq` adaptado):
    - Comentário HTML: `<!-- ============ SLIDE · TOOL-03 · Dashboard wireframe 3 paineis ============ -->`
    - `<section data-background-color="#F1F6FB">`
    - `<div class="deck-slide slide-related slide-bridge">`
    - Marca d'água `<svg class="wm">` per D-95 (slides de conteúdo têm marca d'água per padrão STYLE.md herdado)
    - `<p class="deck-topic"><span class="ps1">&gt;</span>dashboard da aplicação<span class="caret blink"></span></p>` (cabeçalho per D-95a; alternativa `> o dashboard` defere ao checkpoint)
    - Título Figura ABNT: `<p class="eda-title" style="margin-top: 26px; color: #000;">Figura &ndash; Painéis principais do dashboard</p>`
    - 3 `<span class="step">` dentro de `<p class="bridge-seq" style="margin-top: 14px; gap: 16px;">`, cada um com inline-style: `flex-direction: column; color: #000; border-color: #000; font-weight: 400; min-height: 280px; padding: 24px 18px; font-size: 18px;`. Conteúdo de cada step: `<b>Título do painel<br>em duas linhas</b>` + (após `margin-top: 22px`) `<span style="font-size: 14px; color: #5b6472;">[ilustração esquemática]</span>`
    - SEM `<span class="arr">` entre os steps (painéis paralelos, não sequenciais — diferença crítica vs MODEL-05)
    - Conteúdo dos 3 painéis na ordem D-95c: "Respostas de código<br>da turma", "Predição de conhecimento<br>por estudante", "Dificuldade da turma<br>por KC"
    - Legenda Fonte da Figura: `<p class="eda-source">Fonte: elaborado pelo autor; baseado em <i>docs/tcc2_prototipo.html</i>.</p>`
    - Frase de fechamento (foco no professor per D-95e): `<p class="rel-lead" style="margin-top: 18px;">O dashboard auxilia o professor a direcionar intervenções por estudante e por dificuldade.</p>`

    Vocabulário per gates:
    - "aplicação" no cabeçalho per D-92 (não "ferramenta")
    - "estudante" na frase "por estudante" per D-102 (não "aluno")
    - Sem em-dash per D-100; usar vírgulas
    - `<i>docs/tcc2_prototipo.html</i>` em itálico per D-101 (caminho de arquivo como termo técnico)

    NÃO criar `.dash-card-row` / `.dash-card` no CSS ainda — aguardar checkpoint para decidir. Se reviewer rejeitar `.bridge-seq` adaptado, Task 2 cria o fallback.
  </action>
  <verify>
    <automated>grep -c "deck-slide slide-related slide-bridge" apresentacao/index.html | awk '$1 >= 2 {exit 0} {exit 1}' && grep -c "> dashboard da aplicação\|> o dashboard" apresentacao/index.html | awk '$1 >= 1 {exit 0} {exit 1}' && grep -c "Painéis principais do dashboard" apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "Respostas de código" apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "Predição de conhecimento" apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "Dificuldade da turma" apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "<section " apresentacao/index.html | awk '$1 == 30 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<section " apresentacao/index.html` retorna 30 (era 29 após plan 03; +1 TOOL-03)
    - `grep -n "<section " apresentacao/index.html` confirma ordem: MARKER-03 < TOOL-03 < MARKER-04 < END-01
    - `grep -c "dashboard da aplicação\|> o dashboard" apresentacao/index.html` retorna >= 1
    - `grep -c "Respostas de código" apresentacao/index.html` retorna 1
    - `grep -c "Predição de conhecimento" apresentacao/index.html` retorna 1
    - `grep -c "Dificuldade da turma" apresentacao/index.html` retorna 1
    - `grep -c "estudante" apresentacao/index.html` aumenta (nova ocorrência em "por estudante")
    - `grep -c "ferramenta" apresentacao/index.html | awk '{print $1}'` permanece igual (TOOL-03 usa "aplicação", não "ferramenta" — gate D-92)
    - Sem em-dash no markup novo: `grep -c "—" apresentacao/index.html` permanece igual
    - Browser http://127.0.0.1:8000/#/27: 3 painéis lado a lado com bordas pretas finas, cantos retos, fundo branco, sem azul UniFacens, sem setas entre eles; cabeçalho `> dashboard da aplicação` com caret blink; frase de fechamento sobre o professor
  </acceptance_criteria>
  <done>
    TOOL-03 inserido entre MARKER-03 e MARKER-04 com layout `.bridge-seq` adaptado (3 .step com min-height 280px sem .arr). Deck passa de 29 → 30 sections.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual TOOL-03 (RESEARCH Open Question #5: `.bridge-seq` adaptado vs fallback `.dash-card`; Open Question #1: cabeçalho exato)</name>
  <what-built>
    Section TOOL-03 com 3 painéis paralelos via `.bridge-seq` adaptado (`min-height: 280px`, sem `.arr`, `gap: 16px` inline).
  </what-built>
  <how-to-verify>
    1. Subir servidor: `cd apresentacao && python3 -m http.server 8000`
    2. Abrir http://127.0.0.1:8000/#/27
    3. Validar visualmente:
       a. 3 painéis lado a lado com largura uniforme (`flex: 1 1 0` da `.step` distribui igual)
       b. Altura dos 3 painéis IDÊNTICA (`min-height: 280px`)
       c. Borda 1.5px sólida preta `#000` (estilo Word/ABNT), cantos retos `border-radius: 0`, fundo branco
       d. Sem `→` (setas) entre os painéis — painéis paralelos
       e. Cabeçalho `> dashboard da aplicação` com caret piscando em Cascadia
       f. Título da Figura: `Figura – Painéis principais do dashboard` em preto
       g. Legenda: `Fonte: elaborado pelo autor; baseado em docs/tcc2_prototipo.html.`
       h. Frase de fechamento: `O dashboard auxilia o professor a direcionar intervenções por estudante e por dificuldade.`
       i. Sem em-dash no slide; sem azul UniFacens em nenhum painel; vocabulário "aplicação" no cabeçalho, "estudante" na frase
    4. Validar deck completo (#/0 → #/29):
       - Navegação sem erro F12
       - MARKER-04 ainda em #/28 (foi para #/29 com a inserção de TOOL-03 — confirmar via slide bar)
       - END-01 ainda em #/29 — confirmar
    5. Open Questions para decidir:
       - **#1 cabeçalho**: `> dashboard da aplicação` (default atual) vs `> o dashboard` (mais leve)
       - **#5 componente**: `.bridge-seq` adaptado funciona? Se NÃO (painéis com altura desigual, larguras estranhas, overflow), pedir fallback `.dash-card-row` (RESEARCH §Code Example 6 linhas 545-577 — grid 3 colunas)
       - **Ilustração interna** dos 3 painéis: manter `[ilustração esquemática]` placeholder default; ou pedir barras stub / scatter stub / outra ilustração mínima (defere ao reviewer)
       - **Frase de fechamento**: manter ou ajustar texto
  </how-to-verify>
  <resume-signal>
    Digite "approved" para manter como está; "trocar-cabecalho:o-dashboard" para trocar cabeçalho; "fallback-dash-card" para reviewer pedir o fallback CSS (executor então cria `.dash-card-row` + `.dash-card` em theme-unifacens.css e refatora o markup); "ilustracao:[descrição]" para ajustar ilustração interna; ou descreva mudanças e relance checkpoint.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: (Condicional) Anexar fallback `.dash-card-row` + `.dash-card` ao CSS e refatorar markup, SE checkpoint pedir "fallback-dash-card"</name>
  <files>apresentacao/assets/theme-unifacens.css, apresentacao/index.html</files>
  <read_first>
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Code Example 6 linhas 545-577 (CSS + markup do fallback)
    - apresentacao/assets/theme-unifacens.css (fim do arquivo após bloco .slide-end criado no plan 03; ou após o bloco .slide-marker para coesão)
  </read_first>
  <action>
    EXECUTE APENAS SE a Task 2 resolveu com "fallback-dash-card". Caso contrário, marcar esta task como skipped.

    1. Anexar ao `apresentacao/assets/theme-unifacens.css` (após o bloco `.slide-end` do plan 03) o comentário de seção `/* === SLIDE · TOOL-03 · Dashboard wireframe (3 paineis lado a lado, ABNT) === */` seguido das regras CSS:
       - `.dash-card-row`: `display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 18px; margin-top: 22px; max-width: 1180px;`
       - `.dash-card`: `background: #fff; border: 1.5px solid #1f1f1f; border-radius: 0; padding: 24px 20px; font-family: Arial, "Helvetica Neue", sans-serif; display: flex; flex-direction: column; align-items: center; text-align: center; min-height: 280px;`
       - `.dash-card .dash-card-title`: `font-size: 19px; font-weight: 700; color: #000; line-height: 1.3;`
       - `.dash-card .dash-card-illu`: `margin-top: 24px; font-size: 14px; color: #5b6472;`

    2. Refatorar o markup da `<section>` TOOL-03 em index.html:
       - REMOVER o `<p class="bridge-seq" ...>` com 3 `.step` inline-style
       - SUBSTITUIR por `<div class="dash-card-row">` com 3 `<div class="dash-card">` cada um contendo `<span class="dash-card-title">Título<br>em duas linhas</span>` + `<span class="dash-card-illu">[ilustração esquemática]</span>`
       - REMOVER classe `slide-bridge` do wrapper (não usa mais `.bridge-seq`); manter `slide-related`

    3. Validar visualmente no browser que 3 painéis têm largura idêntica (grid 1fr × 3) e altura uniforme (`min-height: 280px`).

    Sem em-dash. Vocabulário "aplicação", "estudantes".
  </action>
  <verify>
    <automated>if grep -q "dash-card-row" apresentacao/index.html; then grep -c "\.dash-card-row[[:space:]]*{" apresentacao/assets/theme-unifacens.css | awk '$1 == 1 {exit 0} {exit 1}'; else echo "skipped"; exit 0; fi</automated>
  </verify>
  <acceptance_criteria>
    - Se Task 2 resolveu "approved": esta task é skipped; `grep -c "dash-card-row" apresentacao/` retorna 0
    - Se Task 2 resolveu "fallback-dash-card": `grep -c "\.dash-card-row\b" apresentacao/assets/theme-unifacens.css` retorna >= 1; `grep -c "dash-card-row" apresentacao/index.html` retorna 1; markup do TOOL-03 não contém mais `.bridge-seq`; 3 painéis renderizam com largura idêntica
    - Em ambos os casos: `grep -c "<section " apresentacao/index.html` permanece 30 (Task 3 não muda contagem)
    - Sem em-dash novo introduzido
  </acceptance_criteria>
  <done>
    OU: Task 2 aprovou .bridge-seq adaptado → esta task é no-op documentado em SUMMARY.
    OU: Task 2 pediu fallback → CSS .dash-card-row+.dash-card anexado e markup TOOL-03 refatorado para grid 3 colunas.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Apresentação HTML/CSS estática sem auth/network/data. Sem trust boundary aplicável. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| n/a | n/a | n/a | n/a | Sem superfície de ataque. |
</threat_model>

<verification>
- TOOL-03 renderiza com 3 painéis paralelos de altura uniforme em estilo Word/ABNT
- Vocabulário "aplicação" no cabeçalho, "estudantes" na frase
- Deck navega #/0 → #/29 sem erro de console
- Reviewer aprova layout no checkpoint (default `.bridge-seq` ou fallback `.dash-card`)
- TOOL-03 não menciona ProgSnap2 (gate herdado PROJECT.md Key Decision linha 183)
</verification>

<success_criteria>
- ROADMAP Phase 5 Success Criterion 2 atendido: "Slide TOOL-03 mostra o dashboard com respostas de código da turma, predição por estudante e dificuldade por KC"
- REQ TOOL-03 coberto
- 3 painéis legíveis em 1280×720; sem overflow horizontal
</success_criteria>

<output>
After completion, create `.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-04-SUMMARY.md` registrando:
- Markup final do TOOL-03 (.bridge-seq adaptado ou .dash-card-row)
- Cabeçalho escolhido no checkpoint
- Conteúdo final da ilustração interna dos 3 painéis (placeholder vs stub específico)
- CSS adicionado se houve fallback (linhas exatas)
- Decisões ad-hoc registradas
- Posição final do slide (esperado #/28 após plan 05 inserir TOOL-01 em #/27)
- Commit hash final
</output>
