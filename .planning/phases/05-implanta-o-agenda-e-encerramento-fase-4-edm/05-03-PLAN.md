---
phase: 05-implanta-o-agenda-e-encerramento-fase-4-edm
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - apresentacao/index.html
  - apresentacao/assets/theme-unifacens.css
autonomous: false
requirements: [END-01]
must_haves:
  truths:
    - "Slide #/30 END-01 renderiza com a palavra `Obrigado.` centralizada vertical+horizontalmente em fundo `#F1F6FB`"
    - "END-01 é o último slide do deck (último `<section>` antes do `</div>` que fecha `<div class=\"slides\">`)"
    - "Slide não tem cabeçalho `.deck-topic` per D-97b (END não é seção; é encerramento)"
    - "Slide não tem rodapé `Fonte:` per D-97c (Agenda e END são estruturais, não derivados)"
    - "Default sem marca d'água `<svg class=\"wm\">` per D-97d, criando bracket narrativo opcional com #/0 slide-cover-brand"
  artifacts:
    - path: "apresentacao/index.html"
      provides: "Section END-01 ao fim do deck com classes `.slide-end`, `.end-thanks` e opcionalmente `.end-credits`"
      contains: "slide-end"
    - path: "apresentacao/assets/theme-unifacens.css"
      provides: "Classes novas: `.slide-end`, `.end-thanks`, opcionalmente `.end-credits`"
      contains: ".slide-end"
  key_links:
    - from: "apresentacao/index.html section END-01"
      to: "apresentacao/assets/theme-unifacens.css `.slide-end`"
      via: "classe `.slide-end` aplicada ao `<div class=\"deck-slide\">`"
      pattern: "deck-slide slide-end"
    - from: "deck navigation"
      to: "#/30 último slide"
      via: "ordem D-92.1: TOOL-01 (#/27) → TOOL-03 (#/28) → MARKER-04 (#/29) → END-01 (#/30)"
      pattern: "END-01 após MARKER-04"
---

<objective>
Adicionar o slide END-01 (último slide do deck, #/30 após inserções da fase 5) com a palavra "Obrigado." centralizada vertical+horizontalmente, layout minimal e (opcionalmente) créditos discretos abaixo. Slide encerra a apresentação criando um bracket narrativo com a capa (#/0 slide-cover-brand) que também é minimal.

Purpose: ordem 3 per RESEARCH §Plan-Level Execution Order — END-01 é baixíssimo risco (slide minimal, CSS isolado), entrega o slide final cedo para validar bracket narrativo de fim a fim (#/0 → #/30) antes dos slides mais densos (TOOL-03 e TOOL-01) serem implementados. END-01 também serve de gabarito para o ritmo final da defesa.

Output:
- 1 nova `<section>` em `apresentacao/index.html` (deck passa de 28 → 29 sections após este plan)
- 3 classes novas em `apresentacao/assets/theme-unifacens.css`: `.slide-end`, `.end-thanks`, opcionalmente `.end-credits` (RESEARCH §Code Example 3)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md
@.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-02-SUMMARY.md
@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css

<interfaces>
<!-- Slides minimais já existentes para referência: slide-cover-brand (#/0 linhas 26-34) e slide-title-tcc (#/1 linhas 37-57). Ambos sem .deck-topic e sem rodapé Fonte:. -->
<!-- END-01 é a 3ª categoria de slide minimal: nem capa nem título; é encerramento. -->
<!-- CSS proposto (RESEARCH §Code Example 3) usa flex column centralizado vertical+horizontal, sem padding extra -->

Padrão de section END-01 alvo (RESEARCH §Code Example 3 linhas 373-381):
<section data-background-color="#F1F6FB">
  <div class="deck-slide slide-end">
    <p class="end-thanks">Obrigado.</p>
    <p class="end-credits">Léo Kuntz · TCC 1 · UniFacens · 2026</p>  <!-- opcional, decidir no checkpoint -->
  </div>
</section>

CSS proposto (RESEARCH §Code Example 3 linhas 385-405):
.slide-end {
  display: flex; flex-direction: column;
  align-items: center; justify-content: center;
  text-align: center;
  background: var(--uni-light);
  padding: 0;
}
.end-thanks {
  font-family: Arial, "Helvetica Neue", sans-serif;
  font-size: 96px; font-weight: 700; color: var(--uni-ink);
  margin: 0;
}
.end-credits {
  margin-top: 28px;
  font-family: Arial, "Helvetica Neue", sans-serif;
  font-size: 18px; color: #5b6472;
}

Discretion no checkpoint (RESEARCH Open Question #3):
- Variante "minimal puro": só "Obrigado." (sem .end-credits)
- Variante "com créditos": "Obrigado." + linha discreta com nome/TCC/ano
- Tamanho da fonte: 96px (proposta), 120px (mais impactante), 72px (mais sóbrio)
- Cor: --uni-ink (preto, default) vs --uni-blue (acento sutil)
- Sem marca d'água per D-97d default; reviewer pode pedir marca d'água para coesão (assumption A8 risco)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar classes CSS `.slide-end` + `.end-thanks` + `.end-credits` ao theme-unifacens.css</name>
  <files>apresentacao/assets/theme-unifacens.css</files>
  <read_first>
    - apresentacao/assets/theme-unifacens.css linhas 355-448 (bloco SLIDE · Marker recém-modificado pelo plan 01, para identificar fim do arquivo e onde anexar o novo bloco)
    - apresentacao/assets/theme-unifacens.css (linhas finais, para anexar após o último bloco de slide existente)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Code Example 3 (CSS literal)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md §D-97a/b/c/d/e
  </read_first>
  <action>
    Anexar ao FIM do `apresentacao/assets/theme-unifacens.css` (ou imediatamente após o bloco `.slide-marker` linha 448) um comentário de seção `/* === SLIDE · Encerramento · "Obrigado." minimal (END-01) === */` seguido pelas 3 regras CSS conforme RESEARCH §Code Example 3 (linhas 385-405):
    - `.slide-end`: flex column centralizado (`display: flex; flex-direction: column; align-items: center; justify-content: center;`), `text-align: center`, `background: var(--uni-light)`, `padding: 0`
    - `.end-thanks`: Arial 96px bold, cor `var(--uni-ink)`, `margin: 0`
    - `.end-credits`: `margin-top: 28px`, Arial 18px, cor `#5b6472`

    Não importar CSS externo (gate STYLE.md). Não usar em-dash em comentários CSS per D-100. As 3 classes são aditivas; não afetam slides existentes.
  </action>
  <verify>
    <automated>grep -c "\.slide-end[[:space:]]*{" apresentacao/assets/theme-unifacens.css | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "\.end-thanks[[:space:]]*{" apresentacao/assets/theme-unifacens.css | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "\.end-credits[[:space:]]*{" apresentacao/assets/theme-unifacens.css | awk '$1 == 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "\.slide-end[[:space:]]*{" apresentacao/assets/theme-unifacens.css` retorna 1
    - `grep -c "\.end-thanks[[:space:]]*{" apresentacao/assets/theme-unifacens.css` retorna 1
    - `grep -c "\.end-credits[[:space:]]*{" apresentacao/assets/theme-unifacens.css` retorna 1
    - Slides existentes #/0..#/28 (após plans 01 + 02) renderizam inalterados; classes novas não colidem com seletores existentes
    - Sem em-dash no comentário CSS novo
  </acceptance_criteria>
  <done>
    3 classes `.slide-end`, `.end-thanks`, `.end-credits` definidas em theme-unifacens.css com comentário de seção; layout flex column centralizado pronto para o markup da Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inserir a `<section>` END-01 como ÚLTIMO slide do deck</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html linhas finais (`</section>` que fecha MARKER-04 inserido pelo plan 01, antes do `</div>` que fecha `<div class="slides">`)
    - apresentacao/index.html linhas 26-34 (slide-cover-brand para referência de slide minimal)
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-CONTEXT.md §Specifics "END-01 markup-alvo" linhas 320-330
    - .planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-RESEARCH.md §Code Example 3 (markup literal)
  </read_first>
  <action>
    Inserir IMEDIATAMENTE após o `</section>` da MARKER-04 (criado no plan 01) e antes do `</div>` que fecha `<div class="slides">` o markup do END-01:
    - Comentário HTML: `<!-- ============ SLIDE · Obrigado ============ -->`
    - `<section data-background-color="#F1F6FB">`
    - `<div class="deck-slide slide-end">`
    - `<p class="end-thanks">Obrigado.</p>`
    - `<p class="end-credits">Léo Kuntz · TCC 1 · UniFacens · 2026</p>` (proposta inicial; defere ao checkpoint Open Question #3 do RESEARCH se vai cortar para "minimal puro" ou ajustar conteúdo dos créditos)
    - Fechar `</div></section>`

    NÃO adicionar `<svg class="wm">` per D-97d default. NÃO adicionar `.deck-topic` per D-97b. NÃO adicionar `<p class="rel-cite">Fonte:</p>` per D-97c. END-01 fica deliberadamente fora do padrão de slide de conteúdo (categoria "slide minimal" junto com slide-cover-brand e slide-title-tcc).

    Usar separador "·" (middle dot U+00B7) nos créditos, NÃO "—" (em-dash); per D-100. Nome do autor pode ser ajustado no checkpoint.
  </action>
  <verify>
    <automated>grep -c "deck-slide slide-end" apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "end-thanks" apresentacao/index.html | awk '$1 == 1 {exit 0} {exit 1}' && grep -c "<section " apresentacao/index.html | awk '$1 == 29 {exit 0} {exit 1}' && grep -c "Obrigado\." apresentacao/index.html | awk '$1 >= 1 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<section " apresentacao/index.html` retorna 29 (era 28 após plan 02; +1 END-01)
    - `grep -c "deck-slide slide-end" apresentacao/index.html` retorna 1
    - `grep -c "Obrigado\." apresentacao/index.html` retorna >= 1 (palavra no slide)
    - END-01 é o ÚLTIMO `<section>` no DOM: `grep -n "<section " apresentacao/index.html | tail -1` aponta para a linha do END-01 e seu `</section>` está antes do `</div>` que fecha `<div class="slides">`
    - Sem em-dash (`—`) no markup novo: comparar `grep -c "—" apresentacao/index.html` antes/depois
    - Browser http://127.0.0.1:8000/#/28 abre END-01 (deck tem 29 sections, índice 0-based vai de 0 a 28; com inserções posteriores de TOOL-01 e TOOL-03 nos plans 04 e 05, a posição final será #/30)
    - F12 console clean
  </acceptance_criteria>
  <done>
    Section END-01 inserida como último slide do deck com classe `.slide-end`, palavra "Obrigado." em `.end-thanks` e créditos opcionais em `.end-credits`. Deck passa de 28 → 29 sections.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual END-01 (RESEARCH Open Question #3: minimal puro vs com créditos)</name>
  <what-built>
    1. 3 classes CSS `.slide-end`, `.end-thanks`, `.end-credits` em theme-unifacens.css (layout flex column centralizado)
    2. Section END-01 como último slide do deck com "Obrigado." + créditos opcionais
  </what-built>
  <how-to-verify>
    1. Subir servidor local: `cd apresentacao && python3 -m http.server 8000`
    2. Abrir o último slide no browser. Após este plan o deck tem 29 sections, então a URL é http://127.0.0.1:8000/#/28
    3. Validar visualmente:
       a. Palavra "Obrigado." centralizada vertical+horizontalmente no slide
       b. Fonte Arial 96px bold preto (default)
       c. Linha de créditos discreta abaixo (Arial 18px cor `#5b6472`), se presente
       d. Fundo `#F1F6FB` (consistente com slides de conteúdo)
       e. Sem cabeçalho `> [seção]` (per D-97b)
       f. Sem rodapé "Fonte:" (per D-97c)
       g. Sem marca d'água Facens (per D-97d default)
    4. Comparar com slide-cover-brand (#/0) para validar o bracket narrativo (capa → encerramento, ambos minimais)
    5. Navegar #/0 → #/1 → #/2 (AGENDA refatorada) → #/26 (MARKER-03) → #/27 (MARKER-04 do plan 01) → #/28 (END-01): toda a navegação sem erro de console
    6. Decidir entre variantes (Open Question #3):
       - **Minimal puro**: remover `.end-credits` inteiro; ficar só "Obrigado."
       - **Com créditos**: manter, ajustar conteúdo (nome formal, email institucional, GitHub, ano)
       - **Tamanho de fonte**: manter 96px ou trocar para 120px (mais impactante) ou 72px (mais sóbrio)
       - **Cor**: manter `--uni-ink` preto ou trocar para `--uni-blue` (acento sutil)
       - **Marca d'água**: manter ausente (default) ou adicionar `<svg class="wm">` para coesão com slides de conteúdo (assumption A8)
  </how-to-verify>
  <resume-signal>Digite "approved" para manter como está; "minimal-puro" para remover créditos; "ajustar: [descrição]" com mudanças específicas (fonte, cor, créditos, marca d'água) que o executor aplica e relança este checkpoint.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| n/a | Apresentação HTML/CSS estática sem auth/network/data. Sem trust boundary aplicável. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| n/a | n/a | n/a | n/a | Sem superfície de ataque. |
</threat_model>

<verification>
- END-01 renderiza com "Obrigado." centralizado em fundo `#F1F6FB`
- Deck navega #/0 → último slide sem erro de console
- Slides existentes #/0..#/27 inalterados visualmente
- Reviewer aprova variante minimal vs com créditos no checkpoint
</verification>

<success_criteria>
- ROADMAP Phase 5 Success Criterion 4 atendido: "Slide END-01 de agradecimento encerra a apresentação"
- Layout flex column centralizado sem regressão de slides existentes
- Bracket narrativo opcional com slide-cover-brand (#/0) validado por reviewer
- REQ END-01 coberto
</success_criteria>

<output>
After completion, create `.planning/phases/05-implanta-o-agenda-e-encerramento-fase-4-edm/05-03-SUMMARY.md` registrando:
- Markup final do END-01 (variante escolhida no checkpoint)
- CSS final das 3 classes
- Decisões de checkpoint (créditos, tamanho de fonte, cor, marca d'água)
- Posição final do slide (esperado #/30 após plans 04 + 05 inserirem TOOL-01 e TOOL-03 entre MARKER-03 e MARKER-04)
- Commit hash final
</output>
