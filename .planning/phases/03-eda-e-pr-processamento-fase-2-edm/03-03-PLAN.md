---
phase: 03-eda-e-pr-processamento-fase-2-edm
plan: 03
type: execute
wave: 3
depends_on:
  - "02"
files_modified:
  - apresentacao/index.html
  - apresentacao/assets/theme-unifacens.css
autonomous: false
requirements:
  - EDA-01
must_haves:
  truths:
    - "Slide EDA-01 carrega no browser sem erro de console na posição #/11 final"
    - "Cabeçalho '> como navegamos o csedm' aparece com caret piscando"
    - "Parágrafo abertura cita 'Shi <i>et al.</i> (2022)' parentético e introduz o achado quantitativo (26% → 20% → 30%)"
    - "Tabela com 5 linhas A1..A5 mostra colunas Assignment / Alunos / Problemas / Taxa de acerto"
    - "Números MainTable Spring 2019 (386/340/361/315/306; 26,15%/20,06%/20,34%/24,72%/30,62%) corretos"
    - "Última coluna 'Taxa de acerto' em azul UniFacens, negrito"
    - "Percentuais formatados pt-BR com vírgula decimal"
    - "Rodapé 'Fonte: análise sobre CSEDM (Spring 2019).'"
    - "ProgSnap2 NÃO aparece no corpo do slide (Pitfall 2; nominalmente único em INTRO-01)"
    - "Code-DKT NÃO aparece (Pitfall 3; gate forte fase 4)"
    - "D-60: EDA-01 abre o bloco da Fase 2 EDM, primeiro slide a entrar após MARKER-01 na ordem narrativa (EDA-01 → EDA-02 → EDA-03 → MARKER-02)"
    - "D-62: EDA-01 cumpre o papel narrativo 'agora navegamos o dataset que descrevemos no INTRO-01', encadeando MARKER-01 → EDA-01 → EDA-02"
  artifacts:
    - path: apresentacao/index.html
      provides: "Section EDA-01 com .rel-lead + table.eda-grid + rodapé"
      contains: "> como navegamos o csedm"
    - path: apresentacao/assets/theme-unifacens.css
      provides: "Componente .eda-grid (~14 linhas; tabela ABNT compacta)"
      contains: ".eda-grid"
  key_links:
    - from: "apresentacao/index.html EDA-01 <table class='eda-grid'>"
      to: "apresentacao/assets/theme-unifacens.css .eda-grid"
      via: "classes .eda-grid + th/td"
      pattern: "class=.eda-grid."
---

<objective>
Inserir slide EDA-01 ("como navegamos o csedm") em apresentacao/index.html, posicionado no gap após MARKER-01 (slot canônico §Gaps reservados linha 130 do STYLE.md) e adicionar ~14 linhas de CSS novo em apresentacao/assets/theme-unifacens.css para o componente reutilizável `.eda-grid` (tabela ABNT compacta). O slide combina parágrafo de abertura ("hub narrativo": encontramos a base via Shi; heterogeneidade dos 5 assignments) com tabela A1..A5 (n alunos, n problemas, taxa de acerto) usando números MainTable Spring 2019 — coerentes com a ponte 413 → 410 → 328/82 do EDA-02 e com INTRO-01.

Purpose: abrir a Fase 2 EDM mostrando granularidade interna do dataset que INTRO-01 só tratou globalmente. Permite à banca comparar dificuldades por assignment (heterogeneidade) sem repetir totais globais.

Output: 1 section novo (~16 linhas HTML) + 1 bloco CSS reusável (~14 linhas). Depende do plan 02 (EDA-02) para garantir coerência narrativa 413 → 410 → 328/82 — o plan 02 deve estar inserido no deck antes que EDA-01 mencione "Encontramos a base via Shi". 1 commit funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-CONTEXT.md
@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md
@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-PATTERNS.md

@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css

<interfaces>
<!-- Template-base + componente novo. Análogo INTRO-01 (linhas 149-164) + estética ABNT borda fina .bridge-seq .step (linhas 201-206 do CSS). -->

Classes existentes consumidas:
- `.slide-related` (host)
- `.deck-topic` + `.ps1` + `.caret.blink`
- `.rel-lead` (parágrafo de abertura)
- `.rel-cite` (rodapé)
- `<svg class="wm">` marca d'água

Classe NOVA criada neste plan (~14 linhas em theme-unifacens.css):
- `.eda-grid` (tabela ABNT compacta; border-collapse, borda 1.5px preta, 5 linhas × 4 colunas)
- `.eda-grid th, .eda-grid td` (padding, alinhamento)
- `.eda-grid tr td:first-child` (primeira coluna negrito + esquerda)
- `.eda-grid tr td:last-child` (última coluna em --uni-blue + negrito)

Números MainTable Spring 2019 travados em 03-RESEARCH §2.1 (cross-validados com cell 51 do notebook 01):

| Assignment | Alunos | Problemas | Taxa de acerto |
|---|---|---|---|
| A1 (439) | 386 | 10 | 26,15% |
| A2 (487) | 340 | 10 | 20,06% |
| A3 (492) | 361 | 10 | 20,34% |
| A4 (494) | 315 | 10 | 24,72% |
| A5 (502) | 306 | 10 | 30,62% |

Phrasing-alvo (03-RESEARCH §2.4 + §7.1):
- Cabeçalho recomendado: `> como navegamos o csedm` (D-63a; fallbacks: `o curso por dentro` / `os 5 assignments`)
- Parágrafo: "Encontramos a base via Shi <i>et al.</i> (2022). Ao navegar o CSEDM, os 5 <i>assignments</i> cobrem dificuldades muito diferentes: a taxa de acerto cai de 26% no primeiro para 20% no terceiro, e sobe para 30% no último."
- Rodapé: "Fonte: análise sobre CSEDM (Spring 2019)." (sem citação Shi no rodapé — a contagem é nossa)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anexar bloco CSS .eda-grid em theme-unifacens.css</name>
  <read_first>
    - apresentacao/assets/theme-unifacens.css linhas 197-210 (.slide-bridge .bridge-seq .step — análogo de estética ABNT borda fina)
    - apresentacao/assets/theme-unifacens.css linhas 290-300 (.kc-box — confirmar uso de border 1.5px solid #1f1f1f sem border-radius)
    - apresentacao/assets/theme-unifacens.css últimas ~20 linhas (final do arquivo — onde anexar; preservar EOL Unix)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md §4.2 (CSS literal sugerido para .eda-grid)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-PATTERNS.md §".eda-grid (utility, ~14 linhas)" (gates obrigatórios)
  </read_first>
  <action>
    Anexar bloco CSS ao final de apresentacao/assets/theme-unifacens.css (após a última regra existente; preservar quebra de linha final Unix). O bloco precede o comentário "===" no padrão dos demais componentes (linhas 81-83, 96-99, 158-161, 194-196, 359-367 etc.).

    Conteúdo literal a anexar (das ~14 linhas conforme 03-RESEARCH §4.2 / 03-PATTERNS §.eda-grid):

    Comentário-cabeçalho de bloco no padrão existente: "SLIDE · EDA · tabela compacta A1..A5 (template B do RESEARCH §4.2). Estética ABNT (borda fina preta 1.5px, sem radius), última coluna em azul UniFacens. Reusável por slides EDA-* que precisem de tabela ABNT."

    Regra `.eda-grid`: width 92%, margin "32px auto 0", border-collapse collapse, font-family `Arial, "Helvetica Neue", sans-serif`, font-size 21px.

    Regra `.eda-grid th, .eda-grid td`: border `1.5px solid #1f1f1f`, padding "12px 18px", text-align center.

    Regra `.eda-grid th`: background `#fff`, font-weight 700, color `var(--uni-ink)`.

    Regra `.eda-grid td`: background `#fff`, color `var(--uni-ink)`.

    Regra `.eda-grid tr td:first-child`: text-align left, font-weight 700.

    Regra `.eda-grid tr td:last-child`: color `var(--uni-blue)`, font-weight 700.

    Gates (per 03-PATTERNS §.eda-grid):
    - Usar apenas variáveis CSS existentes (`var(--uni-blue)`, `var(--uni-ink)`); NÃO introduzir variáveis novas
    - `border-radius: 0` implícito (NÃO declarar radius positivo; mantém coerência com `.bridge-seq` / `.kc-box` / `.marker-pill`)
    - Sem em-dash no comentário do bloco (D-70)
  </action>
  <verify>
    <automated>grep -c '\.eda-grid' apresentacao/assets/theme-unifacens.css | grep -qE '^[5-7]$' &amp;&amp; grep -c 'border: 1.5px solid #1f1f1f' apresentacao/assets/theme-unifacens.css | grep -qE '^[3-9]$' &amp;&amp; [ "$(grep -c 'var(--uni-blue)' apresentacao/assets/theme-unifacens.css)" = "30" ] &amp;&amp; ! tail -25 apresentacao/assets/theme-unifacens.css | grep -q '—'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.eda-grid' apresentacao/assets/theme-unifacens.css` retorna entre 5 e 7 (5 seletores principais + variantes)
    - `grep -c 'var(--uni-blue)' apresentacao/assets/theme-unifacens.css` retorna exatamente 30 (29 baseline pré-task + 1 introduzida em `.eda-grid tr td:last-child`)
    - `grep -c 'var(--uni-ink)' apresentacao/assets/theme-unifacens.css` retorna entre 31 e 32 (29 baseline pré-task + 2 a 3 em `.eda-grid th` e `.eda-grid td`)
    - Comentário de bloco no formato `===...===` presente acima do bloco `.eda-grid`
    - `tail -25 apresentacao/assets/theme-unifacens.css | grep -c '—'` retorna 0 (sem em-dash)
    - Arquivo termina com newline Unix
  </acceptance_criteria>
  <done>
    Bloco CSS `.eda-grid` anexado ao fim do theme-unifacens.css com ~14 linhas no padrão dos demais componentes. Sem em-dash, sem variáveis novas, sem border-radius positivo. Pronto para a Task 2 consumir.
  </done>
</task>

<task type="auto">
  <name>Task 2: Inserir section EDA-01 com .rel-lead + table.eda-grid</name>
  <read_first>
    - apresentacao/index.html linhas 149-164 (INTRO-01 — template a clonar; especialmente estrutura externa, cabeçalho, .rel-lead e .rel-cite)
    - apresentacao/index.html linhas 199-244 (boundary final pós-plans anteriores; ponto de inserção: após MARKER-01 e após/antes do EDA-02 dependendo da ordem real de execução)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md §2.1 (números MainTable Spring 2019 — tabela canônica) + §2.4 (phrasing-alvo polido) + §7.1 (3 variantes de cabeçalho)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-PATTERNS.md §"EDA-01 (controller, layout-sensitive)" (markup-alvo literal + gates)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-CONTEXT.md §D-64 (combo) + §D-64a (números) + §D-64c (NÃO repetir INTRO-01)
  </read_first>
  <action>
    Inserir novo `<section>` em apresentacao/index.html no slot do gap (após MARKER-01 linha 243; ordem final no deck: EDA-01 → EDA-02 → EDA-03 → MARKER-02 conforme D-60). Indentação 6 espaços, EOL Unix.

    Estrutura externa clonada de INTRO-01 (linhas 149-164):

    1. Comentário acima do `<section>`: `<!-- ============ SLIDE · EDA-01 · Como navegamos o CSEDM (Spring 2019) ============ -->`
    2. `<section data-background-color="#F1F6FB">` + `<div class="deck-slide slide-related">` + marca d'água `<svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>`.
    3. Cabeçalho (D-63a, recomendado 03-RESEARCH §7.1 variante (a)): `<p class="deck-topic"><span class="ps1">&gt;</span>como navegamos o csedm<span class="caret blink"></span></p>`. Fallbacks aceitos: `o curso por dentro` ou `os 5 assignments`.
    4. Parágrafo abertura (1 × `.rel-lead`, phrasing 03-RESEARCH §2.4 / §7.1):
       `<p class="rel-lead">Encontramos a base via Shi <i>et al.</i> (2022). Ao navegar o CSEDM, os 5 <i>assignments</i> cobrem dificuldades muito diferentes: a taxa de acerto cai de 26% no primeiro para 20% no terceiro, e sobe para 30% no último.</p>`
    5. Tabela `<table class="eda-grid">` com `<thead>` (Assignment / Alunos / Problemas / Taxa de acerto) + `<tbody>` com 5 linhas. Conteúdo literal (números MainTable Spring 2019 per D-64a reconciliação 03-RESEARCH §2.1):
       - A1 (439) | 386 | 10 | 26,15%
       - A2 (487) | 340 | 10 | 20,06%
       - A3 (492) | 361 | 10 | 20,34%
       - A4 (494) | 315 | 10 | 24,72%
       - A5 (502) | 306 | 10 | 30,62%
       Percentuais com vírgula decimal pt-BR (NÃO ponto).
    6. Rodapé: `<p class="rel-cite">Fonte: análise sobre CSEDM (Spring 2019).</p>` (sem citação Shi no rodapé porque a contagem é nossa, per 03-RESEARCH §2.4 + D-66d analógico).

    Gates de redação obrigatórios (per 03-RESEARCH §2.4 + 03-PATTERNS §EDA-01):
    - Sem `<blockquote>` (D-69)
    - Sem em-dash (D-70)
    - NÃO repetir 413/50/201 mil no corpo (D-64c gate forte — esses números já estão em INTRO-01)
    - NÃO mencionar "ProgSnap2" em nenhum lugar do EDA-01 (Pitfall 2; nominalmente único em INTRO-01)
    - NÃO mencionar Code-DKT (Pitfall 3; gate forte fase 4)
    - Números MainTable Spring 2019 (386/340/361/315/306) — NÃO usar Release/Train (233/224/234/221/222), Pitfall 4
    - `<i>assignments</i>`, `<i>et al.</i>` em itálico (D-72; D-54 herdado)
    - `(Shi <i>et al.</i>, 2022)` parentético no corpo; rodapé sem citação Shi
    - Sem `<h2>` nem `.rel-title` (D-03 herdado)
  </action>
  <verify>
    <automated>grep -c '> como navegamos o csedm\|> o curso por dentro\|> os 5 assignments' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c '<table class="eda-grid">' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c 'A1 (439)' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c '26,15%' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c '20,06%' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c '30,62%' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c 'Fonte: análise sobre CSEDM (Spring 2019).' apresentacao/index.html | grep -qE '^[1-2]$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SLIDE · EDA-01' apresentacao/index.html` retorna 1
    - `grep -c '<table class="eda-grid">' apresentacao/index.html` retorna 1
    - `grep -cE 'A[1-5] \(43[9]|48[7]|49[24]|50[2]\)' apresentacao/index.html` retorna 5 (5 linhas A1..A5)
    - `grep -c '26,15%' apresentacao/index.html` retorna 1
    - `grep -c '20,06%' apresentacao/index.html` retorna 1
    - `grep -c '20,34%' apresentacao/index.html` retorna 1
    - `grep -c '24,72%' apresentacao/index.html` retorna 1
    - `grep -c '30,62%' apresentacao/index.html` retorna 1
    - `grep -c 'Fonte: análise sobre CSEDM (Spring 2019).' apresentacao/index.html` retorna 1 (EDA-01 introduz nova ocorrência; EDA-03 também usará variante similar com "K-Means k=3 com SEED=42")
    - `grep -c 'ProgSnap2' apresentacao/index.html` retorna 1 (única ocorrência permanece em INTRO-01)
    - `awk '/EDA-01/,/<\/section>/' apresentacao/index.html | grep -c 'Code-DKT'` retorna 0
    - `awk '/EDA-01/,/<\/section>/' apresentacao/index.html | grep -cE '233|224|234|221|222'` retorna 0 (nenhum número Release/Train)
    - `awk '/EDA-01/,/<\/section>/' apresentacao/index.html | grep -cE '413 estudantes|201 mil|10 problemas cada'` retorna 0 (sem repetição de INTRO-01; D-64c)
    - `awk '/EDA-01/,/<\/section>/' apresentacao/index.html | grep -c '—'` retorna 0 (sem em-dash)
    - `grep -c '<i>et al.</i>' apresentacao/index.html` retorna entre 10 e 11 (9 baseline pré-task + 1 ou 2 introduzidas em EDA-01 — parágrafo de abertura e potencialmente rodapé)
  </acceptance_criteria>
  <done>
    Section EDA-01 inserido com cabeçalho, parágrafo abertura, tabela A1..A5 com números MainTable Spring 2019 corretos (vírgula decimal pt-BR), rodapé conforme 03-RESEARCH §7.4. Pronto para checkpoint visual.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Checkpoint visual EDA-01 no browser</name>
  <what-built>
    Section EDA-01 inserido em apresentacao/index.html reusando template `.slide-related` + componente novo `.eda-grid` (anexado em theme-unifacens.css na Task 1). Cabeçalho `> como navegamos o csedm`. 1 parágrafo `.rel-lead` introduzindo Shi + heterogeneidade. Tabela 5 linhas × 4 colunas com números MainTable Spring 2019. Última coluna em azul UniFacens, negrito. Rodapé "Fonte: análise sobre CSEDM (Spring 2019)."
  </what-built>
  <how-to-verify>
    1. Em terminal: `cd apresentacao && python3 -m http.server 8001` (porta 8001 porque CSS foi editado; força reload).
    2. Abrir no browser o índice do EDA-01 (posição final: #/11). Se outros plans ainda não entraram, posição pode variar.
    3. Confirmar visualmente:
       - Cabeçalho `> como navegamos o csedm` em Cascadia 24px cinza com caret piscando.
       - Parágrafo abertura em Arial 25px justificado; "Shi et al. (2022)" em itálico ABNT; "assignments" em itálico minúscula.
       - Tabela centralizada, 92% da largura útil:
         - Header em negrito preto.
         - 5 linhas com borda fina 1.5px preta (estética ABNT).
         - Primeira coluna ("A1 (439)") alinhada à esquerda em negrito.
         - Última coluna ("Taxa de acerto") em azul UniFacens `#2667FF` negrito.
         - Todos os percentuais com vírgula decimal pt-BR (`26,15%` não `26.15%`).
       - Rodapé "Fonte: análise sobre CSEDM (Spring 2019)." em Arial 18px cinza alinhado ao fundo.
       - Marca d'água Facens canto superior direito.
       - Layout cabe em 1280×720 sem overflow.
    4. Console do browser sem erros.
    5. Comparar com INTRO-01 (#/7) — granularidade complementar, sem repetição de 413/50/201 mil.
    6. Confirmar AUSÊNCIAS:
       - "ProgSnap2" não aparece no slide EDA-01 (Pitfall 2).
       - "Code-DKT" não aparece (Pitfall 3).
       - Nenhum número Release/Train (233/224/234/221/222) (Pitfall 4).
       - Sem em-dash em nenhuma frase.
       - Sem `<h2>` nem nome de autor no cabeçalho ou corpo.
  </how-to-verify>
  <resume-signal>Digite "approved" se passou todos os checks ou descreva ajustes (densidade da tabela, troca de cabeçalho entre variantes, ajuste de cor/borda da tabela, microcópia do parágrafo) para iteração. Padrão fase 2: média 2 iterações por slide.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma (deck estático) | Slides reveal.js estáticos; sem inputs externos, sem auth, sem persistência |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | nenhum | accept | Contagens per-assignment são públicas (paper Shi 2022 + dataset CSEDM 2021) |
| T-03-03-02 | Tampering | apresentacao/index.html, theme-unifacens.css | accept | Markup e CSS estáticos; alteração inadvertida detectada via `git diff` no checkpoint |

**ASVS L1 evaluation:** N/A — deck estático sem auth nem input externo. Componente `.eda-grid` reutilizável; sem dependência externa.
</threat_model>

<verification>
- `.eda-grid` adicionado em theme-unifacens.css com ~14 linhas e variáveis CSS existentes
- Section EDA-01 com cabeçalho, parágrafo abertura, tabela 5×4 com números MainTable Spring 2019, rodapé
- Percentuais pt-BR (vírgula decimal)
- Gates de ausência: sem ProgSnap2, sem Code-DKT, sem números Release/Train, sem em-dash, sem repetição de INTRO-01
- Checkpoint visual APPROVED
</verification>

<success_criteria>
- Slide EDA-01 visível em #/11 (após plans 01 e 02; ordem final EDA-01 antes de EDA-02)
- Tabela ABNT legível em 1280×720 com 5 linhas e última coluna em azul UniFacens
- Coerência narrativa com EDA-02 (que comunica 413 → 410 → 328/82)
- Commit funcional: `apresentacao: slide EDA-01 - como navegamos o csedm (tabela A1..A5)`
</success_criteria>

<output>
After completion, create `.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-03-SUMMARY.md` registrando:
- Cabeçalho final escolhido + variante real implementada (caso fallback usado)
- Iterações pós-checkpoint, se houver
- Linhas exatas adicionadas em theme-unifacens.css (range + offset)
- Posição final do EDA-01 no deck e validação de coerência com EDA-02
- Decisões ad-hoc (D-XX) registradas para fase 4
</output>
