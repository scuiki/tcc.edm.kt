---
phase: 03-eda-e-pr-processamento-fase-2-edm
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apresentacao/index.html
autonomous: false
requirements:
  - EDA-02
must_haves:
  truths:
    - "Slide EDA-02 carrega no browser sem erro de console na posição #/12 (ou #/11 isolado)"
    - "Cabeçalho '> aproximação ao protocolo' aparece em Cascadia 24px com caret piscando"
    - "Texto comunica a ponte explícita 413 (CSEDM bruto) → 410 (filtro Shi) → 328 treino / 82 teste"
    - "Texto cita truncagem em 50 últimas tentativas (mediana 32, 28% dos pares ultrapassam, máximo 272)"
    - "Voz em primeira pessoa do plural ('Nosso pré-processamento...', 'mantivemos 410...', 'dividimos 328/82...')"
    - "Citação parentética (Shi <i>et al.</i>, 2022) no 1º .rel-lead; rodapé 'Fonte: adaptado de Shi <i>et al.</i> (2022).'"
    - "Sem citação direta literal (D-69), sem em-dash (D-70), sem Code-DKT (Pitfall 3), sem Compile.Error nem threshold binário (D-65b), sem Release/Train"
    - "D-60: EDA-02 entra na sequência prescrita (após EDA-01 narrativamente, antes de EDA-03 e MARKER-02)"
    - "D-62: EDA-02 cumpre o papel narrativo de 'mostrar as decisões de pré-processamento' entre INTRO/MARKER-01 e EDA-03/MARKER-02"
  artifacts:
    - path: apresentacao/index.html
      provides: "Section EDA-02 com 3 .rel-lead + rodapé"
      contains: "> aproximação ao protocolo"
  key_links:
    - from: "apresentacao/index.html section EDA-02"
      to: "apresentacao/assets/theme-unifacens.css (.slide-related, .rel-lead, .rel-cite, .deck-topic)"
      via: "template slide-related reusado de INTRO-03b (linhas 183-198)"
      pattern: "class=.deck-slide slide-related."
---

<objective>
Inserir slide EDA-02 ("aproximação ao protocolo") em apresentacao/index.html, posicionado no gap após MARKER-01 e antes do MARKER-02. O slide justifica a aproximação ao protocolo de Shi et al. (2022) como baseline e lista as 2 etapas concretas de pré-processamento (filtro min_attempts ≥ 3 → 410 alunos; split 80/20 → 328/82; truncagem 50 últimas tentativas) em voz autoral com paráfrase indireta.

Purpose: estabelece, na narrativa Fase 2 EDM, a metodologia de preparação dos dados que liga direto com MODEL-04 (A439 first_auc=72,55%, ±3% do paper). Coerência com CLAUDE.md "dataset primário = MainTable + protocolo Shi" e D-38b da fase 2 que mandata a ponte 413 → 410 → 328/82.

Output: 1 section novo (~20 linhas HTML) reusando template .slide-related (zero CSS novo). 1 commit funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-CONTEXT.md
@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md
@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-PATTERNS.md
@.planning/phases/02-intro-dataset-e-problema-fase-1-edm/PHASE-SUMMARY.md

@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css

@docs/Code-DKT.pdf

<interfaces>
<!-- Template-base reutilizado: INTRO-03b (linhas 183-198 de index.html). Zero CSS novo. -->

Classes consumidas (já existem em theme-unifacens.css):
- `.slide-related` (host)
- `.deck-topic` + `.ps1` + `.caret.blink` (cabeçalho `> seção`)
- `.rel-lead` (parágrafos de corpo, Arial 25px, justificado)
- `.rel-cite` (rodapé `Fonte: ...`, Arial 18px cinza, `margin-top: auto`)
- `<svg class="wm">` (marca d'água Facens)

Phrasing-alvo travado (03-RESEARCH §1.2 + §7.2, números D-65a):
- Parágrafo 1: "Nosso pré-processamento segue o protocolo de Shi <i>et al.</i> (2022) como <i>baseline</i> de comparação, com ênfase em análise."
- Parágrafo 2: "Dos 413 alunos brutos do CSEDM, mantivemos <b>410</b> com pelo menos 3 tentativas de execução, mesma seleção do paper. Em seguida, dividimos em <b>328 estudantes para treino e 82 para teste</b>, na proporção 80/20 com semente fixa."
- Parágrafo 3: "Limitamos cada sequência às <b>50 últimas tentativas</b>. A mediana é 32 tentativas por aluno e <i>assignment</i>; 28% dos pares ultrapassam 50, com cauda longa até 272."
- Rodapé: "Fonte: adaptado de Shi <i>et al.</i> (2022)."

Cabeçalho recomendado: `> aproximação ao protocolo` (D-63b aberto; fallbacks aceitos `> pré-processamento` ou `> do bruto ao split`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inserir section EDA-02 com 3 .rel-lead reusando template slide-related</name>
  <read_first>
    - apresentacao/index.html linhas 183-198 (markup INTRO-03b — template a clonar literalmente)
    - apresentacao/index.html linhas 199-244 (boundary para inserir antes do MARKER-01 ou após, depende da ordem de execução; ponto canônico final é após MARKER-01 linha 243)
    - apresentacao/assets/theme-unifacens.css linhas 158-200 (CSS .slide-related .rel-lead / .rel-cite — confirmar que classes existem; NÃO editar)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md §1.1 (citações literais Shi 2022 §4.1/§4.2) + §1.2 (phrasing-alvo polido) + §7.2 (3 variantes de cabeçalho com avaliação)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-PATTERNS.md §"EDA-02 (controller, determinístico)" (markup-alvo + gates)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-CONTEXT.md §D-65 (split mandatório) + §D-65a (etapas) + §D-65b (o que NÃO entra) + §D-65c (voz)
    - docs/Code-DKT.pdf §4.1 e §4.2 (página 5) — citações literais que sustentam a paráfrase já estão extraídas em 03-RESEARCH §1.1; leitura adicional opcional para confirmação
  </read_first>
  <action>
    Inserir novo `<section>` em apresentacao/index.html após linha 243 (`</section>` do MARKER-01); ponto exato dependendo da ordem de plans já executados. Se MARKER-02 (plan 01) já foi inserido, EDA-02 entra ANTES do MARKER-02; se MARKER-02 ainda não foi inserido, EDA-02 entra entre linhas 243 e 245. Preservar indentação 6 espaços e EOL Unix.

    O markup é cópia da estrutura externa de INTRO-03b (linhas 183-198 de index.html) com adaptações:

    1. Comentário acima do `<section>`: `<!-- ============ SLIDE · EDA-02 · Pré-processamento, aproximação ao protocolo (Shi et al., 2022) ============ -->`
    2. `<section data-background-color="#F1F6FB">` + `<div class="deck-slide slide-related">` + marca d'água `<svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>` (idênticos ao análogo).
    3. Cabeçalho (D-63b, recomendado pelo 03-RESEARCH §7.2 variante (b)): `<p class="deck-topic"><span class="ps1">&gt;</span>aproximação ao protocolo<span class="caret blink"></span></p>`. Fallbacks aceitos: `pré-processamento` ou `do bruto ao split` — escolher e documentar.
    4. Três `<p class="rel-lead">` com phrasing literal de 03-RESEARCH §1.2/§7.2 (per D-65 e D-65a):
       - 1º `.rel-lead`: justificativa (paráfrase Shi 2022 §4.1; menciona <i>baseline</i> em itálico; citação parentética (Shi <i>et al.</i>, 2022)).
       - 2º `.rel-lead`: ponte 413 → 410 → 328/82 (números travados em D-65a; "pelo menos 3 tentativas de execução"; "80/20 com semente fixa"; sem citar `random_state=1` literal).
       - 3º `.rel-lead`: truncagem 50 últimas + mediana 32 + 28% dos pares + máximo 272.
    5. Rodapé: `<p class="rel-cite">Fonte: adaptado de Shi <i>et al.</i> (2022).</p>` (D-65c + D-71).
    6. Fechar `</div>` e `</section>`.

    Gates de redação (per 03-RESEARCH §1.2 e 03-PATTERNS §EDA-02):
    - Sem `<blockquote>` (D-69 proíbe citação direta literal)
    - Sem em-dash em prosa (D-70); usar vírgula, dois-pontos ou parênteses
    - `<i>baseline</i>`, `<i>assignment</i>`, `<i>et al.</i>` em itálico minúsculas (D-72; D-54 herdado)
    - NÃO mencionar Release/Train, Compile.Error nem threshold binário `correct = (Score == 1.0)` (D-65b)
    - NÃO mencionar Code-DKT (Pitfall 3 RESEARCH; gate forte fase 4)
    - Citação parentética `(Shi <i>et al.</i>, 2022)` única no 1º `.rel-lead`; não repetir nos demais
    - Sem `<h2>` nem `.rel-title` (D-03 fase 1 herdado)

    Em caso de dúvida sobre microcópia textual, optar pelo phrasing exato de 03-RESEARCH §7.2 Tabela 2 e iterar no checkpoint visual da Task 2 (padrão da fase 2: média 2 iterações por slide).
  </action>
  <verify>
    <automated>grep -c '> aproximação ao protocolo\|> pré-processamento\|> do bruto ao split' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c 'mantivemos <b>410</b>' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c '328 estudantes para treino e 82 para teste' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c '50 últimas tentativas' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c 'Fonte: adaptado de Shi <i>et al.</i> (2022)' apresentacao/index.html | grep -q '^1$' &amp;&amp; ! awk '/EDA-02/,/<\/section>/' apresentacao/index.html | grep -qE 'Code-DKT|Release/Train|Compile\.Error' &amp;&amp; ! awk '/EDA-02/,/<\/section>/' apresentacao/index.html | grep -q '—'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'SLIDE · EDA-02' apresentacao/index.html` retorna 1
    - `grep -c 'aproximação ao protocolo\|pré-processamento\|do bruto ao split' apresentacao/index.html` retorna 1 (cabeçalho único entre as variantes aceitas)
    - `grep -c '413 alunos brutos do CSEDM' apresentacao/index.html` retorna 1
    - `grep -c '<b>410</b>' apresentacao/index.html` retorna 1
    - `grep -c '328 estudantes para treino e 82 para teste' apresentacao/index.html` retorna 1
    - `grep -c '50 últimas tentativas' apresentacao/index.html` retorna 1
    - `grep -c '28% dos pares' apresentacao/index.html` retorna 1
    - `grep -c 'Fonte: adaptado de Shi <i>et al.</i> (2022)' apresentacao/index.html` aumenta em 1 vs HEAD (já existia 1 em INTRO-03b; agora 2)
    - `grep -c '<i>baseline</i>' apresentacao/index.html` ≥ 1
    - `awk '/EDA-02/,/<\/section>/' apresentacao/index.html | grep -c 'Code-DKT'` retorna 0 (Pitfall 3)
    - `awk '/EDA-02/,/<\/section>/' apresentacao/index.html | grep -cE 'Release/Train|Compile\.Error|Score == 1\.0'` retorna 0 (D-65b)
    - `awk '/EDA-02/,/<\/section>/' apresentacao/index.html | grep -c '—'` retorna 0 (D-70 sem em-dash)
    - `awk '/EDA-02/,/<\/section>/' apresentacao/index.html | grep -c '<blockquote\|<h2'` retorna 0 (D-69, D-03)
    - `git diff --stat apresentacao/assets/theme-unifacens.css` retorna vazio (template existente; zero CSS novo)
  </acceptance_criteria>
  <done>
    Section EDA-02 inserido no gap correto, com cabeçalho temático, 3 parágrafos `.rel-lead` em voz autoral (paráfrase indireta Shi 2022), números D-65a literais, gates de redação passando, rodapé "Fonte: adaptado de Shi <i>et al.</i> (2022)." Pronto para checkpoint visual.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual EDA-02 no browser</name>
  <what-built>
    Section EDA-02 inserido em apresentacao/index.html reusando template .slide-related (zero CSS novo). Cabeçalho `> aproximação ao protocolo` (ou fallback escolhido). 3 parágrafos `.rel-lead` comunicando: (1) justificativa Shi como baseline; (2) ponte 413 → 410 → 328/82; (3) truncagem 50 últimas com mediana 32 / 28% / máximo 272. Rodapé "Fonte: adaptado de Shi et al. (2022)."
  </what-built>
  <how-to-verify>
    1. Em terminal: `cd apresentacao && python3 -m http.server 8000` (porta 8000; se editou só HTML, basta recarregar; se editou CSS por engano, subir em 8001).
    2. Abrir no browser o índice do EDA-02 (posição final no deck depende de quantos plans já entraram: se MARKER-02 já foi inserido antes do plan 02, EDA-02 está em #/11; se a ordem foi EDA-02 antes de MARKER-02, EDA-02 está em #/11 também; ao fim dos 4 plans EDA-02 estará em #/12).
    3. Confirmar visualmente:
       - Cabeçalho `> aproximação ao protocolo` em Cascadia 24px cinza, com `>` em azul UniFacens e caret piscando no fim.
       - 3 parágrafos `.rel-lead` em Arial ~25px, cor preta, justificados.
       - Negritos visíveis: "410", "328 estudantes para treino e 82 para teste", "50 últimas tentativas".
       - Itálico em `baseline`, `assignment`, `et al.` ABNT.
       - Citação parentética `(Shi et al., 2022)` no 1º parágrafo (et al. em itálico).
       - Rodapé "Fonte: adaptado de Shi et al. (2022)." em Arial 18px cinza `#5b6472`, alinhado ao fundo do slide.
       - Marca d'água Facens no canto superior direito.
       - Layout cabe em 1280×720 sem quebra ou overflow.
    4. Console do browser sem erros.
    5. Comparar com INTRO-03b (#/9) — densidade textual similar; tipografia coerente.
    6. Confirmar AUSÊNCIAS:
       - Sem citação direta entre aspas ou em `<blockquote>`.
       - Sem em-dash em qualquer parágrafo.
       - Sem menção a "Code-DKT", "Release/Train", "Compile.Error", "Score == 1.0".
       - Sem `<h2>` nem nome de autor no cabeçalho ou corpo (só no rodapé "Fonte:").
  </how-to-verify>
  <resume-signal>Digite "approved" se passou todos os checks ou descreva ajustes textuais (microcópia, ênfase em negrito, troca de cabeçalho entre as variantes) para iteração. Padrão da fase 2: média 2 iterações textuais por slide.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma (deck estático) | Slides reveal.js estáticos; sem inputs externos, sem auth, sem persistência |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Information Disclosure | nenhum | accept | Números do CSEDM (413, 410, 328/82) são públicos via paper Shi 2022 e dataset CSEDM 2021 |
| T-03-02-02 | Tampering | apresentacao/index.html | accept | Texto estático em pt-BR; alteração inadvertida detectada via `git diff` no checkpoint |

**ASVS L1 evaluation:** N/A — deck estático sem auth nem input externo.
</threat_model>

<verification>
- Cabeçalho temático conforme variantes aceitas
- Números D-65a corretos no markup (413, 410, 328/82, 50, 32, 28%, 272)
- Rodapé "Fonte: adaptado de Shi <i>et al.</i> (2022)."
- Gates de ausência: sem Code-DKT, sem Release/Train, sem em-dash, sem citação direta
- Zero CSS novo
- Checkpoint visual APPROVED
</verification>

<success_criteria>
- Slide EDA-02 visível no browser, layout legível em 1280×720
- 3 parágrafos `.rel-lead` em voz autoral cumprindo D-65/D-65a/D-65c
- Ponte explícita 413 → 410 → 328/82 verbalizada
- Truncagem 50 últimas + estatísticas (mediana 32, 28%, 272) presentes
- Commit funcional: `apresentacao: slide EDA-02 - pré-processamento, aproximação ao protocolo (Shi et al., 2022)`
</success_criteria>

<output>
After completion, create `.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-02-SUMMARY.md` registrando:
- Cabeçalho final escolhido (a, b ou c do 03-RESEARCH §7.2) e justificativa breve
- Iterações pós-checkpoint, se houver (esperado 1-2)
- Posição final no deck
- Decisões ad-hoc (D-XX adicionais) registradas em vocabulário para fase 4
</output>
