---
phase: 03-eda-e-pr-processamento-fase-2-edm
plan: 04
type: execute
wave: 4
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - scripts/build_eda_pca_scatter.py
  - results/sec2_perfis_pca.png
  - apresentacao/assets/eda-perfis-pca.png
  - apresentacao/assets/theme-unifacens.css
  - apresentacao/index.html
  - apresentacao/STYLE.md
autonomous: false
requirements:
  - EDA-03
  - PENDING-02
must_haves:
  truths:
    - "Script scripts/build_eda_pca_scatter.py existe, reproduz K-Means+PCA com SEED=42, e gera results/sec2_perfis_pca.png"
    - "PNG copiado para apresentacao/assets/eda-perfis-pca.png; slide referencia 'assets/eda-perfis-pca.png' (não 'results/' direto)"
    - "Scatter PCA exibe 3 grupos coloridos com paleta UniFacens (#2667FF azul / #F2A516 âmbar / #D7191C vermelho)"
    - "Legenda no scatter inclui (n=...) por grupo, refletindo contagem REAL do script (Alto≈96, Médio≈19, Em risco≈124, total≈239)"
    - "Slide EDA-03 carrega em #/13 com cabeçalho '> três jeitos de aprender' + figura + insight em destaque + rodapé"
    - "Insight central em uma única frase: 'O grupo majoritário não é quem erra muito; é quem tenta pouco.'"
    - "Rodapé: 'Fonte: análise sobre CSEDM (Spring 2019); <i>K-Means</i> k=3 com SEED=42.'"
    - "STYLE.md §Inventário atualizado para refletir 20 sections pós-fase 3"
    - "PENDING-02 marcado como resolvido (gráfico de insight definido = scatter PCA com 3 clusters K-Means)"
    - "D-60: EDA-03 entra após EDA-02 e antes de MARKER-02 conforme ordem prescrita (EDA-01 → EDA-02 → EDA-03 → MARKER-02)"
    - "D-61: após inserção dos 4 slides, slide-code passa de #/11 para #/15; STYLE.md §Gaps reservados linha 130 reflete o gap consumido"
    - "D-62: EDA-03 cumpre o papel narrativo de 'entregar o insight' entre as decisões de pré-processamento (EDA-02) e o fechamento da Fase 2 (MARKER-02)"
    - "D-74: validação visual final percorre #/0 a #/19 no browser (cd apresentacao && python3 -m http.server 8000), confirma navegação sem erro de console, transição limpa nos 4 novos, layout legível, rodapé Fonte em cada slide, MARKER-02 com pills 1+2 done/pill 3 running/pill 4 pending, slide-code em #/15"
  artifacts:
    - path: scripts/build_eda_pca_scatter.py
      provides: "Script Python one-shot para gerar PNG do scatter PCA (SEED=42, paleta UniFacens)"
      contains: "build_eda_pca_scatter"
    - path: results/sec2_perfis_pca.png
      provides: "PNG do scatter (gerado pelo script)"
    - path: apresentacao/assets/eda-perfis-pca.png
      provides: "Cópia do PNG no diretório de assets do deck"
    - path: apresentacao/index.html
      provides: "Section EDA-03 com figura + insight + rodapé"
      contains: "> três jeitos de aprender"
    - path: apresentacao/assets/theme-unifacens.css
      provides: "Componentes .eda-fig + .eda-insight + .eda-subinsight (~16 linhas)"
      contains: ".eda-insight"
    - path: apresentacao/STYLE.md
      provides: "§Inventário pós-fase 3 atualizado + §Gaps reservados com gap consumido removido"
  key_links:
    - from: "scripts/build_eda_pca_scatter.py"
      to: "results/sec2_perfis_pca.png"
      via: "matplotlib savefig com SEED=42"
      pattern: "savefig.*sec2_perfis_pca.png"
    - from: "apresentacao/index.html EDA-03 <img>"
      to: "apresentacao/assets/eda-perfis-pca.png"
      via: "src='assets/eda-perfis-pca.png'"
      pattern: "src=.assets/eda-perfis-pca.png."
    - from: "apresentacao/index.html EDA-03 <figure class='eda-fig'>"
      to: "apresentacao/assets/theme-unifacens.css .eda-fig + .eda-insight"
      via: "classes .eda-fig + .eda-insight"
      pattern: "class=.eda-(fig|insight|subinsight)."
---

<objective>
Entregar o slide EDA-03 ("três jeitos de aprender"), o insight visual central da Fase 2 EDM, em 4 sub-tasks atômicas: (1) criar `scripts/build_eda_pca_scatter.py` reproduzindo K-Means+PCA da cell 46 do notebook 01_eda.ipynb com SEED=42 e paleta UniFacens; (2) executar o script + copiar PNG gerado para `apresentacao/assets/eda-perfis-pca.png`; (3) anexar bloco CSS `.eda-fig` + `.eda-insight` + `.eda-subinsight` (~16 linhas) em theme-unifacens.css e inserir section EDA-03 em index.html; (4) atualizar STYLE.md §Inventário para refletir 20 sections pós-fase 3 e marcar gap consumido.

Purpose: encerrar visualmente a Fase 2 EDM com o insight pedagógico central da apresentação ("o grupo majoritário não é quem erra muito; é quem tenta pouco"); resolver PENDING-02 (qual gráfico de insight de estudantes entra no slide); resolver §Gaps reservados linha 130 do STYLE.md.

Output: 1 script Python novo (~80 linhas), 1 PNG novo (~1200×700, 3 clusters coloridos), 1 cópia do PNG em assets do deck, 1 bloco CSS (~16 linhas), 1 section HTML (~14 linhas), 1 update no STYLE.md (~12 linhas). 1 commit funcional (PNG + script + slide juntos conforme Open Question 1; STYLE.md pode ir como sub-commit ou junto).

Nota sobre tamanho do plan: 5 tasks e 6 arquivos modificados ficam acima do threshold padrão (4 tasks). Tamanho é justificado pela natureza do EDA-03 — único plan da fase 3 que gera artefatos NOVOS (script Python + PNG) em vez de só editar HTML/CSS existentes, e que toca STYLE.md como passo final de sincronização do inventário. Dividir em sub-plans aumentaria overhead de coordenação sem reduzir custo de contexto (todas as 5 tasks compartilham o mesmo modelo mental: gerar artefato, validar, integrar ao deck). Mantido como um plan unitário.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-CONTEXT.md
@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md
@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-PATTERNS.md

@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css

@scripts/build_methodology_figures.py
@notebooks/01_eda.ipynb

<interfaces>
<!-- Interfaces criadas neste plan + análogos a clonar. -->

Script Python novo (`scripts/build_eda_pca_scatter.py`, ~80 linhas):
- Análogo: `scripts/build_methodology_figures.py` (mesmo prefixo, ROOT/Path resolução, matplotlib Agg)
- Imports: `from pathlib import Path`, `random`, `numpy as np`, `pandas as pd`, `matplotlib` (Agg), `matplotlib.pyplot as plt`, `sklearn.cluster.KMeans`, `sklearn.decomposition.PCA`, `sklearn.preprocessing.StandardScaler`
- Constantes: `ROOT = Path(__file__).resolve().parents[1]`; `DATA_ROOT = ROOT / "data" / "CSEDM"`; `RESULTS = ROOT / "results"`; `SEED = 42`
- Inputs lidos: `data/early.csv`, `data/late.csv`, `data/CSEDM/LinkTables/Subject.csv` (verificados em 03-RESEARCH §3.1)
- Output: `results/sec2_perfis_pca.png` (figsize 12×7, dpi 120, bbox_inches "tight", facecolor white)
- Palette: `{"Alto desempenho": "#2667FF", "Médio": "#F2A516", "Em risco": "#D7191C"}` (Pitfall 5)
- Print final: resumo numérico `cluster_features.groupby("perfil")[["X-Grade"]].agg(["count", "mean"])` para validação cruzada

Classes CSS NOVAS (~16 linhas em theme-unifacens.css):
- `.eda-fig` (figura embed centralizada, max-width 78%)
- `.eda-fig img` (width 100%, max-height 460px)
- `.eda-insight` (frase de destaque Arial bold 23px, centralizado)
- `.eda-subinsight` (subtítulo Arial 19px cinza, opcional)

Markup do slide EDA-03 (03-RESEARCH §7.3 + 03-PATTERNS §EDA-03):
- Cabeçalho: `> três jeitos de aprender` (D-63c recomendado; fallbacks: `perfis dos alunos` / `quem tenta, quem desiste`)
- `<figure class="eda-fig"><img src="assets/eda-perfis-pca.png" alt="Scatter PCA 2D dos 3 perfis de alunos (Alto desempenho, Médio, Em risco)"></figure>`
- `<p class="eda-insight">O grupo majoritário não é quem erra muito; é quem tenta pouco.</p>`
- `<p class="rel-lead eda-subinsight">Em risco no CSEDM tem alta taxa de acerto eventual mas poucas tentativas por <i>assignment</i> (2 a 4).</p>`
- `<p class="rel-cite">Fonte: análise sobre CSEDM (Spring 2019); <i>K-Means</i> k=3 com SEED=42.</p>`

Atualizações STYLE.md (03-RESEARCH §8.1 e §8.2):
- §Inventário renomeado de "pós-fase 2" para "pós-fase 3"; tabela expande de 12 → 20 linhas (4 novas em #11..#14; índices antigos 11-15 deslocados para 15-19)
- §Gaps reservados: remover linha 130 ("Após MARKER-01 e antes do trio Martins+fig: EDA-01, EDA-02, EDA-03 + MARKER-02 (fase 3)."); reorganizar gaps remanescentes para fases 4-5
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar scripts/build_eda_pca_scatter.py</name>
  <read_first>
    - scripts/build_methodology_figures.py linhas 1-29 (imports + ROOT pattern; análogo canônico)
    - scripts/build_methodology_figures.py linhas 80-90 (padrão `_save` ou equivalente para savefig)
    - notebooks/01_eda.ipynb cell 35 (load early.csv + late.csv + Subject.csv — confirmar caminhos exatos)
    - notebooks/01_eda.ipynb cell 44 (construção de cluster_features: rate pivot + att pivot + X-Grade join + dropna)
    - notebooks/01_eda.ipynb cell 46 (K-Means k=3 + nomeação por X-Grade + PCA 2D + scatter)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md §6.3 (snippet Python completo, linhas 644-737) + §3.1 (estado atual da cell 46 com outputs preservados)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-PATTERNS.md §"scripts/build_eda_pca_scatter.py" (gates obrigatórios)
  </read_first>
  <action>
    Criar arquivo novo scripts/build_eda_pca_scatter.py reproduzindo a lógica de cluster_features da cell 44 + K-Means+nomeação da cell 46 do notebook 01_eda.ipynb, com SEED=42 fixo em 4 pontos (random.seed, np.random.seed, KMeans random_state, PCA random_state), paleta UniFacens-friendly e print final do resumo numérico.

    Estrutura literal seguindo 03-RESEARCH §6.3:

    1. Docstring (sem em-dash; D-70): explica geração do scatter PCA 2D dos 3 perfis K-Means para o slide EDA-03; saída em results/sec2_perfis_pca.png; SEED=42 fixo.

    2. Imports `from __future__ import annotations`; `random`; `from pathlib import Path`; `numpy as np`; `pandas as pd`; `matplotlib` com `matplotlib.use("Agg")` ANTES de `import matplotlib.pyplot as plt`; `from sklearn.cluster import KMeans`; `from sklearn.decomposition import PCA`; `from sklearn.preprocessing import StandardScaler`.

    3. Constantes: `ROOT = Path(__file__).resolve().parents[1]`; `DATA_ROOT = ROOT / "data" / "CSEDM"`; `RESULTS = ROOT / "results"`; `SEED = 42`.

    4. Seeding: `random.seed(SEED)`; `np.random.seed(SEED)`.

    5. Pipeline de dados (mesma estratégia das cells 35 e 44 do notebook):
       - `early = pd.read_csv(ROOT / "data" / "early.csv")`
       - `late = pd.read_csv(ROOT / "data" / "late.csv")`
       - `all_labels = pd.concat([early, late], ignore_index=True)`
       - `subject = pd.read_csv(DATA_ROOT / "LinkTables" / "Subject.csv")`
       - Pivot `(SubjectID, AssignmentID) → CorrectEventually.mean()` → renomear colunas para `A{c}_rate`
       - Pivot `(SubjectID, AssignmentID) → Attempts.mean()` → renomear para `A{c}_att`
       - Join com `subject.set_index("SubjectID")["X-Grade"]` + `.dropna()`
       - `X_scaled = StandardScaler().fit_transform(cluster_features.values)`

    6. K-Means k=3 com `random_state=SEED, n_init=10`; nomeação por `X-Grade.mean()` ordenado decrescente → labels ["Alto desempenho", "Médio", "Em risco"]; coluna `perfil` adicionada.

    7. PCA `n_components=2, random_state=SEED`; armazenar coords em variável `coords`.

    8. Palette UniFacens-friendly (Pitfall 5): `{"Alto desempenho": "#2667FF", "Médio": "#F2A516", "Em risco": "#D7191C"}`.

    9. `fig, ax = plt.subplots(figsize=(12, 7), dpi=120)`; loop pelos 3 perfis fazendo `ax.scatter(coords[mask, 0], coords[mask, 1], c=color, label=f"{perfil} (n={n})", s=70, alpha=0.78, edgecolors="white", linewidths=0.7)`.

    10. Eixos rotulados `PC1` / `PC2` (fontsize 14); legenda canto inferior esquerdo `loc="lower left", fontsize=13, frameon=True, framealpha=0.95, edgecolor="#1f1f1f"`; grid alpha 0.18; axisbelow True; tight_layout.

    11. Salvar: `output = RESULTS / "sec2_perfis_pca.png"`; `plt.savefig(output, bbox_inches="tight", facecolor="white")`; `plt.close(fig)`; `print(f"Saved: {output}")`.

    12. Resumo numérico ao final: `print(); print("=== Resumo por perfil (alimenta texto do slide EDA-03) ==="); print(cluster_features.groupby("perfil")[["X-Grade"]].agg(["count", "mean"]))`.

    Gates obrigatórios (per 03-PATTERNS §scripts/build_eda_pca_scatter.py):
    - SEED=42 em 4 pontos
    - `matplotlib.use("Agg")` ANTES de `import matplotlib.pyplot`
    - `ROOT = Path(__file__).resolve().parents[1]`
    - Output em `results/`, não em `apresentacao/assets/` direto (Pitfall 6)
    - Print do resumo numérico para validação cruzada com slide
    - Sem em-dash no docstring (D-70)
    - Paleta `#2667FF`/`#F2A516`/`#D7191C` (Pitfall 5)
  </action>
  <verify>
    <automated>test -f scripts/build_eda_pca_scatter.py &amp;&amp; grep -c 'SEED = 42' scripts/build_eda_pca_scatter.py | grep -q '^1$' &amp;&amp; grep -c 'random_state=SEED' scripts/build_eda_pca_scatter.py | grep -qE '^[2-3]$' &amp;&amp; grep -c 'matplotlib.use("Agg")' scripts/build_eda_pca_scatter.py | grep -q '^1$' &amp;&amp; grep -c '#2667FF\|#F2A516\|#D7191C' scripts/build_eda_pca_scatter.py | grep -qE '^[3-6]$' &amp;&amp; ! grep -q '—' scripts/build_eda_pca_scatter.py</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/build_eda_pca_scatter.py` retorna 0 (arquivo existe)
    - `grep -c 'SEED = 42' scripts/build_eda_pca_scatter.py` retorna 1
    - `grep -c 'random_state=SEED' scripts/build_eda_pca_scatter.py` retorna 2 (KMeans + PCA)
    - `grep -c 'random.seed(SEED)\|np.random.seed(SEED)' scripts/build_eda_pca_scatter.py` retorna 2
    - `grep -c 'matplotlib.use("Agg")' scripts/build_eda_pca_scatter.py` retorna 1
    - `grep -c '#2667FF\|#F2A516\|#D7191C' scripts/build_eda_pca_scatter.py` retorna entre 3 e 6 (3 cores na palette + possíveis usos em edgecolor)
    - `grep -c 'results/sec2_perfis_pca.png\|RESULTS / "sec2_perfis_pca.png"' scripts/build_eda_pca_scatter.py` ≥ 1
    - `grep -c '—' scripts/build_eda_pca_scatter.py` retorna 0 (sem em-dash)
    - `python3 -c "import ast; ast.parse(open('scripts/build_eda_pca_scatter.py').read())"` não retorna erro (Python válido)
  </acceptance_criteria>
  <done>
    Script Python válido, com SEED fixo em 4 pontos, paleta UniFacens, e print do resumo numérico. Pronto para execução na Task 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Executar script + copiar PNG para apresentacao/assets/</name>
  <read_first>
    - scripts/build_eda_pca_scatter.py (script recém-criado)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md §6.4 (comando de execução e validação)
    - .planning/codebase/STRUCTURE.md (convenção: PNG em apresentacao/assets/, não referenciar results/ direto)
  </read_first>
  <action>
    Executar o script e copiar o PNG resultante para o diretório de assets do deck.

    Comandos:
    1. `python3 scripts/build_eda_pca_scatter.py` — espera saída `Saved: /path/to/results/sec2_perfis_pca.png` + resumo numérico tipo:
       ```
       === Resumo por perfil (alimenta texto do slide EDA-03) ===
                            X-Grade
                              count      mean
       perfil
       Alto desempenho           96  0.80...
       Em risco                 124  0.60...
       Médio                     19  0.65...
       ```
       Capturar os valores reais do print (count por perfil) para validar a legenda do PNG e o texto do insight do slide.

    2. Verificar visualmente o PNG (`xdg-open results/sec2_perfis_pca.png` ou abrir manualmente): 3 grupos coloridos (azul / âmbar / vermelho), legenda canto inferior esquerdo com `(n=...)`, eixos PC1/PC2.

    3. Copiar PNG para o diretório de assets do deck: `cp results/sec2_perfis_pca.png apresentacao/assets/eda-perfis-pca.png`. Pitfall 6: NÃO referenciar `results/` direto no slide.

    Se o script falhar (e.g. early.csv não existe em `data/`), checar 03-RESEARCH §6.3 e cell 35 do notebook 01_eda.ipynb para reconfirmar caminhos. Caminhos confirmados em 03-RESEARCH §Environment Availability: `data/early.csv` (9106 linhas), `data/late.csv` (5722 linhas), `data/CSEDM/LinkTables/Subject.csv`.

    Se a contagem de perfis divergir significativamente do esperado (Alto≈96 / Médio≈19 / Em risco≈124, total≈239), confirmar que o seed=42 está sendo aplicado em todos os 4 pontos da Task 1 e re-rodar. Pequenas variações (±5 alunos) são aceitáveis; variações grandes (>30) indicam bug no seeding.
  </action>
  <verify>
    <automated>test -f results/sec2_perfis_pca.png &amp;&amp; test -f apresentacao/assets/eda-perfis-pca.png &amp;&amp; [ "$(stat -c '%s' results/sec2_perfis_pca.png)" -gt 30000 ] &amp;&amp; [ "$(stat -c '%s' apresentacao/assets/eda-perfis-pca.png)" -gt 30000 ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -f results/sec2_perfis_pca.png` retorna 0 (PNG gerado)
    - `test -f apresentacao/assets/eda-perfis-pca.png` retorna 0 (PNG copiado)
    - Tamanho do PNG > 30 KB (validação grosseira de que não é arquivo vazio)
    - `file results/sec2_perfis_pca.png` retorna "PNG image data"
    - Print do script capturou contagem por perfil; valores reais documentados em SUMMARY (espera Alto≈96, Médio≈19, Em risco≈124, mas valores exatos podem variar marginalmente)
    - Visual: 3 cores distintas no scatter; legenda `(n=...)` legível; eixos rotulados PC1/PC2
  </acceptance_criteria>
  <done>
    PNG gerado, validado visualmente, copiado para o diretório de assets. Contagens reais capturadas para uso na Task 3.
  </done>
</task>

<task type="auto">
  <name>Task 3: Anexar CSS .eda-fig/.eda-insight + inserir section EDA-03 no index.html</name>
  <read_first>
    - apresentacao/assets/theme-unifacens.css (últimas ~30 linhas; final do arquivo onde anexar)
    - apresentacao/assets/theme-unifacens.css linhas 306-319 (.slide-fig — análogo de figura embed)
    - apresentacao/index.html linhas 183-198 (INTRO-03b — análogo de estrutura externa .slide-related)
    - apresentacao/assets/eda-perfis-pca.png (PNG gerado na Task 2 — confirmar existência)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md §6.5 (CSS sugerido) + §7.3 (markup-alvo + variantes de cabeçalho)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-PATTERNS.md §"EDA-03 (controller, figura + insight)" + §".eda-fig + .eda-insight (utility)"
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-CONTEXT.md §D-66 (gráfico) + §D-66a (insight) + §D-66d (rodapé) + §D-66e (caveat NÃO entra no slide)
  </read_first>
  <action>
    Duas edições atômicas:

    PARTE A — Anexar bloco CSS ao fim de apresentacao/assets/theme-unifacens.css (após o bloco `.eda-grid` da Task 1 do plan 03; preservar newline final Unix). Conteúdo conforme 03-RESEARCH §6.5 / 03-PATTERNS §.eda-fig:

    Comentário-cabeçalho de bloco: "SLIDE · EDA-03 · scatter PCA + insight em destaque. `.eda-fig` envolve o PNG (centraliza, limita altura). `.eda-insight` é a frase de destaque (Arial bold 23px). `.eda-subinsight` é o subtítulo secundário (Arial 19px cinza, opcional)."

    Regras:
    - `.eda-fig` { margin 16px auto 0; max-width 78%; display flex; justify-content center; }
    - `.eda-fig img` { width 100%; height auto; max-height 460px; object-fit contain; }
    - `.eda-insight` { margin 20px auto 0; max-width 92%; text-align center; font-family Arial; font-size 23px; font-weight 700; color var(--uni-ink); line-height 1.35; }
    - `.eda-subinsight` { margin-top 10px; font-size 19px; color #5b6472; text-align center; font-weight 400; }

    Gates CSS: variáveis CSS existentes (`var(--uni-ink)`); sem em-dash no comentário (D-70); `text-align: center` no insight é exceção legítima ao left-align padrão (não tentar herdar de `.rel-lead`).

    PARTE B — Inserir section EDA-03 em apresentacao/index.html no slot do gap (D-60 ordem final: EDA-01 → EDA-02 → EDA-03 → MARKER-02; portanto EDA-03 entra entre EDA-02 e MARKER-02 já inseridos). Indentação 6 espaços, EOL Unix.

    Markup conforme 03-RESEARCH §7.3 / 03-PATTERNS §EDA-03:

    1. Comentário: `<!-- ============ SLIDE · EDA-03 · Três perfis de alunos, scatter PCA (CSEDM Spring 2019) ============ -->`
    2. `<section data-background-color="#F1F6FB">` + `<div class="deck-slide slide-related">` + marca d'água `<svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>`.
    3. Cabeçalho (D-63c, recomendado 03-RESEARCH §7.3 variante (b)): `<p class="deck-topic"><span class="ps1">&gt;</span>três jeitos de aprender<span class="caret blink"></span></p>`. Fallbacks: `perfis dos alunos` ou `quem tenta, quem desiste`.
    4. Figura: `<figure class="eda-fig"><img src="assets/eda-perfis-pca.png" alt="Scatter PCA 2D dos 3 perfis de alunos (Alto desempenho, Médio, Em risco)"></figure>`. Importante: src `assets/eda-perfis-pca.png` (relativo ao index.html), NÃO `../results/sec2_perfis_pca.png` (Pitfall 6).
    5. Insight central (D-66a, frase única): `<p class="eda-insight">O grupo majoritário não é quem erra muito; é quem tenta pouco.</p>`. Variantes aceitas D-66a: "O grupo em risco é maioritário e tenta pouco, não erra muito." ou "Maioria (~55%) acerta quando tenta. O risco é o desengajamento."

    Nota sobre omissão de percentual no insight: a variante recomendada acima omite "(~55%)" conscientemente — D-66a aceita explicitamente as três variantes, e a frase central ganha impacto retórico sem o número entre parênteses. No checkpoint visual (Task 4), o reviewer humano pode decidir adicionar percentual real calculado a partir do output do script da Task 2: cell 46 do notebook 01_eda.ipynb (origem da lógica) reporta 124/239 = 51,9% no perfil "Em risco"; portanto, se reviewer optar pela versão quantitativa, usar "~52%" (e não "~55%" da minuta original D-66a). Decisão final fica registrada no SUMMARY.
    6. Subtítulo opcional: `<p class="rel-lead eda-subinsight">Em risco no CSEDM tem alta taxa de acerto eventual mas poucas tentativas por <i>assignment</i> (2 a 4).</p>`. Reviewer pode pedir remoção no checkpoint se ficar denso.
    7. Rodapé (D-66d literal): `<p class="rel-cite">Fonte: análise sobre CSEDM (Spring 2019); <i>K-Means</i> k=3 com SEED=42.</p>`.
    8. Fechar `</div>` e `</section>`.

    Gates de redação obrigatórios (per 03-PATTERNS §EDA-03):
    - PNG existe em apresentacao/assets/eda-perfis-pca.png (validar antes — Task 2 deveria ter copiado)
    - `grep -E 'src=.*results' apresentacao/index.html` retorna vazio (Pitfall 6)
    - Sem `<blockquote>` (D-69), sem em-dash (D-70)
    - NÃO mencionar Shi nem Code-DKT (cluster é nosso, gate forte)
    - NÃO incluir caveat silhouette/k=3 vs k=2 (D-66e; nota privada para banca)
    - `<i>assignment</i>`, `<i>K-Means</i>` em itálico minúsculas (D-72)
    - Insight em UMA frase (não 2, não 3); voz neutra sem "vemos que..."
    - Sem `<h2>` nem nome de autor (D-03)
  </action>
  <verify>
    <automated>grep -c '\.eda-fig\|\.eda-insight\|\.eda-subinsight' apresentacao/assets/theme-unifacens.css | grep -qE '^[5-9]$' &amp;&amp; grep -c '> três jeitos de aprender\|> perfis dos alunos\|> quem tenta' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c '<figure class="eda-fig">' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c 'src="assets/eda-perfis-pca.png"' apresentacao/index.html | grep -q '^1$' &amp;&amp; ! grep -E 'src=.*results' apresentacao/index.html &amp;&amp; grep -c 'K-Means</i> k=3 com SEED=42' apresentacao/index.html | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.eda-fig\|\.eda-insight\|\.eda-subinsight' apresentacao/assets/theme-unifacens.css` retorna entre 5 e 9
    - Comentário de bloco no formato `===...===` acima do bloco
    - `grep -c 'SLIDE · EDA-03' apresentacao/index.html` retorna 1
    - `grep -c 'eda-fig\|eda-insight\|eda-subinsight' apresentacao/index.html` retorna entre 3 e 5
    - `grep -c 'src="assets/eda-perfis-pca.png"' apresentacao/index.html` retorna 1
    - `grep -cE 'src=.*results' apresentacao/index.html` retorna 0 (Pitfall 6)
    - `grep -c 'K-Means' apresentacao/index.html` retorna 1 (rodapé EDA-03)
    - `grep -c 'SEED=42' apresentacao/index.html` retorna 1
    - `awk '/EDA-03/,/<\/section>/' apresentacao/index.html | grep -cE 'Shi|Code-DKT'` retorna 0
    - `awk '/EDA-03/,/<\/section>/' apresentacao/index.html | grep -c '—'` retorna 0
    - `awk '/EDA-03/,/<\/section>/' apresentacao/index.html | grep -cE 'silhouette|k=2'` retorna 0 (caveat fora do slide, D-66e)
    - Total de sections em index.html agora é 20 (16 originais + 4 da fase 3, após este plan e os outros 3 já estarem em HEAD)
  </acceptance_criteria>
  <done>
    CSS .eda-fig/.eda-insight anexado; section EDA-03 inserido com figura, insight, subtítulo e rodapé. Pronto para checkpoint visual.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint visual EDA-03 + validação fim a fim do deck</name>
  <what-built>
    Slide EDA-03 em apresentacao/index.html com figura PNG do scatter PCA (3 clusters K-Means coloridos, SEED=42), insight central em destaque ("O grupo majoritário não é quem erra muito; é quem tenta pouco."), subtítulo opcional, e rodapé com K-Means + SEED. CSS .eda-fig/.eda-insight/.eda-subinsight anexado em theme-unifacens.css. PNG gerado por script Python reproduzível.

    Concurrentemente, validação fim a fim do deck: 20 sections esperados, navegação #/0 → #/19 sem quebra, slide-code agora em #/15.
  </what-built>
  <how-to-verify>
    1. Em terminal: `cd apresentacao && python3 -m http.server 8001` (porta 8001 para forçar reload do CSS).

    2. Validação do EDA-03 em #/13:
       - Cabeçalho `> três jeitos de aprender` (ou fallback escolhido) em Cascadia 24px cinza com caret piscando.
       - Figura PCA scatter centralizada, max-width 78%, max-height 460px, sem distorção (object-fit contain).
       - 3 grupos visíveis no scatter:
         - Azul UniFacens `#2667FF` ≈ "Alto desempenho" (~96 alunos).
         - Âmbar `#F2A516` ≈ "Médio" (~19 alunos).
         - Vermelho `#D7191C` ≈ "Em risco" (~124 alunos).
       - Legenda canto inferior esquerdo, formato `Perfil (n=N)`.
       - Eixos rotulados "PC1" / "PC2".
       - Insight central em destaque: "O grupo majoritário não é quem erra muito; é quem tenta pouco." em Arial bold 23px, cor `--uni-ink`, centralizado, line-height 1.35.
       - Subtítulo abaixo (se mantido): "Em risco no CSEDM tem alta taxa de acerto eventual mas poucas tentativas por *assignment* (2 a 4)." em Arial 19px cinza centralizado.
       - Rodapé: "Fonte: análise sobre CSEDM (Spring 2019); *K-Means* k=3 com SEED=42." em Arial 18px cinza, K-Means em itálico.
       - Marca d'água Facens canto superior direito.
       - Layout cabe em 1280×720 sem overflow.

    3. Validação fim a fim do deck (D-74 herdado):
       - Total de sections em `<div class="slides">` = 20 (confirmar via `grep -c '<section' apresentacao/index.html`).
       - Percorrer #/0 → #/19 com setas do reveal.js.
       - Verificar mapa final de slides (ordem D-60):
         - #/0..#/9: inalterados desde pós-fase 2.
         - #/10: MARKER-01 (slide-marker--phase1).
         - #/11: EDA-01.
         - #/12: EDA-02.
         - #/13: EDA-03.
         - #/14: MARKER-02 (slide-marker--phase2).
         - #/15: slide-code (deslocado de #/11 para #/15, conforme 03-CONTEXT D-61).
         - #/16: slide-kcfig.
         - #/17: slide-problem (Martins p2).
         - #/18: slide-problem (Martins p3).
         - #/19: slide-fig.
       - Console do browser sem erros durante navegação completa.

    4. Confirmar AUSÊNCIAS no EDA-03:
       - Não menciona "Shi" nem "Code-DKT" (cluster é nosso).
       - Sem caveat silhouette / k=2 vs k=3 (D-66e; reservado para banca).
       - Sem em-dash.
       - Sem `<h2>` nem nome de autor.
       - PNG não referencia `results/` direto (src é `assets/eda-perfis-pca.png`).
  </how-to-verify>
  <resume-signal>Digite "approved" se todos os checks passam ou descreva ajustes (cores do scatter, troca da frase do insight, remoção do subtítulo, troca de cabeçalho) para iteração. Padrão fase 2: 1-2 iterações por slide.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Atualizar STYLE.md §Inventário + §Gaps reservados pós-fase 3</name>
  <read_first>
    - apresentacao/STYLE.md linhas 108-132 (§Inventário pós-fase 2 + §Gaps reservados — texto a substituir)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md §8.1 (texto novo pronto do §Inventário) + §8.2 (texto novo do §Gaps)
  </read_first>
  <action>
    Atualizar duas seções de apresentacao/STYLE.md conforme 03-RESEARCH §8:

    PARTE A — §Inventário de slides (linhas 108-125):
    1. Renomear título de "## Inventário de slides (ordem atual, pós-fase 1)" (linha 108) para "## Inventário de slides (ordem atual, pós-fase 3)".
    2. Substituir a tabela existente (12 linhas) por uma tabela com 20 linhas conforme 03-RESEARCH §8.1, refletindo:
       - #/0..#/10 inalterados (cabeçalho corrige #/7 INTRO-01, #/8 INTRO-03a, #/9 INTRO-03b, #/10 MARKER-01 que já entraram na fase 2 mas a tabela em HEAD do STYLE.md ainda mostra estado pós-fase 1)
       - Linhas novas #/11 a #/14: EDA-01 / EDA-02 / EDA-03 / MARKER-02
       - Linhas deslocadas #/15 a #/19: slide-code / slide-kcfig / Martins p2 / Martins p3 / slide-fig
    3. Atualizar parágrafo "Estado do deck:" (linha 125) de "12 slides após a fase 1" para "20 slides após a fase 3 (era 16 pós-fase 2; 4 novos da fase 3 inseridos no gap após MARKER-01)".

    PARTE B — §Gaps reservados (linhas 127-132):
    1. Remover a linha 130 do HEAD ("Após MARKER-01 e antes do trio Martins+fig: EDA-01, EDA-02, EDA-03 + MARKER-02 (fase 3).") porque o gap deixou de existir.
    2. Reorganizar os gaps remanescentes para fases 4-5, mantendo as referências aos slides existentes (MODEL-01..05, CLOSE-01/02/03, MARKER-03, TOOL-01/03, MARKER-04, END-01, AGENDA-01).
    3. Atualizar lead "Gaps reservados para fases 2-5" (linha 127) para "Gaps reservados para fases 4-5".

    Gates: sem em-dash em texto novo; sentenças do §Gaps refletem que o gap da fase 3 foi consumido; indices da tabela §Inventário batem com estado real do index.html pós-inserção (20 sections, slide-code em #/15).
  </action>
  <verify>
    <automated>grep -c 'pós-fase 3' apresentacao/STYLE.md | grep -qE '^[1-2]$' &amp;&amp; [ "$(grep -c 'pós-fase 1' apresentacao/STYLE.md)" = "0" ] &amp;&amp; grep -c 'EDA-01\|EDA-02\|EDA-03\|MARKER-02' apresentacao/STYLE.md | grep -qE '^[4-9]$' &amp;&amp; ! grep -q 'Após MARKER-01 e antes do trio Martins+fig: EDA-01, EDA-02, EDA-03 + MARKER-02' apresentacao/STYLE.md &amp;&amp; grep -c '20 slides após a fase 3' apresentacao/STYLE.md | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pós-fase 3' apresentacao/STYLE.md` retorna ≥ 1
    - `grep -c 'pós-fase 1' apresentacao/STYLE.md` retorna 0 (título antigo substituído; baseline HEAD = 1)
    - `grep -c '20 slides' apresentacao/STYLE.md` retorna ≥ 1
    - Tabela §Inventário tem 20 linhas de dados (não contando header)
    - Linha original "Após MARKER-01 e antes do trio Martins+fig: EDA-01, EDA-02, EDA-03 + MARKER-02 (fase 3)" foi REMOVIDA
    - Lead "Gaps reservados" referencia fases 4-5, não 2-5
    - `grep -c '—' apresentacao/STYLE.md | head -1` retorna 0 nas linhas novas (verificar com awk se necessário)
  </acceptance_criteria>
  <done>
    STYLE.md §Inventário atualizado para refletir 20 sections; §Gaps reservados com gap da fase 3 consumido. Pronto para commit final da fase.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma (deck estático + script Python local one-shot) | Script lê CSVs locais (early.csv, late.csv, Subject.csv); gera PNG local; sem rede, sem auth, sem persistência além de filesystem local |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Information Disclosure | scripts/build_eda_pca_scatter.py | accept | CSVs são dados anonimizados do CSEDM público (SubjectID já é hash); script não expõe nada além do que já está no notebook 01 |
| T-03-04-02 | Tampering | results/sec2_perfis_pca.png | accept | PNG gerado de forma reprodutível via SEED=42; alteração inadvertida re-gerável; commitar PNG ao git como asset |
| T-03-04-03 | Tampering | apresentacao/index.html + theme-unifacens.css + STYLE.md | accept | Markup, CSS e docs estáticos; alteração inadvertida detectada via `git diff` |
| T-03-04-04 | Denial of Service | nenhum | accept | Reveal.js carrega assets locais; PNG <100KB; sem risco de DoS |

**ASVS L1 evaluation:** N/A — deck estático + script Python one-shot local. Dataset CSEDM é público; sem PII direta (SubjectID hash). Sem auth, sem input externo, sem rede.
</threat_model>

<verification>
- Script existe, SEED=42 em 4 pontos, paleta UniFacens
- PNG gerado em results/ + copiado para apresentacao/assets/
- CSS .eda-fig/.eda-insight/.eda-subinsight em theme-unifacens.css
- Section EDA-03 em index.html com figura + insight + rodapé
- Gates de ausência: sem Shi, sem Code-DKT, sem caveat silhouette, sem em-dash, sem ref a results/ no slide
- STYLE.md §Inventário pós-fase 3 com 20 sections; §Gaps com gap consumido removido
- Checkpoint visual EDA-03 + validação fim a fim 20 sections APPROVED
- PENDING-02 marcado como resolvido (scatter PCA com 3 clusters K-Means)
</verification>

<success_criteria>
- Slide EDA-03 visível em #/13 com scatter PCA legível e insight central
- Deck navega #/0 → #/19 sem quebra; slide-code agora em #/15
- 20 sections em `<div class="slides">`
- STYLE.md sincronizado com estado real
- Commits funcionais: `apresentacao: slide EDA-03 - perfis dos alunos (scatter PCA com SEED=42)` (junto com script + PNG conforme Open Question 1) + `docs(style): atualizar inventário e gaps pós-fase 3`
</success_criteria>

<output>
After completion, create `.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-04-SUMMARY.md` registrando:
- Cabeçalho final escolhido para EDA-03 (a/b/c do 03-RESEARCH §7.3)
- Contagem REAL por perfil capturada pelo print do script (Alto/Médio/Em risco)
- Iterações pós-checkpoint (esperado 1-2)
- Decisão sobre subtítulo (.eda-subinsight) mantido ou removido
- Linhas exatas adicionadas em theme-unifacens.css
- Tamanho final do PNG (KB)
- Posição final do EDA-03 no deck e estado fim a fim (20 sections, slide-code em #/15)
- Confirmação de resolução de PENDING-02 (gráfico de insight = scatter PCA com 3 clusters K-Means)
- PHASE-SUMMARY agregando os 4 plans (criar `.planning/phases/03-eda-e-pr-processamento-fase-2-edm/PHASE-SUMMARY.md` se for o último plan da fase)
- Decisões ad-hoc (D-XX) registradas para fase 4
</output>
