---
phase: 03-eda-e-pr-processamento-fase-2-edm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apresentacao/index.html
autonomous: false
requirements:
  - MARKER-02
must_haves:
  truths:
    - "Slide MARKER-02 carrega no browser sem erro de console em #/14 após inserção"
    - "Pill 1 (Definição do Problema) e pill 2 (Preparação dos Dados) aparecem com check em fundo azul UniFacens"
    - "Pill 3 (Modelagem e Avaliação) exibe ícone reload girando suavemente (animação marker-spin)"
    - "Pill 4 (Implantação) aparece com círculo cinza estático em ring cinza"
    - "Badges [done] [done] [running] aparecem abaixo das pills 1-3; pill 4 sem badge visível"
    - "Rodapé 'Fonte: adaptado de Zorić (2020).' centralizado abaixo do track"
    - "Zero linhas adicionadas em apresentacao/assets/theme-unifacens.css (D-67d)"
    - "MARKER-01 (slide #/10) permanece inalterado"
    - "D-60: MARKER-02 entra na ordem prescrita (EDA-01 → EDA-02 → EDA-03 → MARKER-02), ocupando a posição final do bloco de 4 inserções após MARKER-01 (#/10) e antes do slide-code"
    - "D-62: sequência narrativa MARKER-01 (Fase 1 ✓) → EDA-01/02/03 → MARKER-02 (Fase 2 ✓) preservada; MARKER-02 fecha a Fase 2 EDM com Modelagem em running"
    - "D-67: MARKER-02 é implementação mecânica do componente .slide-marker (commit 5d44606); apenas modificadores de pills mudam vs MARKER-01"
  artifacts:
    - path: apresentacao/index.html
      provides: "Section MARKER-02 inserida após linha 243"
      contains: "slide-marker--phase2"
  key_links:
    - from: "apresentacao/index.html section MARKER-02"
      to: "apresentacao/assets/theme-unifacens.css"
      via: "classes .slide-marker + .marker-pill--done/--running/--pending + animação marker-spin"
      pattern: "class=.deck-slide slide-marker slide-marker--phase2."
---

<objective>
Inserir o slide MARKER-02 ("Preparação dos Dados ✓") em apresentacao/index.html, posicionado entre o MARKER-01 (linhas 200-243) e o slide-code (linha 245+), reusando integralmente o componente .slide-marker redesenhado em commit 5d44606 (D-67). O slide fecha visualmente a Fase 2 da EDM (Preparação dos Dados) e abre a Fase 3 (Modelagem e Avaliação) em execução.

Purpose: cumprir a sequência narrativa da Fase 2 EDM. MARKER-01 fecha a Fase 1; MARKER-02 fecha a Fase 2 e sinaliza Fase 3 em andamento, mantendo o padrão visual do pipeline CI/CD ABNT-friendly.

Output: 1 section novo (~44 linhas HTML) inserido no slot canônico do gap §Gaps reservados linha 130 do STYLE.md. Zero CSS novo. 1 commit funcional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-CONTEXT.md
@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md
@.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-PATTERNS.md

@apresentacao/STYLE.md
@apresentacao/index.html
@apresentacao/assets/theme-unifacens.css

<interfaces>
<!-- Classes/identifiers consumidos pelo MARKER-02. Extraídos do CSS theme-unifacens.css linhas 359-453 + index.html linhas 200-243. -->

CSS já pronto (NÃO editar):
- `.slide-marker` (host)
- `.slide-marker--phase1` / `.slide-marker--phase2` (modificadores semânticos)
- `.marker-title` (título superior em Arial bold 24px com caret)
- `.marker-track` (container horizontal das 4 pills)
- `.marker-stage` (cada pill + badge)
- `.marker-pill` + `.marker-pill--done` / `.marker-pill--running` / `.marker-pill--pending`
- `.marker-pill-icon` (ícone &check; / &#x21BB; / &#x25CB;)
- `.marker-pill-name` (nome da fase)
- `.marker-badge` + `.marker-badge--empty` (esconde via visibility:hidden)
- `.marker-arrow` (`&rarr;` entre pills)
- `.rel-cite` herdado (Fonte: ...)
- Animação `@keyframes marker-spin` aplicada via `.marker-pill--running .marker-pill-icon` linhas 419-423

HTML análogo literal (MARKER-01, linhas 200-243 de index.html, copiar e aplicar 4 deltas):
- Estrutura externa: `<section data-background-color="#F1F6FB"><div class="deck-slide slide-marker slide-marker--phase1">...</div></section>`
- Marca d'água: `<svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>`
- Título: `<p class="marker-title"><span class="ps1">&gt;</span>AS QUATRO FASES DA EDM<span class="caret blink"></span></p>`
- Pill 1 (done): `&check;` + nome "Definição do Problema" + badge `[done]`
- Pill 2 HEAD: `--running` + `&#x21BB;` + "Preparação dos Dados" + badge `[running]`
- Pill 3 HEAD: `--pending` + `&#x25CB;` + "Modelagem e Avaliação" + badge `marker-badge--empty []`
- Pill 4 (pending, inalterada): `&#x25CB;` + "Implantação" + badge `marker-badge--empty []`
- Rodapé: `<p class="rel-cite">Fonte: adaptado de Zorić (2020).</p>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inserir section MARKER-02 com 4 deltas mecânicos vs MARKER-01 HEAD</name>
  <read_first>
    - apresentacao/index.html linhas 200-243 (markup completo do MARKER-01 a clonar)
    - apresentacao/index.html linhas 244-246 (boundary do slide-code; ponto de inserção é entre linha 243 e linha 245)
    - apresentacao/assets/theme-unifacens.css linhas 359-453 (CSS .slide-marker completo, só para confirmar que classes existem; NÃO editar)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-RESEARCH.md §5.2 e §5.3 (tabela dos 4 deltas + markup-alvo literal copy-paste)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-PATTERNS.md §"MARKER-02 (controller, reuso mecânico)" (deltas vs análogo)
    - .planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-CONTEXT.md §D-67 (modificadores oficiais)
  </read_first>
  <action>
    Inserir novo `<section>` entre linha 243 (`</section>` do MARKER-01) e linha 245 (comentário do slide-code) de apresentacao/index.html, preservando indentação de 6 espaços para `<section>` raiz e EOL Unix. O markup é cópia literal do MARKER-01 (linhas 200-243) com exatamente 4 deltas mecânicos conforme 03-RESEARCH §5.2 e 03-PATTERNS §"MARKER-02":

    Delta 1 (comentário linha 200): "MARKER · As quatro fases da EDM, fase 1 concluida (Zoric, 2020)" → "MARKER · As quatro fases da EDM, fase 2 concluida (Zoric, 2020)" (manter "Zoric" sem diacrítico por consistência com o comentário existente; drive-by para "Zorić" diferido conforme Open Question 5).

    Delta 2 (classe do `<div>` interno linha 202): `slide-marker slide-marker--phase1` → `slide-marker slide-marker--phase2`.

    Delta 3 (pill 2 "Preparação dos Dados", linhas 217-221): trocar 3 elementos juntos — `marker-pill marker-pill--running` → `marker-pill marker-pill--done`, ícone `&#x21BB;` → `&check;`, e badge `<span class="marker-badge">[running]</span>` → `<span class="marker-badge">[done]</span>`.

    Delta 4 (pill 3 "Modelagem e Avaliação", linhas 224-229): trocar 3 elementos juntos — `marker-pill marker-pill--pending` → `marker-pill marker-pill--running`, ícone `&#x25CB;` → `&#x21BB;`, e badge `<span class="marker-badge marker-badge--empty">[]</span>` → `<span class="marker-badge">[running]</span>`.

    Pill 1 (Definição, linhas 208-214) e Pill 4 (Implantação, linhas 232-238) ficam idênticas ao MARKER-01. Rodapé `<p class="rel-cite">Fonte: adaptado de Zorić (2020).</p>` linha 241 idêntico. Marca d'água Facens `<svg class="wm">` idêntica.

    NÃO editar apresentacao/assets/theme-unifacens.css (D-67d: zero CSS novo). Markup completo final em 03-RESEARCH §5.3 e 03-PATTERNS Code Example 2 — usar como referência literal de copy-paste.

    Após inserção: total de sections em `<div class="slides">` passa de 16 para 17 (ainda só este plan); ao fim dos 4 plans serão 20. Slide MARKER-02 estará acessível em #/11 enquanto este é o único plan executado (sobe para #/14 quando EDA-01/02/03 entrarem antes).
  </action>
  <verify>
    <automated>grep -c 'slide-marker--phase2' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c 'slide-marker--phase1' apresentacao/index.html | grep -q '^1$' &amp;&amp; grep -c 'marker-pill--done' apresentacao/index.html | grep -q '^3$' &amp;&amp; grep -c 'marker-pill--running' apresentacao/index.html | grep -q '^2$' &amp;&amp; grep -c 'Fonte: adaptado de Zorić' apresentacao/index.html | grep -q '^2$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'slide-marker--phase2' apresentacao/index.html` retorna 1
    - `grep -c 'slide-marker--phase1' apresentacao/index.html` retorna 1 (MARKER-01 inalterado)
    - `grep -c 'marker-pill marker-pill--done' apresentacao/index.html` retorna 3 (1 do MARKER-01 + 2 do MARKER-02)
    - `grep -c 'marker-pill marker-pill--running' apresentacao/index.html` retorna 2 (1 do MARKER-01 + 1 do MARKER-02)
    - `grep -c 'marker-pill marker-pill--pending' apresentacao/index.html` retorna 3 (2 do MARKER-01 + 1 do MARKER-02)
    - `grep -c 'Fonte: adaptado de Zorić (2020)' apresentacao/index.html` retorna 2 (MARKER-01 + MARKER-02)
    - `grep -c 'fase 2 concluida' apresentacao/index.html` retorna 1
    - `grep -E "src=.*results|—" apresentacao/index.html | grep -E '(MARKER-02|fase 2 concluida)'` retorna vazio (sem em-dash; sem ref a results/)
    - `git diff --stat apresentacao/assets/theme-unifacens.css` retorna vazio (zero CSS novo, D-67d)
    - `grep -n '<section' apresentacao/index.html | wc -l` aumentou em 1 vs HEAD pré-task
  </acceptance_criteria>
  <done>
    Section MARKER-02 inserido entre linhas 243 e 245 do index.html com os 4 deltas conforme 03-RESEARCH §5.2. Zero alteração no CSS. Comentário, indentação e EOL Unix preservados. Pronto para o checkpoint visual da Task 2.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint visual MARKER-02 no browser</name>
  <what-built>
    Section MARKER-02 inserido em apresentacao/index.html após MARKER-01, reusando o componente .slide-marker (zero CSS novo). Pills 1 e 2 em --done com check; pill 3 em --running com reload girando; pill 4 em --pending com círculo cinza. Badges `[done] [done] [running]`. Rodapé "Fonte: adaptado de Zorić (2020)."
  </what-built>
  <how-to-verify>
    1. Em terminal: `cd apresentacao && python3 -m http.server 8000` (porta 8000; se já estiver ocupada, subir em 8001).
    2. Abrir http://127.0.0.1:8000/#/11 no browser (este é o índice do MARKER-02 enquanto este é o único plan executado; quando EDA-01/02/03 forem inseridos antes dele, sobe para #/14).
    3. Confirmar visualmente:
       - Título "&gt; AS QUATRO FASES DA EDM" em Arial bold 24px com caret piscando ao lado.
       - 4 pills em linha horizontal, separadas por setas `→` pretas.
       - Pill 1 ("Definição do Problema"): fundo azul UniFacens com check branco.
       - Pill 2 ("Preparação dos Dados"): fundo azul UniFacens com check branco (NÃO mais reload girando como em MARKER-01).
       - Pill 3 ("Modelagem e Avaliação"): ícone reload `&#x21BB;` em azul UniFacens, girando suavemente (animação marker-spin 2.4s linear infinite).
       - Pill 4 ("Implantação"): círculo cinza estático em ring cinza.
       - Linha de badges abaixo das pills: `[done]  [done]  [running]` (pill 4 sem badge visível).
       - Rodapé centralizado: "Fonte: adaptado de Zorić (2020)."
       - Marca d'água Facens no canto superior direito.
    4. Voltar para #/10 (MARKER-01) e confirmar que continua inalterado: pill 1 done com check, pill 2 running girando, pills 3-4 pending com círculo.
    5. Console do browser sem erros (DevTools → Console).
    6. Navegação fluida entre #/10 → #/11 (MARKER-01 → MARKER-02) sem layout quebrado.
  </how-to-verify>
  <resume-signal>Digite "approved" se o slide passa todos os checks visuais ou descreva discrepâncias (cor da pill, animação não roda, badge fora do lugar, etc.) para iteração.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nenhuma (deck estático) | Slides reveal.js estáticos servidos via `python3 -m http.server`; sem inputs externos, sem auth, sem persistência |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | apresentacao/index.html | accept | Conteúdo é estático em pt-BR controlado pelo autor; sem dados de usuário; alteração inadvertida detectada via `git diff` no checkpoint visual |
| T-03-01-02 | Information Disclosure | nenhum | accept | Não há informação sensível; números do dataset são públicos (CSEDM 2021) |

**ASVS L1 evaluation:** N/A — deck estático sem auth, input externo nem armazenamento. Superfície de ameaça mínima.
</threat_model>

<verification>
- `grep -c 'slide-marker--phase2' apresentacao/index.html` = 1
- `grep -c 'slide-marker--phase1' apresentacao/index.html` = 1 (preservado)
- `git diff --stat apresentacao/assets/theme-unifacens.css` vazio (D-67d)
- Browser #/11 (ou #/14 após outros plans): pill 1+2 done, pill 3 running, pill 4 pending; badges corretos; rodapé Zorić centralizado
- Checkpoint visual aprovado pelo reviewer humano
</verification>

<success_criteria>
- Section MARKER-02 inserido após linha 243 com os 4 deltas mecânicos exatos.
- Zero linhas adicionadas em apresentacao/assets/theme-unifacens.css.
- Checkpoint visual APPROVED.
- Commit funcional: `apresentacao: slide MARKER-02 - preparação dos dados ✓ (zero CSS novo)`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-eda-e-pr-processamento-fase-2-edm/03-01-SUMMARY.md` registrando:
- Linhas exatas onde MARKER-02 foi inserido (offset relativo ao HEAD pré-task)
- Iterações pós-checkpoint, se houver
- Confirmação de zero CSS novo
- Posição final no deck (#/14 após todos os plans da fase 3 entrarem, ou #/11 se for o único plan executado neste momento)
</output>
