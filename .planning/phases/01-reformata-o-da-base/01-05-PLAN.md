---
phase: 01-reformata-o-da-base
plan: 05
type: execute
wave: 1
depends_on: ["01-04"]
files_modified:
  - apresentacao/index.html
autonomous: true
requirements:
  - REFORMAT-02
must_haves:
  truths:
    - "Slide Zorić p3 (slide-phases) tem cabeçalho `> as quatro fases da edm` como única linha de cabeçalho"
    - "O `<h2 class=\"phases-title\">As quatro fases do processo de EDM</h2>` que existia dentro de `.phases-head` foi removido"
    - "A lista ordenada `.phases-list` com os 4 itens (Definição do problema / Preparação e coleta dos dados / Modelagem e avaliação / Implantação) é preservada intacta"
    - "A nota `.phases-note` (O processo é iterativo...) e o rodapé `Fonte: Zorić (2020).` são preservados"
    - "Deck navega fim-a-fim sem erro de console"
  artifacts:
    - path: "apresentacao/index.html"
      provides: "slide-phases reformatado"
      contains: "exatamente 1 ocorrência de '> as quatro fases da edm'"
  key_links:
    - from: "slide-phases"
      to: "cabeçalho único"
      via: ".phases-head sem <h2>"
      pattern: "as quatro fases da edm com caret blink"
---

<objective>
**Reformatar o slide Zorić p3** (`slide-phases`, atualmente vem logo após o slide Zorić fundido, criado no plan 02) trocando o texto do `.deck-topic` interno de `> trabalhos correlatos` para `> as quatro fases da edm` e **removendo o `<h2 class="phases-title">As quatro fases do processo de EDM</h2>`** que vive dentro do wrapper `.phases-head`.

Purpose: o slide já tem `.deck-topic` (o markup do "depois" já está implementado), apenas com o texto antigo. A reformatação é estrutural mínima: trocar o texto do tópico e apagar o `<h2>` redundante. O título "As quatro fases do processo de EDM" deixa de existir como `<h2>` porque o próprio cabeçalho temático `> as quatro fases da edm` já comunica isso, e a `.phases-list` numerada que segue já entrega os 4 itens.

Output: 2-3 linhas alteradas em `apresentacao/index.html`; resto do slide intacto; deck continua navegável; commit atômico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-reformata-o-da-base/01-CONTEXT.md
@.planning/phases/01-reformata-o-da-base/01-PATTERNS.md
@.planning/phases/01-reformata-o-da-base/SKELETON.md
@./CLAUDE.md
@apresentacao/STYLE.md
@apresentacao/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reformatar slide-phases (Zorić p3) com cabeçalho `> as quatro fases da edm`</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html linhas 325-345 (estado atual do slide-phases; localizar pela presença de `<div class="phases-head">` e `<h2 class="phases-title">As quatro fases do processo de EDM</h2>`)
    - .planning/phases/01-reformata-o-da-base/01-PATTERNS.md § "Padrão 'antes' #2 — .deck-topic + <h2> interno" (variante do slide-phases) e § "Cabeçalho-alvo único"
    - .planning/phases/01-reformata-o-da-base/01-CONTEXT.md D-03, D-05
    - apresentacao/STYLE.md
  </read_first>
  <action>
**No slide-phases** (atualmente linhas 325-345 do `apresentacao/index.html`, identificável por `<div class="deck-slide slide-phases">`):

**Atualizar o comentário** acima da `<section>`:
- Substituir: `<!-- ============ SLIDE 5 · As 4 fases do EDM (fluxo horizontal formal · Zorić, 2020) ============ -->`
- Por: `<!-- ============ SLIDE · As 4 fases da EDM (Zorić, 2020) ============ -->`
- (O "SLIDE 5" virou impreciso após os DOM moves dos plans 02/06; tirar o número fixo.)

**Dentro de `<div class="deck-slide slide-phases">`, dentro do wrapper `<div class="phases-head">`:**

DELETAR (linhas 331-332 do estado atual):
```
            <p class="deck-topic"><span class="ps1">&gt;</span>trabalhos correlatos<span class="caret blink"></span></p>
            <h2 class="phases-title">As quatro fases do processo de EDM</h2>
```

INSERIR no mesmo lugar (mesma indentação de 12 spaces):
```
            <p class="deck-topic"><span class="ps1">&gt;</span>as quatro fases da edm<span class="caret blink"></span></p>
```

**Restrições obrigatórias:**

- Texto exato dentro do `.deck-topic`: `as quatro fases da edm` (tudo minúsculo, "edm" minúsculo segue convenção `> [seção]` mono Cascadia; STYLE.md "Tipografia").
- Caret piscante `<span class="caret blink"></span>` como ÚLTIMO filho do `<p class="deck-topic">`.
- O wrapper `<div class="phases-head">...</div>` (linha 330 abertura, linha 333 fechamento) é MANTIDO — ele controla margem/posicionamento do cabeçalho contra a `.phases-list`. Apenas o `<h2 class="phases-title">` interno é removido.
- A `<ol class="phases-list">` com os 4 `<li>` (Definição do problema / Preparação e coleta dos dados / Modelagem e avaliação / Implantação) é PRESERVADA intacta (linhas 335-340 atuais).
- A `<p class="phases-note">O processo é iterativo: a saída pode iniciar um novo ciclo (Zorić, 2020).</p>` é PRESERVADA (linha 342).
- Rodapé `<p class="phases-fonte">Fonte: Zorić (2020).</p>` é PRESERVADO (linha 343, D-23).
- `<svg class="wm">` preservado.
- Classe `slide-phases` no `<div>` preservada (D-03).

**Verificação visual após o smoke test:** o gap visual entre o tópico `> as quatro fases da edm` e o primeiro `<li>` da `.phases-list` precisa continuar legível. Se o `<h2>` removido estava criando um spacing extra que o `.phases-list` dependia, o `.phases-head` ou o `:first-child` da `.phases-list` pode precisar de ajuste. Verificar visualmente: se houver colapso visual desagradável, o ajuste de CSS (margin-top em `.phases-list` ou margin-bottom em `.phases-head`) é uma micro-task de continuação no mesmo commit. Se ficar OK visualmente, nada a ajustar.

**Commit:** `apresentacao: reformatar slide-phases com > as quatro fases da edm (REFORMAT-02)`.
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && grep -c '> as quatro fases da edm' apresentacao/index.html | grep -q '^1$' && grep -c '<h2 class="phases-title">' apresentacao/index.html | grep -q '^0$' && grep -c '<ol class="phases-list">' apresentacao/index.html | grep -q '^1$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '> as quatro fases da edm' apresentacao/index.html` retorna `1`
    - `grep -c '<h2 class="phases-title">' apresentacao/index.html` retorna `0` (h2 removido)
    - `grep -c '<ol class="phases-list">' apresentacao/index.html` retorna `1` (lista preservada)
    - `grep -c 'phases-head' apresentacao/index.html` retorna `2` (abertura + fechamento da div wrapper, preservada)
    - `grep -c 'phases-note' apresentacao/index.html` retorna `1` (nota "processo é iterativo" preservada)
    - `grep -c 'class="phases-fonte">Fonte: Zorić' apresentacao/index.html` retorna `1` (rodapé preservado)
    - Browser smoke test: navegar até o slide-phases, ver cabeçalho `> as quatro fases da edm` em Cascadia com caret, lista ordenada com 4 itens em Arial, nota em Arial menor, rodapé "Fonte: Zorić (2020)."; spacing visual aceitável; console sem erro
    - Commit em `git log --oneline -1`: `apresentacao: reformatar slide-phases com > as quatro fases da edm`
  </acceptance_criteria>
  <done>slide-phases reformatado; `<h2 class="phases-title">` removido; `.phases-list` intacta; deck navegável; commit atômico.</done>
</task>

</tasks>

<verification>
- Cabeçalho travado D-05: `> as quatro fases da edm` (1 ocorrência)
- `<h2 class="phases-title">` removido
- `.phases-list` preservada
- Browser smoke test OK
- Commit atômico criado
</verification>

<success_criteria>
- REFORMAT-02 satisfeito (Roadmap Success Criterion #3: slide Zorić p3 exibe `> as quatro fases da edm` com conteúdo das fases preservado)
- D-03 cumprido (h2 interno removido; só `.deck-topic` resta como cabeçalho)
- Deck navega fim-a-fim
</success_criteria>

<output>
Criar `.planning/phases/01-reformata-o-da-base/01-05-SUMMARY.md` com:
- Hash do commit
- Linha do novo `.deck-topic` no slide-phases
- Confirmação dos grep gates
- Próximo plan: 01-06 (REFORMAT-04 + REFORMAT-05a/b/c — DOM move dos 5 slides finais)
</output>
