---
phase: 01-reformata-o-da-base
plan: 07
type: execute
wave: 1
depends_on: ["01-06"]
files_modified:
  - apresentacao/STYLE.md
  - apresentacao/assets/theme-unifacens.css
autonomous: false
requirements:
  - REFORMAT-01
  - REFORMAT-02
  - REFORMAT-03
  - REFORMAT-04
  - REFORMAT-05
  - MERGE-01
  - REMOVE-01
must_haves:
  truths:
    - "apresentacao/STYLE.md tem a seção `## Cabeçalho de todo slide após a AGENDA` reescrita para descrever o `> [nome da seção]` único"
    - "apresentacao/STYLE.md NÃO contém mais o bullet `Regra dos correlatos: todo autor novo é introduzido em um slide > trabalhos correlatos`"
    - "apresentacao/STYLE.md tem a tabela `## Inventário de slides (ordem atual)` reescrita refletindo o estado pós-fase 1 (12 slides, Corbett removido, Zorić e Yağcí fundidos, 5 slides reposicionados, cabeçalhos `> [seção]` documentados)"
    - "Regras CSS órfãs de `.slide-related .rel-kicker`/`.rel-title`/`.rel-sub` em theme-unifacens.css foram REMOVIDAS (se e somente se grep confirmar que ficaram órfãs)"
    - "Browser smoke test final do deck inteiro passa: 12 slides navegáveis fim-a-fim, console DevTools sem erro"
  artifacts:
    - path: "apresentacao/STYLE.md"
      provides: "design contract atualizado refletindo fase 1"
      contains: "> [nome da seção] como cabeçalho único; inventário com 12 slides; nenhum 'Regra dos correlatos'"
    - path: "apresentacao/index.html"
      provides: "deck completo da fase 1, validado fim-a-fim"
      contains: "12 sections em ordem D-17 com cabeçalhos travados"
  key_links:
    - from: "apresentacao/STYLE.md"
      to: "apresentacao/index.html"
      via: "design contract documenta o novo padrão `.deck-topic` único que o markup adota"
      pattern: "STYLE.md refletindo o markup atual"
---

<objective>
Plan de fechamento da fase 1:

1. **Atualizar `apresentacao/STYLE.md`** (D-21) para refletir o novo padrão `> [seção]` único, remover a "Regra dos correlatos" e reescrever o inventário de slides.
2. **Opcional: limpar regras CSS órfãs** em `apresentacao/assets/theme-unifacens.css` (`.slide-related .rel-kicker`, `.rel-title`, `.rel-sub`) se o grep confirmar que viraram órfãs após os plans 01-06.
3. **Checkpoint humano final** validando o deck fim-a-fim no browser e atestando que TODOS os 8 Success Criteria do ROADMAP fase 1 estão satisfeitos.

Purpose: o STYLE.md é o design contract da apresentação inteira (fase 1-5). Mantê-lo desatualizado depois da fase 1 levaria as fases 2-5 a aplicar o padrão antigo. O CSS órfão é cosmético mas a higiene técnica vale a passada. O checkpoint final é o gate de saída da fase 1: a próxima fase só começa depois que o usuário confirma que o deck fim-a-fim funciona e os 8 Success Criteria batem.

Output: STYLE.md atualizado; CSS limpo se aplicável; checkpoint humano confirmado; commit(s) atômico(s).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-reformata-o-da-base/01-CONTEXT.md
@.planning/phases/01-reformata-o-da-base/01-PATTERNS.md
@.planning/phases/01-reformata-o-da-base/SKELETON.md
@./CLAUDE.md
@apresentacao/STYLE.md
@apresentacao/assets/theme-unifacens.css
@apresentacao/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar STYLE.md — seção "Cabeçalho de todo slide após a AGENDA"</name>
  <files>apresentacao/STYLE.md</files>
  <read_first>
    - apresentacao/STYLE.md linhas 37-52 (seção atual a substituir)
    - .planning/phases/01-reformata-o-da-base/01-PATTERNS.md § "Pattern Assignment para STYLE.md (D-21)" > "seção 'Cabeçalho de todo slide após a AGENDA'"
    - .planning/phases/01-reformata-o-da-base/01-CONTEXT.md D-21 (primeiro bullet)
  </read_first>
  <action>
**No `apresentacao/STYLE.md`, substituir** a seção `## Cabeçalho de todo slide após a AGENDA` (linhas 37-52 do arquivo atual) por:

```markdown
## Cabeçalho de todo slide após a AGENDA

Padrão obrigatório: **uma única linha de cabeçalho** com o nome da seção, em
formato `> [nome da seção]`, sem subtítulo e sem `<h2>` com nome do autor.

```html
<p class="deck-topic"><span class="ps1">&gt;</span>nome da seção<span class="caret blink"></span></p>
```

- Classe única: `.deck-topic`. Aplica a TODOS os slides de conteúdo (após a AGENDA),
  incluindo os que antes usavam o template `.slide-related` com par
  `.rel-kicker.kicker` + `.rel-title` + `.rel-sub`.
- Texto do cabeçalho: minúsculo, em Cascadia (`--mono`) 24px, cor `#5b6472`, com
  o `>` em azul UniFacens (`--uni-blue`). Exemplos travados na fase 1:
  `> introdução`, `> mineração de dados educacionais`, `> as quatro fases da edm`,
  `> da edm ao knowledge tracing`, `> retomando o problema`,
  `> kcs semânticos extraídos`, `> evolução por dificuldade`,
  `> o que o code-dkt olha`.
- Nome do autor **não aparece** no corpo, nem em `<h2>`, nem em `.rel-sub`. Migra
  para a linha "Fonte:" no rodapé do slide (ver `Convenções de citação`).
- Caret piscante (`<span class="caret blink">`) como último filho de
  `<p class="deck-topic">`.
- Cabeçalhos travados por seção/fase ficam documentados nos PLAN.md de cada fase
  (D-04..D-11 da fase 1).
```

**Restrições:**
- Substituir EXATAMENTE essa seção (até o fim do bloco da seção atual, antes do `## Caret / blink` na linha 54).
- Não tocar em outras seções neste task.
- Manter codeblocks com 4 backticks ou crase tripla conforme convenção do arquivo (o original usa crase tripla; replicar).
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && grep -c 'uma única linha de cabeçalho' apresentacao/STYLE.md | grep -q '^1$' && grep -c 'Classe única: `.deck-topic`' apresentacao/STYLE.md | grep -q '^1$' && grep -c 'rel-kicker.kicker.*rel-title' apresentacao/STYLE.md | awk '$1 <= 1 { exit 0 } { exit 1 }' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Seção `## Cabeçalho de todo slide após a AGENDA` contém a frase "uma única linha de cabeçalho"
    - Contém "Classe única: `.deck-topic`"
    - Contém pelo menos 3 dos 8 cabeçalhos travados nomeados (`> introdução`, `> mineração de dados educacionais`, etc.) como exemplos
    - Frase "Hoje todos os slides de conteúdo usam o tópico `> trabalhos correlatos`" foi REMOVIDA (era impreciso antes; agora impossível depois da fase 1)
    - `## Caret / blink` (linha seguinte) intocada
  </acceptance_criteria>
  <done>Seção "Cabeçalho..." reescrita conforme D-21.</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar STYLE.md — remover "Regra dos correlatos" e ajustar regras de redação</name>
  <files>apresentacao/STYLE.md</files>
  <read_first>
    - apresentacao/STYLE.md linhas 75-81 (seção `## Regras de redação` com o bullet "Regra dos correlatos" atual)
    - .planning/phases/01-reformata-o-da-base/01-PATTERNS.md § "Pattern Assignment para STYLE.md (D-21)" > "bullet 'Regra dos correlatos'"
    - .planning/phases/01-reformata-o-da-base/01-CONTEXT.md D-21 (segundo bullet)
  </read_first>
  <action>
**No `apresentacao/STYLE.md`, dentro da seção `## Regras de redação`:**

REMOVER o bullet inteiro (linhas 80-81 do estado atual):
```
- **Regra dos correlatos:** todo autor novo é introduzido em um slide
  `> trabalhos correlatos` ANTES do slide que usa seus resultados.
```

SUBSTITUIR por:
```
- **Apresentação de autores:** autores são introduzidos no momento da relevância
  via cabeçalho temático `> [nome da seção]`, nunca em slide dedicado de
  "trabalhos correlatos". O nome do autor não aparece no corpo nem no cabeçalho,
  apenas em `Fonte:` no rodapé do slide.
- **Voz própria como padrão:** paráfrase indireta com autor parentético é o
  padrão. Voz em primeira pessoa do plural quando aplicável ("nosso trabalho
  aplica", "nós seguimos", "implementamos"). Citação direta literal só quando a
  frase específica é o argumento (ex.: dados quantitativos em Martins p2/p3).
```

**Restrições:**
- Os 2 bullets anteriores ("Termos estrangeiros em itálico e minúsculas..." e "Sem travessões (em-dash)...") ficam INTACTOS — não tocar.
- A regra `**Sem travessões (em-dash)**` continua valendo (CLAUDE.md memória `feedback_no_em_dashes.md`). Apenas o bullet "Regra dos correlatos" muda.
- Adicionar a nova regra "Voz própria como padrão" reflete D-25 do CONTEXT (paráfrase é o padrão; citação direta é exceção).
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && grep -c 'Regra dos correlatos' apresentacao/STYLE.md | grep -q '^0$' && grep -c 'Apresentação de autores' apresentacao/STYLE.md | grep -q '^1$' && grep -c 'Voz própria como padrão' apresentacao/STYLE.md | grep -q '^1$' && grep -c 'Sem travessões (em-dash)' apresentacao/STYLE.md | grep -q '^1$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Regra dos correlatos' apresentacao/STYLE.md` retorna `0`
    - `grep -c 'Apresentação de autores' apresentacao/STYLE.md` retorna `1`
    - `grep -c 'Voz própria como padrão' apresentacao/STYLE.md` retorna `1`
    - `grep -c 'Sem travessões (em-dash)' apresentacao/STYLE.md` retorna `1` (regra preservada)
    - Outros bullets de `## Regras de redação` intactos
  </acceptance_criteria>
  <done>Regras de redação atualizadas; "Regra dos correlatos" removida; "Apresentação de autores" + "Voz própria como padrão" adicionadas.</done>
</task>

<task type="auto">
  <name>Task 3: Atualizar STYLE.md — tabela "Inventário de slides (ordem atual)"</name>
  <files>apresentacao/STYLE.md</files>
  <read_first>
    - apresentacao/STYLE.md linhas 94-119 (tabela atual + parágrafo "Linhagem de KT no deck")
    - .planning/phases/01-reformata-o-da-base/01-PATTERNS.md § "Pattern Assignment para STYLE.md (D-21)" > "tabela 'Inventário de slides'"
    - .planning/phases/01-reformata-o-da-base/SKELETON.md § "Inventário de slides após a fase 1 (estado-alvo)"
    - .planning/phases/01-reformata-o-da-base/01-06-SUMMARY.md (para confirmar a ordem real entre slide-code e slide-kcfig que o plan 06 escolheu)
  </read_first>
  <action>
**No `apresentacao/STYLE.md`, substituir** a tabela `## Inventário de slides (ordem atual)` (linhas 94-119 do estado atual, incluindo o parágrafo "Linhagem de KT no deck...") por:

```markdown
## Inventário de slides (ordem atual, pós-fase 1)

| # | classe | cabeçalho | conteúdo |
|---|---|---|---|
| 0 | slide-cover-brand | (sem cabeçalho) | Abertura (logo + tagline) |
| 1 | slide-title-tcc | (sem cabeçalho) | Capa do TCC (autores em grafite) |
| 2 | slide-agenda | (sem cabeçalho temático) | Agenda |
| 3 | slide-related | `> introdução` | Recorte do problema (Martins, Marin e Alves, 2024) |
| 4 | slide-related | `> mineração de dados educacionais` | EDM como processo (Zorić, 2020) — fundido p1+p2 |
| 5 | slide-phases | `> as quatro fases da edm` | As 4 fases (Zorić, 2020) |
| 6 | slide-related slide-bridge | `> da edm ao knowledge tracing` | Ponte EDM → KT (Yağcı, 2022) — fundido p1+p2 |
| 7 | slide-code OU slide-kcfig | `> o que o code-dkt olha` OU `> kcs semânticos extraídos` | (ordem definida pelo plan 06; consultar SUMMARY) |
| 8 | (o outro entre slide-code/slide-kcfig) | (o outro cabeçalho) | (idem) |
| 9 | slide-problem | `> retomando o problema` | Dificuldades dos alunos (Martins p2, 13 autores; citação direta mantida porque o número é o argumento) |
| 10 | slide-problem | `> retomando o problema` | Dentro dos conceitos técnicos (Martins p3, 10 autores; idem) |
| 11 | slide-fig | `> evolução por dificuldade` | Curva de aprendizado do Code-DKT por sub-dificuldade |

**Estado do deck:** 12 slides após a fase 1 (era 16 antes; Corbett ×2 removidos; Zorić p1+p2 e Yağcí p1+p2 fundidos; 5 slides reposicionados ao fim do `<div class="slides">`).

**Gaps reservados para fases 2-5:**

- Após `> introdução` (slide 3): INTRO-01 "nosso dataset" + INTRO-03 "Shi e o problema" + MARKER-01 (fase 2).
- Após `> da edm ao knowledge tracing` (slide 6): EDA-01/02/03 + MARKER-02 (fase 3).
- Antes do trio Martins+fig (entre slide-code/slide-kcfig e Martins p2): MODEL-01, MODEL-03, MODEL-04, MODEL-05 (fase 4); slide-code vira MODEL-03 reaproveitado; slide-kcfig é a saída do pipeline MODEL-05; slide-fig é o CLOSE-03.
- Após slide-fig: MARKER-03 (fim da fase 4 da EDM); depois TOOL-01, TOOL-03, MARKER-04, END-01 (fase 5); AGENDA-01 revisado.

**Linhagem de KT (preenchida em fases futuras):** Corbett & Anderson (1995) volta na cronologia do MODEL-01 (fase 4), seguida por Piech (2015) DKT e Shi (2022) Code-DKT. Yağcı (2022) ocupa o slide-bridge (slide 6) e Duan (2025) é introduzido no MODEL-05 (fase 4).
```

**Restrições:**
- A coluna "cabeçalho" reflete EXATAMENTE os cabeçalhos travados D-04..D-11.
- O slide 7 e o slide 8 deixam a ordem entre slide-code/slide-kcfig flexível porque a Claude's Discretion do plan 06 decide; o SUMMARY do plan 06 vai dizer qual ficou onde. Idealmente: depois que o plan 06 rodar, este task 3 substitui as duas linhas pelo conteúdo real (slide-code primeiro OU slide-kcfig primeiro), removendo o "OU" das colunas.
- O parágrafo "Linhagem de KT no deck" original (linhas 115-119) é REESCRITO como o parágrafo "Linhagem de KT (preenchida em fases futuras)" acima.
- "Gaps reservados" prepara o leitor para as fases 2-5.
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && grep -c 'pós-fase 1' apresentacao/STYLE.md | grep -q '^1$' && grep -c '> introdução' apresentacao/STYLE.md | awk '$1 >= 1 { exit 0 } { exit 1 }' && grep -c '> retomando o problema' apresentacao/STYLE.md | awk '$1 >= 1 { exit 0 } { exit 1 }' && grep -c 'slide-corbett' apresentacao/STYLE.md | grep -q '^0$' && grep -c '12 slides após a fase 1' apresentacao/STYLE.md | grep -q '^1$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Tabela `## Inventário de slides (ordem atual, pós-fase 1)` presente (título atualizado)
    - Nenhuma linha menciona `slide-corbett` (Corbett removido)
    - Cabeçalhos `> introdução`, `> mineração de dados educacionais`, `> as quatro fases da edm`, `> da edm ao knowledge tracing`, `> retomando o problema` aparecem na tabela
    - Frase "12 slides após a fase 1" presente
    - Bloco "Gaps reservados para fases 2-5" presente
    - Parágrafo original "Linhagem de KT no deck: Corbett & Anderson..." foi substituído pelo novo "Linhagem de KT (preenchida em fases futuras)"
  </acceptance_criteria>
  <done>Inventário e linhagem reescritos refletindo o estado pós-fase 1.</done>
</task>

<task type="auto">
  <name>Task 4: Limpeza CSS órfão (condicional — só se grep confirmar zero uso)</name>
  <files>apresentacao/assets/theme-unifacens.css</files>
  <read_first>
    - apresentacao/assets/theme-unifacens.css linhas 164-167 (regras candidatas a remoção: `.slide-related .rel-kicker`, `.slide-related .rel-kicker .ps1`, `.slide-related .rel-title`, `.slide-related .rel-sub`)
    - .planning/phases/01-reformata-o-da-base/01-PATTERNS.md § "CSS Cleanup — opcional (theme-unifacens.css)"
    - .planning/phases/01-reformata-o-da-base/01-CONTEXT.md "Claude's Discretion" (CSS cleanup opcional)
  </read_first>
  <action>
**Verificar primeiro se as regras CSS ficaram órfãs:**

```bash
cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt
ORPHANS=$(grep -c 'rel-kicker\|rel-title\|rel-sub' apresentacao/index.html)
echo "Ocorrências em index.html: $ORPHANS"
```

**Branch (A) — se ORPHANS == 0** (todas as 3 classes deixaram de existir no markup):

Deletar do `apresentacao/assets/theme-unifacens.css` as 4 linhas:
```css
.slide-related .rel-kicker { font-size: 24px; color: #5b6472; margin: 0 0 16px; }
.slide-related .rel-kicker .ps1 { color: var(--uni-blue); }
.slide-related .rel-title { font-family: Arial, "Helvetica Neue", sans-serif; font-size: 30px; font-weight: 700; color: var(--uni-ink); margin-top: 0; }
.slide-related .rel-sub { font-family: Arial, "Helvetica Neue", sans-serif; font-size: 22px; color: #5b6472; margin-top: 2px; }
```

Atenção: NÃO deletar outras classes do mesmo arquivo. Especificamente, MANTER:
- `.rel-quote`, `.rel-quote .src` (citações Martins ainda usam — `.prob-quote` é o seletor real, mas verificar)
- `.rel-intro`, `.rel-points`, `.rel-points li`, `.rel-points .ps1` (alguns slides reformatados podem ainda usar listas; verificar no HTML)
- `.rel-lead` (Martins p1, Zorić fundido e Yağcı fundido usam)
- `.rel-finding`, `.rel-aim`, `.rel-src`, `.rel-cite` (rodapé "Fonte:" usa `.rel-cite` em todos os slide-related)

Antes de deletar, rodar `grep -c '<class>' apresentacao/index.html` para cada classe candidata e confirmar > 0 ocorrências para as classes que ficam.

**Branch (B) — se ORPHANS > 0**:

Não deletar nada. Documentar no SUMMARY o motivo: "regras `.rel-kicker`/`.rel-title`/`.rel-sub` mantidas porque ainda há $ORPHANS ocorrências no index.html (provavelmente em commentário ou markup residual)". Marcar como follow-up para investigar antes da fase 2.

**Commit (somente se branch A):** `apresentacao: limpar regras CSS órfãs (.rel-kicker/.rel-title/.rel-sub)`.
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && bash -c 'O=$(grep -c "rel-kicker\|rel-title\|rel-sub" apresentacao/index.html); C=$(grep -c "rel-kicker\|rel-title\|rel-sub" apresentacao/assets/theme-unifacens.css); echo "html=$O css=$C"; if [ "$O" -eq 0 ]; then [ "$C" -eq 0 ] && echo OK || { echo "html limpo mas css ainda tem $C"; exit 1; }; else echo "OK (deferred: $O em html)"; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Branch A (cleanup aplicado): `grep -c 'rel-kicker\|rel-title\|rel-sub' apresentacao/assets/theme-unifacens.css` retorna `0`; commit existe
    - Branch B (cleanup deferred): nenhuma mudança no CSS; SUMMARY documenta o motivo
    - `.rel-quote`, `.rel-lead`, `.rel-cite`, `.rel-points` permanecem definidas no CSS (verificar com grep individual)
    - Browser smoke test: navegar pelo deck inteiro, validar visualmente que NENHUM slide perdeu estilo (especialmente os rodapés "Fonte:" via `.rel-cite` e os parágrafos `.rel-lead` nos slides fundidos)
  </acceptance_criteria>
  <done>CSS órfão removido se aplicável, OU cleanup deferred com documentação; deck visual intacto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Validação final fim-a-fim no browser</name>
  <action>Bloquear até o usuário validar fim-a-fim no browser. Não rodar Task 6 antes da resposta. Apresentar o checklist dos 8 Success Criteria do ROADMAP fase 1 e os comandos automatizados de gate final descritos em <how-to-verify>. Caso o usuário reporte problemas, abrir plan de gap-closure (`/gsd-plan-phase 1 --gaps`) e NÃO marcar a fase como completa.</action>
  <what-built>
**Estado final esperado da fase 1:**
- 12 slides em `apresentacao/index.html` (era 16 antes).
- 8 cabeçalhos travados aplicados (D-04..D-11):
  - `> introdução` (Martins p1)
  - `> mineração de dados educacionais` (Zorić fundido)
  - `> as quatro fases da edm` (slide-phases)
  - `> da edm ao knowledge tracing` (Yağcı fundido)
  - `> o que o code-dkt olha` (slide-code)
  - `> kcs semânticos extraídos` (slide-kcfig)
  - `> retomando o problema` (Martins p2 + Martins p3, ambos)
  - `> evolução por dificuldade` (slide-fig)
- Trio adjacente no fim: Martins p2 → Martins p3 → slide-fig (D-17a, D-17b).
- slide-code e slide-kcfig precedem o trio (D-17c).
- STYLE.md atualizado.
- CSS órfão limpo (se branch A do Task 4).
- 8-10 commits atômicos criados ao longo dos plans 01-07 (1 REMOVE-01, 1 MERGE-01, 1 REFORMAT-03, 1 REFORMAT-01, 1 REFORMAT-02, 2 REFORMAT-04+05 do plan 06, 3-4 do plan 07).
  </what-built>
  <how-to-verify>
**1. Iniciar dev server:**
```bash
cd apresentacao && python3 -m http.server 8000
```

**2. Abrir browser em http://127.0.0.1:8000 com DevTools (F12) aberto na aba Console.**

**3. Navegar com tecla → de slide 0 até o último (slide 11). Para cada slide, verificar:**
- Cabeçalho `> [seção]` em Cascadia 24px, `#5b6472`, com caret piscando azul.
- Slides 0-2 (capa, título TCC, agenda): inalterados.
- Slide 3 (Martins p1): cabeçalho `> introdução`; 3 parágrafos `.rel-lead`; rodapé `Fonte: Martins, Marin e Alves (2024).` SEM h2 com nome do autor no corpo.
- Slide 4 (Zorić fundido): cabeçalho `> mineração de dados educacionais`; parágrafo único começando com "Nosso trabalho aplica..."; rodapé `Fonte: Zorić (2020).`; SEM `<blockquote>` com aspas.
- Slide 5 (slide-phases): cabeçalho `> as quatro fases da edm`; lista numerada com 4 itens; nota "processo é iterativo"; rodapé `Fonte: Zorić (2020).`; SEM `<h2 class="phases-title">`.
- Slide 6 (Yağcı fundido): cabeçalho `> da edm ao knowledge tracing`; parágrafo "Yağcı (2022) mostrou..."; `.bridge-seq` horizontal com 3 caixas (mineração → predição → knowledge tracing); rodapé `Fonte: Yağcı (2022).`; grafia Yağcı correta (com ğ e ı); SEM `<blockquote>`.
- Slide 7 (slide-code OU slide-kcfig — conforme plan 06): cabeçalho correspondente; corpo intacto (código real OU diagrama de KCs); rodapé `Fonte:` preservado.
- Slide 8 (o outro entre slide-code e slide-kcfig): idem.
- Slide 9 (Martins p2): cabeçalho `> retomando o problema`; `<blockquote class="prob-quote">` com "mencionada por 13 autores" PRESENTE (citação direta mantida); chart ASCII animado de 7 barras.
- Slide 10 (Martins p3): cabeçalho `> retomando o problema` (mesmo do slide 9); `<blockquote class="prob-quote">` com "citado por 10 autores" PRESENTE; chart ASCII animado de 5 barras.
- Slide 11 (slide-fig): cabeçalho `> evolução por dificuldade`; figura `fig-codedkt-martins-curves.png`; texto de leitura; rodapé.

**4. Console DevTools deve estar SEM erro** (warnings ignorables; errors vermelhos NÃO ignorables).

**5. Confirmar os 8 Success Criteria do ROADMAP fase 1:**
- [ ] #1 `apresentacao/index.html` abre no browser sem erro de console e a navegação reveal.js funciona do primeiro ao último slide
- [ ] #2 Slide Martins p1 exibe cabeçalho `> introdução` (caret piscando); autor aparece apenas em "Fonte:" no rodapé
- [ ] #3 Slide Zorić p3 exibe cabeçalho `> as quatro fases da edm` com o conteúdo das fases preservado
- [ ] #4 Slide Yağcí exibe cabeçalho `> da edm ao knowledge tracing` com gancho explícito sobre acompanhamento ao longo do tempo
- [ ] #5 Slide Zorić fundido (p1+p2) tem cabeçalho `> mineração de dados educacionais` e mostra Zorić como autor + ferramentas/metodologias num único slide
- [ ] #6 Os 2 slides de Corbett & Anderson removidos; busca por `slide-corbett` no `index.html` retorna 0 ocorrências
- [ ] #7 Slides Martins p2/p3 movidos para posição próxima ao final do deck
- [ ] #8 Slides `slide-fig`, `slide-code` e `slide-kcfig` reformatados ao novo padrão de cabeçalho

**6. Comandos automatizados de gate final** (rodar e colar o output no SUMMARY):
```bash
cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt
echo "section count:" && grep -c '<section data-background' apresentacao/index.html
echo "slide-corbett:" && grep -c 'slide-corbett' apresentacao/index.html
echo "cabeçalhos travados:"
for hdr in "introdução" "mineração de dados educacionais" "as quatro fases da edm" "da edm ao knowledge tracing" "retomando o problema" "kcs semânticos extraídos" "evolução por dificuldade" "o que o code-dkt olha"; do
  echo "  > $hdr: $(grep -c "> $hdr" apresentacao/index.html)"
done
echo "trabalhos correlatos (excluindo comentários):"
grep -v '^[[:space:]]*<!--' apresentacao/index.html | grep -c 'trabalhos correlatos'
echo "citações Martins mantidas:"
echo "  13 autores: $(grep -c 'mencionada por 13 autores' apresentacao/index.html)"
echo "  10 autores: $(grep -c 'citado por 10 autores' apresentacao/index.html)"
```

Esperado:
- section count: 12
- slide-corbett: 0
- introdução: 1; mineração de dados educacionais: 1; as quatro fases da edm: 1; da edm ao knowledge tracing: 1; retomando o problema: 2; kcs semânticos extraídos: 1; evolução por dificuldade: 1; o que o code-dkt olha: 1
- "trabalhos correlatos" fora de comentário: 0
- 13 autores: 1; 10 autores: 1
  </how-to-verify>
  <resume-signal>Responder `approved` se TODOS os 8 Success Criteria batem e o console está limpo. Responder com a lista de problemas se algo falha (o executor vai abrir um plan de gap-closure na fase 1).</resume-signal>
  <acceptance_criteria>
    - Usuário respondeu `approved` após validação fim-a-fim
    - Todos os 8 Success Criteria do ROADMAP fase 1 marcados como atendidos
    - Console DevTools sem erro vermelho
    - Comandos automatizados de gate final retornam os valores esperados (anotados no SUMMARY)
    - Phase 1 marcada como `Complete` em STATE.md (próximo task após este checkpoint)
  </acceptance_criteria>
  <done>Fase 1 validada fim-a-fim; usuário aprovou; pronto para iniciar fase 2.</done>
</task>

<task type="auto">
  <name>Task 6: Commit final do STYLE.md e atualização do STATE.md</name>
  <files>apresentacao/STYLE.md, .planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (estado atual do projeto)
    - .planning/ROADMAP.md (para confirmar status da fase 1)
  </read_first>
  <action>
**(a) Commit do STYLE.md** atualizado (tasks 1-3):

```bash
git add apresentacao/STYLE.md
git commit -m "apresentacao: atualizar STYLE.md para padrão > [seção] (D-21)"
```

(Se o Task 4 fez cleanup de CSS, esse já foi commitado separadamente; este commit cobre apenas o STYLE.md.)

**(b) Atualizar `.planning/STATE.md`:**

- Marcar a linha da fase 1 na tabela `## Phases` como `Complete` (era `Pending`).
- Atualizar `**Current focus:**` para a fase 2 (Intro, Dataset e Problema), com `(próximo passo: /gsd-discuss-phase 2)`.
- Atualizar `## Recent commits` listando os commits gerados durante a fase 1 (top 8-10 commits relevantes do `apresentacao/`).
- Atualizar `## Next action` para `/gsd-discuss-phase 2`.
- Atualizar o `last_updated` no frontmatter.
- Incrementar `completed_phases: 1` e `completed_plans: 7` no frontmatter `progress`.

**(c) Atualizar `.planning/ROADMAP.md`** se a tabela `## Visão geral` (linhas 12-18) tem coluna de status: mudar fase 1 de "Pending" para "Complete" (ou similar).

**(d) Marcar todos os 7 requirements REFORMAT-01..05, MERGE-01, REMOVE-01 como `[x]`** (checked) em `.planning/REQUIREMENTS.md` e atualizar a coluna `Status` da tabela de Traceability na mesma seção.

**Commit final:**
```bash
git add .planning/STATE.md .planning/REQUIREMENTS.md .planning/ROADMAP.md
git commit -m "docs: marcar fase 1 (reformatação da base) como concluída"
```
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && git log --oneline -3 | grep -q 'fase 1' && grep -q 'Current focus.*[Pp]hase 2' .planning/STATE.md && grep -q 'REFORMAT-01.*Complete' .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Commit do STYLE.md existe e está na história git
    - Commit final "docs: marcar fase 1 (reformatação da base) como concluída" existe
    - `.planning/STATE.md` marca fase 1 como `Complete` e current focus aponta para fase 2
    - `.planning/REQUIREMENTS.md` tem REFORMAT-01..05, MERGE-01, REMOVE-01 como `[x]` ou `Complete` na tabela
    - `.planning/ROADMAP.md` (se tem coluna de status) reflete fase 1 completa
  </acceptance_criteria>
  <done>STATE/REQUIREMENTS/ROADMAP atualizados; commits criados; pronto para `/gsd-discuss-phase 2`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Static deck local — sem rede / auth / dados. STYLE.md e theme-unifacens.css são documentos / regras de estilo.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | apresentacao/STYLE.md desincronizado do index.html | mitigate | Tasks 1-3 reescrevem STYLE.md a partir do estado real do markup; Task 5 checkpoint valida visualmente |
| T-01-04 | Denial of Service | regressão CSS após remoção de regras | mitigate | Task 4 só aplica branch A se `grep -c` confirma órfão; Task 5 smoke test browser captura regressões visuais |
| T-01-05 | Information Disclosure | n/a | accept | nada confidencial no deck |

Sem ameaças HIGH/CRITICAL.
</threat_model>

<verification>
- STYLE.md: 3 seções reescritas (cabeçalho, regras de redação, inventário)
- CSS: órfão removido OU cleanup deferred com documentação
- Browser fim-a-fim: 12 slides navegáveis, console limpo, todos os 8 cabeçalhos visíveis
- Gate comandos automatizados retornam os valores esperados
- Usuário confirmou os 8 Success Criteria do ROADMAP fase 1
- STATE.md, REQUIREMENTS.md, ROADMAP.md atualizados
- Commits atômicos criados
</verification>

<success_criteria>
- TODOS os 8 Success Criteria do ROADMAP fase 1 satisfeitos
- D-21 e D-22 cumpridos (STYLE.md atualizado, commit dedicado ao fim da fase)
- CSS órfão tratado (limpo ou deferred com motivo)
- Fase 1 marcada como concluída em STATE.md / REQUIREMENTS.md / ROADMAP.md
- Próximo comando claro para o usuário: `/gsd-discuss-phase 2`
</success_criteria>

<output>
Criar `.planning/phases/01-reformata-o-da-base/01-07-SUMMARY.md` com:
- Hashes dos 1-2 commits gerados neste plan (STYLE.md + opcional CSS)
- Branch A ou B do Task 4 (CSS cleanup aplicado ou deferred)
- Output dos comandos automatizados de gate final (Task 5)
- Confirmação do checkpoint humano (Task 5) — resposta do usuário
- Atualização do STATE.md confirmada (Task 6)
- Phase 1 marcada como complete
- Próximo passo: `/gsd-discuss-phase 2` (fase 2: Intro, Dataset e Problema — Fase 1 EDM)

E criar `.planning/phases/01-reformata-o-da-base/PHASE-SUMMARY.md` agregando os 7 plans:
- Resumo do que foi entregue na fase
- Inventário final dos slides (12, com classes e cabeçalhos)
- Lista de todos os commits da fase
- Decisões da Discretion aplicadas (Task 1 do plan 01: working tree; Task 2 do plan 06: ordem entre slide-code/slide-kcfig)
- Trade-offs e learnings para a fase 2 (se houver)
</output>
