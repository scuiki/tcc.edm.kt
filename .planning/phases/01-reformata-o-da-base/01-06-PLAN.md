---
phase: 01-reformata-o-da-base
plan: 06
type: execute
wave: 1
depends_on: ["01-05"]
files_modified:
  - apresentacao/index.html
autonomous: true
requirements:
  - REFORMAT-04
  - REFORMAT-05
must_haves:
  truths:
    - "Os 5 slides afetados (slide-problem Martins p2, slide-problem Martins p3, slide-kcfig, slide-fig, slide-code) foram MOVIDOS para o fim de `<div class=\"slides\">` em UMA passada"
    - "Os 5 slides afetados têm cabeçalhos travados aplicados: Martins p2 e p3 ambos com `> retomando o problema`; slide-kcfig com `> kcs semânticos extraídos`; slide-fig com `> evolução por dificuldade`; slide-code com `> o que o code-dkt olha`"
    - "Os `<h2>`/`.kcfig-title` internos dos 5 slides foram removidos"
    - "Restrição D-17a: Martins p2 e Martins p3 são adjacentes no DOM final"
    - "Restrição D-17b: slide-fig vem imediatamente após Martins p3 no DOM final"
    - "Restrição D-17c: slide-code e slide-kcfig precedem o trio Martins+fig (vêm antes de Martins p2)"
    - "As 2 citações diretas dos slide-problem Martins p2 e p3 (`mencionada por 13 autores` p.19 e `citado por 10 autores` p.20) são MANTIDAS intactas (D-28, exceção legítima à D-25)"
    - "Conteúdo interno dos 5 slides (ascii-charts, kcfig-map, fig-wrap, code-card) é preservado intacto"
    - "Deck navega fim-a-fim sem erro de console"
  artifacts:
    - path: "apresentacao/index.html"
      provides: "5 slides reformatados e reposicionados no fim do deck"
      contains: "exatamente 2 ocorrências de '> retomando o problema' (Martins p2 + p3) e 1 de cada outro cabeçalho travado"
  key_links:
    - from: "Martins p2"
      to: "Martins p3 (adjacente)"
      via: "ordem de section no DOM"
      pattern: "Martins p2 imediatamente seguido por Martins p3"
    - from: "Martins p3"
      to: "slide-fig (adjacente)"
      via: "ordem de section no DOM"
      pattern: "Martins p3 imediatamente seguido por slide-fig"
    - from: "slide-code/slide-kcfig"
      to: "trio Martins+fig"
      via: "precedência no DOM"
      pattern: "ambos aparecem antes de Martins p2"
---

<objective>
**Mover 5 slides para o fim do `<div class="slides">` e reformatar os cabeçalhos de todos eles em UMA passada coupling-aware.**

Os 5 slides:
1. slide-problem Martins p2 ("O problema") — REFORMAT-04
2. slide-problem Martins p3 ("Dentro dos conceitos técnicos") — REFORMAT-04
3. slide-kcfig — REFORMAT-05a
4. slide-fig — REFORMAT-05b
5. slide-code — REFORMAT-05c

Ordem final no DOM (após as plans 01-05 já terem rodado, e ANTES de aplicar este plan, esses 5 slides estão dispersos entre a Agenda e o slide-phases; depois deste plan, eles ficam todos juntos no fim, nesta ordem):

`slide-code` OU `slide-kcfig` (ordem livre entre os 2) → o outro → **Martins p2** → **Martins p3** → **slide-fig** (trio adjacente, sem nada entre).

Restrições obrigatórias do CONTEXT (D-17):
- (a) Martins p2 e Martins p3 adjacentes
- (b) slide-fig imediatamente após Martins p3
- (c) slide-code e slide-kcfig precedem o trio Martins+fig

Purpose: este plan é o pivô estrutural da fase 1. Justificativa do acoplamento (D-15 + D-30):
- REFORMAT-04 e REFORMAT-05 são MOVIMENTAÇÕES de DOM + REFORMATAÇÕES de cabeçalho. Fazer em passadas separadas dobra o risco de quebrar a navegação reveal.js entre operações.
- O cabeçalho `> retomando o problema` só faz sentido com Martins p2/p3 já reposicionados ao fim (não como "trabalhos correlatos" lá em cima).
- Fazer agora economiza um segundo round de movimentação na fase 4 (D-15). A fase 4 vai inserir MODEL-01..08 nos gaps abertos por este plan (CONTEXT D-18).

Output: 5 sections movidas para o fim de `<div class="slides">`, com cabeçalhos travados e h2s removidos; corpo de cada slide intacto; deck navega fim-a-fim; commit atômico.

**EXCEÇÃO IMPORTANTE (D-28):** as 2 citações diretas Martins (em `<blockquote class="prob-quote">` no Martins p2 e p3) **PERMANECEM**. Os números "mencionada por 13 autores" (p. 19) e "citado por 10 autores" (p. 20) SÃO o argumento quantitativo da revisão sistemática; paráfrase enfraqueceria. Esta é a única exceção legítima nesta fase à regra D-25 (paráfrase é o padrão).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-reformata-o-da-base/01-CONTEXT.md
@.planning/phases/01-reformata-o-da-base/01-PATTERNS.md
@.planning/phases/01-reformata-o-da-base/SKELETON.md
@./CLAUDE.md
@apresentacao/STYLE.md
@apresentacao/index.html
@docs/Artigo+2+Desafios+na+aprendizagem+de+lógica+de+programação.pdf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reformatar cabeçalhos dos 5 slides afetados (sem mover ainda)</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html (estado completo após plan 05; identificar os 5 slides por classe: `slide-problem` ×2, `slide-kcfig`, `slide-fig`, `slide-code`)
    - .planning/phases/01-reformata-o-da-base/01-PATTERNS.md § "Padrão 'antes' #2 — .deck-topic + <h2> interno" (cobre todos os 5 slides; cada um tem um `<h2>` ou `.kcfig-title` a remover)
    - .planning/phases/01-reformata-o-da-base/01-CONTEXT.md D-03, D-07, D-08, D-09, D-10, D-28
    - docs/Artigo+2+Desafios+na+aprendizagem+de+lógica+de+programação.pdf (já lido no plan 04; confirmar que as 2 citações Martins p.19 e p.20 a serem MANTIDAS correspondem ao Quadro 3 do artigo)
  </read_first>
  <action>
**Reformatar os 5 cabeçalhos in-place** (sem mover ainda; a movimentação é Task 2 — separamos para reduzir o blast radius por commit). Para cada um dos 5 slides:

**(1) Slide-problem Martins p2 ("O problema") — REFORMAT-04, cabeçalho D-07 `> retomando o problema`**

Localizar pela presença de `<h2 class="prob-head">O problema</h2>`.

Atualizar comentário acima:
- DE: `<!-- ============ SLIDE 4 · O problema (Martins, Marin e Alves, 2024) ============ -->`
- PARA: `<!-- ============ SLIDE · Retomando o problema · Martins p2 (Martins, Marin e Alves, 2024) ============ -->`

Dentro do `<div class="deck-slide slide-problem">`:
DELETAR:
```
          <p class="deck-topic"><span class="ps1">&gt;</span>trabalhos correlatos<span class="caret blink"></span></p>
          <h2 class="prob-head">O problema</h2>
```
INSERIR no mesmo lugar:
```
          <p class="deck-topic"><span class="ps1">&gt;</span>retomando o problema<span class="caret blink"></span></p>
```

**MANTER INTACTOS (D-28, exceção):**
- `<blockquote class="prob-quote">&ldquo;A compreensão dos conceitos técnicos da programação pode ser considerada um desafio complexo. Esta é a dificuldade mais comum entre os estudantes, mencionada por 13 autores&rdquo; <span class="src">(Martins; Marin; Alves, 2024, p. 19)</span>.</blockquote>` (citação direta)
- `<p class="prob-sub">Dificuldades enfrentadas pelos alunos e o número de ocorrências nos estudos analisados por Martins <i>et al.</i> (2024):</p>`
- `<div class="ascii-chart" data-ascii>...</div>` (7 barras animadas)
- `<p class="prob-cite">Fonte: adaptado de Martins, Marin e Alves (2024).</p>`

**(2) Slide-problem Martins p3 ("Dentro dos conceitos técnicos") — REFORMAT-04, cabeçalho D-07 `> retomando o problema` (mesmo cabeçalho do Martins p2, deliberado)**

Localizar pela presença de `<h2 class="prob-head">Dentro dos conceitos técnicos</h2>`.

Atualizar comentário acima:
- DE: `<!-- ============ SLIDE · Dentro dos conceitos técnicos (decomposição · Martins et al., 2024, p. 20-21) ============ -->`
- PARA: `<!-- ============ SLIDE · Retomando o problema · Martins p3 — dentro dos conceitos técnicos (Martins, Marin e Alves, 2024) ============ -->`

DELETAR:
```
          <p class="deck-topic"><span class="ps1">&gt;</span>trabalhos correlatos<span class="caret blink"></span></p>
          <h2 class="prob-head">Dentro dos conceitos técnicos</h2>
```
INSERIR:
```
          <p class="deck-topic"><span class="ps1">&gt;</span>retomando o problema<span class="caret blink"></span></p>
```

**MANTER INTACTOS (D-28):**
- `<blockquote class="prob-quote">&ldquo;Destaca-se que o entendimento das estruturas de controle é o conceito mais desafiador para os alunos, citado por 10 autores&rdquo; <span class="src">(Martins; Marin; Alves, 2024, p. 20)</span>.</blockquote>` (citação direta)
- `<p class="prob-sub">Martins <i>et al.</i> (2024) subdividem essa dificuldade em conceitos técnicos específicos (nº de autores que citam cada um):</p>`
- `<div class="ascii-chart" data-ascii>...</div>` (5 barras)
- `<p class="prob-cite">Fonte: adaptado de Martins, Marin e Alves (2024).</p>`

**(3) Slide-kcfig — REFORMAT-05a, cabeçalho D-08 `> kcs semânticos extraídos`**

Localizar pela presença de `<div class="deck-slide slide-kcfig">` e `<p class="kcfig-title">KCs (KCGen-KT) e as dificuldades em conceitos de programação</p>`.

Atualizar comentário acima:
- DE: `<!-- ============ SLIDE · Figura: KCs (KCGen-KT) ligados às dificuldades (Martins et al., 2024) ============ -->` + linha de comentário auxiliar `<!-- modelo "Word": quadrados ligados; tudo em Arial; nomes PT-BR de results/kc_translations.json -->`
- PARA: `<!-- ============ SLIDE · KCs semânticos extraídos (Duan et al., 2025; Martins, Marin e Alves, 2024) ============ -->` + linha auxiliar `<!-- modelo "Word": quadrados ligados; nomes PT-BR de results/kc_translations.json -->`

DELETAR:
```
          <p class="deck-topic"><span class="ps1">&gt;</span>trabalhos correlatos<span class="caret blink"></span></p>
          <p class="kcfig-title">KCs (KCGen-KT) e as dificuldades em conceitos de programação</p>
```
INSERIR:
```
          <p class="deck-topic"><span class="ps1">&gt;</span>kcs semânticos extraídos<span class="caret blink"></span></p>
```

(Nota: `.kcfig-title` é `<p>`, não `<h2>`, mas funciona como título. Apaga-se por consistência com D-03 — cabeçalho único `> kcs semânticos extraídos`.)

**MANTER INTACTOS:**
- `<div class="kcfig-map">...</div>` (6 `.kc-row`, cada uma com `.kc-diff` + `.kc-link` + `.kc-group` com 3 `.kc-box`)
- `<p class="kcfig-fonte">Fonte: elaborado pelos autores, com base em Duan <i>et al.</i> (2025) e Martins, Marin e Alves (2024).</p>` (D-24: rodapé com múltiplas referências preservado)

**(4) Slide-fig — REFORMAT-05b, cabeçalho D-09 `> evolução por dificuldade`**

Localizar pela presença de `<div class="deck-slide slide-fig">` e `<h2 class="fig-title">Quão difícil de aprender? Curva de aprendizado do Code-DKT por dificuldade</h2>`.

Atualizar comentário acima:
- DE: `<!-- ============ SLIDE · Dificuldade de aprendizado (Code-DKT) por dificuldade de Martins ============ -->` + linha auxiliar
- PARA: `<!-- ============ SLIDE · Evolução por dificuldade · Curva Code-DKT (Code-DKT, Shi et al., 2022; Martins, Marin e Alves, 2024) ============ -->` + linha auxiliar mantida

DELETAR:
```
          <p class="deck-topic"><span class="ps1">&gt;</span>trabalhos correlatos<span class="caret blink"></span></p>
          <h2 class="fig-title">Quão difícil de aprender? Curva de aprendizado do Code-DKT por dificuldade</h2>
```
INSERIR:
```
          <p class="deck-topic"><span class="ps1">&gt;</span>evolução por dificuldade<span class="caret blink"></span></p>
```

**MANTER INTACTOS:**
- `<div class="fig-wrap"><img src="assets/fig-codedkt-martins-curves.png" alt="..."></div>`
- `<p class="fig-read">...</p>` (a leitura do gráfico)
- `<p class="fig-fonte">Fonte: elaborado pelos autores...</p>`

**(5) Slide-code — REFORMAT-05c, cabeçalho D-10 `> o que o code-dkt olha`**

Localizar pela presença de `<div class="deck-slide slide-code">` e `<h2 class="code-title">O que o Code-DKT &ldquo;olha&rdquo; ao prever erro</h2>`.

Atualizar comentário acima:
- DE: `<!-- ============ SLIDE · O que o Code-DKT olha ao prever erro (exemplo de código real) ============ -->`
- PARA: `<!-- ============ SLIDE · O que o Code-DKT olha (Code-DKT, Shi et al., 2022) ============ -->`

DELETAR:
```
          <p class="deck-topic"><span class="ps1">&gt;</span>trabalhos correlatos<span class="caret blink"></span></p>
          <h2 class="code-title">O que o Code-DKT &ldquo;olha&rdquo; ao prever erro</h2>
```
INSERIR:
```
          <p class="deck-topic"><span class="ps1">&gt;</span>o que o code-dkt olha<span class="caret blink"></span></p>
```

**MANTER INTACTOS:**
- `<p class="code-lead">Diferente de BKT e DKT...</p>`
- `<div class="code-card">...</div>` (com `<pre class="devcpp__gutter">` e `<pre class="devcpp__code">` com o exemplo dateFashion completo)
- `<p class="code-take">...</p>`
- `<p class="code-fonte">Fonte: elaborado pelos autores...</p>`

**Restrições gerais para o Task 1:**
- Não tocar no corpo de nenhum dos 5 slides; só o cabeçalho.
- Todos os 5 cabeçalhos novos têm caret blink dentro do `.deck-topic`.
- Todos minúsculos: `retomando o problema`, `kcs semânticos extraídos`, `evolução por dificuldade`, `o que o code-dkt olha` (note `code-dkt` com hífen e minúsculo).
- Não mover sections ainda; isso é o Task 2.

**Commit:** `apresentacao: reformatar cabeçalhos dos 5 slides do bloco final (REFORMAT-04 + REFORMAT-05)`.
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && grep -c '> retomando o problema' apresentacao/index.html | grep -q '^2$' && grep -c '> kcs semânticos extraídos' apresentacao/index.html | grep -q '^1$' && grep -c '> evolução por dificuldade' apresentacao/index.html | grep -q '^1$' && grep -c '> o que o code-dkt olha' apresentacao/index.html | grep -q '^1$' && grep -c '<h2 class="prob-head">' apresentacao/index.html | grep -q '^0$' && grep -c '<h2 class="fig-title">' apresentacao/index.html | grep -q '^0$' && grep -c '<h2 class="code-title">' apresentacao/index.html | grep -q '^0$' && grep -c '<p class="kcfig-title">' apresentacao/index.html | grep -q '^0$' && grep -c 'mencionada por 13 autores' apresentacao/index.html | grep -q '^1$' && grep -c 'citado por 10 autores' apresentacao/index.html | grep -q '^1$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '> retomando o problema' apresentacao/index.html` retorna `2` (Martins p2 + p3 com MESMO cabeçalho, D-07)
    - `grep -c '> kcs semânticos extraídos' apresentacao/index.html` retorna `1`
    - `grep -c '> evolução por dificuldade' apresentacao/index.html` retorna `1`
    - `grep -c '> o que o code-dkt olha' apresentacao/index.html` retorna `1`
    - `grep -c '<h2 class="prob-head">' apresentacao/index.html` retorna `0` (2 h2s removidos)
    - `grep -c '<h2 class="fig-title">' apresentacao/index.html` retorna `0`
    - `grep -c '<h2 class="code-title">' apresentacao/index.html` retorna `0`
    - `grep -c '<p class="kcfig-title">' apresentacao/index.html` retorna `0` (kcfig-title removido por consistência D-03)
    - `grep -c 'mencionada por 13 autores' apresentacao/index.html` retorna `1` (citação Martins p2 PRESERVADA, D-28)
    - `grep -c 'citado por 10 autores' apresentacao/index.html` retorna `1` (citação Martins p3 PRESERVADA, D-28)
    - `grep -c 'class="ascii-chart"' apresentacao/index.html` retorna `2` (charts dos 2 Martins preservados)
    - `grep -c 'class="kcfig-map"' apresentacao/index.html` retorna `1` (mapa kcfig preservado)
    - `grep -c 'class="fig-wrap"' apresentacao/index.html` retorna `1` (figura preservada)
    - `grep -c 'class="code-card"' apresentacao/index.html` retorna `1` (código preservado)
    - Browser smoke test: navegar pelo deck inteiro, ver os 5 cabeçalhos novos nos lugares atuais, animações ASCII continuam funcionando, console sem erro
    - Commit em `git log --oneline -1`: `apresentacao: reformatar cabeçalhos dos 5 slides do bloco final`
  </acceptance_criteria>
  <done>5 cabeçalhos reformatados in-place; h2s/kcfig-title removidos; corpo intacto; 2 citações Martins preservadas; deck ainda navegável; commit atômico.</done>
</task>

<task type="auto">
  <name>Task 2: Mover os 5 sections para o fim de `<div class="slides">` na ordem travada</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html (estado completo após Task 1; localizar as 5 sections reformatadas em suas posições atuais espalhadas)
    - .planning/phases/01-reformata-o-da-base/01-PATTERNS.md § "DOM Move Pattern (REFORMAT-04 + REFORMAT-05)" (estado final exigido + restrições obrigatórias)
    - .planning/phases/01-reformata-o-da-base/01-CONTEXT.md D-15, D-16, D-17, D-18 (D-18 é informativo para fase 4; este plan respeita D-16 e D-17)
  </read_first>
  <action>
**Recortar os 5 sections (e seus comentários `<!-- ============ SLIDE · ... ============ -->` acima)** e **colá-los, nesta ordem exata, no FIM de `<div class="slides">`** (imediatamente antes do `    </div>\n  </div>\n\n  <script src="https://cdn.jsdelivr.net/npm/reveal.js@5.1.0/dist/reveal.js"></script>`):

**Ordem de cola obrigatória (D-16, D-17):**

1. **slide-code** OU **slide-kcfig** — Claude's Discretion: o executor escolhe qual dos 2 fica primeiro. Recomendação: slide-code primeiro (porque na narrativa do TCC 2 a interpretabilidade do código vem antes da figura de KCs), mas qualquer ordem entre os 2 cumpre D-16. Documentar a escolha no SUMMARY.
2. **slide-kcfig** OU **slide-code** — o outro dos 2.
3. **Martins p2** (slide-problem "O problema") — começo do TRIO adjacente.
4. **Martins p3** (slide-problem "Dentro dos conceitos técnicos") — adjacente a Martins p2 (D-17a).
5. **slide-fig** (curva Code-DKT) — imediatamente após Martins p3 (D-17b).

**O que sai (recorte) — para cada uma das 5 sections:**
- Recortar a `<section data-background-color="#F1F6FB">...</section>` inteira.
- Recortar o comentário `<!-- ============ SLIDE · ... ============ -->` (e linha auxiliar se houver) IMEDIATAMENTE acima da `<section>`. O comentário vai JUNTO com a section quando ela for movida (PATTERNS.md § "Template comment").

**O que entra (cola)** — no fim de `<div class="slides">`, antes do `</div></div><script>...`, nesta ordem:

```
      <!-- ============ SLIDE · O que o Code-DKT olha (Code-DKT, Shi et al., 2022) ============ -->
      <section data-background-color="#F1F6FB">
        <div class="deck-slide slide-code">
          ...corpo intacto do slide-code (já reformatado no Task 1)...
        </div>
      </section>

      <!-- ============ SLIDE · KCs semânticos extraídos (Duan et al., 2025; Martins, Marin e Alves, 2024) ============ -->
      <!-- modelo "Word": quadrados ligados; nomes PT-BR de results/kc_translations.json -->
      <section data-background-color="#F1F6FB">
        <div class="deck-slide slide-kcfig">
          ...corpo intacto do slide-kcfig...
        </div>
      </section>

      <!-- ============ SLIDE · Retomando o problema · Martins p2 (Martins, Marin e Alves, 2024) ============ -->
      <section data-background-color="#F1F6FB">
        <div class="deck-slide slide-problem">
          ...corpo intacto do Martins p2, incluindo blockquote prob-quote p.19...
        </div>
      </section>

      <!-- ============ SLIDE · Retomando o problema · Martins p3 — dentro dos conceitos técnicos (Martins, Marin e Alves, 2024) ============ -->
      <section data-background-color="#F1F6FB">
        <div class="deck-slide slide-problem">
          ...corpo intacto do Martins p3, incluindo blockquote prob-quote p.20...
        </div>
      </section>

      <!-- ============ SLIDE · Evolução por dificuldade · Curva Code-DKT (Code-DKT, Shi et al., 2022; Martins, Marin e Alves, 2024) ============ -->
      <section data-background-color="#F1F6FB">
        <div class="deck-slide slide-fig">
          ...corpo intacto do slide-fig...
        </div>
      </section>

    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/reveal.js@5.1.0/dist/reveal.js"></script>
```

(Se o executor escolher slide-kcfig antes de slide-code, basta inverter os 2 primeiros blocos; tudo abaixo do trio Martins+fig permanece.)

**Restrições obrigatórias:**

- **NÃO** alterar o conteúdo interno das 5 sections; só recortar/colar com seus comentários.
- **NÃO** deixar buraco vazio nas posições originais (ex.: se Martins p2 estava entre Agenda e slide-kcfig, depois do recorte a Agenda fica adjacente ao próximo slide remanescente; sem linhas em branco extras nem `<!-- TODO -->`).
- Indentação: cada `<section>` vai com indentação de 6 espaces (igual ao padrão do arquivo). Comentários acima da section têm a mesma indentação.
- O `<svg class="wm">` continua dentro de cada `<div class="deck-slide">`; nada a mover individualmente.
- O fechamento de `<div class="slides">` (`    </div>`) e o do wrapper `<div class="reveal">` (`  </div>`) e o `<script>` final ficam na posição original, depois do último slide colado (slide-fig).
- Verificar visualmente que não há `<section>` órfã: contar `<section data-background` deve dar 11 (3 capa/título/agenda + 1 Martins p1 + 1 Zorić fundido + 1 slide-phases + 1 Yağcı fundido + 5 movidos = 12 — confere com SKELETON.md).

**Commit:** `apresentacao: mover trio Martins+fig e slide-code/slide-kcfig para o fim (REFORMAT-04 + REFORMAT-05, D-16/D-17)`.
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && python3 -c "
import re
content = open('apresentacao/index.html').read()
# Get the order of slide classes in the file
sections = re.findall(r'class=\"deck-slide ([\w\s-]+?)\"', content)
print('Order:', sections)
# Check D-17a: Martins p2 adjacent to Martins p3 (both slide-problem)
prob_indices = [i for i, s in enumerate(sections) if s == 'slide-problem']
assert len(prob_indices) == 2, f'Expected 2 slide-problem, got {len(prob_indices)}'
assert prob_indices[1] == prob_indices[0] + 1, f'D-17a violated: slide-problem not adjacent (indices {prob_indices})'
# Check D-17b: slide-fig comes right after the 2nd slide-problem
fig_index = next((i for i, s in enumerate(sections) if s == 'slide-fig'), -1)
assert fig_index == prob_indices[1] + 1, f'D-17b violated: slide-fig (index {fig_index}) not right after Martins p3 (index {prob_indices[1]})'
# Check D-17c: slide-code and slide-kcfig precede the trio
code_index = next((i for i, s in enumerate(sections) if s == 'slide-code'), -1)
kcfig_index = next((i for i, s in enumerate(sections) if s == 'slide-kcfig'), -1)
assert code_index < prob_indices[0], f'D-17c violated: slide-code (index {code_index}) not before Martins p2 (index {prob_indices[0]})'
assert kcfig_index < prob_indices[0], f'D-17c violated: slide-kcfig (index {kcfig_index}) not before Martins p2 (index {prob_indices[0]})'
# Check that trio is at the END (slide-fig is the last slide-XYZ deck-slide)
non_basic = [s for s in sections if s not in ('slide-cover-brand', 'slide-title-tcc', 'slide-agenda')]
assert non_basic[-1] == 'slide-fig', f'slide-fig should be last; got {non_basic[-1]}'
print('OK: all D-17 constraints satisfied')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - DOM order check (via python3 inline above) passa: D-17a, D-17b, D-17c satisfeitos
    - `grep -c '<section data-background' apresentacao/index.html` retorna `12` (no estado final da fase: 3 abertura + 1 Martins p1 + 1 Zorić fundido + 1 slide-phases + 1 Yağcı fundido + 5 movidos = 12)
    - slide-fig é a ÚLTIMA `<section>` antes de `</div></div><script>`: validar com `tail -30 apresentacao/index.html | grep -c 'slide-fig'` retorna ≥ 1
    - Browser smoke test (CRÍTICO): `cd apresentacao && python3 -m http.server 8000`; abrir http://127.0.0.1:8000; navegar com → do slide 0 ao último; em cada slide, confirmar que o cabeçalho `> [seção]` está visível; no trio final, confirmar adjacência (Martins p2 → Martins p3 → slide-fig); console DevTools SEM erro; animações ASCII funcionam nos slides Martins (ao entrar via setas)
    - Commit em `git log --oneline -1`: `apresentacao: mover trio Martins+fig e slide-code/slide-kcfig para o fim`
  </acceptance_criteria>
  <done>5 sections movidas para o fim na ordem D-17; deck navega fim-a-fim sem erro; commit atômico.</done>
</task>

</tasks>

<verification>
- 2× `> retomando o problema` (Martins p2 e p3)
- 1× cada `> kcs semânticos extraídos`, `> evolução por dificuldade`, `> o que o code-dkt olha`
- 0× `<h2 class="prob-head">`, `<h2 class="fig-title">`, `<h2 class="code-title">`, `<p class="kcfig-title">`
- 2 citações diretas Martins preservadas (p.19 e p.20)
- DOM order check Python passa (D-17a/b/c)
- Section count = 12
- Browser navega fim-a-fim sem erro de console
- 2 commits atômicos (Task 1 e Task 2)
</verification>

<success_criteria>
- REFORMAT-04 satisfeito (Roadmap Success Criterion #7: slides Martins p2/p3 movidos para o fim do deck)
- REFORMAT-05 satisfeito (Roadmap Success Criterion #8: slides slide-fig, slide-code, slide-kcfig reformatados ao novo padrão de cabeçalho)
- D-07, D-08, D-09, D-10 cumpridos (cabeçalhos travados aplicados)
- D-15, D-16, D-17 cumpridos (movimentação na fase 1, ordem travada, restrições de adjacência)
- D-28 cumprido (citações diretas Martins p.19 e p.20 preservadas)
- Deck navega fim-a-fim no browser sem erro
</success_criteria>

<output>
Criar `.planning/phases/01-reformata-o-da-base/01-06-SUMMARY.md` com:
- Hashes dos 2 commits (Task 1 e Task 2)
- Decisão da Discretion D-16: slide-code OU slide-kcfig primeiro (qual o executor escolheu, e justificativa breve)
- Confirmação dos grep gates e do DOM order check
- Print/captura do estado final do `<div class="slides">` (lista de classes na ordem final, ex.: `[slide-cover-brand, slide-title-tcc, slide-agenda, slide-related, slide-related, slide-phases, slide-related slide-bridge, slide-code, slide-kcfig, slide-problem, slide-problem, slide-fig]`)
- Próximo plan: 01-07 (STYLE.md update D-21 + CSS cleanup opcional + smoke test final)
</output>
