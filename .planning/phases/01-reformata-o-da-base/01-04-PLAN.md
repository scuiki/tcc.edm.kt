---
phase: 01-reformata-o-da-base
plan: 04
type: execute
wave: 1
depends_on: ["01-03"]
files_modified:
  - apresentacao/index.html
autonomous: true
requirements:
  - REFORMAT-01
must_haves:
  truths:
    - "Slide Martins p1 (slide-related, atualmente o 1º slide após Agenda) tem cabeçalho `> introdução` como única linha de cabeçalho"
    - "O par `.rel-kicker.kicker` + `.rel-title` + (se existir) `.rel-sub` foi removido e substituído por uma única linha `.deck-topic`"
    - "O nome `Martins, Marin e Alves (2024)` desapareceu do corpo do slide e do `<h2>`; restou apenas em `Fonte: Martins, Marin e Alves (2024).` no rodapé"
    - "Os 3 parágrafos `.rel-lead` originais (programação beneficia / revisão sistemática / estratégias de ensino) são preservados intactos"
    - "Deck continua navegável fim-a-fim sem erro de console"
  artifacts:
    - path: "apresentacao/index.html"
      provides: "slide Martins p1 reformatado"
      contains: "exatamente 1 ocorrência de '> introdução'"
  key_links:
    - from: "slide Martins p1"
      to: "cabeçalho deck-topic único"
      via: ".deck-topic com '> introdução' + caret blink"
      pattern: "introdução com caret blink"
---

<objective>
**Reformatar o slide Martins p1** (`slide-related`, primeiro slide de conteúdo após Agenda) substituindo o par `.rel-kicker` + `.rel-title` (`> trabalhos correlatos` + `Martins, Marin e Alves (2024)`) por uma **única linha** `<p class="deck-topic">` com o cabeçalho travado `> introdução`.

Purpose: este é o slide de entrada do conteúdo. No padrão antigo, ele era apresentado como "trabalho correlato" porque a regra era "todo autor novo é introduzido em um slide `> trabalhos correlatos` ANTES do slide que usa seus resultados". A nova política da defesa (CONTEXT D-01..D-02) elimina essa regra: o autor aparece no momento da relevância via cabeçalho temático (`> introdução` aqui), e o nome do autor desaparece do corpo e do título, indo somente para `Fonte:`. Isso protagoniza a NARRATIVA da defesa, não os autores citados.

Output: 3-4 linhas de markup substituídas no `apresentacao/index.html`; resto do slide intacto; deck continua navegável; commit atômico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-reformata-o-da-base/01-CONTEXT.md
@.planning/phases/01-reformata-o-da-base/01-PATTERNS.md
@.planning/phases/01-reformata-o-da-base/SKELETON.md
@./CLAUDE.md
@apresentacao/STYLE.md
@apresentacao/index.html
@docs/Artigo+2+Desafios+na+aprendizagem+de+lógica+de+programação.pdf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ler a referência Martins, Marin e Alves (2024) antes de tocar no slide</name>
  <files>(nenhum — leitura)</files>
  <read_first>
    - docs/Artigo+2+Desafios+na+aprendizagem+de+lógica+de+programação.pdf (foco: resumo, objetivo, e o Quadro 3 que lista as 13 dificuldades; é a base não só do Martins p1 mas também dos Martins p2/p3 do plan 06)
    - apresentacao/index.html linhas 82-100 (estado atual do slide Martins p1)
  </read_first>
  <action>
Convenção vinculante do CLAUDE.md: "Antes de redigir ou alterar slide que cite um autor, ler a referência completa em docs/". Esta tarefa cumpre a leitura.

Ler `docs/Artigo+2+Desafios+na+aprendizagem+de+lógica+de+programação.pdf` (Martins, Marin e Alves 2024) com foco em:
- Resumo e introdução: motivação (altos índices de reprovação e desistência em disciplinas introdutórias de programação)
- Metodologia: revisão sistemática da literatura; objetivo de identificar principais desafios dos alunos
- Conclusão: utilidade do estudo para elaboração de estratégias de ensino

Como o slide Martins p1 atual JÁ tem 3 parágrafos `.rel-lead` que sintetizam fielmente esses pontos (linhas 91-96 do `index.html`), a leitura serve para confirmar que **nenhum texto do corpo precisa ser ajustado**, só o cabeçalho. Se algum parágrafo soar incorreto à luz da fonte primária, anotar e levantar antes do Task 2.

Observação: este slide NÃO está sob política de paráfrase (D-29) porque o corpo dele JÁ é paráfrase (não tem `<blockquote class="rel-quote">`). A reformatação é puramente estrutural (cabeçalho).
  </action>
  <verify>
    <automated>test -f "docs/Artigo+2+Desafios+na+aprendizagem+de+lógica+de+programação.pdf" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - PDF Martins lido
    - Executor confirma que os 3 `.rel-lead` atuais (programação beneficia / revisão sistemática / estratégias de ensino) refletem fielmente Martins 2024; nenhum ajuste necessário no corpo
    - Caso encontre divergência fatual com a fonte, anota e levanta antes de aplicar o Task 2 (não silenciar)
  </acceptance_criteria>
  <done>Referência Martins lida; corpo do slide validado contra fonte primária; pronto para reformatar só o cabeçalho.</done>
</task>

<task type="auto">
  <name>Task 2: Substituir cabeçalho do Martins p1 por `> introdução`</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html linhas 82-100 (slide Martins p1 completo, estado atual)
    - .planning/phases/01-reformata-o-da-base/01-PATTERNS.md § "Padrão 'antes' #1 — par .rel-kicker + .rel-title" e § "Cabeçalho-alvo único (padrão 'depois')"
    - .planning/phases/01-reformata-o-da-base/01-CONTEXT.md D-01, D-02, D-03, D-04
    - apresentacao/STYLE.md (tipografia .deck-topic)
  </read_first>
  <action>
**No slide Martins p1** (atualmente linhas 82-100 do `apresentacao/index.html`, identificável pela presença de `<h2 class="rel-title">Martins, Marin e Alves (2024)</h2>`):

**Atualizar o comentário** acima da `<section>`:
- Substituir as 2 linhas atuais `<!-- ============ SLIDE · Trabalho correlato: Martins, Marin e Alves (2024) ============ -->` + `<!-- regra do deck: todo autor novo é introduzido como trabalho correlato antes de ser usado -->`
- por uma única linha: `<!-- ============ SLIDE · Introdução · Martins, Marin e Alves (2024) — recorte do problema ============ -->`
- O segundo comentário ("regra do deck") deixa de existir porque a regra foi revogada (ver D-21, plan 07).

**Dentro da `<div class="deck-slide slide-related">` do slide Martins p1:**

DELETAR (linhas 88-89 do estado atual):
```
          <p class="rel-kicker kicker"><span class="ps1">&gt;</span>trabalhos correlatos<span class="caret blink"></span></p>
          <h2 class="rel-title">Martins, Marin e Alves (2024)</h2>
```

INSERIR no mesmo lugar (mesma indentação de 10 spaces):
```
          <p class="deck-topic"><span class="ps1">&gt;</span>introdução<span class="caret blink"></span></p>
```

**Restrições obrigatórias:**

- Texto exato dentro do `.deck-topic`: `introdução` (minúsculo, conforme D-04 e STYLE.md "Tipografia": `> [seção]` mono Cascadia em `#5b6472`).
- Caret piscante `<span class="caret blink"></span>` como ÚLTIMO filho do `<p class="deck-topic">`, fora do texto da seção (PATTERNS.md § "Caret piscante").
- Nenhum `.rel-sub` no slide Martins p1 atual (o slide não tem subtítulo), então nada a deletar nesse aspecto.
- Os 3 parágrafos `<p class="rel-lead">` (linhas 91, 93, 95-96 atuais) ficam INTACTOS. Não tocar no corpo.
- `<svg class="wm">` (linha 86) preservado.
- Rodapé `<p class="rel-cite">Fonte: Martins, Marin e Alves (2024).</p>` (linha 98) preservado intacto (D-23). Note que aqui o autor sobrevive APENAS porque está no rodapé "Fonte:" (D-02).
- `<div class="deck-slide slide-related">` preserva a classe `slide-related` (D-03: classes CSS visuais permanecem; só o markup do cabeçalho muda).

**Commit:** `apresentacao: reformatar Martins p1 com > introdução (REFORMAT-01)`.
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && grep -c 'introdução<span class="caret blink"' apresentacao/index.html | grep -q '^1$' && grep -c 'rel-title">Martins, Marin e Alves' apresentacao/index.html | grep -q '^0$' && grep -c 'Fonte: Martins, Marin e Alves (2024)' apresentacao/index.html | awk '$1 >= 1 { exit 0 } { exit 1 }' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'introdução<span class="caret blink"' apresentacao/index.html` retorna `1` (cabeçalho novo presente)
    - `grep -c 'rel-title">Martins, Marin e Alves' apresentacao/index.html` retorna `0` (h2 antigo removido; o nome só pode existir em "Fonte:" e nos comentários do markup)
    - `grep -c 'rel-kicker' apresentacao/index.html` diminui em 1 vs estado pré-task (uma `.rel-kicker` a menos no markup, deixa de ser usada neste slide)
    - `grep -c 'Fonte: Martins, Marin e Alves (2024)' apresentacao/index.html` retorna ≥ 1 (o autor segue no rodapé)
    - `grep -c '<p class="rel-lead">' apresentacao/index.html` mantém pelo menos 3 ocorrências dentro do slide Martins p1 (corpo intacto)
    - Browser smoke test: navegar até o slide Martins p1, ver cabeçalho `> introdução` em Cascadia `#5b6472` com caret piscando, 3 parágrafos `.rel-lead`, rodapé "Fonte: Martins, Marin e Alves (2024).", console sem erro
    - Commit em `git log --oneline -1 apresentacao/index.html`: `apresentacao: reformatar Martins p1 com > introdução`
  </acceptance_criteria>
  <done>Slide Martins p1 reformatado com cabeçalho `> introdução`; corpo intacto; deck navegável; commit atômico.</done>
</task>

</tasks>

<verification>
- Cabeçalho travado D-04: `> introdução` (1 ocorrência) no slide-related Martins p1
- `<h2 class="rel-title">Martins, Marin e Alves (2024)</h2>` deletado
- 3 `.rel-lead` intactos
- Rodapé "Fonte:" preservado
- Browser smoke test OK
</verification>

<success_criteria>
- REFORMAT-01 satisfeito (Roadmap Success Criterion #2: slide Martins p1 exibe `> introdução` com caret piscando; autor aparece apenas em "Fonte:" no rodapé)
- D-01, D-02, D-03, D-04 cumpridos
- Deck navega fim-a-fim
</success_criteria>

<output>
Criar `.planning/phases/01-reformata-o-da-base/01-04-SUMMARY.md` com:
- Hash do commit
- Linha onde o novo `.deck-topic` aparece no `apresentacao/index.html`
- Confirmação dos grep gates
- Próximo plan: 01-05 (REFORMAT-02 Zorić p3 / slide-phases)
</output>
