---
phase: 01-reformata-o-da-base
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apresentacao/index.html
autonomous: false
requirements:
  - REMOVE-01
must_haves:
  truths:
    - "Working tree atual (mudanças não commitadas em apresentacao/) tem decisão explícita do usuário antes de qualquer edição"
    - "Após este plano, busca por 'slide-corbett' em apresentacao/index.html retorna 0"
    - "Deck continua navegável fim-a-fim no browser (15 slides, sem Corbett ×2, com Yağcí ainda em 2 slides separados; outras reformatações ainda não aplicadas)"
  artifacts:
    - path: "apresentacao/index.html"
      provides: "deck com Corbett removido"
      contains: "exatamente 14 ocorrências de '<section data-background-color=\"#F1F6FB\">' (estado original 16 sections - 2 Corbett removidos = 14 sections de conteúdo + 2 sections de cover/title-tcc com cores diferentes = continuam 14 sections F1F6FB; agenda é gradient)"
  key_links:
    - from: "deck navegação reveal.js"
      to: "última section restante após Corbett"
      via: "tecla → navega normalmente"
      pattern: "console limpo após percorrer fim-a-fim"
---

<objective>
**Decidir o destino do working tree atual** (mudanças não commitadas em `apresentacao/index.html` e `apresentacao/assets/theme-unifacens.css`) ANTES de tocar em qualquer slide, e **remover os 2 slides Corbett & Anderson** (`slide-related slide-corbett`) do deck.

Purpose: a fase 1 inteira opera sobre `apresentacao/index.html`. Decidir o ponto de partida antes de editar evita perda de trabalho e conflitos. Remover Corbett primeiro libera espaço, simplifica os DOM moves dos plans 06-07, e mantém o deck navegável depois de uma operação de baixo risco.

Output: working tree em estado conhecido (integrado / stashed / committed-WIP / discarded conforme decisão do usuário); `apresentacao/index.html` com os 2 slides Corbett deletados e nenhum placeholder/comentário residual; deck navegável de slide 0 a slide 13 (era 15) sem erro de console.

Justificativa do escopo: REMOVE-01 é a operação atomicamente mais segura (não muda nenhum cabeçalho, só apaga 2 sections inteiras com comentários). Faz sentido como "plano de aquecimento" que valida o ritual de commit-por-slide + smoke test antes de começar as reformatações textuais.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-reformata-o-da-base/01-CONTEXT.md
@.planning/phases/01-reformata-o-da-base/01-PATTERNS.md
@.planning/phases/01-reformata-o-da-base/SKELETON.md
@./CLAUDE.md
@apresentacao/STYLE.md
@apresentacao/index.html
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Decidir destino do working tree atual</name>
  <read_first>
    - apresentacao/index.html (estado atual, working tree)
    - apresentacao/assets/theme-unifacens.css (estado atual, working tree)
    - .planning/phases/01-reformata-o-da-base/01-CONTEXT.md § "Claude's Discretion" (linha 79: working tree carrega Erick/Leonardo/Victor na capa e os 2 slides Yağcí novos)
  </read_first>
  <action>Apresentar as 4 opções de tratamento do working tree (integrate / commit-wip / stash / discard) ao usuário e bloquear até receber resposta. Não tocar em nenhum arquivo de `apresentacao/` antes da decisão. Após a resposta: se `commit-wip`, criar commit `apresentacao: WIP working tree antes da fase 1` agora; se `integrate`, prosseguir sem nada a fazer; se `stash`, rodar `git stash push -u apresentacao/`; se `discard`, exigir confirmação extra do usuário antes de aplicar `git checkout -- apresentacao/` (`discard` é proibido sem confirmação).</action>
  <decision>O working tree atual de `apresentacao/` tem mudanças não commitadas (index.html +119 linhas; theme-unifacens.css +63 linhas; STYLE.md untracked). Como tratá-las antes de começar a fase 1?</decision>
  <context>
**Observado via `git diff --stat apresentacao/` e `git status apresentacao/`:**
- `apresentacao/index.html` modificado: working tree carrega slides Yağcí #12 (slide-related) e #13 (slide-related slide-bridge, com `.bridge-seq`) que serão fundidos em REFORMAT-03 (plan 03 desta fase); também tem os nomes dos autores na capa TCC (Erick Miranda Viana, Leonardo Kuntz Oliveira, Victor Santos Borba).
- `apresentacao/assets/theme-unifacens.css` modificado: CSS adicionado para `.bridge-seq`, `.deck-topic`, e outros.
- `apresentacao/STYLE.md` untracked: o design contract da apresentação ainda não está versionado.

**Os 4 caminhos possíveis:**
- **integrate**: deixar as mudanças no working tree e seguir editando em cima (os slides Yağcí novos viram a base do REFORMAT-03; CSS de `.bridge-seq` é necessário; STYLE.md é referência viva). Risco: se algo der errado, voltar é mais difícil. Recomendado por CONTEXT.md linha 79.
- **commit-WIP**: criar um commit `apresentacao: WIP working tree antes da fase 1` agora (incluindo STYLE.md), para deixar o estado nomeado antes de começar. Vantagem: cada plan vira diff limpo a partir de um ponto âncora.
- **stash**: `git stash push -u apresentacao/` para guardar, começar com HEAD limpo, e re-aplicar manualmente o que for necessário. Risco: perder os 2 slides Yağcí que o REFORMAT-03 vai usar como base.
- **discard**: `git checkout -- apresentacao/` + remover STYLE.md. Risco MUITO alto: perde os slides Yağcí e o CSS de `.bridge-seq`. NÃO recomendado.
  </context>
  <options>
    <option id="integrate">
      <name>integrate (recomendado por CONTEXT.md)</name>
      <pros>Slides Yağcí novos já estão prontos para serem fundidos em REFORMAT-03 (plan 03); CSS de `.bridge-seq` já carregado; nada a re-aplicar; segue convenção "commits atômicos por slide concluído" naturalmente nos próximos plans.</pros>
      <cons>Diff dos próximos commits começa a partir de um estado não-commitado; quem auditar o histórico precisa olhar o working tree atual além do HEAD.</cons>
    </option>
    <option id="commit-wip">
      <name>commit-WIP antes de começar</name>
      <pros>Cada plan da fase 1 fica como diff isolado contra esse commit âncora; STYLE.md entra no histórico; estado nomeado.</pros>
      <cons>Adiciona um commit "técnico" no histórico antes dos commits-por-slide; convenção do projeto não usa esse padrão (commits são funcionais, não administrativos).</cons>
    </option>
    <option id="stash">
      <name>stash</name>
      <pros>Começa de HEAD limpo.</pros>
      <cons>Perde os slides Yağcí no working tree; REFORMAT-03 (plan 03) precisaria recriar `.bridge-seq` do zero ou re-aplicar o stash; trabalho desnecessário. NÃO RECOMENDADO.</cons>
    </option>
    <option id="discard">
      <name>discard</name>
      <pros>Nenhum (estado completamente limpo é meta enganadora aqui).</pros>
      <cons>Perde os 2 slides Yağcí, perde `.bridge-seq` CSS, perde nomes dos autores na capa, perde STYLE.md. PROIBIDO sem confirmação extra.</cons>
    </option>
  </options>
  <resume-signal>Responder com `integrate`, `commit-wip`, `stash` ou `discard`. Sem resposta, NÃO prosseguir.</resume-signal>
  <acceptance_criteria>
    - Usuário respondeu uma das 4 opções
    - Working tree em estado conforme decisão (verificar com `git status apresentacao/`)
    - Se `commit-wip`: commit aparece em `git log --oneline -1` com mensagem aproximada `apresentacao: WIP working tree antes da fase 1`
    - Se `integrate`: working tree intocado (`git status` continua mostrando os mesmos arquivos modificados)
  </acceptance_criteria>
  <done>Estado do working tree confirmado e auditável via `git status apresentacao/` antes do Task 2 começar.</done>
</task>

<task type="auto">
  <name>Task 2: Deletar os 2 slides Corbett & Anderson (REMOVE-01)</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html (linhas 386-439, sections #14 e #15 a deletar)
    - .planning/phases/01-reformata-o-da-base/01-PATTERNS.md § "REMOVE-01 — deleção limpa, sem placeholder"
    - .planning/phases/01-reformata-o-da-base/01-CONTEXT.md D-19, D-20
  </read_first>
  <action>
Deletar do `apresentacao/index.html` exatamente os 2 blocos abaixo, INCLUINDO o comentário `<!-- ============ SLIDE · ... ============ -->` imediatamente acima de cada `<section>`:

**Bloco 1 (atualmente linhas 386-407, slide #14 — Corbett motivação):**
- Linha começando com `<!-- ============ SLIDE · Trabalho correlato: Corbett e Anderson (1995) — origem do KT (motivação) ============ -->`
- Section inteira que segue: `<section data-background-color="#F1F6FB">` ... `</section>` cujo `<div>` tem `class="deck-slide slide-related slide-corbett"` e `<h2 class="rel-title">Corbett e Anderson (1995)</h2>` + `<p class="rel-sub">A origem do <i>knowledge tracing</i></p>`

**Bloco 2 (atualmente linhas 409-439, slide #15 — Corbett modelo BKT):**
- Linha começando com `<!-- ============ SLIDE · Corbett e Anderson (1995) — modelo de dois estados e os 4 parâmetros (base do BKT) ============ -->`
- Section inteira que segue: `<section data-background-color="#F1F6FB">` ... `</section>` cujo `<div>` tem `class="deck-slide slide-related slide-corbett"` e `<h2 class="rel-title">Corbett e Anderson (1995)</h2>` + `<p class="rel-sub">O modelo de dois estados (base do BKT)</p>`

**Restrições (D-19):**
- NÃO deixar comentário/placeholder/`<!-- TODO -->` no markup.
- NÃO adicionar nota em REQUIREMENTS.md ou STYLE.md mencionando "removido por enquanto" ou "voltará na fase 4". A cronologia "BKT → DKT → Code-DKT" volta natural na fase 4 (MODEL-01), sem precisar de marca aqui.
- A linha em branco que separava as 2 sections de Corbett também sai.
- Estado final do arquivo: a section anterior (slide-bridge Yağcí, atualmente linhas 369-384) é seguida por `    </div>\n  </div>\n\n  <script src="https://cdn.jsdelivr.net/npm/reveal.js@5.1.0/...` (a tag de fechamento de `.slides` e o `<script>` final, sem nenhuma section Corbett entre eles).

**Commit:** `apresentacao: remover slides Corbett (REMOVE-01)` — convenção do projeto (lowercase, dois-pontos, sem "feat:/fix:"; ver `.planning/codebase/CONVENTIONS.md` linhas 261-285).
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && grep -c 'slide-corbett' apresentacao/index.html | grep -q '^0$' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'slide-corbett' apresentacao/index.html` retorna `0`
    - `grep -c 'Corbett e Anderson' apresentacao/index.html` retorna `0` (D-19: nenhuma referência residual a Corbett no markup; volta natural na fase 4)
    - `grep -c 'TODO\|placeholder\|futuro slide' apresentacao/index.html` não aumenta em relação ao estado pré-task (sem marcas novas)
    - Section count: `grep -c '<section data-background' apresentacao/index.html` retorna 14 (era 16, menos 2 Corbett)
    - Browser smoke test (manual): `cd apresentacao && python3 -m http.server 8000`, abrir http://127.0.0.1:8000, navegar com → do slide 0 ao último, console DevTools sem erro
    - Commit existe em `git log --oneline -1 apresentacao/index.html` com mensagem começando por `apresentacao: remover slides Corbett`
  </acceptance_criteria>
  <done>2 sections Corbett deletadas; deck navegável; commit atômico criado; nenhum placeholder ou referência residual.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Static deck local, sem rede / auth / dados.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | `apresentacao/index.html` markup | accept | Repo Git versionado; rollback via `git checkout HEAD~1 -- apresentacao/index.html` |
| T-01-02 | Denial of Service | navegação reveal.js após delete | mitigate | Smoke test obrigatório (acceptance_criteria do Task 2): navegar fim-a-fim no browser com console aberto |

Sem ameaças HIGH/CRITICAL; deck é estático local.
</threat_model>

<verification>
- `grep -c 'slide-corbett' apresentacao/index.html` retorna 0
- Browser smoke test passa (navegação fim-a-fim sem erro de console)
- `git log --oneline -1 apresentacao/index.html` mostra commit do Task 2
- Working tree em estado conforme decisão do Task 1
</verification>

<success_criteria>
- REMOVE-01 satisfeito (Roadmap Success Criterion #6)
- Working tree em estado documentado e auditável
- Deck continua navegando fim-a-fim no browser
- Commit atômico criado para REMOVE-01
</success_criteria>

<output>
Após completion, criar `.planning/phases/01-reformata-o-da-base/01-01-SUMMARY.md` com:
- Decisão tomada no Task 1 (integrate/commit-wip/stash/discard)
- Confirmação de delete (`grep -c slide-corbett` = 0)
- Hash do commit do Task 2
- Estado do working tree pós-task
- Próximo plan: 01-02 (MERGE-01 Zorić fundido)
</output>
