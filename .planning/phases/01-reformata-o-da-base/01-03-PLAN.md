---
phase: 01-reformata-o-da-base
plan: 03
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - apresentacao/index.html
autonomous: true
requirements:
  - REFORMAT-03
must_haves:
  decisions_covered:
    - "D-01: cabeçalho `.deck-topic` com `> [seção]` + caret blink substitui tópico anterior E h2 com nome do autor"
    - "D-02: autor (Yağcı) só no rodapé `Fonte:`, nunca no corpo"
    - "D-03: slide-related perde `.rel-kicker` + `.rel-title`; classes CSS permanecem"
    - "D-06: cabeçalho travado `> da edm ao knowledge tracing`"
    - "D-12: os 2 slides Yağcı viram 1 <section> único"
    - "D-13: corpo do slide fundido na ordem markup definida (paráfrase + .bridge-seq + .bridge-text), com a citação direta da p.2 substituída por paráfrase D-27"
    - "D-14: descartar do p1 a citação inicial, subtítulo `Predição do desempenho acadêmico...` e os 3 bullets de algoritmos/acurácia/1854 alunos; manter `Yağcı` no rodapé"
    - "D-23: rodapé `Fonte: Yağcı (2022).` preservado"
    - "D-25: paráfrase indireta como padrão"
    - "D-27: substituir citação direta Yağcı p.2 por paráfrase em voz própria centrada em `acompanhamos o conhecimento ao longo do tempo`"
  truths:
    - "Os 2 slides Yağcı (slide-related Yağcı p1 + slide-related slide-bridge Yağcı p2) viram UM único <section> com classes `slide-related slide-bridge`"
    - "O slide fundido tem cabeçalho `> da edm ao knowledge tracing` como única linha de cabeçalho"
    - "A citação direta atual do p2 (`Outra dimensão da análise de aprendizagem é prever...` Yağcı 2022 p.2) é substituída por paráfrase D-27 centrada em `acompanhamos o conhecimento ao longo do tempo`"
    - "A sequência horizontal `.bridge-seq` (3 passos: `mineração de dados educacionais → predição de desempenho → knowledge tracing`) é preservada como-é"
    - "Rodapé `Fonte: Yağcı (2022).` é preservado"
    - "O caractere `ı` (sem ponto) e `ğ` (com breve) em `Yağcı` são copiados do markup existente, nunca redigitados como `Yagci`"
  artifacts:
    - path: "apresentacao/index.html"
      provides: "1 slide Yağcí fundido"
      contains: "exatamente 1 ocorrência de '> da edm ao knowledge tracing'"
  key_links:
    - from: "slide Yağcí fundido"
      to: "paráfrase D-27"
      via: "<p class=rel-lead> com texto literal"
      pattern: "Nós seguimos o passo seguinte"
    - from: "slide Yağcí fundido"
      to: "bridge-seq preservada"
      via: "3 spans .step intactos"
      pattern: "knowledge tracing</i></span>"
---

<objective>
**Fundir os 2 slides Yağcı (slide-related Yağcı p1 e slide-related slide-bridge Yağcı p2)** num único `<section>` com cabeçalho `> da edm ao knowledge tracing`, **substituindo a citação direta** (Yağcı, 2022, p. 2, tradução nossa) por **paráfrase D-27** centrada em "nós seguimos o passo seguinte". A sequência horizontal `.bridge-seq` é **preservada literalmente** do slide #13 atual.

Purpose: o slide-bridge Yağcı é o ponto narrativo crítico onde o deck transita de "predição de desempenho como aplicação de EDM" para "knowledge tracing como o nosso ângulo". A citação literal sobre "alunos em risco" gasta 8-12 segundos de leitura sem que a frase SEJA o argumento; a paráfrase D-27 traz a voz dos defendentes para o gancho ("nós seguimos o passo seguinte: acompanhamos o conhecimento ao longo do tempo"), alinhando com D-25 (paráfrase indireta + autor parentético).

Output: 1 `<section>` fundida em `apresentacao/index.html` no lugar dos 2 Yağcı atuais; `.bridge-seq` intacta; deck continua navegável; commit atômico.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-reformata-o-da-base/01-CONTEXT.md
@.planning/phases/01-reformata-o-da-base/01-PATTERNS.md
@.planning/phases/01-reformata-o-da-base/SKELETON.md
@./CLAUDE.md
@apresentacao/STYLE.md
@apresentacao/index.html
@docs/edm_prediction.pdf
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ler a referência Yağcı (2022) antes de redigir a paráfrase</name>
  <files>(nenhum — leitura)</files>
  <read_first>
    - docs/edm_prediction.pdf (ler ao menos a seção de introdução e a discussão de "predição como dimensão da análise de aprendizagem" — origem da citação p. 2 que vai ser substituída)
    - apresentacao/index.html linhas 347-384 (estado atual dos 2 slides Yağcı: section #12 com 3 bullets a descartar; section #13 slide-bridge sobrevivente com `.bridge-seq` e `.bridge-text`)
  </read_first>
  <action>
Convenção vinculante do CLAUDE.md: "Antes de redigir ou alterar slide que cite um autor, ler a referência completa em docs/". Esta tarefa cumpre a leitura.

Ler `docs/edm_prediction.pdf` (Yağcı 2022) com foco em:
- Introdução: posicionamento de EDM como ferramenta para explorar relações nos dados educacionais (origem da citação p. 1 do slide #12 antigo, descartada per D-14).
- p. 2 (a citação a ser substituída): "Outra dimensão da análise de aprendizagem é prever o desempenho acadêmico dos estudantes [...] e determinar os estudantes potencialmente em risco de reprovação". Confirmar que Yağcı argumenta predição como uma DIMENSÃO da análise de aprendizagem dentro do EDM. A paráfrase D-27 ("Yağcı (2022) mostrou o valor de prever desempenho acadêmico para identificar alunos em risco. Nós seguimos o passo seguinte: ...") precisa ser fiel a esse posicionamento.
- Confirmar grafia "Yağcı" (com "ğ" com breve e "ı" sem ponto). Não usar "Yagci".

Resultado: confirmar internamente que a paráfrase D-27 cabe na voz de Yağcı e que a transição "nós seguimos o passo seguinte" não distorce o argumento original (Yağcı não fala em "knowledge tracing"; a transição é nossa autoria, marcada por "Nós seguimos").
  </action>
  <verify>
    <automated>test -f docs/edm_prediction.pdf && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `docs/edm_prediction.pdf` foi lido
    - Executor confirma internamente que a paráfrase D-27 reflete fielmente o argumento de Yağcı (predição de desempenho como dimensão da análise de aprendizagem) e que a transição "Nós seguimos o passo seguinte" é marcada como autoria própria (não atribuída a Yağcı)
    - Grafia "Yağcı" (com `ğ` e `ı`) confirmada como obrigatória em qualquer texto novo
  </acceptance_criteria>
  <done>Referência Yağcı lida; paráfrase D-27 validada contra a fonte primária.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fundir slides Yağcı p1+p2 num único section com paráfrase D-27</name>
  <files>apresentacao/index.html</files>
  <read_first>
    - apresentacao/index.html linhas 347-384 (sections #12 e #13 atuais)
    - .planning/phases/01-reformata-o-da-base/01-PATTERNS.md § "REFORMAT-03 Yağcí fundido — `.bridge-seq` preservada" (markup canônico antes/depois)
    - .planning/phases/01-reformata-o-da-base/01-CONTEXT.md D-06, D-12, D-13, D-14, D-27
    - apresentacao/STYLE.md
  </read_first>
  <action>
**Substituir** os 2 `<section>` Yağcı atuais (sections #12 e #13, cada qual com seu comentário `<!-- ============ SLIDE · ... ============ -->` acima) por **UM único `<section>`** conforme markup-alvo abaixo. **Estratégia recomendada:** trabalhar em cima da section #13 (sobrevivente) — ela já tem `.bridge-seq` e a classe `slide-bridge`; apagar a section #12 e o comentário acima dela, e reescrever apenas o cabeçalho e a área de texto da section #13.

**O QUE DELETAR:**

1. Comentário acima da section #12: `<!-- ============ SLIDE · Trabalho correlato: Yağcı (2022) — predição de desempenho ============ -->`
2. Section #12 inteira (atualmente `<section data-background-color="#F1F6FB">` ... `</section>` cujo `<div>` tem `class="deck-slide slide-related"` e `<h2 class="rel-title">Yağcı (2022)</h2>` + `<p class="rel-sub">Predição do desempenho acadêmico com mineração de dados educacionais</p>` + `<blockquote class="rel-quote">"A mineração de dados educacionais tornou-se uma ferramenta eficaz..." (Yağcı, 2022, p. 1, tradução nossa)` + 3 `<li>` em `.rel-points`).
3. Dentro da section #13 (a sobrevivente): apagar
   - `<p class="rel-kicker kicker"><span class="ps1">&gt;</span>trabalhos correlatos<span class="caret blink"></span></p>` (linha 373)
   - `<h2 class="rel-title">Da predição ao <i>knowledge tracing</i></h2>` (linha 374)
   - `<blockquote class="rel-quote">&ldquo;Outra dimensão da análise de aprendizagem é prever o desempenho acadêmico dos estudantes [...] e determinar os estudantes potencialmente em risco de reprovação&rdquo; <span class="src">(Yağcı, 2022, p. 2, tradução nossa)</span>.</blockquote>` (linha 376)

**O QUE INSERIR / MANTER na section sobrevivente:**

Após edição, a section única deve ficar assim (cole linha-a-linha; copie `.bridge-seq` do estado atual sem retransliterar):

```html
      <!-- ============ SLIDE · Da EDM ao knowledge tracing (Yağcı, 2022) — fusão p1+p2 ============ -->
      <section data-background-color="#F1F6FB">
        <div class="deck-slide slide-related slide-bridge">
          <svg class="wm" viewBox="0 0 136.7 139.78" aria-hidden="true"><use href="#sym"/></svg>

          <p class="deck-topic"><span class="ps1">&gt;</span>da edm ao knowledge tracing<span class="caret blink"></span></p>

          <p class="rel-lead">Yağcı (2022) mostrou o valor de prever desempenho acadêmico para identificar alunos em risco. Nós seguimos o passo seguinte: em vez de uma previsão única ao fim do curso, <b>acompanhamos o conhecimento ao longo do tempo</b>, a cada nova tentativa, via <i>knowledge tracing</i>.</p>

          <p class="bridge-seq"><span class="step">mineração de dados educacionais</span><span class="arr">&rarr;</span><span class="step">predição de desempenho</span><span class="arr">&rarr;</span><span class="step"><i>knowledge tracing</i></span></p>

          <p class="rel-cite">Fonte: Yağcı (2022).</p>
        </div>
      </section>
```

**Restrições obrigatórias:**

- Texto da paráfrase é LITERAL conforme acima (D-27): "Yağcı (2022) mostrou o valor de prever desempenho acadêmico para identificar alunos em risco. Nós seguimos o passo seguinte: em vez de uma previsão única ao fim do curso, **acompanhamos o conhecimento ao longo do tempo**, a cada nova tentativa, via *knowledge tracing*."
- Grafia "Yağcı" (com `ğ` e `ı`): **copiar do markup existente**, não redigitar. Comando de auxílio sugerido: copiar o `<p class="rel-cite">Fonte: Yağcı (2022).</p>` da section #13 atual como source-of-truth da grafia.
- "acompanhamos o conhecimento ao longo do tempo" em `<b>` (negrito); "*knowledge tracing*" em `<i>` (itálico, termo estrangeiro per STYLE.md linha 77).
- Nenhum `<blockquote class="rel-quote">` no slide fundido. Sem aspas literais. Sem "tradução nossa".
- Nenhum em-dash (`—`) na prosa. Usar vírgulas, dois-pontos ou parênteses. (STYLE.md linha 79 + CLAUDE.md vinculante.)
- Classes: `<div class="deck-slide slide-related slide-bridge">` (preservar `slide-bridge` porque o CSS de `.slide-bridge .bridge-seq` em `theme-unifacens.css` linhas ~201-210 estiliza a sequência horizontal).
- A `.bridge-seq` (3 passos `mineração de dados educacionais → predição de desempenho → knowledge tracing`) é **copiada literalmente** do estado atual da section #13. NÃO reordenar, NÃO retransliterar.
- A `.bridge-text` da section #13 atual ("Yağcı (2022) posiciona a predição do desempenho [...] dá o passo seguinte: em vez de uma única previsão ao fim do curso, acompanha o conhecimento do estudante ao longo do tempo, a cada nova tentativa.") é **DESCARTADA** porque seu conteúdo já está absorvido na paráfrase nova (que é uma evolução desse texto, agora com voz "nós" e o gancho ao knowledge tracing explicitado).
- A `.rel-sub` ("Predição do desempenho acadêmico com mineração de dados educacionais") da section #12 antiga é DESCARTADA (D-03).
- Os 3 bullets `.rel-points` da section #12 antiga (Yağcı propõe X, compara 6 algoritmos com acurácia 70-75% sobre 1854 alunos, identifica alunos em risco) são DESCARTADOS (D-14). Esses dados quantitativos não entram em paráfrase nova porque a defesa não usa Yağcı como evidência numérica; usa Yağcı só como ponte argumentativa entre EDM e knowledge tracing.
- A primeira citação direta da p.1 do slide #12 antigo ("A mineração de dados educacionais tornou-se uma ferramenta eficaz...") é DESCARTADA (D-14).
- `<svg class="wm">` preservado.
- Caret piscante (`<span class="caret blink"></span>`) dentro do `.deck-topic`.
- Rodapé `Fonte: Yağcı (2022).` preservado (D-23).

**Commit:** `apresentacao: fundir slides Yağcı p1+p2 com paráfrase (REFORMAT-03, D-27)`.
  </action>
  <verify>
    <automated>cd /home/leokuntz/Documents/repositories/studies/tcc.edm.kt && grep -c '> da edm ao knowledge tracing' apresentacao/index.html | grep -q '^1$' && grep -c 'Nós seguimos o passo seguinte' apresentacao/index.html | grep -q '^1$' && grep -c 'acompanhamos o conhecimento ao longo do tempo' apresentacao/index.html | grep -q '^1$' && grep -c 'Outra dimensão da análise de aprendizagem é prever' apresentacao/index.html | grep -q '^0$' && grep -c 'A mineração de dados educacionais tornou-se uma ferramenta eficaz' apresentacao/index.html | grep -q '^0$' && grep -c 'class="bridge-seq"' apresentacao/index.html | grep -q '^1$' && grep -c 'Yağcı' apresentacao/index.html | awk '{ if ($1 >= 2) exit 0; else exit 1 }' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '> da edm ao knowledge tracing' apresentacao/index.html` retorna `1`
    - `grep -c 'Nós seguimos o passo seguinte' apresentacao/index.html` retorna `1` (paráfrase D-27 literal)
    - `grep -c 'acompanhamos o conhecimento ao longo do tempo' apresentacao/index.html` retorna `1`
    - `grep -c 'Outra dimensão da análise de aprendizagem é prever' apresentacao/index.html` retorna `0` (citação direta p.2 removida)
    - `grep -c 'A mineração de dados educacionais tornou-se uma ferramenta eficaz' apresentacao/index.html` retorna `0` (citação direta p.1 removida; D-14)
    - `grep -c 'class="bridge-seq"' apresentacao/index.html` retorna `1` (sequência horizontal preservada)
    - `grep -c 'Yağcı' apresentacao/index.html` retorna ≥ 2 (a paráfrase + o `Fonte:` mantêm a grafia correta; NUNCA `Yagci`)
    - `grep -c 'Yagci' apresentacao/index.html` retorna `0` (grafia errada bloqueada)
    - Section count: `grep -c '<section data-background' apresentacao/index.html` retorna 12 (era 13 após plan 02, menos 1 do merge)
    - Browser smoke test: navegar até o slide Yağcı fundido, ver cabeçalho Cascadia com caret, paráfrase em Arial com "acompanhamos o conhecimento ao longo do tempo" em negrito e "knowledge tracing" em itálico, `.bridge-seq` horizontal com 3 caixas, rodapé `Fonte: Yağcı (2022).`, console sem erro
    - Commit em `git log --oneline -1 apresentacao/index.html`: `apresentacao: fundir slides Yağcı p1+p2`
  </acceptance_criteria>
  <done>Slide Yağcı fundido com paráfrase D-27 e `.bridge-seq` preservada; deck navegável; commit atômico.</done>
</task>

</tasks>

<verification>
- Cabeçalho travado D-06: `> da edm ao knowledge tracing` (1 ocorrência)
- Paráfrase D-27 aplicada literal
- 0 citações diretas Yağcı restantes
- `.bridge-seq` preservada (1 ocorrência)
- Grafia "Yağcı" correta; "Yagci" zerada
- Section count -1 vs estado pós-plan 02
- Browser smoke test OK
- Commit atômico criado
</verification>

<success_criteria>
- REFORMAT-03 satisfeito (Roadmap Success Criterion #4: slide Yağcí exibe `> da edm ao knowledge tracing` com gancho explícito sobre acompanhamento ao longo do tempo)
- D-25 e D-27 cumpridos (paráfrase indireta + autor parentético + sem `<blockquote class="rel-quote">` + sem "tradução nossa")
- `.bridge-seq` preservada como elemento visual da ponte EDM → KT
- Deck navega fim-a-fim sem erro
</success_criteria>

<output>
Criar `.planning/phases/01-reformata-o-da-base/01-03-SUMMARY.md` com:
- Hash do commit
- Linha do slide Yağcı fundido em `apresentacao/index.html`
- Confirmação dos grep gates (cabeçalho + paráfrase + bridge-seq + grafia + ausência de citação direta)
- Próximo plan: 01-04 (REFORMAT-01 Martins p1)
</output>
